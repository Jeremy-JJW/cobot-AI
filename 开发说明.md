# 开发说明：如何新增 Skill

## 目录结构

```
skills/                  # 每个 Skill 一个 py 文件
skill_registry.json      # Skill 清单、参数、安全边界
rule_parser.py           # 固定话术规则解析
executor.py              # 白名单执行器
robot_session.py         # 基于 DobotDemo + dobot_api 的封装
positions.json           # 命名点位
nl_robot.py              # 入口
```

---

## 支持的说法示例（全量）

以下均为规则解析可识别的话术（`rule_parser.py`），测试方式：

```powershell
.\.venv\Scripts\python nl_robot.py --dry-run "你的说法"
```

**move_relative_linear — 单步相对直线（0.1～200mm）**

向 X 正方向移动 100 毫米 · 向 X 负方向移动 50mm · 向 Y 正方向移动 80 毫米 · 向 Y 负方向移动 50 毫米 · 向 Z 正方向移动 100 毫米 · 向 Z 负方向移动 50 毫米 · 抬高 20 毫米 · 抬高100毫米 · 上升 30 毫米 · 向上 50mm · 下降 50 毫米 · 降低 30 毫米 · 向下 20mm · 向 Z 正方向移动 5 厘米

**move_relative_sequence — 多步相对直线（2～10 步，每步 0.1～200mm）**

先 x+50mm，再 y+50mm · 先 x+50mm再 y+50mm · 先向 X 正方向移动 50 毫米，再向 Y 正方向移动 50 毫米 · 先抬高 10 毫米，然后向 X 正方向移动 20 毫米，接着向 Y 正方向移动 20 毫米 · 第一步向 X 正方向移动 30 毫米，第二步向 Y 正方向移动 30 毫米，第三步抬高 20 毫米 · 先 x+20，再 y+20，再 z+20，再 x-20，再 y-20，再 z-20，再 x+10，再 y+10，再 z+10，再 x-10

连接词：先 / 再 / 然后 / 接着 / 随后 / 接下来 / 之后 / 紧接着 / 完了再 / 然后再 / 再然后 / 又 / 以及 / 并且 / 而且 / 再接着 / 再继续 / 继续 / 第一步…第十步 / ， , 、 ； ;

**move_to_named_point — 去命名点**

去 A 点 · 去A点 · 去 point_a · 去 B 点 · 去 point_b · 移动到 A 点 · 移动到 point_b · 回到 home · 回到home · 回到原点 · 回到零点 · 到达 home

**move_between_points — 点到点**

从 A 点移动到 B 点 · 从 point_a 到 point_b · 从 home 到 A 点 · 从 A 点到 B 点

**motion_pattern — 20 种预置复合路线（可说尺寸，如「画圆 30 毫米」）**

画圆 · 画圆 30 毫米 · 在 XY 平面画圆 · XZ 画圆 · YZ 画圆 · 走圆弧 · 走圆弧 30 毫米 · XZ 圆弧 · YZ 弧线 · 走方形路线 · 走方形路线 50 毫米 · 走矩形路线 · 走三角形 · 走菱形 · 走十字形 · 走锯齿路线 · 走波浪路线 · 走 U 型路线 · 走 L 型路线 · 走箱体轮廓 · 走三维螺旋运动 · 走三维螺旋运动 40 毫米 · 先运动 X+ 再运动 Y+ 再运动 Z+ 再回到原点 · 走 XZ 抬升返回 · 走 YZ 抬升返回

**set_speed — 调速度（1～50%）**

速度设置为 30% · 速度调到 20 · 用 40% 速度 · 慢一点 · 快一点

**wait — 等待（0.1～10 秒）**

等待 2 秒 · 暂停 1.5 秒 · 停 3 秒 · 等一下 · 暂停一下

**read_pose — 读位姿 / 标定点**

读取当前位置 · 查询当前位姿 · 记录当前位置为 home · 保存当前位置为 point_a · 标定当前位置为 point_b

**stop_or_disable — 停止 / 下使能**

停止运动 · 停住 · 急停 · 下使能

---

## 新增一个 Skill 的步骤

**1. 在 skill_registry.json 注册**

```json
"my_skill": {
  "description": "说明",
  "params": {
    "param1": { "type": "number", "required": true, "min": 0, "max": 100 }
  },
  "examples": ["示例话术"]
}
```

**2. 写 skills/my_skill.py**

```python
def run(session, params: dict, context: dict) -> None:
    # session: RobotSession 实例
    # params: 已校验的参数
    # context: named_points, registry 等
    pass
```

**3. 在 skills/__init__.py 注册 handler**

```python
from skills.my_skill import run as my_skill
SKILL_HANDLERS["my_skill"] = my_skill
```

**4. 在 rule_parser.py 加固定话术（可选）** — 规则能匹配的不走大模型，响应最快。

**5. 测试**

```powershell
.\.venv\Scripts\python nl_robot.py --dry-run "你的测试话术"
```

---

## 已有 Skill 与底层 API

| Skill | 文件 | 底层 |
|-------|------|------|
| move_relative_linear | skills/move_relative_linear.py | GetPose + MovL |
| move_relative_sequence | skills/move_relative_sequence.py | 多次 GetPose + MovL（最多 10 步） |
| move_to_named_point | skills/move_to_named_point.py | MovJ / MovL |
| move_between_points | skills/move_between_points.py | 连续 MovJ/MovL |
| motion_pattern | skills/motion_pattern.py | GetPose + MovL / Arc / Circle |
| set_speed | skills/set_speed.py | SpeedFactor |
| wait | skills/wait.py | sleep |
| read_pose | skills/read_pose.py | GetPose |
| stop_or_disable | skills/stop_or_disable.py | Stop / DisableRobot |

| 封装方法 | 原厂来源 |
|----------|----------|
| `session.connect()` / `enable()` | [DobotDemo.py](DobotDemo.py) |
| `session.movj()` / `movl()` / `arc()` / `circle()` | [dobot_api.py](dobot_api.py) |
| `session.get_pose()` | `GetPose` |
| `session.set_speed()` | `SpeedFactor` |
| `session.stop()` | `Stop` |

不要在 Skill 里直接复制 Demo 逻辑，统一走 `robot_session.py`。`motion_pattern` 的 20 个路线名：`circle_xy` `circle_xz` `circle_yz` `arc_xy` `arc_xz` `arc_yz` `square_xy` `rectangle_xy` `triangle_xy` `diamond_xy` `cross_xy` `zigzag_x` `wave_y` `u_shape_xy` `xyz_step_return` `xz_lift_return` `yz_lift_return` `l_shape_xyz` `box_outline` `spiral_z`。新增路线只允许调用 `dobot_api.py` 已有接口。

---

## 解析与执行链路

1. 规则解析优先（`rule_parser.py`）— 固定话术不走大模型
2. 规则失败时可选 `--llm`（`llm_planner.py`）— 只输出 Skill + params JSON
3. 统一经 `executor.py` 白名单执行，禁止临时生成控制 Python

## 安全建议

- 在 `skill_registry.json` 的 `global_limits` 设最大距离、步数、速度
- 新增 Skill 必须写 `min` / `max`
- 演示前用 `--dry-run` 检查 `generated/last_plan.json`

客户演示话术详见 [使用说明.md](使用说明.md)。
