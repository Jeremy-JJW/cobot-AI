# COBOT AI 機械臂控制系統 — 使用說明書（繁體中文）

---

## 零、啟動方式

### 0.1 執行步驟

1. 開啟終端機（命令提示字元或 PowerShell）
2. 切換到專案目錄：
   ```
   例：cd Z:\Cursor_contents\cobot+AI應用
   ```
3. 執行 Web 服務：
   ```
   例：.venv\Scripts\python web_app.py
   ```
4. 看到以下輸出即表示啟動成功：
   ```
   COBOT AI Web 演示版: http://127.0.0.1:5000
   ```
5. 打開瀏覽器，網址列輸入：**http://127.0.0.1:5000**

### 0.2 環境需求

- Python 3.10+
- 依賴套件已安裝（`pip install -r requirements.txt`）
- 如需使用 AI 解析，需設定 `OPENAI_API_KEY` 環境變數（或在 `.env` 檔案中設定）

---

## 一、網頁操作說明

### 1.1 介面總覽

```
┌─────────────────────────────────────────────────┐
│  COBOT AI > 聊天     [清除報警] [重啟服務] [狀態] │
├─────────────────────────────────────────────────┤
│                                                 │
│  AI  你好，請輸入機械臂動作指令...               │
│      範例：抬高 20 毫米 / 去 A 點 / 畫一個圓    │
│                                                 │
│      你用過的命令都會顯示在這裡                  │
│                                                 │
├─────────────────────────────────────────────────┤
│  [請輸入機械臂動作指令（Enter 發送）] [🎤] [➤] │
└─────────────────────────────────────────────────┘
```

### 1.2 基本操作


| 操作       | 方式                                    |
| -------- | ------------------------------------- |
| **發送指令** | 在輸入框輸入文字，按 **Enter** 或點擊 **➤ 發送按鈕**   |
| **語音輸入** | 點擊 **🎤 麥克風按鈕**，說話後自動轉為文字（支援繁體中文語音辨識） |
| **清除報警** | 點擊頭部 **⚠️ 清除報警按鈕**（機器人報警時會自動顯示）       |
| **重啟服務** | 點擊 **🔄 重啟服務按鈕**（會短暫中斷後自動恢復）          |


### 1.3 狀態指示燈


| 狀態      | 顏色   | 說明                 |
| ------- | ---- | ------------------ |
| **未連接** | 灰色   | 尚未連接到機械臂，請檢查網路與電源  |
| **已就緒** | 綠色   | 機械臂已連線，可以下達指令      |
| **分析中** | 紅色閃爍 | 系統正在理解您的指令         |
| **執行中** | 黃色閃爍 | 機械臂正在運動中           |
| **錯誤**  | 紅色   | 指令執行失敗，請檢查輸入或機械臂狀態 |
| **報警**  | 紅色   | 機械臂處於報警狀態，需先清除報警   |


---

## 二、機械臂報警處理

### 2.1 報警徵兆

- 頁面頂部出現紅色 **⚠️ 機械臂報警中** 橫幅
- 狀態顯示為 **報警**（紅色）
- 機械臂停止所有運動

### 2.2 報警清除步驟

1. **先排除報警原因**（如碰撞、過載、緊急停止被觸發等）
2. **先点击重啟服務重启页面🔄**
3. 點擊頁面中的 **「清除報警」按鈕⚠️**
4. 系統會自動重連機械臂並清除報警狀態
5. 狀態恢復為 **「已就緒」** 即可繼續操作

> 若清除報警失敗，請檢查：
>
> - 機械臂電源是否開啟
> - 網路連線是否正常
> - 示教器上是否已切換到 **TCP/IP 遠程控制模式**
> - 29999 / 30004 連接埠是否被佔用

### 2.3 緊急停止

- 實際緊急情況請優先使用實體緊急停止按鈕

---

## 三、可用指令大全

### 3.1 單步直線移動（move_relative_linear）

沿 X / Y / Z 軸正方向或負方向移動指定的距離。

**範例指令：**


| 指令                 | 效果              |
| ------------------ | --------------- |
| `抬高 50 毫米`         | 沿 Z 正方向移動 50mm  |
| `下降 30 毫米`         | 沿 Z 負方向移動 30mm  |
| `向 X 正方向移動 100 毫米` | 沿 X 正方向移動 100mm |
| `向左 50`            | 向左移動 50mm       |
| `X+200`            | 沿 X 正方向移動 200mm |


**安全限制：**

- 單次移動距離：**0.1 ~ 200mm**
- 速度：**1% ~ 50%**（預設 30%）

### 3.2 多步順序移動（move_relative_sequence）

在一條指令中依序執行多段直線移動。

**範例指令：**


| 指令                     | 效果                      |
| ---------------------- | ----------------------- |
| `先 X+50mm，再 Y+50mm`    | 先沿 X 走 50mm，再沿 Y 走 50mm |
| `先抬高 10，再向右 20，再向前 30` | 依序執行三段移動                |


**安全限制：**

- 步驟數：**2 ~ 10 步**
- 每一步距離：**0.1 ~ 200mm**

### 3.3 移動到命名點（move_to_named_point）

移動到預先設定好的位置點。

**可用點位：**


| 名稱             | 說明      |
| -------------- | ------- |
| `home`（原點）     | 機械臂原點位置 |
| `A 點`（point_a） | 預設位置 A  |
| `B 點`（point_b） | 預設位置 B  |
| `C`            | 預設位置 C  |


**範例指令：**


| 指令        | 效果      |
| --------- | ------- |
| `去 A 點`   | 移動到 A 點 |
| `回到 home` | 回到原點    |
| `移動到 B 點` | 移動到 B 點 |


### 3.4 兩點間移動（move_between_points）

從一個命名點移動到另一個命名點。

**範例指令：**


| 指令             | 效果            |
| -------------- | ------------- |
| `從 A 點移動到 B 點` | 從 A 點到 B 點    |
| `先去 A 點再去 B 點` | 依序到 A 點再到 B 點 |
| `從 home 到 A 點` | 從原點到 A 點      |


### 3.5 複合運動路線（motion_pattern）

執行預設的固定軌跡路線（執行完後回到起點）。


| 路線                | 範例指令                | 說明       |
| ----------------- | ------------------- | -------- |
| `circle_xy`       | `畫圓`、`在桌面上畫一個圓`     | XY 平面畫圓  |
| `circle_xz`       | `XZ 畫圓`             | XZ 平面畫圓  |
| `circle_yz`       | `YZ 畫圓`             | YZ 平面畫圓  |
| `arc_xy`          | `走一段圓弧`             | XY 平面畫弧  |
| `square_xy`       | `走一個方形`             | 正方形路線    |
| `rectangle_xy`    | `走矩形路線`             | 矩形路線     |
| `triangle_xy`     | `走三角形路線`            | 三角形路線    |
| `diamond_xy`      | `走菱形`               | 菱形路線     |
| `cross_xy`        | `走十字形`              | 十字路線     |
| `zigzag_x`        | `走鋸齒路線`             | 鋸齒（折線）路線 |
| `wave_y`          | `走波浪路線`             | 波浪路線     |
| `u_shape_xy`      | `走 U 型路線`           | U 型軌跡    |
| `l_shape_xyz`     | `走 L 型路線`           | L 型軌跡    |
| `box_outline`     | `走箱體輪廓`             | 立體方框輪廓   |
| `spiral_z`        | `走三維螺旋`             | 立體螺旋上升   |
| `xyz_step_return` | `先 X 再 Y 再 Z 再回到原點` | 三軸步進後返回  |


**尺寸參數安全限制：**

- `size_mm`：**5 ~ 200mm**（預設 50）
- `radius_mm`：**5 ~ 100mm**（預設 30，適用圓、圓弧、螺旋）
- `height_mm`：**-200 ~ 200mm**（預設 50）
- `turns`：**1 ~ 5 圈**（預設 2，僅螺旋）

### 3.6 關節旋轉（move_joint）

控制機械臂六個關節（J1~J6）中的單個關節旋轉。

**範例指令：**


| 指令               | 效果           |
| ---------------- | ------------ |
| `J1 正方向旋轉 180 度` | J1 關節正轉 180° |
| `J2 負方向轉 90 度`   | J2 關節反轉 90°  |
| `關節 1 正轉 45 度`   | J1 關節正轉 45°  |


**安全限制：**

- 旋轉角度：**1° ~ 360°**
- 各關節限制範圍（由安全區域設定）：
  - J1：±360°、J2：±360°、J3：±160°
  - J4：±360°、J5：±360°、J6：±360°

### 3.7 速度設定（set_speed）

調整機械臂的全局運動速度。

**範例指令：**


| 指令          | 效果        |
| ----------- | --------- |
| `速度設定為 30%` | 速度設為 30%  |
| `速度調到 20`   | 速度設為 20%  |
| `慢一點`       | 速度降為 20%  |
| `快一點`       | 速度提高為 40% |


**安全限制：** 速度範圍 **1% ~ 50%**

### 3.8 等待（wait）

讓機械臂暫停指定的秒數。

**範例指令：**


| 指令        | 效果       |
| --------- | -------- |
| `等待 2 秒`  | 暫停 2 秒   |
| `停 1.5 秒` | 暫停 1.5 秒 |
| `等一下`     | 暫停 1 秒   |


**安全限制：** 等待時間 **0.1 ~ 10 秒**

### 3.9 讀取位姿（read_pose）

讀取機械臂當前的座標位置。

**範例指令：**


| 指令             | 效果                          |
| -------------- | --------------------------- |
| `讀取當前位置`       | 顯示當前位姿（X, Y, Z, RX, RY, RZ） |
| `記錄當前位置為 home` | 將目前位置儲存為命名點                 |


### 3.10 使能 / 停止


| 指令                 | 效果            |
| ------------------ | ------------- |
| `上使能`              | 開啟機械臂使能（準備運動） |
| `下使能`              | 關閉機械臂使能       |
| `停止` / `停住` / `急停` | 停止當前所有運動      |


---

## 四、安全範圍總覽

### 4.1 工作空間邊界

```
            Y↑
             │
    ┌────────┼────────┐
    │        │        │
    │  工作空間        │
    │  最大半徑 1050mm  │
    │        │        │
    └────────┼────────┘
             │────────────────→ X
            Z↑ （高度 160~1040mm）
```


| 軸    | 最小（mm） | 最大（mm） |
| ---- | ------ | ------ |
| X    | -1000  | 1030   |
| Y    | -970   | 860    |
| Z    | 160    | 1040   |
| 水平半徑 | —      | 1050   |


### 4.2 運動參數限制總表


| 項目                     | 最小值    | 最大值   | 預設值  |
| ---------------------- | ------ | ----- | ---- |
| 單步移動距離                 | 0.1mm  | 200mm | —    |
| 多步步數                   | 2 步    | 10 步  | —    |
| 每步距離                   | 0.1mm  | 200mm | —    |
| 運動速度                   | 1%     | 50%   | 30%  |
| 等待時間                   | 0.1s   | 10s   | —    |
| pattern 尺寸 (size_mm)   | 5mm    | 200mm | 50mm |
| pattern 半徑 (radius_mm) | 5mm    | 100mm | 30mm |
| pattern 高度 (height_mm) | -200mm | 200mm | 50mm |
| 螺旋圈數 (turns)           | 1      | 5     | 2    |
| 關節旋轉角度                 | 1°     | 360°  | —    |


> 超出上述範圍的指令會被系統自動攔截並提示錯誤。

---

## 五、注意事項

1. **首次使用**：確保機械臂電源開啟，網路連通，並切換到 **TCP/IP 遠程控制模式**
2. **速度建議**：初次使用建議用 **10% ~ 20%** 的低速測試，確認動作方向正確後再加快
3. **報警處理**：發生報警時，請先確認機械臂周圍無障礙物再清除報警
4. **暫不支援**：抓取物體、視覺辨識、設定 IO、繪畫創作等功能目前無法使用
5. **環境變數**：伺服器 IP、連接埠等設定可透過 `.env` 檔案配置

