# COBOT AI — 演示话术（一页速查）

> 浏览器打开 Web 页面 → 输入指令 → 机械臂自动执行

---

## 启动方式

```powershell
.\.venv\Scripts\python web_app.py
# 浏览器访问 http://127.0.0.1:5000
```

---

## 支持的动作一览

### 单步移动
| 说法示例 | 效果 |
|----------|------|
| 抬高 / 上升 / 向上 20 毫米 | Z 正方向移动 |
| 下降 / 降低 / 向下 50 毫米 | Z 负方向移动 |
| 向 X / Y 正 / 负方向移动 100 毫米 | 指定轴向移动 |
| 向 Z 正方向移动 50 毫米，速度 20% | 带速度的单步 |

### 多步移动
| 说法示例 | 效果 |
|----------|------|
| 先 x+50mm，再 y+50mm | 两步顺序执行 |
| 先抬高 10mm，然后向 X 正方向移动 20mm，接着向 Y 正方向移动 20mm | 三步顺序 |
| 第一步向 X 正方向移动 30mm，第二步抬高 20mm，第三步向 Y 负方向移动 10mm | 三步顺序 |

### 去命名点
| 说法示例 | 效果 |
|----------|------|
| 去 A 点 / 去 B 点 / 去 point_a | 移动到命名点 |
| 回到 home / 回到原点 / 回到零点 | 回到 home 点 |

### 点到点移动
| 说法示例 | 效果 |
|----------|------|
| 从 A 点移动到 B 点 | A → B |
| 从 home 到 A 点 | home → A |

### 复合轨迹（20 种）
| 说法示例 | 效果 |
|----------|------|
| 画圆 / 走圆弧 / 走方形路线 / 走矩形路线 | 基本几何轨迹 |
| 走三角形 / 走菱形 / 走十字形 / 走锯齿路线 | 特殊轨迹 |
| 走波浪路线 / 走 U 型路线 / 走 L 型路线 | 实用轨迹 |
| 走箱体轮廓 / 走三维螺旋运动 | 立体轨迹 |
| 走 XZ 抬升返回 / 走 YZ 抬升返回 | 抬升返回 |
| 先运动 X+ 再运动 Y+ 再运动 Z+ 再回到原点 | 预置路线 |

> 所有复合轨迹可加尺寸参数，如「画圆 30 毫米」「走方形路线 50 毫米」

### 关节控制（J1～J6）
| 说法示例 | 效果 |
|----------|------|
| J1 正方向旋转 180 度 | J1 正向旋转 180° |
| J2 负方向转 90 度 | J2 负向旋转 90° |
| J3 正转 45 度 | J3 正向旋转 45° |
| J4 反转 30 度 | J4 负向旋转 30° |
| J5+90度，速度 20% | J5 正向 90°，速度 20% |

### 其他
| 说法示例 | Skill | 效果 |
|----------|-------|------|
| 速度设置为 30% / 慢一点 / 快一点 | set_speed | 调全局速度 |
| 等待 2 秒 / 暂停一下 / 停 3 秒 | wait | 暂停 |
| 读取当前位置 / 查询当前位姿 | read_pose | 显示位姿坐标 |
| 记录当前位置为 home / 保存当前位置为 point_a | read_pose | 标定命名点 |
| 停止运动 / 停住 / 下使能 | stop_or_disable | 停止/下使能 |

---

## 安全限制

| 项目 | 上限 |
|------|------|
| 单步移动距离 | 0.1～200 mm |
| 多步步数 | 2～10 步 |
| 运动速度 | ≤ 50%（默认 30%） |
| 等待时间 | 0.1～10 秒 |
| 复合轨迹尺寸 | 5～200 mm |
| 画圆/圆弧/螺旋半径 | 5～100 mm |

---

## 演示推荐顺序

```
抬高 100mm → 先 x+200mm 再 y+200mm → 读取当前位置
→ 去 A 点 → 画圆 100mm → 回到 home → 停止运动
```

## 注意

- 示教器需切换到 **TCP/IP 远程控制**
- 确保急停按钮位置已知
- 标定点位用「记录当前位置为 xxx」或在 `positions.json` 中编辑

## 报警异常处理

当机械臂发生碰撞、逆解算错误等情况时，页面对话框会显示「执行失败: 机械臂运动执行超时」或「未连接机械臂」。按以下步骤恢复：

```
1. 点击标题栏 🔄 按钮 → 确认「重启服务」
   （页面会自动刷新重连，等待 3～5 秒）
2. 点击标题栏 ✅ 按钮 → 「清除报警」
   （后端自动重连机械臂并清除报警状态）
3. 状态灯恢复「已就绪」后，即可继续发送指令
```

> 整个恢复过程不需要打开上位机软件或操作示教器，直接在 Web 页面内完成。
