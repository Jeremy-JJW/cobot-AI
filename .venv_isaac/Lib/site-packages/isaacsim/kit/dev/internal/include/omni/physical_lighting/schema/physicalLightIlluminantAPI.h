//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef PHYSICALLIGHTING_GENERATED_PHYSICALLIGHTILLUMINANTAPI_H
#define PHYSICALLIGHTING_GENERATED_PHYSICALLIGHTILLUMINANTAPI_H

/// \file physicalLighting/physicalLightIlluminantAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// PHYSICALLIGHTILLUMINANTAPI                                                 //
// -------------------------------------------------------------------------- //

/// \class PhysicalLightingPhysicalLightIlluminantAPI
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref PhysicalLightingTokens.
/// So to set an attribute to the value "rightHanded", use PhysicalLightingTokens->rightHanded
/// as the value.
///
class PhysicalLightingPhysicalLightIlluminantAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a PhysicalLightingPhysicalLightIlluminantAPI on UsdPrim \p prim .
    /// Equivalent to PhysicalLightingPhysicalLightIlluminantAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysicalLightingPhysicalLightIlluminantAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a PhysicalLightingPhysicalLightIlluminantAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysicalLightingPhysicalLightIlluminantAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysicalLightingPhysicalLightIlluminantAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    PHYSICALLIGHTING_API
    virtual ~PhysicalLightingPhysicalLightIlluminantAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    PHYSICALLIGHTING_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a PhysicalLightingPhysicalLightIlluminantAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysicalLightingPhysicalLightIlluminantAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    PHYSICALLIGHTING_API
    static PhysicalLightingPhysicalLightIlluminantAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    PHYSICALLIGHTING_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysicalLightIlluminantAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid PhysicalLightingPhysicalLightIlluminantAPI object is returned upon success. 
    /// An invalid (or empty) PhysicalLightingPhysicalLightIlluminantAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    PHYSICALLIGHTING_API
    static PhysicalLightingPhysicalLightIlluminantAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    PHYSICALLIGHTING_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    PHYSICALLIGHTING_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    PHYSICALLIGHTING_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // PHYSICALILLUMINANT 
    // --------------------------------------------------------------------- //
    /// The illuminant spectrum of the light in spectral radiance. The default of "white" specifies that the spectrum
    /// matches the whitepoint of the rendering color space. "blackbody" specifies a blackbody spectrum of temperature
    /// defined by "colorTemperature", "illuminant D" specifies a CIE standard illuminant D spectrum defined by 
    /// "colorTemperature" (for example D65 is 6504K), and "custom" specifies that the spectrum in "physical:customIlluminant"
    /// should be used. "white" and "blackbody" are defined at 5nm intervals over the domain [380nm, 780nm].
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token physical:illuminant = "white"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref PhysicalLightingTokens "Allowed Values" | white, blackbody, illuminant D, custom |
    PHYSICALLIGHTING_API
    UsdAttribute GetPhysicalIlluminantAttr() const;

    /// See GetPhysicalIlluminantAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    PHYSICALLIGHTING_API
    UsdAttribute CreatePhysicalIlluminantAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // PHYSICALCUSTOMILLUMINANT 
    // --------------------------------------------------------------------- //
    /// An array of (nanometer, radiance) pairs specifying the emission spectrum of the light. The pairs must be ordered
    /// such that the wavelength values are monotonically increasing. The wavelengths of the first and last pair in the
    /// array define the bounds of the emission range, outside of which emission is zero. The illuminant may be specified
    /// at arbitrary intervals, and will be resampled to 5nm intervals according to ASTM-E308.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float2[] physical:customIlluminant = []` |
    /// | C++ Type | VtArray<GfVec2f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float2Array |
    PHYSICALLIGHTING_API
    UsdAttribute GetPhysicalCustomIlluminantAttr() const;

    /// See GetPhysicalCustomIlluminantAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    PHYSICALLIGHTING_API
    UsdAttribute CreatePhysicalCustomIlluminantAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
