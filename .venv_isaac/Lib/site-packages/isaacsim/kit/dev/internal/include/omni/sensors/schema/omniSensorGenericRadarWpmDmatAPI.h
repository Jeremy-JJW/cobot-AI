//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORGENERICRADARWPMDMATAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORGENERICRADARWPMDMATAPI_H

/// \file omniSensor/omniSensorGenericRadarWpmDmatAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORGENERICRADARWPMDMATAPI                                           //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorGenericRadarWpmDmatAPI
///
/// API Defines generic radar using WpmDMAT model
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniSensorTokens.
/// So to set an attribute to the value "rightHanded", use OmniSensorTokens->rightHanded
/// as the value.
///
class OmniSensorOmniSensorGenericRadarWpmDmatAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a OmniSensorOmniSensorGenericRadarWpmDmatAPI on UsdPrim \p prim .
    /// Equivalent to OmniSensorOmniSensorGenericRadarWpmDmatAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorGenericRadarWpmDmatAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a OmniSensorOmniSensorGenericRadarWpmDmatAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniSensorOmniSensorGenericRadarWpmDmatAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniSensorOmniSensorGenericRadarWpmDmatAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorGenericRadarWpmDmatAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a OmniSensorOmniSensorGenericRadarWpmDmatAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniSensorOmniSensorGenericRadarWpmDmatAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericRadarWpmDmatAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniSensorGenericRadarWpmDmatAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid OmniSensorOmniSensorGenericRadarWpmDmatAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorGenericRadarWpmDmatAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericRadarWpmDmatAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelName = "RadarWPMDMAT"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelNameAttr() const;

    /// See GetOmniSensorModelNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVERSION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVersion = "0.0.0"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVersionAttr() const;

    /// See GetOmniSensorModelVersionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVersionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVENDOR 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVendor = "NVIDIA"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVendorAttr() const;

    /// See GetOmniSensorModelVendorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVendorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMARKETNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:marketName = "Generic"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorMarketNameAttr() const;

    /// See GetOmniSensorMarketNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorMarketNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORTICKRATE 
    // --------------------------------------------------------------------- //
    /// desired framerate of the radar; Runtime changes not supported, yet.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:tickRate = 20` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorTickRateAttr() const;

    /// See GetOmniSensorTickRateAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorTickRateAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATELEMENTSCOORDSTYPE 
    // --------------------------------------------------------------------- //
    /// sets the desired coordinate system for the output basic elements (not auxiliary data))
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:WpmDmat:elementsCoordsType = "SPHERICAL"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CARTESIAN, SPHERICAL |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatElementsCoordsTypeAttr() const;

    /// See GetOmniSensorWpmDmatElementsCoordsTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatElementsCoordsTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATOUTPUTFRAMEOFREFERENCE 
    // --------------------------------------------------------------------- //
    /// sets the desired frame of reference for all outputs (including auxiliary data)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:WpmDmat:outputFrameOfReference = "SENSOR"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | SENSOR, WORLD, CUSTOM |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatOutputFrameOfReferenceAttr() const;

    /// See GetOmniSensorWpmDmatOutputFrameOfReferenceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatOutputFrameOfReferenceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATCUSTOMFRAMEOFREFERENCETRAFO 
    // --------------------------------------------------------------------- //
    /// used only if outputFrameOfReference = CUSTOM. defines the transformation for the custom frame of reference. structured as [x,y,z,roll,pitch,yaw]
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:WpmDmat:customFrameOfReferenceTrafo = [0, 0, 0, 0, 0, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatCustomFrameOfReferenceTrafoAttr() const;

    /// See GetOmniSensorWpmDmatCustomFrameOfReferenceTrafoAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatCustomFrameOfReferenceTrafoAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATWAVELENGTHMM 
    // --------------------------------------------------------------------- //
    /// operational wavelength of the radar
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmDmat:waveLengthMm = 3.9` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatWaveLengthMmAttr() const;

    /// See GetOmniSensorWpmDmatWaveLengthMmAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatWaveLengthMmAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATTRACETREEDEPTH 
    // --------------------------------------------------------------------- //
    /// max number of bounces to trace
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:WpmDmat:traceTreeDepth = 3` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatTraceTreeDepthAttr() const;

    /// See GetOmniSensorWpmDmatTraceTreeDepthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatTraceTreeDepthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATINSTANCETIMEOFFSETUSEC 
    // --------------------------------------------------------------------- //
    /// time offset of this radar instance to simulate continuous firing in a group of radars
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:WpmDmat:instanceTimeOffsetUsec = 5000` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatInstanceTimeOffsetUsecAttr() const;

    /// See GetOmniSensorWpmDmatInstanceTimeOffsetUsecAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatInstanceTimeOffsetUsecAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMDMATCFARMODE 
    // --------------------------------------------------------------------- //
    /// cfar processing mode
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:WpmDmat:cfarMode = "2D"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | 2D, 4D |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmDmatCfarModeAttr() const;

    /// See GetOmniSensorWpmDmatCfarModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmDmatCfarModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
