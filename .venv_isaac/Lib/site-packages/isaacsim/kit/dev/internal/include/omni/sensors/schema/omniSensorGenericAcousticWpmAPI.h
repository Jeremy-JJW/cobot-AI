//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORGENERICACOUSTICWPMAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORGENERICACOUSTICWPMAPI_H

/// \file omniSensor/omniSensorGenericAcousticWpmAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORGENERICACOUSTICWPMAPI                                            //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorGenericAcousticWpmAPI
///
/// API Defines generic acoustic sensor using WPM model
/// 
/// Default configuration: Single sensor at origin with minimal topology.
/// Users can add more sensor mounts, Rx groups, and firing sequences by applying additional schemas:
/// - Add mounts: prepend apiSchemas = ["OmniSensorWpmAcousticSensorMountAPI:m002", ...]
/// - Add Rx groups: prepend apiSchemas = ["OmniSensorWpmAcousticRxGroupAPI:g002", ...]
/// - Add firing sequences: prepend apiSchemas = ["OmniSensorWpmAcousticFiringSeqAPI:seq002", ...]
/// 
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniSensorTokens.
/// So to set an attribute to the value "rightHanded", use OmniSensorTokens->rightHanded
/// as the value.
///
class OmniSensorOmniSensorGenericAcousticWpmAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a OmniSensorOmniSensorGenericAcousticWpmAPI on UsdPrim \p prim .
    /// Equivalent to OmniSensorOmniSensorGenericAcousticWpmAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorGenericAcousticWpmAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a OmniSensorOmniSensorGenericAcousticWpmAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniSensorOmniSensorGenericAcousticWpmAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniSensorOmniSensorGenericAcousticWpmAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorGenericAcousticWpmAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a OmniSensorOmniSensorGenericAcousticWpmAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniSensorOmniSensorGenericAcousticWpmAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericAcousticWpmAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniSensorGenericAcousticWpmAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid OmniSensorOmniSensorGenericAcousticWpmAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorGenericAcousticWpmAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericAcousticWpmAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelName = "AcousticWPM"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelNameAttr() const;

    /// See GetOmniSensorModelNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVERSION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVersion = "0.0.0"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVersionAttr() const;

    /// See GetOmniSensorModelVersionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVersionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVENDOR 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVendor = "NVIDIA"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVendorAttr() const;

    /// See GetOmniSensorModelVendorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVendorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMARKETNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:marketName = "Generic"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorMarketNameAttr() const;

    /// See GetOmniSensorMarketNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorMarketNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORTICKRATE 
    // --------------------------------------------------------------------- //
    /// desired framerate of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:tickRate = 30` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorTickRateAttr() const;

    /// See GetOmniSensorTickRateAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorTickRateAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICAZSPANDEG 
    // --------------------------------------------------------------------- //
    /// azimuth span in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:azSpanDeg = 90` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticAzSpanDegAttr() const;

    /// See GetOmniSensorWpmAcousticAzSpanDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticAzSpanDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICELSPANDEG 
    // --------------------------------------------------------------------- //
    /// elevation span in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:elSpanDeg = 90` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticElSpanDegAttr() const;

    /// See GetOmniSensorWpmAcousticElSpanDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticElSpanDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICRAYSPERDEG 
    // --------------------------------------------------------------------- //
    /// ray density per degree
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:WpmAcoustic:raysPerDeg = 3` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticRaysPerDegAttr() const;

    /// See GetOmniSensorWpmAcousticRaysPerDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticRaysPerDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICTRACETREEDEPTH 
    // --------------------------------------------------------------------- //
    /// maximum number of ray bounces
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:WpmAcoustic:traceTreeDepth = 2` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticTraceTreeDepthAttr() const;

    /// See GetOmniSensorWpmAcousticTraceTreeDepthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticTraceTreeDepthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICMEMBRANEDIAMETER 
    // --------------------------------------------------------------------- //
    /// membrane diameter in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:membraneDiameter = 0.03` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticMembraneDiameterAttr() const;

    /// See GetOmniSensorWpmAcousticMembraneDiameterAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticMembraneDiameterAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCENTERFREQUENCY 
    // --------------------------------------------------------------------- //
    /// center frequency in Hz
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:centerFrequency = 51200` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCenterFrequencyAttr() const;

    /// See GetOmniSensorWpmAcousticCenterFrequencyAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCenterFrequencyAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICBANDWIDTH 
    // --------------------------------------------------------------------- //
    /// bandwidth in Hz
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:bandwidth = 3000` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticBandwidthAttr() const;

    /// See GetOmniSensorWpmAcousticBandwidthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticBandwidthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICPULSEDURATION 
    // --------------------------------------------------------------------- //
    /// pulse duration in seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:pulseDuration = 0.0025` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticPulseDurationAttr() const;

    /// See GetOmniSensorWpmAcousticPulseDurationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticPulseDurationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICPULSEPOWER 
    // --------------------------------------------------------------------- //
    /// dimensionless pulse power
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:pulsePower = 513.57` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticPulsePowerAttr() const;

    /// See GetOmniSensorWpmAcousticPulsePowerAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticPulsePowerAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICSAMPLEDURATION 
    // --------------------------------------------------------------------- //
    /// time sampling of waveform in seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:sampleDuration = 0.0001024` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticSampleDurationAttr() const;

    /// See GetOmniSensorWpmAcousticSampleDurationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticSampleDurationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSERANGE 
    // --------------------------------------------------------------------- //
    /// Close range threshold in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeRange = 1.4233605` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseRangeAttr() const;

    /// See GetOmniSensorWpmAcousticCloseRangeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseRangeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSERANGEDECAY 
    // --------------------------------------------------------------------- //
    /// Close range decay factor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeRangeDecay = 1.2608873` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseRangeDecayAttr() const;

    /// See GetOmniSensorWpmAcousticCloseRangeDecayAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseRangeDecayAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSEINDIRECTAMPLBASE 
    // --------------------------------------------------------------------- //
    /// Base amplitude for indirect close range reflections
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeIndirectAmplBase = 1.1213053` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseIndirectAmplBaseAttr() const;

    /// See GetOmniSensorWpmAcousticCloseIndirectAmplBaseAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseIndirectAmplBaseAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSEDIRECTAMPLBASE 
    // --------------------------------------------------------------------- //
    /// Base amplitude for direct close range reflections
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeDirectAmplBase = 1.3868006` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseDirectAmplBaseAttr() const;

    /// See GetOmniSensorWpmAcousticCloseDirectAmplBaseAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseDirectAmplBaseAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSEINDIRECTAMPL 
    // --------------------------------------------------------------------- //
    /// Amplitude multiplier for indirect close range reflections
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeIndirectAmpl = 17.638103` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseIndirectAmplAttr() const;

    /// See GetOmniSensorWpmAcousticCloseIndirectAmplAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseIndirectAmplAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICCLOSEDIRECTAMPL 
    // --------------------------------------------------------------------- //
    /// Amplitude multiplier for direct close range reflections
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:closeDirectAmpl = 12.655099` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticCloseDirectAmplAttr() const;

    /// See GetOmniSensorWpmAcousticCloseDirectAmplAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticCloseDirectAmplAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICNOISEMIN 
    // --------------------------------------------------------------------- //
    /// Minimum noise level
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:noiseMin = 0.3` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticNoiseMinAttr() const;

    /// See GetOmniSensorWpmAcousticNoiseMinAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticNoiseMinAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICNOISEMAX 
    // --------------------------------------------------------------------- //
    /// Maximum noise level
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:WpmAcoustic:noiseMax = 1.7` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticNoiseMaxAttr() const;

    /// See GetOmniSensorWpmAcousticNoiseMaxAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticNoiseMaxAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICGAINCOEFFS 
    // --------------------------------------------------------------------- //
    /// Gain polynomial coefficients for signal processing
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:WpmAcoustic:gainCoeffs = [-0.116709456, 1.8119696, -1.3599885, -0.3146348, 2.1916203, -0.33233774, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticGainCoeffsAttr() const;

    /// See GetOmniSensorWpmAcousticGainCoeffsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticGainCoeffsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICDIRECTIVITYCOEFFS 
    // --------------------------------------------------------------------- //
    /// Directivity polynomial coefficients for beam pattern modeling
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:WpmAcoustic:directivityCoeffs = [0.71161604, 0.25792894, -2.1090562, 2.3128457, -1.0783362, 0.38831222, -0.15894862]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticDirectivityCoeffsAttr() const;

    /// See GetOmniSensorWpmAcousticDirectivityCoeffsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticDirectivityCoeffsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICSIGNALMODE 
    // --------------------------------------------------------------------- //
    /// signal mode
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:WpmAcoustic:signalMode = "CHIRP"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CHIRP, AM |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticSignalModeAttr() const;

    /// See GetOmniSensorWpmAcousticSignalModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticSignalModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICENABLEINTERFERENCE 
    // --------------------------------------------------------------------- //
    /// enable interference model
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:WpmAcoustic:enableInterference = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticEnableInterferenceAttr() const;

    /// See GetOmniSensorWpmAcousticEnableInterferenceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticEnableInterferenceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICSENSORMOUNTM001POSITION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float3 omni:sensor:WpmAcoustic:sensorMount:m001:position = (0, 0, 0)` |
    /// | C++ Type | GfVec3f |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float3 |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticSensorMountM001PositionAttr() const;

    /// See GetOmniSensorWpmAcousticSensorMountM001PositionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticSensorMountM001PositionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICSENSORMOUNTM001ROTATION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float3 omni:sensor:WpmAcoustic:sensorMount:m001:rotation = (0, 0, 0)` |
    /// | C++ Type | GfVec3f |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float3 |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticSensorMountM001RotationAttr() const;

    /// See GetOmniSensorWpmAcousticSensorMountM001RotationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticSensorMountM001RotationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICRXGROUPG001RECEIVERINDICES 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omni:sensor:WpmAcoustic:rxGroup:g001:receiverIndices = [0]` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticRxGroupG001ReceiverIndicesAttr() const;

    /// See GetOmniSensorWpmAcousticRxGroupG001ReceiverIndicesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticRxGroupG001ReceiverIndicesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICFIRINGSEQSEQ001EVENTTIMENS 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:WpmAcoustic:firingSeq:seq001:eventTimeNs = [0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticFiringSeqSeq001EventTimeNsAttr() const;

    /// See GetOmniSensorWpmAcousticFiringSeqSeq001EventTimeNsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticFiringSeqSeq001EventTimeNsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICFIRINGSEQSEQ001TXSENSORID 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omni:sensor:WpmAcoustic:firingSeq:seq001:txSensorId = [0]` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticFiringSeqSeq001TxSensorIdAttr() const;

    /// See GetOmniSensorWpmAcousticFiringSeqSeq001TxSensorIdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticFiringSeqSeq001TxSensorIdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICFIRINGSEQSEQ001RXGROUPID 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omni:sensor:WpmAcoustic:firingSeq:seq001:rxGroupId = [0]` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticFiringSeqSeq001RxGroupIdAttr() const;

    /// See GetOmniSensorWpmAcousticFiringSeqSeq001RxGroupIdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticFiringSeqSeq001RxGroupIdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORWPMACOUSTICFIRINGSEQSEQ001CHANNEL 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omni:sensor:WpmAcoustic:firingSeq:seq001:channel = [0]` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorWpmAcousticFiringSeqSeq001ChannelAttr() const;

    /// See GetOmniSensorWpmAcousticFiringSeqSeq001ChannelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorWpmAcousticFiringSeqSeq001ChannelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
