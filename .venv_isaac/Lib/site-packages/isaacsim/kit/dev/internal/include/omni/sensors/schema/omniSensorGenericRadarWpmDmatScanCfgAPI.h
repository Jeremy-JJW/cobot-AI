//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORGENERICRADARWPMDMATSCANCFGAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORGENERICRADARWPMDMATSCANCFGAPI_H

/// \file omniSensor/omniSensorGenericRadarWpmDmatScanCfgAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORGENERICRADARWPMDMATSCANCFGAPI                                    //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI
///
/// API Defines generic radar scan configuration
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniSensorTokens.
/// So to set an attribute to the value "rightHanded", use OmniSensorTokens->rightHanded
/// as the value.
///
class OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::MultipleApplyAPI;

    /// Construct a OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI on UsdPrim \p prim with
    /// name \p name . Equivalent to
    /// OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI::Get(
    ///    prim.GetStage(),
    ///    prim.GetPath().AppendProperty(
    ///        "omni:sensor:WpmDmat:scan:name"));
    ///
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI(
        const UsdPrim& prim=UsdPrim(), const TfToken &name=TfToken())
        : UsdAPISchemaBase(prim, /*instanceName*/ name)
    { }

    /// Construct a OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI on the prim held by \p schemaObj with
    /// name \p name.  Should be preferred over
    /// OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI(schemaObj.GetPrim(), name), as it preserves
    /// SchemaBase state.
    explicit OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI(
        const UsdSchemaBase& schemaObj, const TfToken &name)
        : UsdAPISchemaBase(schemaObj, /*instanceName*/ name)
    { }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes for a given instance name.  Does not
    /// include attributes that may be authored by custom/extended methods of
    /// the schemas involved. The names returned will have the proper namespace
    /// prefix.
    OMNISENSOR_API
    static TfTokenVector
    GetSchemaAttributeNames(bool includeInherited, const TfToken &instanceName);

    /// Returns the name of this multiple-apply schema instance
    TfToken GetName() const {
        return _GetInstanceName();
    }

    /// Return a OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  \p path must be of the format
    /// <path>.omni:sensor:WpmDmat:scan:name .
    ///
    /// This is shorthand for the following:
    ///
    /// \code
    /// TfToken name = SdfPath::StripNamespace(path.GetToken());
    /// OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI(
    ///     stage->GetPrimAtPath(path.GetPrimPath()), name);
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);

    /// Return a OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI with name \p name holding the
    /// prim \p prim. Shorthand for OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI(prim, name);
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI
    Get(const UsdPrim &prim, const TfToken &name);

    /// Return a vector of all named instances of OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI on the 
    /// given \p prim.
    OMNISENSOR_API
    static std::vector<OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI>
    GetAll(const UsdPrim &prim);

    /// Checks if the given name \p baseName is the base name of a property
    /// of OmniSensorGenericRadarWpmDmatScanCfgAPI.
    OMNISENSOR_API
    static bool
    IsSchemaPropertyBaseName(const TfToken &baseName);

    /// Checks if the given path \p path is of an API schema of type
    /// OmniSensorGenericRadarWpmDmatScanCfgAPI. If so, it stores the instance name of
    /// the schema in \p name and returns true. Otherwise, it returns false.
    OMNISENSOR_API
    static bool
    IsOmniSensorGenericRadarWpmDmatScanCfgAPIPath(const SdfPath &path, TfToken *name);

    /// Returns true if this <b>multiple-apply</b> API schema can be applied,
    /// with the given instance name, \p name, to the given \p prim. If this 
    /// schema can not be a applied the prim, this returns false and, if 
    /// provided, populates \p whyNot with the reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, const TfToken &name, 
             std::string *whyNot=nullptr);

    /// Applies this <b>multiple-apply</b> API schema to the given \p prim 
    /// along with the given instance name, \p name. 
    /// 
    /// This information is stored by adding "OmniSensorGenericRadarWpmDmatScanCfgAPI:<i>name</i>" 
    /// to the token-valued, listOp metadata \em apiSchemas on the prim.
    /// For example, if \p name is 'instance1', the token 
    /// 'OmniSensorGenericRadarWpmDmatScanCfgAPI:instance1' is added to 'apiSchemas'.
    /// 
    /// \return A valid OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for 
    /// conditions resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericRadarWpmDmatScanCfgAPI 
    Apply(const UsdPrim &prim, const TfToken &name);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // RAYSPERDEG 
    // --------------------------------------------------------------------- //
    /// ray tracing density.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float raysPerDeg = 8` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRaysPerDegAttr() const;

    /// See GetRaysPerDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRaysPerDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TIMEOFFSETUSEC 
    // --------------------------------------------------------------------- //
    /// time offset of the scan from the frame time to simulate non-equidistant scans
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint timeOffsetUsec = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetTimeOffsetUsecAttr() const;

    /// See GetTimeOffsetUsecAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTimeOffsetUsecAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // POWERFACTOR 
    // --------------------------------------------------------------------- //
    /// power factor for the scan tx power
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float powerFactor = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetPowerFactorAttr() const;

    /// See GetPowerFactorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreatePowerFactorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // BINSFROMSPEC 
    // --------------------------------------------------------------------- //
    /// deduce number of bins from spec parameters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool binsFromSpec = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetBinsFromSpecAttr() const;

    /// See GetBinsFromSpecAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateBinsFromSpecAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ENANGALIASING 
    // --------------------------------------------------------------------- //
    /// enable angular aliasing
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool enAngAliasing = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetEnAngAliasingAttr() const;

    /// See GetEnAngAliasingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateEnAngAliasingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ENABLERANGEALIASING 
    // --------------------------------------------------------------------- //
    /// enable range aliasing
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool enableRangeAliasing = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetEnableRangeAliasingAttr() const;

    /// See GetEnableRangeAliasingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateEnableRangeAliasingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DETVALFROMBINIDX 
    // --------------------------------------------------------------------- //
    /// calculate detection value from bin index of the detection peak
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool detValFromBinIdx = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetDetValFromBinIdxAttr() const;

    /// See GetDetValFromBinIdxAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDetValFromBinIdxAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ELEVMODE 
    // --------------------------------------------------------------------- //
    /// elevation mode
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token elevMode = "FULL_EL"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | NO_EL, POS_EL, FULL_EL |
    OMNISENSOR_API
    UsdAttribute GetElevModeAttr() const;

    /// See GetElevModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateElevModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DATACUBEMODE 
    // --------------------------------------------------------------------- //
    /// datacube processing mode
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token datacubeMode = "FAST"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | FAST, WAVEFORM |
    OMNISENSOR_API
    UsdAttribute GetDatacubeModeAttr() const;

    /// See GetDatacubeModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDatacubeModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // WAVEFORMMODE 
    // --------------------------------------------------------------------- //
    /// waveform processing mode: 0=per-contribution, 1=per-bin, 2+=reserved for future modes
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint waveformMode = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetWaveformModeAttr() const;

    /// See GetWaveformModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateWaveformModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // MAXRANGEM 
    // --------------------------------------------------------------------- //
    /// max detection range of the scan in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float maxRangeM = 200` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetMaxRangeMAttr() const;

    /// See GetMaxRangeMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateMaxRangeMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // MAXAZANGDEG 
    // --------------------------------------------------------------------- //
    /// max azimuth angle of the scan in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float maxAzAngDeg = 66` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetMaxAzAngDegAttr() const;

    /// See GetMaxAzAngDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateMaxAzAngDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // MAXELANGDEG 
    // --------------------------------------------------------------------- //
    /// max elevation angle of the scan in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float maxElAngDeg = 20` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetMaxElAngDegAttr() const;

    /// See GetMaxElAngDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateMaxElAngDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RANGERESM 
    // --------------------------------------------------------------------- //
    /// range resolution of the scan in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rangeResM = 0.4` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRangeResMAttr() const;

    /// See GetRangeResMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRangeResMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // VELRESMPS 
    // --------------------------------------------------------------------- //
    /// velocity resolution of the scan in meters per second
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float velResMps = 0.147` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetVelResMpsAttr() const;

    /// See GetVelResMpsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateVelResMpsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // BOREAZRESDEG 
    // --------------------------------------------------------------------- //
    /// azimuth resolution of the scan in degrees at bore sight
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float boreAzResDeg = 1.3` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetBoreAzResDegAttr() const;

    /// See GetBoreAzResDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateBoreAzResDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // BOREELRESDEG 
    // --------------------------------------------------------------------- //
    /// elavation resolution of the scan in degrees at bore sight
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float boreElResDeg = 5` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetBoreElResDegAttr() const;

    /// See GetBoreElResDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateBoreElResDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RBINS 
    // --------------------------------------------------------------------- //
    /// number of range bins of the scan
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint rBins = 112` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetRBinsAttr() const;

    /// See GetRBinsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRBinsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // VBINS 
    // --------------------------------------------------------------------- //
    /// number of velocity bins of the scan
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint vBins = 160` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetVBinsAttr() const;

    /// See GetVBinsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateVBinsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // AZBINS 
    // --------------------------------------------------------------------- //
    /// number of azimuth bins of the scan
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint azBins = 12` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetAzBinsAttr() const;

    /// See GetAzBinsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateAzBinsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ELBINS 
    // --------------------------------------------------------------------- //
    /// number of elevation bins of the scan
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint elBins = 2` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetElBinsAttr() const;

    /// See GetElBinsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateElBinsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // SLOWTIMESAMPLES 
    // --------------------------------------------------------------------- //
    /// slow-time number of samples
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint slowTimeSamples = 379` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetSlowTimeSamplesAttr() const;

    /// See GetSlowTimeSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateSlowTimeSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // FASTTIMESAMPLES 
    // --------------------------------------------------------------------- //
    /// fast-time number of samples
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint fastTimeSamples = 625` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetFastTimeSamplesAttr() const;

    /// See GetFastTimeSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateFastTimeSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // AZIMUTHPADDINGSAMPLES 
    // --------------------------------------------------------------------- //
    /// number of zero-padding samples in azimuth
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint azimuthPaddingSamples = 128` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetAzimuthPaddingSamplesAttr() const;

    /// See GetAzimuthPaddingSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateAzimuthPaddingSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ELEVATIONPADDINGSAMPLES 
    // --------------------------------------------------------------------- //
    /// number of zero-padding samples in elevation
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint elevationPaddingSamples = 32` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetElevationPaddingSamplesAttr() const;

    /// See GetElevationPaddingSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateElevationPaddingSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // SLOWTIMEPADDINGSAMPLES 
    // --------------------------------------------------------------------- //
    /// number of zero-padding samples in slow-time
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint slowTimePaddingSamples = 512` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetSlowTimePaddingSamplesAttr() const;

    /// See GetSlowTimePaddingSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateSlowTimePaddingSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // FASTTIMEPADDINGSAMPLES 
    // --------------------------------------------------------------------- //
    /// number of zero-padding samples in fast-time
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint fastTimePaddingSamples = 1024` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetFastTimePaddingSamplesAttr() const;

    /// See GetFastTimePaddingSamplesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateFastTimePaddingSamplesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CARRIERFREQUENCY 
    // --------------------------------------------------------------------- //
    /// carrier frequency in Hz
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float carrierFrequency = 77000000000` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetCarrierFrequencyAttr() const;

    /// See GetCarrierFrequencyAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCarrierFrequencyAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CHIRPDURATION 
    // --------------------------------------------------------------------- //
    /// chirp duration in seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float chirpDuration = 0.000025` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetChirpDurationAttr() const;

    /// See GetChirpDurationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateChirpDurationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CHIRPBANDWIDTH 
    // --------------------------------------------------------------------- //
    /// chirp bandwidth in Hz
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float chirpBandwidth = 375000000` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetChirpBandwidthAttr() const;

    /// See GetChirpBandwidthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateChirpBandwidthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CHIRPREPETITIONTIME 
    // --------------------------------------------------------------------- //
    /// chirp repetition time (>= chirp duration) in seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float chirpRepetitionTime = 0.000035` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetChirpRepetitionTimeAttr() const;

    /// See GetChirpRepetitionTimeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateChirpRepetitionTimeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // FREQUENCYSTEPBETWEENCHIRPS 
    // --------------------------------------------------------------------- //
    /// frequency step between chirps in Hz
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float frequencyStepBetweenChirps = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetFrequencyStepBetweenChirpsAttr() const;

    /// See GetFrequencyStepBetweenChirpsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateFrequencyStepBetweenChirpsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // SUPPORTFACTOR 
    // --------------------------------------------------------------------- //
    /// support factor in units of main lobe width (1 = one main lobe)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float supportFactor = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetSupportFactorAttr() const;

    /// See GetSupportFactorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateSupportFactorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // WINDOWSLOWTIME 
    // --------------------------------------------------------------------- //
    /// Window kind for slow time (Doppler)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token windowSlowTime = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetWindowSlowTimeAttr() const;

    /// See GetWindowSlowTimeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateWindowSlowTimeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // WINDOWFASTTIME 
    // --------------------------------------------------------------------- //
    /// Window kind for fast time (range)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token windowFastTime = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetWindowFastTimeAttr() const;

    /// See GetWindowFastTimeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateWindowFastTimeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXANTENNAGAINMODE 
    // --------------------------------------------------------------------- //
    /// antenna gain profile for Tx element
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token txAntennaGainMode = "COSINE_FALLOFF"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CONSTANT, COSINE_FALLOFF, CUSTOM, PATCH_ARRAY_MODEL |
    OMNISENSOR_API
    UsdAttribute GetTxAntennaGainModeAttr() const;

    /// See GetTxAntennaGainModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAntennaGainModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXARRAYAZIMUTHELEMENTS 
    // --------------------------------------------------------------------- //
    /// Tx: number of azimuth elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint txArrayAzimuthElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetTxArrayAzimuthElementsAttr() const;

    /// See GetTxArrayAzimuthElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxArrayAzimuthElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXARRAYELEVATIONELEMENTS 
    // --------------------------------------------------------------------- //
    /// Tx: number of elevation elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint txArrayElevationElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetTxArrayElevationElementsAttr() const;

    /// See GetTxArrayElevationElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxArrayElevationElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXAZIMUTHELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Tx: azimuth element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float txAzimuthElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetTxAzimuthElementSpacingAttr() const;

    /// See GetTxAzimuthElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAzimuthElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXELEVATIONELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Tx: elevation element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float txElevationElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetTxElevationElementSpacingAttr() const;

    /// See GetTxElevationElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxElevationElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXWINDOWAZIMUTH 
    // --------------------------------------------------------------------- //
    /// Tx: window kind for azimuth
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token txWindowAzimuth = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetTxWindowAzimuthAttr() const;

    /// See GetTxWindowAzimuthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxWindowAzimuthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXWINDOWELEVATION 
    // --------------------------------------------------------------------- //
    /// Tx: window kind for elevation
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token txWindowElevation = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetTxWindowElevationAttr() const;

    /// See GetTxWindowElevationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxWindowElevationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXAZIMUTHSTEERINGANGLEDEG 
    // --------------------------------------------------------------------- //
    /// Tx: azimuth steering angle in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float txAzimuthSteeringAngleDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetTxAzimuthSteeringAngleDegAttr() const;

    /// See GetTxAzimuthSteeringAngleDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAzimuthSteeringAngleDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXELEVATIONSTEERINGANGLEDEG 
    // --------------------------------------------------------------------- //
    /// Tx: elevation steering angle in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float txElevationSteeringAngleDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetTxElevationSteeringAngleDegAttr() const;

    /// See GetTxElevationSteeringAngleDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxElevationSteeringAngleDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXANTENNAGAINMODE 
    // --------------------------------------------------------------------- //
    /// antenna gain profile for Rx element
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token rxAntennaGainMode = "COSINE_FALLOFF"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CONSTANT, COSINE_FALLOFF, CUSTOM, PATCH_ARRAY_MODEL |
    OMNISENSOR_API
    UsdAttribute GetRxAntennaGainModeAttr() const;

    /// See GetRxAntennaGainModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAntennaGainModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXARRAYAZIMUTHELEMENTS 
    // --------------------------------------------------------------------- //
    /// Rx: number of azimuth elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint rxArrayAzimuthElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetRxArrayAzimuthElementsAttr() const;

    /// See GetRxArrayAzimuthElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxArrayAzimuthElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXARRAYELEVATIONELEMENTS 
    // --------------------------------------------------------------------- //
    /// Rx: number of elevation elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint rxArrayElevationElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetRxArrayElevationElementsAttr() const;

    /// See GetRxArrayElevationElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxArrayElevationElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXAZIMUTHELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Rx: azimuth element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rxAzimuthElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRxAzimuthElementSpacingAttr() const;

    /// See GetRxAzimuthElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAzimuthElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXELEVATIONELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Rx: elevation element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rxElevationElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRxElevationElementSpacingAttr() const;

    /// See GetRxElevationElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxElevationElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXWINDOWAZIMUTH 
    // --------------------------------------------------------------------- //
    /// Rx: window kind for azimuth
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token rxWindowAzimuth = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetRxWindowAzimuthAttr() const;

    /// See GetRxWindowAzimuthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxWindowAzimuthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXWINDOWELEVATION 
    // --------------------------------------------------------------------- //
    /// Rx: window kind for elevation
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token rxWindowElevation = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetRxWindowElevationAttr() const;

    /// See GetRxWindowElevationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxWindowElevationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXAZIMUTHSTEERINGANGLEDEG 
    // --------------------------------------------------------------------- //
    /// Rx: azimuth steering angle in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rxAzimuthSteeringAngleDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRxAzimuthSteeringAngleDegAttr() const;

    /// See GetRxAzimuthSteeringAngleDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAzimuthSteeringAngleDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXELEVATIONSTEERINGANGLEDEG 
    // --------------------------------------------------------------------- //
    /// Rx: elevation steering angle in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rxElevationSteeringAngleDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRxElevationSteeringAngleDegAttr() const;

    /// See GetRxElevationSteeringAngleDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxElevationSteeringAngleDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREARRAYAZIMUTHELEMENTS 
    // --------------------------------------------------------------------- //
    /// Aperture: number of azimuth elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint apertureArrayAzimuthElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetApertureArrayAzimuthElementsAttr() const;

    /// See GetApertureArrayAzimuthElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureArrayAzimuthElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREARRAYELEVATIONELEMENTS 
    // --------------------------------------------------------------------- //
    /// Aperture: number of elevation elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint apertureArrayElevationElements = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetApertureArrayElevationElementsAttr() const;

    /// See GetApertureArrayElevationElementsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureArrayElevationElementsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREAZIMUTHELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Aperture: azimuth element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float apertureAzimuthElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetApertureAzimuthElementSpacingAttr() const;

    /// See GetApertureAzimuthElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureAzimuthElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREELEVATIONELEMENTSPACING 
    // --------------------------------------------------------------------- //
    /// Aperture: elevation element spacing in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float apertureElevationElementSpacing = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetApertureElevationElementSpacingAttr() const;

    /// See GetApertureElevationElementSpacingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureElevationElementSpacingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREWINDOWAZIMUTH 
    // --------------------------------------------------------------------- //
    /// Aperture: window kind for azimuth
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token apertureWindowAzimuth = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetApertureWindowAzimuthAttr() const;

    /// See GetApertureWindowAzimuthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureWindowAzimuthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // APERTUREWINDOWELEVATION 
    // --------------------------------------------------------------------- //
    /// Aperture: window kind for elevation
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token apertureWindowElevation = "RECT"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RECT, HANN, HAMMING, BLACKMAN |
    OMNISENSOR_API
    UsdAttribute GetApertureWindowElevationAttr() const;

    /// See GetApertureWindowElevationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateApertureWindowElevationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARRNT 
    // --------------------------------------------------------------------- //
    /// cfar number of test cells for the scan in range dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarRnT = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarRnTAttr() const;

    /// See GetCfarRnTAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarRnTAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARRNG 
    // --------------------------------------------------------------------- //
    /// cfar number of guard cells for the scan in range dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarRnG = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarRnGAttr() const;

    /// See GetCfarRnGAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarRnGAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARVNT 
    // --------------------------------------------------------------------- //
    /// cfar number of test cells for the scan in velocity dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarVnT = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarVnTAttr() const;

    /// See GetCfarVnTAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarVnTAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARVNG 
    // --------------------------------------------------------------------- //
    /// cfar number of guard cells for the scan in velocity dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarVnG = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarVnGAttr() const;

    /// See GetCfarVnGAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarVnGAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARAZNT 
    // --------------------------------------------------------------------- //
    /// cfar number of test cells for the scan in azimuth dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarAznT = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarAznTAttr() const;

    /// See GetCfarAznTAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarAznTAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARAZNG 
    // --------------------------------------------------------------------- //
    /// cfar number of guard cells for the scan in azimuth dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarAznG = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarAznGAttr() const;

    /// See GetCfarAznGAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarAznGAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARELNT 
    // --------------------------------------------------------------------- //
    /// cfar number of test cells for the scan in elevation dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarElnT = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarElnTAttr() const;

    /// See GetCfarElnTAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarElnTAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARELNG 
    // --------------------------------------------------------------------- //
    /// cfar number of guard cells for the scan in elevation dimension
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint cfarElnG = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetCfarElnGAttr() const;

    /// See GetCfarElnGAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarElnGAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARMINVAL 
    // --------------------------------------------------------------------- //
    /// minimum energy value in a bin to be used in cfar detection
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float cfarMinVal = 7e-17` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetCfarMinValAttr() const;

    /// See GetCfarMinValAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarMinValAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFAROFFSET 
    // --------------------------------------------------------------------- //
    /// cfar threshold offset value (multiplier)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float cfarOffset = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetCfarOffsetAttr() const;

    /// See GetCfarOffsetAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarOffsetAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARNOISEMEAN 
    // --------------------------------------------------------------------- //
    /// noise mean value for cfar detection
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float cfarNoiseMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetCfarNoiseMeanAttr() const;

    /// See GetCfarNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // CFARNOISESDEV 
    // --------------------------------------------------------------------- //
    /// noise standard deviation value for cfar detection
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float cfarNoiseSDev = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetCfarNoiseSDevAttr() const;

    /// See GetCfarNoiseSDevAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateCfarNoiseSDevAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // MAXVELMPSSEQUENCE 
    // --------------------------------------------------------------------- //
    /// max unambiguous velocity sequence of the scan in meters per second
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] maxVelMpsSequence = [50, 55]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetMaxVelMpsSequenceAttr() const;

    /// See GetMaxVelMpsSequenceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateMaxVelMpsSequenceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RCSTUNINGCOEFFICIENTS 
    // --------------------------------------------------------------------- //
    /// RCS tuning polynomial coefficients. 0: detections with RCS in dBsm lower than this threshold will be removed; 1: multiplied with RCS before conversion to dBsm. Set to 1 if no change is needed; 2: standard deviation of rcs noise in dBsm. Set to 0 if no noise is needed.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] rcsTuningCoefficients = [-12, 150, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetRcsTuningCoefficientsAttr() const;

    /// See GetRcsTuningCoefficientsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRcsTuningCoefficientsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // AZIMUTHRADNOISEMEAN 
    // --------------------------------------------------------------------- //
    /// mean value of the azimuth noise in radians
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float azimuthRadNoiseMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetAzimuthRadNoiseMeanAttr() const;

    /// See GetAzimuthRadNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateAzimuthRadNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // AZIMUTHRADNOISESDEV 
    // --------------------------------------------------------------------- //
    /// standard deviation of the azimuth noise in radians
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float azimuthRadNoiseSDev = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetAzimuthRadNoiseSDevAttr() const;

    /// See GetAzimuthRadNoiseSDevAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateAzimuthRadNoiseSDevAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RANGENOISEMEAN 
    // --------------------------------------------------------------------- //
    /// mean value of the range noise in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rangeNoiseMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRangeNoiseMeanAttr() const;

    /// See GetRangeNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRangeNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RANGENOISESDEV 
    // --------------------------------------------------------------------- //
    /// standard deviation of the range noise in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float rangeNoiseSDev = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetRangeNoiseSDevAttr() const;

    /// See GetRangeNoiseSDevAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRangeNoiseSDevAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // VELOCITYNOISEMEAN 
    // --------------------------------------------------------------------- //
    /// mean value of the velocity noise in meters per second
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float velocityNoiseMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetVelocityNoiseMeanAttr() const;

    /// See GetVelocityNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateVelocityNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // VELOCITYNOISESDEV 
    // --------------------------------------------------------------------- //
    /// standard deviation of the velocity noise in meters per second
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float velocityNoiseSDev = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetVelocityNoiseSDevAttr() const;

    /// See GetVelocityNoiseSDevAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateVelocityNoiseSDevAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ELEVATIONRADNOISEMEAN 
    // --------------------------------------------------------------------- //
    /// mean value of the elevation noise in radians
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float elevationRadNoiseMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetElevationRadNoiseMeanAttr() const;

    /// See GetElevationRadNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateElevationRadNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // ELEVATIONRADNOISESDEV 
    // --------------------------------------------------------------------- //
    /// standard deviation of the elevation noise in radians
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float elevationRadNoiseSDev = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetElevationRadNoiseSDevAttr() const;

    /// See GetElevationRadNoiseSDevAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateElevationRadNoiseSDevAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // EXPONENTIALDECAYFACTOR 
    // --------------------------------------------------------------------- //
    /// exponential decay factor for the scan
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float exponentialDecayFactor = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetExponentialDecayFactorAttr() const;

    /// See GetExponentialDecayFactorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateExponentialDecayFactorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DEBUGFORCEDETECTION 
    // --------------------------------------------------------------------- //
    /// force detection mode for debugging
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool debugForceDetection = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetDebugForceDetectionAttr() const;

    /// See GetDebugForceDetectionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDebugForceDetectionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DEBUGFORCERANGE 
    // --------------------------------------------------------------------- //
    /// forced range value for debugging in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float debugForceRange = 20` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetDebugForceRangeAttr() const;

    /// See GetDebugForceRangeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDebugForceRangeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DEBUGFORCEV 
    // --------------------------------------------------------------------- //
    /// forced velocity value for debugging in m/s
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float debugForceV = -40` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetDebugForceVAttr() const;

    /// See GetDebugForceVAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDebugForceVAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DEBUGFORCEAZ 
    // --------------------------------------------------------------------- //
    /// forced azimuth value for debugging in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float debugForceAz = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetDebugForceAzAttr() const;

    /// See GetDebugForceAzAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDebugForceAzAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // DEBUGFORCEEL 
    // --------------------------------------------------------------------- //
    /// forced elevation value for debugging in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float debugForceEl = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetDebugForceElAttr() const;

    /// See GetDebugForceElAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateDebugForceElAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXANTENNAAZDEG 
    // --------------------------------------------------------------------- //
    /// Directivity data for CUSTOM antenna gain mode. Vector of azimuth angles in degrees for the Rx antenna gain pattern grid. Must be ordered lowest to highest.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] rxAntennaAzDeg = [-66, 66]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetRxAntennaAzDegAttr() const;

    /// See GetRxAntennaAzDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAntennaAzDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXANTENNAELDEG 
    // --------------------------------------------------------------------- //
    /// Directivity data for CUSTOM antenna gain mode. Vector of elevation angles in degrees for the Rx antenna gain pattern grid. Must be ordered lowest to highest.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] rxAntennaElDeg = [-20, 20]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetRxAntennaElDegAttr() const;

    /// See GetRxAntennaElDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAntennaElDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXANTENNACOGAIN 
    // --------------------------------------------------------------------- //
    /// Vector of linear co-polarization gain values for Rx antenna. Represents a 2D grid (Az x El) mapped to 1D in row-major order, corresponding to `rxAntennaAzDeg` and `rxAntennaElDeg`.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] rxAntennaCoGain = [0.8, 0.7, 0.6, 0.9]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetRxAntennaCoGainAttr() const;

    /// See GetRxAntennaCoGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAntennaCoGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // RXANTENNACROSSGAIN 
    // --------------------------------------------------------------------- //
    /// Vector of linear cross-polarization gain values for Rx antenna. Represents a 2D grid (Az x El) mapped to 1D in row-major order, corresponding to `rxAntennaAzDeg` and `rxAntennaElDeg`.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] rxAntennaCrossGain = [0.2, 0.3, 0.4, 0.1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetRxAntennaCrossGainAttr() const;

    /// See GetRxAntennaCrossGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateRxAntennaCrossGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXANTENNAAZDEG 
    // --------------------------------------------------------------------- //
    /// Directivity data for CUSTOM antenna gain mode. Vector of azimuth angles in degrees for the Tx antenna gain pattern grid. Must be ordered lowest to highest.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] txAntennaAzDeg = [-66, 66]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetTxAntennaAzDegAttr() const;

    /// See GetTxAntennaAzDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAntennaAzDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXANTENNAELDEG 
    // --------------------------------------------------------------------- //
    /// Directivity data for CUSTOM antenna gain mode. Vector of elevation angles in degrees for the Tx antenna gain pattern grid. Must be ordered lowest to highest.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] txAntennaElDeg = [-20, 20]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetTxAntennaElDegAttr() const;

    /// See GetTxAntennaElDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAntennaElDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXANTENNACOGAIN 
    // --------------------------------------------------------------------- //
    /// Vector of linear co-polarization gain values for Tx antenna. Represents a 2D grid (Az x El) mapped to 1D in row-major order, corresponding to `txAntennaAzDeg` and `txAntennaElDeg`.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] txAntennaCoGain = [0.8, 0.7, 0.6, 0.9]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetTxAntennaCoGainAttr() const;

    /// See GetTxAntennaCoGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAntennaCoGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // TXANTENNACROSSGAIN 
    // --------------------------------------------------------------------- //
    /// Vector of linear cross-polarization gain values for Tx antenna. Represents a 2D grid (Az x El) mapped to 1D in row-major order, corresponding to `txAntennaAzDeg` and `txAntennaElDeg`.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] txAntennaCrossGain = [0.2, 0.3, 0.4, 0.1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetTxAntennaCrossGainAttr() const;

    /// See GetTxAntennaCrossGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateTxAntennaCrossGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
