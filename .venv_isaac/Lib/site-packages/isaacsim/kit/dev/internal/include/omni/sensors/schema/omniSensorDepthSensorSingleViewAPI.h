//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORDEPTHSENSORSINGLEVIEWAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORDEPTHSENSORSINGLEVIEWAPI_H

/// \file omniSensor/omniSensorDepthSensorSingleViewAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORDEPTHSENSORSINGLEVIEWAPI                                         //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorDepthSensorSingleViewAPI
///
/// API Defines generic depth sensor camera using Core model
///
class OmniSensorOmniSensorDepthSensorSingleViewAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a OmniSensorOmniSensorDepthSensorSingleViewAPI on UsdPrim \p prim .
    /// Equivalent to OmniSensorOmniSensorDepthSensorSingleViewAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorDepthSensorSingleViewAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a OmniSensorOmniSensorDepthSensorSingleViewAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniSensorOmniSensorDepthSensorSingleViewAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniSensorOmniSensorDepthSensorSingleViewAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorDepthSensorSingleViewAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a OmniSensorOmniSensorDepthSensorSingleViewAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniSensorOmniSensorDepthSensorSingleViewAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorDepthSensorSingleViewAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniSensorDepthSensorSingleViewAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid OmniSensorOmniSensorDepthSensorSingleViewAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorDepthSensorSingleViewAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorDepthSensorSingleViewAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORENABLED 
    // --------------------------------------------------------------------- //
    /// Enable stereo disparity generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/enabled')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:rtx:post:depthSensor:enabled = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorEnabledAttr() const;

    /// See GetOmniRtxPostDepthSensorEnabledAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORBASELINEMM 
    // --------------------------------------------------------------------- //
    /// Distance between the two cameras in millimeters.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/baselineMM')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:baselineMM = 55` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorBaselineMMAttr() const;

    /// See GetOmniRtxPostDepthSensorBaselineMMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorBaselineMMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORFOCALLENGTHPIXEL 
    // --------------------------------------------------------------------- //
    /// Focal length of the camera in pixels.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/focalLengthPixel')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:focalLengthPixel = 897` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorFocalLengthPixelAttr() const;

    /// See GetOmniRtxPostDepthSensorFocalLengthPixelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorFocalLengthPixelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORSENSORSIZEPIXEL 
    // --------------------------------------------------------------------- //
    /// Size of the camera sensor in pixels.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/sensorSizePixel')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:sensorSizePixel = 1280` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorSensorSizePixelAttr() const;

    /// See GetOmniRtxPostDepthSensorSensorSizePixelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorSensorSizePixelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORMAXDISPARITYPIXEL 
    // --------------------------------------------------------------------- //
    /// Maximum disparity value in pixels.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/maxDisparityPixel')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:maxDisparityPixel = 110` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorMaxDisparityPixelAttr() const;

    /// See GetOmniRtxPostDepthSensorMaxDisparityPixelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorMaxDisparityPixelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORNOISEMEAN 
    // --------------------------------------------------------------------- //
    /// Mean value for noise generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/noiseMean')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:noiseMean = 0.25` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorNoiseMeanAttr() const;

    /// See GetOmniRtxPostDepthSensorNoiseMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorNoiseMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORNOISESIGMA 
    // --------------------------------------------------------------------- //
    /// Standard deviation for noise generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/noiseSigma')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:noiseSigma = 0.25` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorNoiseSigmaAttr() const;

    /// See GetOmniRtxPostDepthSensorNoiseSigmaAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorNoiseSigmaAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORNOISEDOWNSCALEFACTORPIXEL 
    // --------------------------------------------------------------------- //
    /// Factor to downscale noise texture in pixels.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/noiseDownscaleFactorPixel')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:noiseDownscaleFactorPixel = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorNoiseDownscaleFactorPixelAttr() const;

    /// See GetOmniRtxPostDepthSensorNoiseDownscaleFactorPixelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorNoiseDownscaleFactorPixelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORCONFIDENCETHRESHOLD 
    // --------------------------------------------------------------------- //
    /// Threshold for confidence filtering.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/confidenceThreshold')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:confidenceThreshold = 0.7` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorConfidenceThresholdAttr() const;

    /// See GetOmniRtxPostDepthSensorConfidenceThresholdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorConfidenceThresholdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSOROUTLIERREMOVALENABLED 
    // --------------------------------------------------------------------- //
    /// Remove outliers caused by reconstruction and reprojection.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/outlierRemovalEnabled')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:rtx:post:depthSensor:outlierRemovalEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorOutlierRemovalEnabledAttr() const;

    /// See GetOmniRtxPostDepthSensorOutlierRemovalEnabledAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorOutlierRemovalEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORMINDISTANCE 
    // --------------------------------------------------------------------- //
    /// Minimum distance for disparity generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/minDistance')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:minDistance = 0.5` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorMinDistanceAttr() const;

    /// See GetOmniRtxPostDepthSensorMinDistanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorMinDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORMAXDISTANCE 
    // --------------------------------------------------------------------- //
    /// Maximum distance for disparity generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/maxDistance')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:maxDistance = 10000000` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorMaxDistanceAttr() const;

    /// See GetOmniRtxPostDepthSensorMaxDistanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorMaxDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORRGBDEPTHOUTPUTMODE 
    // --------------------------------------------------------------------- //
    /// RGB output mode.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/rgbDepthOutputMode')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:rtx:post:depthSensor:rgbDepthOutputMode = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorRgbDepthOutputModeAttr() const;

    /// See GetOmniRtxPostDepthSensorRgbDepthOutputModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorRgbDepthOutputModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSORSHOWDISTANCE 
    // --------------------------------------------------------------------- //
    /// Show distance for disparity generation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/showDistance')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:rtx:post:depthSensor:showDistance = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorShowDistanceAttr() const;

    /// See GetOmniRtxPostDepthSensorShowDistanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorShowDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNIRTXPOSTDEPTHSENSOREFFECTIVEDISTANCE 
    // --------------------------------------------------------------------- //
    /// Emulates an IR-projection with a distance based confidence fall-off metric.
    /// When enabled, combines distance confidence with texture-based confidence.
    /// Set to 0 to disable IR emulation.
    /// (deprecated carb setting path: '/rtx/post/depthSensor/effectiveDistance')
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:rtx:post:depthSensor:effectiveDistance = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniRtxPostDepthSensorEffectiveDistanceAttr() const;

    /// See GetOmniRtxPostDepthSensorEffectiveDistanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniRtxPostDepthSensorEffectiveDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
