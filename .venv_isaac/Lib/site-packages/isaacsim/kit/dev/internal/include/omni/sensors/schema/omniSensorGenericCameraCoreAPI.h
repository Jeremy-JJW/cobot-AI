//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORGENERICCAMERACOREAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORGENERICCAMERACOREAPI_H

/// \file omniSensor/omniSensorGenericCameraCoreAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORGENERICCAMERACOREAPI                                             //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorGenericCameraCoreAPI
///
/// API Defines generic camera sensor and ISP using Core model
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniSensorTokens.
/// So to set an attribute to the value "rightHanded", use OmniSensorTokens->rightHanded
/// as the value.
///
class OmniSensorOmniSensorGenericCameraCoreAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a OmniSensorOmniSensorGenericCameraCoreAPI on UsdPrim \p prim .
    /// Equivalent to OmniSensorOmniSensorGenericCameraCoreAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorGenericCameraCoreAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a OmniSensorOmniSensorGenericCameraCoreAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniSensorOmniSensorGenericCameraCoreAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniSensorOmniSensorGenericCameraCoreAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorGenericCameraCoreAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a OmniSensorOmniSensorGenericCameraCoreAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniSensorOmniSensorGenericCameraCoreAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericCameraCoreAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniSensorGenericCameraCoreAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid OmniSensorOmniSensorGenericCameraCoreAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorGenericCameraCoreAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericCameraCoreAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelName = "CameraCore"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelNameAttr() const;

    /// See GetOmniSensorModelNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVERSION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVersion = "0.0.0"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVersionAttr() const;

    /// See GetOmniSensorModelVersionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVersionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVENDOR 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVendor = "NVIDIA"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVendorAttr() const;

    /// See GetOmniSensorModelVendorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVendorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMARKETNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:marketName = "Generic"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorMarketNameAttr() const;

    /// See GetOmniSensorMarketNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorMarketNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORTICKRATE 
    // --------------------------------------------------------------------- //
    /// desired framerate of the camera; Runtime changes not supported, yet.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:tickRate = 30` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorTickRateAttr() const;

    /// See GetOmniSensorTickRateAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorTickRateAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONREDBLUESWAP 
    // --------------------------------------------------------------------- //
    /// Indicates whether the first and the third color component shall be swapped during color correction.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorCorrectionRedBlueSwap = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionRedBlueSwapAttr() const;

    /// See GetOmniSensorCoreColorCorrectionRedBlueSwapAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionRedBlueSwapAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONFULLWELLBLACK 
    // --------------------------------------------------------------------- //
    /// Maximal allowed value of the input array until it saturates during color correction processing.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionFullwellBlack = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionFullwellBlackAttr() const;

    /// See GetOmniSensorCoreColorCorrectionFullwellBlackAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionFullwellBlackAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONOUTPUTFLOAT16 
    // --------------------------------------------------------------------- //
    /// Indicates whether the color correction output should be in float16 format.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorCorrectionOutputFloat16 = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionOutputFloat16Attr() const;

    /// See GetOmniSensorCoreColorCorrectionOutputFloat16Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionOutputFloat16Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONOUTPUTFLOAT32 
    // --------------------------------------------------------------------- //
    /// Indicates whether the color correction output should be in float32 format.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorCorrectionOutputFloat32 = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionOutputFloat32Attr() const;

    /// See GetOmniSensorCoreColorCorrectionOutputFloat32Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionOutputFloat32Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONAPPLYSATURATION 
    // --------------------------------------------------------------------- //
    /// Indicates whether black level and fullwell saturation shall be applied during color correction.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorCorrectionApplySaturation = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionApplySaturationAttr() const;

    /// See GetOmniSensorCoreColorCorrectionApplySaturationAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionApplySaturationAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONSENSORRESPONSESCALE 
    // --------------------------------------------------------------------- //
    /// Scalar sensor response multiplier applied to input values.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionSensorResponseScale = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionSensorResponseScaleAttr() const;

    /// See GetOmniSensorCoreColorCorrectionSensorResponseScaleAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionSensorResponseScaleAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONBLACK 
    // --------------------------------------------------------------------- //
    /// Black floor level for color correction processing.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionBlack = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionBlackAttr() const;

    /// See GetOmniSensorCoreColorCorrectionBlackAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionBlackAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXRR 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Red channel contribution to output Red (Red from Red).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixRr = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixRrAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixRrAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixRrAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXRG 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Green channel contribution to output Red (Red from Green).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixRg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixRgAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixRgAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixRgAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXRB 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Blue channel contribution to output Red (Red from Blue).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixRb = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixRbAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixRbAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixRbAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXGR 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Red channel contribution to output Green (Green from Red).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixGr = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixGrAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixGrAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixGrAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXGG 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Green channel contribution to output Green (Green from Green).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixGg = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixGgAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixGgAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixGgAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXGB 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Blue channel contribution to output Green (Green from Blue).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixGb = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixGbAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixGbAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixGbAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXBR 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Red channel contribution to output Blue (Blue from Red).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixBr = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixBrAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixBrAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixBrAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXBG 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Green channel contribution to output Blue (Blue from Green).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixBg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixBgAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixBgAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixBgAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONMATRIXBB 
    // --------------------------------------------------------------------- //
    /// Color correction matrix coefficient: Blue channel contribution to output Blue (Blue from Blue).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:colorCorrectionMatrixBb = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionMatrixBbAttr() const;

    /// See GetOmniSensorCoreColorCorrectionMatrixBbAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionMatrixBbAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORCORRECTIONWHITEBALANCE 
    // --------------------------------------------------------------------- //
    /// White balance correction factors for RGB channels to compensate for light source color temperature.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:core:colorCorrectionWhiteBalance = [1, 1, 1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorCorrectionWhiteBalanceAttr() const;

    /// See GetOmniSensorCoreColorCorrectionWhiteBalanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorCorrectionWhiteBalanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYCFACF00 
    // --------------------------------------------------------------------- //
    /// Top Left pixel RGB coefficients of the 2x2 Color Filter Array pattern.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:core:colorFilterArrayCfaCf00 = [0, 1, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayCfaCf00Attr() const;

    /// See GetOmniSensorCoreColorFilterArrayCfaCf00Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayCfaCf00Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYCFACF01 
    // --------------------------------------------------------------------- //
    /// Top Right pixel RGB coefficients of the 2x2 Color Filter Array pattern.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:core:colorFilterArrayCfaCf01 = [1, 0, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayCfaCf01Attr() const;

    /// See GetOmniSensorCoreColorFilterArrayCfaCf01Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayCfaCf01Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYCFACF10 
    // --------------------------------------------------------------------- //
    /// Bottom Left pixel RGB coefficients of the 2x2 Color Filter Array pattern.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:core:colorFilterArrayCfaCf10 = [0, 0, 1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayCfaCf10Attr() const;

    /// See GetOmniSensorCoreColorFilterArrayCfaCf10Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayCfaCf10Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYCFACF11 
    // --------------------------------------------------------------------- //
    /// Bottom Right pixel RGB coefficients of the 2x2 Color Filter Array pattern.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:core:colorFilterArrayCfaCf11 = [0, 1, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayCfaCf11Attr() const;

    /// See GetOmniSensorCoreColorFilterArrayCfaCf11Attr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayCfaCf11Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYCFASEMANTIC 
    // --------------------------------------------------------------------- //
    /// Semantic description of the Color Filter Array pattern. Valid values: RGGB, GRBG, BGGR, RCCB, CRBC, BCCR, RCCC, CCCR, or CRCC.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:core:colorFilterArrayCfaSemantic = "GRBG"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RGGB, GRBG, BGGR, RCCB, CRBC, BCCR, RCCC, CCCR, CRCC |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayCfaSemanticAttr() const;

    /// See GetOmniSensorCoreColorFilterArrayCfaSemanticAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayCfaSemanticAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYOUTPUTDATATYPEFORMAT 
    // --------------------------------------------------------------------- //
    /// Data type format for the Color Filter Array output. Valid values: FLOAT32 or UINT32.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:core:colorFilterArrayOutputDataTypeFormat = "UINT32"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | FLOAT32, UINT32 |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayOutputDataTypeFormatAttr() const;

    /// See GetOmniSensorCoreColorFilterArrayOutputDataTypeFormatAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayOutputDataTypeFormatAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYMAXIMALVALUE 
    // --------------------------------------------------------------------- //
    /// The maximal value of the internal processing. Default for 24 bits: 16777215, for 20 bits: 1048575, for passthrough: 1.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:colorFilterArrayMaximalValue = 16777215` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayMaximalValueAttr() const;

    /// See GetOmniSensorCoreColorFilterArrayMaximalValueAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayMaximalValueAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYFLIPHORIZONTAL 
    // --------------------------------------------------------------------- //
    /// Enable horizontal mirroring of the Color Filter Array output.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorFilterArrayFlipHorizontal = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayFlipHorizontalAttr() const;

    /// See GetOmniSensorCoreColorFilterArrayFlipHorizontalAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayFlipHorizontalAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOLORFILTERARRAYFLIPVERTICAL 
    // --------------------------------------------------------------------- //
    /// Enable vertical mirroring of the Color Filter Array output.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:colorFilterArrayFlipVertical = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreColorFilterArrayFlipVerticalAttr() const;

    /// See GetOmniSensorCoreColorFilterArrayFlipVerticalAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreColorFilterArrayFlipVerticalAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENOISEDARKSHOTNOISEGAIN 
    // --------------------------------------------------------------------- //
    /// Gain applied to the dark shot noise component during noise simulation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:noiseDarkShotNoiseGain = 10` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNoiseDarkShotNoiseGainAttr() const;

    /// See GetOmniSensorCoreNoiseDarkShotNoiseGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNoiseDarkShotNoiseGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENOISEDARKSHOTNOISESIGMA 
    // --------------------------------------------------------------------- //
    /// Standard deviation (sigma) of the dark shot noise for realistic noise simulation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:core:noiseDarkShotNoiseSigma = 0.5` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNoiseDarkShotNoiseSigmaAttr() const;

    /// See GetOmniSensorCoreNoiseDarkShotNoiseSigmaAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNoiseDarkShotNoiseSigmaAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENOISEHDRCOMBINATIONDATA 
    // --------------------------------------------------------------------- //
    /// Array of HDR region parameters as [gain, exposure_time] pairs defining the gains and exposure times for different HDR regions in noise simulation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float2[] omni:sensor:core:noiseHdrCombinationData = [(5.8, 4000), (58, 8000), (70, 16000)]` |
    /// | C++ Type | VtArray<GfVec2f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float2Array |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNoiseHdrCombinationDataAttr() const;

    /// See GetOmniSensorCoreNoiseHdrCombinationDataAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNoiseHdrCombinationDataAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOMPANDINGALIGNMENT 
    // --------------------------------------------------------------------- //
    /// Alignment of the MSB (Most Significant Bit) of the companding operation, with LSB starting at 0.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:compandingAlignment = 15` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCompandingAlignmentAttr() const;

    /// See GetOmniSensorCoreCompandingAlignmentAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCompandingAlignmentAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOMPANDINGPOSTPEDESTAL 
    // --------------------------------------------------------------------- //
    /// Pedestal value added after applying the companding operation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:compandingPostPedestal = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCompandingPostPedestalAttr() const;

    /// See GetOmniSensorCoreCompandingPostPedestalAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCompandingPostPedestalAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOMPANDINGPREPEDESTAL 
    // --------------------------------------------------------------------- //
    /// Pedestal value added before applying the companding operation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:compandingPrePedestal = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCompandingPrePedestalAttr() const;

    /// See GetOmniSensorCoreCompandingPrePedestalAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCompandingPrePedestalAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECOMPANDINGLINEARCOMPANDCOEFF 
    // --------------------------------------------------------------------- //
    /// Array of companding parameters defining the non-linear compression curve as [input_value, output_value] pairs.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float2[] omni:sensor:core:compandingLinearCompandCoeff = [(0, 0), (512, 512), (1024, 851), (2048, 1415), (4096, 1430), (8192, 1454), (16384, 1494), (32768, 1562), (65536, 1674), (131072, 1860), (262144, 2169), (524288, 2232), (1048576, 2338), (2097152, 2513), (4194304, 2805), (8388608, 3289), (16777215, 4095)]` |
    /// | C++ Type | VtArray<GfVec2f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float2Array |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCompandingLinearCompandCoeffAttr() const;

    /// See GetOmniSensorCoreCompandingLinearCompandCoeffAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCompandingLinearCompandCoeffAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREISPSMODELCAMERAPROGRAM 
    // --------------------------------------------------------------------- //
    /// Base64 encoded ISP (Image Signal Processor) program file for advanced camera image processing.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:ispSmodelCameraProgram = ""` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIspSmodelCameraProgramAttr() const;

    /// See GetOmniSensorCoreIspSmodelCameraProgramAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIspSmodelCameraProgramAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREISPSMODELRAWDATAPADDINGMODE 
    // --------------------------------------------------------------------- //
    /// Raw data padding mode for ISP processing.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:ispSmodelRawDataPaddingMode = "NONE"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    /// | \ref OmniSensorTokens "Allowed Values" | NONE, RIGHT, LEFT |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIspSmodelRawDataPaddingModeAttr() const;

    /// See GetOmniSensorCoreIspSmodelRawDataPaddingModeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIspSmodelRawDataPaddingModeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREISPSMODELSENSORRAWFORMAT 
    // --------------------------------------------------------------------- //
    /// Sensor raw format for ISP processing.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:ispSmodelSensorRawFormat = "RAW8"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    /// | \ref OmniSensorTokens "Allowed Values" | RAW8, RAW10, RAW12, RAW14, RAW20, RAW24 |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIspSmodelSensorRawFormatAttr() const;

    /// See GetOmniSensorCoreIspSmodelSensorRawFormatAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIspSmodelSensorRawFormatAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTFILE 
    // --------------------------------------------------------------------- //
    /// Enable file saving.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionOutputFile = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputFileAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputFileAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputFileAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTFILEEACHFRAMEONEFILE 
    // --------------------------------------------------------------------- //
    /// Indicates whether all frames shall be concatenated into a single file or stored as separate files.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionOutputFileEachFrameOneFile = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputFileEachFrameOneFileAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputFileEachFrameOneFileAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputFileEachFrameOneFileAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTFILEONLYLASTFRAME 
    // --------------------------------------------------------------------- //
    /// Save only the most recent frame. Only works when saving each frame in one file with the 'EachFrameOneFile' option.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionOutputFileOnlyLastFrame = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputFileOnlyLastFrameAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputFileOnlyLastFrameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputFileOnlyLastFrameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTFILEDIRECTORY 
    // --------------------------------------------------------------------- //
    /// Location where output files will be saved.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:introspectionOutputFileDirectory = ""` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputFileDirectoryAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputFileDirectoryAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputFileDirectoryAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTVIZ 
    // --------------------------------------------------------------------- //
    /// Enable texture viewing in the Sensor Visualizer panel.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionOutputViz = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputVizAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputVizAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputVizAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTVIZINPUTCHANNEL 
    // --------------------------------------------------------------------- //
    /// Channel for input texture visualization.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionOutputVizInputChannel = 101` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputVizInputChannelAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputVizInputChannelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputVizInputChannelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTVIZRAWCHANNEL 
    // --------------------------------------------------------------------- //
    /// Channel for RAW texture visualization.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionOutputVizRawChannel = 102` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputVizRawChannelAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputVizRawChannelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputVizRawChannelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONOUTPUTVIZOUTPUTCHANNEL 
    // --------------------------------------------------------------------- //
    /// Channel for output texture visualization.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionOutputVizOutputChannel = 103` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionOutputVizOutputChannelAttr() const;

    /// See GetOmniSensorCoreIntrospectionOutputVizOutputChannelAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionOutputVizOutputChannelAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAW 
    // --------------------------------------------------------------------- //
    /// Inject a RAW input file that will be processed by the ISP.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionInputRaw = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWPATH 
    // --------------------------------------------------------------------- //
    /// Path to the RAW input file.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:introspectionInputRawPath = ""` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawPathAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawPathAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawPathAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWWIDTH 
    // --------------------------------------------------------------------- //
    /// Image Width.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionInputRawWidth = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawWidthAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawWidthAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawWidthAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWHEIGHT 
    // --------------------------------------------------------------------- //
    /// Image Height.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionInputRawHeight = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawHeightAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawHeightAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawHeightAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWSKIPFRONT 
    // --------------------------------------------------------------------- //
    /// Bytes to skip before each frame.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionInputRawSkipFront = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawSkipFrontAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawSkipFrontAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawSkipFrontAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWSKIPBACK 
    // --------------------------------------------------------------------- //
    /// Bytes to skip after each frame.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int omni:sensor:core:introspectionInputRawSkipBack = 0` |
    /// | C++ Type | int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawSkipBackAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawSkipBackAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawSkipBackAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWFILEDATATYPE 
    // --------------------------------------------------------------------- //
    /// Data type of the RGB.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:core:introspectionInputRawFileDataType = "UINT16"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    /// | \ref OmniSensorTokens "Allowed Values" | UINT8, UINT16, UINT32, FLOAT16, FLOAT32 |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawFileDataTypeAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawFileDataTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawFileDataTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTROSPECTIONINPUTRAWLOOP 
    // --------------------------------------------------------------------- //
    /// Indicates whether the input file shall be loop infinitely.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:core:introspectionInputRawLoop = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntrospectionInputRawLoopAttr() const;

    /// See GetOmniSensorCoreIntrospectionInputRawLoopAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntrospectionInputRawLoopAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
