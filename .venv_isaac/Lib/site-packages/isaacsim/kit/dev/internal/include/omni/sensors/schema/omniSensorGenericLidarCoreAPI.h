//
// Copyright 2016 Pixar
//
// Licensed under the terms set forth in the LICENSE.txt file available at
// https://openusd.org/license.
//
#ifndef OMNISENSOR_GENERATED_OMNISENSORGENERICLIDARCOREAPI_H
#define OMNISENSOR_GENERATED_OMNISENSORGENERICLIDARCOREAPI_H

/// \file omniSensor/omniSensorGenericLidarCoreAPI.h

#include "pxr/pxr.h"
#include "./api.h"
#include "pxr/usd/usd/apiSchemaBase.h"
#include "pxr/usd/usd/prim.h"
#include "pxr/usd/usd/stage.h"
#include "./tokens.h"

#include "pxr/base/vt/value.h"

#include "pxr/base/gf/vec3d.h"
#include "pxr/base/gf/vec3f.h"
#include "pxr/base/gf/matrix4d.h"

#include "pxr/base/tf/token.h"
#include "pxr/base/tf/type.h"

PXR_NAMESPACE_OPEN_SCOPE

class SdfAssetPath;

// -------------------------------------------------------------------------- //
// OMNISENSORGENERICLIDARCOREAPI                                              //
// -------------------------------------------------------------------------- //

/// \class OmniSensorOmniSensorGenericLidarCoreAPI
///
/// API Defines generic lidar using Core model
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniSensorTokens.
/// So to set an attribute to the value "rightHanded", use OmniSensorTokens->rightHanded
/// as the value.
///
class OmniSensorOmniSensorGenericLidarCoreAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    /// Construct a OmniSensorOmniSensorGenericLidarCoreAPI on UsdPrim \p prim .
    /// Equivalent to OmniSensorOmniSensorGenericLidarCoreAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniSensorOmniSensorGenericLidarCoreAPI(const UsdPrim& prim=UsdPrim())
        : UsdAPISchemaBase(prim)
    {
    }

    /// Construct a OmniSensorOmniSensorGenericLidarCoreAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniSensorOmniSensorGenericLidarCoreAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniSensorOmniSensorGenericLidarCoreAPI(const UsdSchemaBase& schemaObj)
        : UsdAPISchemaBase(schemaObj)
    {
    }

    /// Destructor.
    OMNISENSOR_API
    virtual ~OmniSensorOmniSensorGenericLidarCoreAPI();

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    OMNISENSOR_API
    static const TfTokenVector &
    GetSchemaAttributeNames(bool includeInherited=true);

    /// Return a OmniSensorOmniSensorGenericLidarCoreAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniSensorOmniSensorGenericLidarCoreAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericLidarCoreAPI
    Get(const UsdStagePtr &stage, const SdfPath &path);


    /// Returns true if this <b>single-apply</b> API schema can be applied to 
    /// the given \p prim. If this schema can not be a applied to the prim, 
    /// this returns false and, if provided, populates \p whyNot with the 
    /// reason it can not be applied.
    /// 
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to 
    /// apply a schema.
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static bool 
    CanApply(const UsdPrim &prim, std::string *whyNot=nullptr);

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniSensorGenericLidarCoreAPI" to the 
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    /// 
    /// \return A valid OmniSensorOmniSensorGenericLidarCoreAPI object is returned upon success. 
    /// An invalid (or empty) OmniSensorOmniSensorGenericLidarCoreAPI object is returned upon 
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions 
    /// resulting in failure. 
    /// 
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    ///
    OMNISENSOR_API
    static OmniSensorOmniSensorGenericLidarCoreAPI 
    Apply(const UsdPrim &prim);

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    OMNISENSOR_API
    UsdSchemaKind _GetSchemaKind() const override;

private:
    // needs to invoke _GetStaticTfType.
    friend class UsdSchemaRegistry;
    OMNISENSOR_API
    static const TfType &_GetStaticTfType();

    static bool _IsTypedSchema();

    // override SchemaBase virtuals.
    OMNISENSOR_API
    const TfType &_GetTfType() const override;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelName = "LidarCore"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelNameAttr() const;

    /// See GetOmniSensorModelNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVERSION 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVersion = "0.0.0"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVersionAttr() const;

    /// See GetOmniSensorModelVersionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVersionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMODELVENDOR 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:modelVendor = "NVIDIA"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorModelVendorAttr() const;

    /// See GetOmniSensorModelVendorAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorModelVendorAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORMARKETNAME 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:marketName = "Generic"` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorMarketNameAttr() const;

    /// See GetOmniSensorMarketNameAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorMarketNameAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORTICKRATE 
    // --------------------------------------------------------------------- //
    /// 
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:tickRate = 10` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorTickRateAttr() const;

    /// See GetOmniSensorTickRateAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorTickRateAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREELEMENTSCOORDSTYPE 
    // --------------------------------------------------------------------- //
    /// sets the desired coordinate system for the output basic elements (not auxiliary data))
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:elementsCoordsType = "SPHERICAL"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CARTESIAN, SPHERICAL |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreElementsCoordsTypeAttr() const;

    /// See GetOmniSensorCoreElementsCoordsTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreElementsCoordsTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREOUTPUTMOTIONCOMPENSATIONSTATE 
    // --------------------------------------------------------------------- //
    /// sets the desired motion compensation state for all outputs (including auxiliary data)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:outputMotionCompensationState = "NONCOMPENSATED"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | NONCOMPENSATED, COMPENSATED |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreOutputMotionCompensationStateAttr() const;

    /// See GetOmniSensorCoreOutputMotionCompensationStateAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreOutputMotionCompensationStateAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREOUTPUTFRAMEOFREFERENCE 
    // --------------------------------------------------------------------- //
    /// sets the desired frame of reference for all outputs (including auxiliary data)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:outputFrameOfReference = "SENSOR"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | SENSOR, WORLD, CUSTOM |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreOutputFrameOfReferenceAttr() const;

    /// See GetOmniSensorCoreOutputFrameOfReferenceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreOutputFrameOfReferenceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECUSTOMFRAMEOFREFERENCETRAFO 
    // --------------------------------------------------------------------- //
    /// used only if outputFrameOfReference = CUSTOM. defines the transformation for the custom frame of reference. structured as [x,y,z,roll,pitch,yaw]
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:customFrameOfReferenceTrafo = [0, 0, 0, 0, 0, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCustomFrameOfReferenceTrafoAttr() const;

    /// See GetOmniSensorCoreCustomFrameOfReferenceTrafoAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCustomFrameOfReferenceTrafoAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORESKIPDROPPINGINVALIDPOINTS 
    // --------------------------------------------------------------------- //
    /// when false the model will drop invalid points
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:Core:skipDroppingInvalidPoints = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreSkipDroppingInvalidPointsAttr() const;

    /// See GetOmniSensorCoreSkipDroppingInvalidPointsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreSkipDroppingInvalidPointsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREACCUMULATEOUTPUTS 
    // --------------------------------------------------------------------- //
    /// when true the model will accumulate the outputs until one scan is complete.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:Core:accumulateOutputs = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreAccumulateOutputsAttr() const;

    /// See GetOmniSensorCoreAccumulateOutputsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreAccumulateOutputsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINSTANTLIDAR 
    // --------------------------------------------------------------------- //
    /// when true the model produce one full scan at frame end timepoint disregarding actual timing and scan rates.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omni:sensor:Core:instantLidar = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreInstantLidarAttr() const;

    /// See GetOmniSensorCoreInstantLidarAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreInstantLidarAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORESCANTYPE 
    // --------------------------------------------------------------------- //
    /// Scanning principle of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:scanType = "ROTARY"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | ROTARY, SOLID_STATE |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreScanTypeAttr() const;

    /// See GetOmniSensorCoreScanTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreScanTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTENSITYPROCESSING 
    // --------------------------------------------------------------------- //
    /// Intensity processing mode
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:intensityProcessing = "NORMALIZATION"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | RAW, NORMALIZATION, CORRECTION |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntensityProcessingAttr() const;

    /// See GetOmniSensorCoreIntensityProcessingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntensityProcessingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREROTATIONDIRECTION 
    // --------------------------------------------------------------------- //
    /// Rotation direction of the sensor -- only for rotary sensors
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:rotationDirection = "CW"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | CW, CCW |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRotationDirectionAttr() const;

    /// See GetOmniSensorCoreRotationDirectionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRotationDirectionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERAYTYPE 
    // --------------------------------------------------------------------- //
    /// Ray type of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:rayType = "IDEALIZED"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | IDEALIZED, GAUSSIAN_BEAM, UNIFORM_BEAM |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRayTypeAttr() const;

    /// See GetOmniSensorCoreRayTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRayTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENEARRANGEM 
    // --------------------------------------------------------------------- //
    /// Minimum range of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:nearRangeM = 0.3` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNearRangeMAttr() const;

    /// See GetOmniSensorCoreNearRangeMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNearRangeMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREFARRANGEM 
    // --------------------------------------------------------------------- //
    /// Maximum range of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:farRangeM = 200` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreFarRangeMAttr() const;

    /// See GetOmniSensorCoreFarRangeMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreFarRangeMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREREFLECTIONPOWERFRACTION 
    // --------------------------------------------------------------------- //
    /// Fraction of the reflection power for multiple returns.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:reflectionPowerFraction = 0.5` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreReflectionPowerFractionAttr() const;

    /// See GetOmniSensorCoreReflectionPowerFractionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreReflectionPowerFractionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORETRANSMISSIONPOWERFRACTION 
    // --------------------------------------------------------------------- //
    /// Fraction of the transmission power for multiple returns.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:transmissionPowerFraction = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreTransmissionPowerFractionAttr() const;

    /// See GetOmniSensorCoreTransmissionPowerFractionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreTransmissionPowerFractionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORESTARTAZIMUTHOFFSETDEG 
    // --------------------------------------------------------------------- //
    /// Start azimuth offset angle of the sensor in degrees -- in sensor market model frame
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:startAzimuthOffsetDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreStartAzimuthOffsetDegAttr() const;

    /// See GetOmniSensorCoreStartAzimuthOffsetDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreStartAzimuthOffsetDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREVALIDSTARTAZIMUTHDEG 
    // --------------------------------------------------------------------- //
    /// Valid start azimuth angle of the sensor in degrees -- in sensor market model frame
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:validStartAzimuthDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreValidStartAzimuthDegAttr() const;

    /// See GetOmniSensorCoreValidStartAzimuthDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreValidStartAzimuthDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREVALIDENDAZIMUTHDEG 
    // --------------------------------------------------------------------- //
    /// Valid end azimuth angle of the sensor in degrees -- in sensor market model frame
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:validEndAzimuthDeg = 360` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreValidEndAzimuthDegAttr() const;

    /// See GetOmniSensorCoreValidEndAzimuthDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreValidEndAzimuthDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMINAZIMUTHROI 
    // --------------------------------------------------------------------- //
    /// Start azimuth point of the ROI part of the pattern -- in sensor market model frame
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:minAzimuthROI = 400` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMinAzimuthROIAttr() const;

    /// See GetOmniSensorCoreMinAzimuthROIAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMinAzimuthROIAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMAXAZIMUTHROI 
    // --------------------------------------------------------------------- //
    /// End azimuth point of the ROI part of the pattern -- in sensor market model frame
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:maxAzimuthROI = 400` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMaxAzimuthROIAttr() const;

    /// See GetOmniSensorCoreMaxAzimuthROIAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMaxAzimuthROIAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGERESOLUTIONM 
    // --------------------------------------------------------------------- //
    /// Range resolution of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:rangeResolutionM = 0.004` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangeResolutionMAttr() const;

    /// See GetOmniSensorCoreRangeResolutionMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangeResolutionMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGEACCURACYM 
    // --------------------------------------------------------------------- //
    /// Range accuracy of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:rangeAccuracyM = 0.02` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangeAccuracyMAttr() const;

    /// See GetOmniSensorCoreRangeAccuracyMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangeAccuracyMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMINDISTBETWEENECHOSM 
    // --------------------------------------------------------------------- //
    /// Minimum distance between two echos in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:minDistBetweenEchosM = 0.4` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMinDistBetweenEchosMAttr() const;

    /// See GetOmniSensorCoreMinDistBetweenEchosMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMinDistBetweenEchosMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGEOFFSETM 
    // --------------------------------------------------------------------- //
    /// Range for which objects are invisible -- useful for sensors inside objects
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:rangeOffsetM = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangeOffsetMAttr() const;

    /// See GetOmniSensorCoreRangeOffsetMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangeOffsetMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREPEAKPOWERW 
    // --------------------------------------------------------------------- //
    /// Peak power per emitter in watts
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:peakPowerW = 10` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCorePeakPowerWAttr() const;

    /// See GetOmniSensorCorePeakPowerWAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCorePeakPowerWAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMINREFLECTANCE 
    // --------------------------------------------------------------------- //
    /// Minimum reflectance value of an object for a guarantueed Pointcloud at minReflectionRange
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:minReflectance = 0.1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMinReflectanceAttr() const;

    /// See GetOmniSensorCoreMinReflectanceAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMinReflectanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMINREFLECTIONRANGEM 
    // --------------------------------------------------------------------- //
    /// Minimum range for a guaranteed detection of an object with minReflectance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:minReflectionRangeM = 200` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMinReflectionRangeMAttr() const;

    /// See GetOmniSensorCoreMinReflectionRangeMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMinReflectionRangeMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREWAVELENGTHNM 
    // --------------------------------------------------------------------- //
    /// Wavelength of the sensor in nm
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:waveLengthNm = 903` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreWaveLengthNmAttr() const;

    /// See GetOmniSensorCoreWaveLengthNmAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreWaveLengthNmAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREPULSETIMENS 
    // --------------------------------------------------------------------- //
    /// Pulse time of the sensor in ns
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:pulseTimeNs = 6` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCorePulseTimeNsAttr() const;

    /// See GetOmniSensorCorePulseTimeNsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCorePulseTimeNsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREFOCUSDISTM 
    // --------------------------------------------------------------------- //
    /// Focus distance of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:focusDistM = 0.15` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreFocusDistMAttr() const;

    /// See GetOmniSensorCoreFocusDistMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreFocusDistMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMSQUARED 
    // --------------------------------------------------------------------- //
    /// M squared value of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:Msquared = 1.4` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMsquaredAttr() const;

    /// See GetOmniSensorCoreMsquaredAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMsquaredAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREBEAMWAISTHORM 
    // --------------------------------------------------------------------- //
    /// Beam waist horizontal of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:beamWaistHorM = 0.002` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreBeamWaistHorMAttr() const;

    /// See GetOmniSensorCoreBeamWaistHorMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreBeamWaistHorMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREBEAMWAISTVERM 
    // --------------------------------------------------------------------- //
    /// Beam waist vertical of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:beamWaistVerM = 0.002` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreBeamWaistVerMAttr() const;

    /// See GetOmniSensorCoreBeamWaistVerMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreBeamWaistVerMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREDIVERGENCEHORDEG 
    // --------------------------------------------------------------------- //
    /// Divergence horizontal of the sensor in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:divergenceHorDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreDivergenceHorDegAttr() const;

    /// See GetOmniSensorCoreDivergenceHorDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreDivergenceHorDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREDIVERGENCEVERDEG 
    // --------------------------------------------------------------------- //
    /// Divergence vertical of the sensor in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:divergenceVerDeg = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreDivergenceVerDegAttr() const;

    /// See GetOmniSensorCoreDivergenceVerDegAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreDivergenceVerDegAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREASPECTRATIO 
    // --------------------------------------------------------------------- //
    /// Aspect ratio of the detector
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:aspectRatio = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreAspectRatioAttr() const;

    /// See GetOmniSensorCoreAspectRatioAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreAspectRatioAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREEFFECTIVEAPERTURESIZEM 
    // --------------------------------------------------------------------- //
    /// Effective aperture size of the sensor in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:effectiveApertureSizeM = 0.01` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreEffectiveApertureSizeMAttr() const;

    /// See GetOmniSensorCoreEffectiveApertureSizeMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreEffectiveApertureSizeMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREQUANTUMEFFICIENCY 
    // --------------------------------------------------------------------- //
    /// Quantum efficiency of the detector
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:quantumEfficiency = 0.9` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreQuantumEfficiencyAttr() const;

    /// See GetOmniSensorCoreQuantumEfficiencyAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreQuantumEfficiencyAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREPIXELPITCH 
    // --------------------------------------------------------------------- //
    /// Pixel pitch of the detector
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:pixelPitch = 40` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCorePixelPitchAttr() const;

    /// See GetOmniSensorCorePixelPitchAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCorePixelPitchAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORECALIBRATIONGAIN 
    // --------------------------------------------------------------------- //
    /// Calibration gain of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:calibrationGain = 40` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreCalibrationGainAttr() const;

    /// See GetOmniSensorCoreCalibrationGainAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreCalibrationGainAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREBITDEPTHRESOLUTION 
    // --------------------------------------------------------------------- //
    /// Bit depth resolution of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:bitDepthResolution = 65536` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreBitDepthResolutionAttr() const;

    /// See GetOmniSensorCoreBitDepthResolutionAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreBitDepthResolutionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORESCANRATEBASEHZ 
    // --------------------------------------------------------------------- //
    /// Scan rate base of the sensor in Hz;
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:scanRateBaseHz = 10` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreScanRateBaseHzAttr() const;

    /// See GetOmniSensorCoreScanRateBaseHzAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreScanRateBaseHzAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREPATTERNFIRINGRATEHZ 
    // --------------------------------------------------------------------- //
    /// Pattern firing rate of the sensor in Hz;
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:patternFiringRateHz = 36000` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCorePatternFiringRateHzAttr() const;

    /// See GetOmniSensorCorePatternFiringRateHzAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCorePatternFiringRateHzAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENUMBEROFEMITTERS 
    // --------------------------------------------------------------------- //
    /// Number of emitters of the sensor;
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:numberOfEmitters = 128` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNumberOfEmittersAttr() const;

    /// See GetOmniSensorCoreNumberOfEmittersAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNumberOfEmittersAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENUMBEROFCHANNELS 
    // --------------------------------------------------------------------- //
    /// Number of channels of the sensor;
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:numberOfChannels = 128` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNumberOfChannelsAttr() const;

    /// See GetOmniSensorCoreNumberOfChannelsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNumberOfChannelsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREMAXRETURNS 
    // --------------------------------------------------------------------- //
    /// Maximum number of returns of the sensor;
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:maxReturns = 2` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreMaxReturnsAttr() const;

    /// See GetOmniSensorCoreMaxReturnsAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreMaxReturnsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREAZIMUTHERRORMEAN 
    // --------------------------------------------------------------------- //
    /// Mean value of the azimuth error in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:azimuthErrorMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreAzimuthErrorMeanAttr() const;

    /// See GetOmniSensorCoreAzimuthErrorMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreAzimuthErrorMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREAZIMUTHERRORSTD 
    // --------------------------------------------------------------------- //
    /// Standard deviation of the azimuth error in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:azimuthErrorStd = 0.005` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreAzimuthErrorStdAttr() const;

    /// See GetOmniSensorCoreAzimuthErrorStdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreAzimuthErrorStdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREELEVATIONERRORMEAN 
    // --------------------------------------------------------------------- //
    /// Mean value of the elevation error in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:elevationErrorMean = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreElevationErrorMeanAttr() const;

    /// See GetOmniSensorCoreElevationErrorMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreElevationErrorMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREELEVATIONERRORSTD 
    // --------------------------------------------------------------------- //
    /// Standard deviation of the elevation error in degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:elevationErrorStd = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreElevationErrorStdAttr() const;

    /// See GetOmniSensorCoreElevationErrorStdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreElevationErrorStdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREORIGINERRORMEAN 
    // --------------------------------------------------------------------- //
    /// Mean value of the origin error in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:originErrorMean = [0, 0, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreOriginErrorMeanAttr() const;

    /// See GetOmniSensorCoreOriginErrorMeanAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreOriginErrorMeanAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREORIGINERRORSTD 
    // --------------------------------------------------------------------- //
    /// Standard deviation of the origin error in meters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:originErrorStd = [0, 0, 0]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreOriginErrorStdAttr() const;

    /// See GetOmniSensorCoreOriginErrorStdAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreOriginErrorStdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTENSITYMAPPINGTYPE 
    // --------------------------------------------------------------------- //
    /// Intensity mapping type
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `token omni:sensor:Core:intensityMappingType = "LINEAR"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref OmniSensorTokens "Allowed Values" | LINEAR, NONLINEAR, NONLINEAR_ENCODING_ONLY, NONLINEAR_DECODING_ONLY |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntensityMappingTypeAttr() const;

    /// See GetOmniSensorCoreIntensityMappingTypeAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntensityMappingTypeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTENSITYSCALEPERCENT 
    // --------------------------------------------------------------------- //
    /// Intensity scale percent
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omni:sensor:Core:intensityScalePercent = 255` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntensityScalePercentAttr() const;

    /// See GetOmniSensorCoreIntensityScalePercentAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntensityScalePercentAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTENSITYMAPPINGENCODING 
    // --------------------------------------------------------------------- //
    /// Intensity mapping encoding
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:intensityMappingEncoding = []` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntensityMappingEncodingAttr() const;

    /// See GetOmniSensorCoreIntensityMappingEncodingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntensityMappingEncodingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREINTENSITYMAPPINGDECODING 
    // --------------------------------------------------------------------- //
    /// Intensity mapping decoding
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:intensityMappingDecoding = []` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreIntensityMappingDecodingAttr() const;

    /// See GetOmniSensorCoreIntensityMappingDecodingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreIntensityMappingDecodingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENUMLINES 
    // --------------------------------------------------------------------- //
    /// Number of lines of the sensor
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:numLines = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNumLinesAttr() const;

    /// See GetOmniSensorCoreNumLinesAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNumLinesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORENUMRAYSPERLINE 
    // --------------------------------------------------------------------- //
    /// Number of points per line of the sensor; Has numLines elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omni:sensor:Core:numRaysPerLine = []` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreNumRaysPerLineAttr() const;

    /// See GetOmniSensorCoreNumRaysPerLineAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreNumRaysPerLineAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGECOUNT 
    // --------------------------------------------------------------------- //
    /// Number of range regions of the emitters
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:rangeCount = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangeCountAttr() const;

    /// See GetOmniSensorCoreRangeCountAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangeCountAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGESMINM 
    // --------------------------------------------------------------------- //
    /// Minimum ranges of the emitters in meters; Has rangeCount elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:rangesMinM = []` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangesMinMAttr() const;

    /// See GetOmniSensorCoreRangesMinMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangesMinMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORERANGESMAXM 
    // --------------------------------------------------------------------- //
    /// Maximum ranges of the emitters in meters; Has rangeCount elements
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omni:sensor:Core:rangesMaxM = []` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreRangesMaxMAttr() const;

    /// See GetOmniSensorCoreRangesMaxMAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreRangesMaxMAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORESTATERESOLUTIONSTEP 
    // --------------------------------------------------------------------- //
    /// State resolution step of the sensor -- Handles switching between states
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:stateResolutionStep = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreStateResolutionStepAttr() const;

    /// See GetOmniSensorCoreStateResolutionStepAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreStateResolutionStepAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCOREEMITTERSTATESFILE 
    // --------------------------------------------------------------------- //
    /// File containing the emitter states of the sensor; Not supported, yet.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `string omni:sensor:Core:emitterStatesFile = ""` |
    /// | C++ Type | std::string |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->String |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreEmitterStatesFileAttr() const;

    /// See GetOmniSensorCoreEmitterStatesFileAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreEmitterStatesFileAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // --------------------------------------------------------------------- //
    // OMNISENSORCORETILEDSUBSAMPLING 
    // --------------------------------------------------------------------- //
    /// Sensor workload should be sub sampled by this factor (e.g. a value of 2 could equal half the rays traced).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint omni:sensor:Core:tiledSubSampling = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    OMNISENSOR_API
    UsdAttribute GetOmniSensorCoreTiledSubSamplingAttr() const;

    /// See GetOmniSensorCoreTiledSubSamplingAttr(), and also 
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    OMNISENSOR_API
    UsdAttribute CreateOmniSensorCoreTiledSubSamplingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const;

public:
    // ===================================================================== //
    // Feel free to add custom code below this line, it will be preserved by 
    // the code generator. 
    //
    // Just remember to: 
    //  - Close the class declaration with }; 
    //  - Close the namespace with PXR_NAMESPACE_CLOSE_SCOPE
    //  - Close the include guard with #endif
    // ===================================================================== //
    // --(BEGIN CUSTOM CODE)--
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
