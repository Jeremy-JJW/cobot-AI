// Copyright (c) 2022-2025, NVIDIA CORPORATION. All rights reserved.
//
// NVIDIA CORPORATION and its licensors retain all intellectual property
// and proprietary rights in and to this software, related documentation
// and any modifications thereto. Any use, reproduction, disclosure or
// distribution of this software and related documentation without an express
// license agreement from NVIDIA CORPORATION is strictly prohibited.
//

#pragma once

#include <carb/RString.h>
#include <carb/cpp/Optional.h>

namespace omni::usd
{
class UsdContext;
}

namespace carb::eventdispatcher
{
class IMessageQueue;
}

namespace omni::kit::usd::layers
{

/**
 * Regards of edit mode, it's extended by Kit to support customized workflow.
 * USD supports to switch edit target so that all authoring will take place in
 * that layer. When it's working with multiple sublayers, this kind of freedom
 * may cause experience issues. The user has to be aware the changes he/she made are
 * not overrided in any stronger layer. We extend a new mode called "auto authoring"
 * is to improve this. In this mode, all changes will firstly go into a middle delegate
 * layer and it will distribute them into their corresponding layers where it has the strongest
 * opinions. So it cannot switch edit target freely, and user does not need to be
 * aware of existence of multi-sublayers and how USD will compose the changes.
 * Besides, there is one more extended mode called "spec linking", which is based on
 * auto authoring. In this mode, user can customize the target layer of specific specs.
 */
enum class LayerEditMode
{
    eNormal, // Normal USD mode that user can freely switch edit target and creates deltas.
    eAutoAuthoring, // Auto authoring mode that user cannot switch authoring layer freely. And all
                    // deltas will be merged to its original layer where the prim specs have strongest opinions.
                    // @see IWorkflowAutoAuthoring for interfaces about auto authoring.
    eSpecsLinking, // Spec linking mode will support to link specified spec changes into specific layers,
                   // including forwarding changes of whole prim, or specific group of attributes.
};

enum class LayerEventType
{
    eInfoChanged, // Layer info changed, which includes:
                  // PXR_NS::UsdGeomTokens->upAxis          PXR_NS::UsdGeomTokens->metersPerUnit
                  // PXR_NS::SdfFieldKeys->StartTimeCode    PXR_NS::SdfFieldKeys->EndTimeCode
                  // PXR_NS::SdfFieldKeys->FramesPerSecond  PXR_NS::SdfFieldKeys->TimeCodesPerSecond
                  // PXR_NS::SdfFieldKeys->SubLayerOffsets  PXR_NS::SdfFieldKeys->Comment
                  // PXR_NS::SdfFieldKeys->Documentation

    eMutenessScopeChanged, // For vanilla USD, muteness is only local and not persistent. Omniverse extends
                           // it so it could be persistent and sharable with other users. The muteness scope
                           // is a concept about this that supports to switch between local and global scope.
                           // In local scope, muteness will not be persistent In contract, global scope will
                           // save muteness across sessions. This event is to notify change of muteness scope.

    eMutenessStateChanged, // Change of muteness state.

    eLockStateChanged, // Change of lock state. Lock is a customized concept in Omniverse that supports to
                       // lock a layer it's not editable and savable.

    eDirtyStateChanged, // Change of dirtiness state.

    eOutdateStateChanged, // Change of outdate state. This is only emitted for layer in Nucleus when layer content
                          // is out of sync with server one.

    ePrimSpecsChanged, // Change of layer. It will be emitted along with changed paths of prim specs.

    eSublayersChanged, // Change of sublayers.

    eSpecsLockingChanged, // Change of lock states of specs. It will be emitted along with the changed paths.

    eSpecsLinkingChanged, // Change of linking states of specs. It will be emitted along with the changed paths.

    eEditTargetChanged, // Change of edit target.

    eEditModeChanged, // Change of edit mode. @see LayerEditMode for details.

    eDefaultLayerChanged, // Change default layer. Default layer is an extended concept that's only available in auto
                          // authoring mode. It's to host all newly created prims from auto authoring layer.

    eLiveSessionStateChanged, // Live session started or stopped

    eLiveSessionListChanged, // List of live sessions refreshed, which may because of a session is created or removed.

    eLiveSessionUserJoined, // User joined.

    eLiveSessionUserLeft, // User left.

    eLiveSessionMergeStarted, // Starting to merge live session.

    eLiveSessionMergeEnded,   // Finished to merge live session.

    eUsedLayersChanged,      // Used layers changed in stage. Used layers includes all sublayers, references, payloads or any external layers loaded into stage.
                            // If eSublayersChanged is sent, this event will be sent also.

    eLiveSessionJoining,     // The event will be sent before Live Session is joined.

    eLayerFilePermissionChanged, // The event will be sent when file write/read permission is changed.

    eNumEvents // Must be last
};

/**
 * Retrieves the name for a given LayerEventType.
 * @param eventType The LayerEventType to get the name for.
 * @returns a `carb::RString` event name for the given event and this UsdContext in the format
 *   `omni.kit.usd.layers::layer:<event>`.
 */
carb::RString layerEventName(LayerEventType eventType);

/**
 * Parses the LayerEventType from a layer event RString.
 * @param eventName The event name, typically generated from \ref layerEventName().
 * @returns A valid `optional` containing the determined `LayerEventType` if parsing is successful. Otherwise,
 *   `carb::cpp::nullopt` is returned to indicate that the given event name is not a valid layer event name.
 */
carb::cpp::optional<LayerEventType> layerEventType(carb::RString eventName);

const auto kGlobalEventAutoReloadLayersChanged = carb::RString("omni.kit.usd.layers::layer:auto_reload_layers_changed");

enum class LayerErrorType
{
    eSuccess,

    eReadOnly,                      // Destination folder or file is read-only
    eNotFound,                      // File/folder/live session is not found, or layer is not in the local layer stack.
    eAlreadyExists,                 // File/folder/layer/live session already existes.
    eInvalidStage,                  // No stage is attached to the UsdContext.
    eInvalidParam,                  // Invalid param passed to API.

    eLiveSessionInvalid,            // The live session does not match the base layer. Or The live session exists,
                                    // but it is broken on disk, for example, toml file is corrupted.
    eLiveSessionVersionMismatch,
    eLiveSessionBaseLayerMismatch,  // The base layer of the live session does not match the specified one.
                                    // It's normally because user wants to join session of layer B for layer A.
    eLiveSessionAlreadyJoined,
    eLiveSessionNoMergePermission,
    eLiveSessionNotSupported,       // Live session is not supported for the layer if layer is not in Nucleus or already has .live as extension.
                                    // Or it's reported for joining a live session for a reference or payload prim if prim is invalid, or it has
                                    // no references or payloads.
    eLiveSessionNotJoined,

    eUnkonwn
};

class ILayersInstance : public carb::IObject
{
public:
    [[deprecated("Use layerEventName() with IEventDispatcher instead")]] virtual carb::events::IEventStream* getEventStream() const = 0;

    virtual LayerEditMode getEditMode() const = 0;

    virtual void setEditMode(LayerEditMode editMode) = 0;

    virtual LayerErrorType getLastErrorType() const = 0;

    virtual const char* getLastErrorString() const = 0;

    // ILayers 1.1 required for below
    virtual omni::usd::UsdContext* getUsdContext() const = 0;

    virtual carb::eventdispatcher::IMessageQueue* getMessageQueue() = 0;

    virtual void deliverQueuedEvents() = 0;

    // Helper functions
    std::pair<carb::RStringKey, uintptr_t> getEventKey() const
    {
        static const carb::RStringKey kContext("context");
        return std::make_pair(kContext, carb::cpp::bit_cast<uintptr_t>(getUsdContext()));
    }
};

inline carb::RString layerEventName(LayerEventType eventType)
{
    // clang-format off
    static carb::RString mapping[] = {
        carb::RString("omni.kit.usd.layers::layer:info_changed"),
        carb::RString("omni.kit.usd.layers::layer:muteness_scope_changed"),
        carb::RString("omni.kit.usd.layers::layer:muteness_state_changed"),
        carb::RString("omni.kit.usd.layers::layer:lock_state_changed"),
        carb::RString("omni.kit.usd.layers::layer:dirty_state_changed"),
        carb::RString("omni.kit.usd.layers::layer:outdate_state_changed"),
        carb::RString("omni.kit.usd.layers::layer:prim_specs_changed"),
        carb::RString("omni.kit.usd.layers::layer:sublayers_changed"),
        carb::RString("omni.kit.usd.layers::layer:specs_locking_changed"),
        carb::RString("omni.kit.usd.layers::layer:specs_linking_changed"),
        carb::RString("omni.kit.usd.layers::layer:edit_target_changed"),
        carb::RString("omni.kit.usd.layers::layer:edit_mode_changed"),
        carb::RString("omni.kit.usd.layers::layer:default_layer_changed"),
        carb::RString("omni.kit.usd.layers::layer:live_session_state_changed"),
        carb::RString("omni.kit.usd.layers::layer:live_session_list_changed"),
        carb::RString("omni.kit.usd.layers::layer:live_session_user_joined"),
        carb::RString("omni.kit.usd.layers::layer:live_session_user_left"),
        carb::RString("omni.kit.usd.layers::layer:live_session_merge_started"),
        carb::RString("omni.kit.usd.layers::layer:live_session_merge_ended"),
        carb::RString("omni.kit.usd.layers::layer:used_layers_changed"),
        carb::RString("omni.kit.usd.layers::layer:live_session_joining"),
        carb::RString("omni.kit.usd.layers::layer:layer_file_permission_changed"),
    };
    // clang-format on
    static_assert(size_t(LayerEventType::eNumEvents) == CARB_COUNTOF(mapping), "Add missing entry");
    CARB_ASSERT(size_t(eventType) < CARB_COUNTOF(mapping));
    return mapping[size_t(eventType)];
}

inline carb::cpp::optional<LayerEventType> layerEventType(carb::RString eventName)
{
    constexpr static carb::cpp::string_view kPrefix("omni.kit.usd.layers::layer:");
    auto sv = carb::cpp::string_view(eventName);
    if (!sv.starts_with(kPrefix))
        return carb::cpp::nullopt;

    auto name = sv.substr(kPrefix.length());
    switch (carb::hashBuffer(name.data(), name.length()))
    {
    case CARB_HASH_STRING("info_changed"): return LayerEventType::eInfoChanged;
    case CARB_HASH_STRING("muteness_scope_changed"): return LayerEventType::eMutenessScopeChanged;
    case CARB_HASH_STRING("muteness_state_changed"): return LayerEventType::eMutenessStateChanged;
    case CARB_HASH_STRING("lock_state_changed"): return LayerEventType::eLockStateChanged;
    case CARB_HASH_STRING("dirty_state_changed"): return LayerEventType::eDirtyStateChanged;
    case CARB_HASH_STRING("outdate_state_changed"): return LayerEventType::eOutdateStateChanged;
    case CARB_HASH_STRING("prim_specs_changed"): return LayerEventType::ePrimSpecsChanged;
    case CARB_HASH_STRING("sublayers_changed"): return LayerEventType::eSublayersChanged;
    case CARB_HASH_STRING("specs_locking_changed"): return LayerEventType::eSpecsLockingChanged;
    case CARB_HASH_STRING("specs_linking_changed"): return LayerEventType::eSpecsLinkingChanged;
    case CARB_HASH_STRING("edit_target_changed"): return LayerEventType::eEditTargetChanged;
    case CARB_HASH_STRING("edit_mode_changed"): return LayerEventType::eEditModeChanged;
    case CARB_HASH_STRING("default_layer_changed"): return LayerEventType::eDefaultLayerChanged;
    case CARB_HASH_STRING("live_session_state_changed"): return LayerEventType::eLiveSessionStateChanged;
    case CARB_HASH_STRING("live_session_list_changed"): return LayerEventType::eLiveSessionListChanged;
    case CARB_HASH_STRING("live_session_user_joined"): return LayerEventType::eLiveSessionUserJoined;
    case CARB_HASH_STRING("live_session_user_left"): return LayerEventType::eLiveSessionUserLeft;
    case CARB_HASH_STRING("live_session_merge_started"): return LayerEventType::eLiveSessionMergeStarted;
    case CARB_HASH_STRING("live_session_merge_ended"): return LayerEventType::eLiveSessionMergeEnded;
    case CARB_HASH_STRING("used_layers_changed"): return LayerEventType::eUsedLayersChanged;
    case CARB_HASH_STRING("live_session_joining"): return LayerEventType::eLiveSessionJoining;
    case CARB_HASH_STRING("layer_file_permission_changed"): return LayerEventType::eLayerFilePermissionChanged;
    }
    static_assert(size_t(LayerEventType::eNumEvents) == 22, "Add missing entry");
    return carb::cpp::nullopt;
}

} // namespace omni::kit::usd::layers
