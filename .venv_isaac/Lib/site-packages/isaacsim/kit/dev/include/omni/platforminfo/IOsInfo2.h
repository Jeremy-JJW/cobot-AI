// SPDX-FileCopyrightText: Copyright (c) 2023-2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: LicenseRef-NvidiaProprietary
//
// NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
// property and proprietary rights in and to this material, related
// documentation and any modifications thereto. Any use, reproduction,
// disclosure or distribution of this material and related documentation
// without an express license agreement from NVIDIA CORPORATION or
// its affiliates is strictly prohibited.
/** @file
 *  @brief Helper interface to retrieve operating system info.
 */
#pragma once

#include "IOsInfo.h"


namespace omni
{
/** Platform and operating system info namespace. */
namespace platforminfo
{

/** Forward declaration of the IOSInfo2 API object. */
class IOsInfo2;


/** Base type for the flags used in omni::platforminfo::IOsInfo2::getMachineId(). */
using MachineIdFlags OMNI_ATTR("flag, prefix=fMachineIdFlag") = uint32_t;

/** Flag to indicate that the default functionality should be used when generating the machine
 *  ID value.
 */
constexpr MachineIdFlags fMachineIdFlagDefault = 0;

/** Flag to indicate that the generated machine ID should be created with the addition of extra
 *  information that changes frequently.  Multiple calls to generate a machine ID using this
 *  flag will produce different results even within the same process.  Note that this will
 *  only be guaranteed to be statistically unique on the same machine.  There is a very slim
 *  probability that a machine ID generated on another machine with identical components and
 *  OS and using this flag could generate the same value.
 */
constexpr MachineIdFlags fMachineIdFlagMakeUnique = 0x00000001;

/** Flag to indicate that the generated machine ID should be created with the addition of extra
 *  information that stays the same for the duration of the calling process.  Machine IDs
 *  generated by another process even on the same machine will generate a different value.
 *  Note that if the `omni.platforminfo` plugin is unloaded and loaded again in the same
 *  process, machine IDs generated with this flag will differ from previous ones.  There is a
 *  very slim probability that a machine ID generated on another machine with identical components
 *  and OS and using this flag could generate the same value.
 *
 *  @note If this flag is combined with @ref omni::platforminfo::fMachineIdFlagMakeUnique the
 *        generated machine ID will not be repeatable from another call.
 */
constexpr MachineIdFlags fMachineIdFlagSessionUnique = 0x00000002;


/** Base type for the version numbers for the way that machine IDs are generated. */
using MachineIdVersion OMNI_ATTR("constant, prefix=kMachineId") = uint32_t;

/** Name for the first version of the machine ID generation algorithm.  Future versions may
 *  change the algorithm and therefore generate different machine IDs.
 */
constexpr MachineIdVersion kMachineIdBaseVersion = 1;

/** Placeholder name for the current version of the machine ID generation algorithm supported
 *  by this implementation of this plugin.
 */
constexpr MachineIdVersion kMachineIdCurrent = kMachineIdBaseVersion;


/** Extended interface to collect and retrieve more information about the operating system.
 *  This inherits from omni::platforminfo::IOsInfo and also provides all of its functionality.
 */
class IOsInfo2_abi : public omni::core::Inherits<omni::platforminfo::IOsInfo, OMNI_TYPE_ID("omni.platforminfo.IOsInfo2")>
{
protected:
    /** [Windows] Tests whether this process is running under compatibility mode.
     *
     *  @returns `true` if this process is running in compatibility mode for an older version
     *           of Windows.  Returns `false` otherwise.  Returns `false` on all non-Windows
     *           platforms.
     *
     *  @remarks Windows 10 and up allow a way for apps to run in 'compatibility mode'.  This
     *           causes many of the OS version functions to return values that represent an
     *           older version of windows (ie: Win8.1 and earlier) instead of the actual version
     *           information.  This can allow some apps that don't fully support Win10 and up to
     *           start properly.
     */
    virtual bool isCompatibilityModeEnabled_abi() noexcept = 0;

    /** Generates a repeatable machine ID.
     *
     *  @param[in] extraData    A caller provided null terminated string containing extra
     *                          information to include in the machine ID calculation.  This
     *                          can be used to make the generated machine ID more unique to
     *                          the specific user or device.  This may be `nullptr` if no
     *                          extra information is needed.
     *  @param[in] flags        Flags to control how the machine ID is generated.  In most
     *                          cases this should only pass in the
     *                          @ref omni::platforminfo::fMachineIdFlagDefault flag.  Note
     *                          that using different flags will also change the value of
     *                          the generated machine ID.
     *  @param[in] version      The version of the machine ID to generate.  This should be
     *                          @ref omni::platforminfo::kMachineIdCurrent in most cases.
     *                          Though note that the generated machine ID may change from
     *                          one version to the next.  If this version number is out of
     *                          the range of what is supported by the current plugin version,
     *                          a machine ID of 0 will be returned instead.
     *  @returns A 64-bit integer representing the generated machine ID on success.  Returns
     *           0 if the requested version number is out of range of what the current version
     *           of the plugin can provide.
     *
     *  @remarks This generates a machine ID value that can be used to describe or identify
     *           the calling machine in an anonymous way.  The returned machine ID will be
     *           a hash of the names of various hardware components and OS version information,
     *           plus the provided extra data.  Even if the current user's personal information
     *           is included in this hash through the @p extraData string, it will not be
     *           recoverable since both the remainder and order of the information included
     *           in the hash will be implementation specific and not fully known externally.
     *
     *  @note When generated without using any of the flags that make the machine ID unique,
     *        the generated value can be safely cached for a given @p extraData value since
     *        future attempts to generate it with the same value will yield the same result.
     *
     *  @note The value of the generated machine ID will change from one run to the next on
     *        the same machine if certain hardware is changed (ie: new CPU, memory upgrade,
     *        new graphics adapter, etc) or the OS is upgraded.  This is expected behavior.
     */
    virtual uint64_t getMachineIdEx_abi(OMNI_ATTR("c_str, in") const char* extraData,
                                        MachineIdFlags flags,
                                        MachineIdVersion version) noexcept = 0;
};

} // namespace platforminfo
} // namespace omni

#define OMNI_BIND_INCLUDE_INTERFACE_DECL
#include "IOsInfo2.gen.h"

/** @copydoc omni::platforminfo::IOsInfo2_abi */
class omni::platforminfo::IOsInfo2 : public omni::core::Generated<omni::platforminfo::IOsInfo2_abi>
{
public:
    /** Generates a repeatable machine ID with the current algorithm.
     *
     *  @param[in] extraData    A caller provided null terminated string containing extra
     *                          information to include in the machine ID calculation.  This
     *                          can be used to make the generated machine ID more unique to
     *                          the specific user or device.  This may be `nullptr` if no
     *                          extra information is needed.
     *  @param[in] flags        Flags to control how the machine ID is generated.  In most
     *                          cases this should only pass in the
     *                          @ref omni::platforminfo::fMachineIdFlagDefault flag.  Note
     *                          that using different flags will also change the value of
     *                          the generated machine ID.
     *  @returns A 64-bit integer representing the generated machine ID on success.  Returns
     *           0 if the requested version number is out of range of what the current version
     *           of the plugin can provide.
     *
     *  @see See omni::platforminfo::IOsInfo2::getMachineIdEx() for more information on the
     *       usage of this function.
     */
    uint64_t getMachineId(const char* extraData = nullptr, MachineIdFlags flags = fMachineIdFlagDefault) noexcept
    {
        return getMachineIdEx_abi(extraData, flags, kMachineIdCurrent);
    }
};

#define OMNI_BIND_INCLUDE_INTERFACE_IMPL
#include "IOsInfo2.gen.h"
