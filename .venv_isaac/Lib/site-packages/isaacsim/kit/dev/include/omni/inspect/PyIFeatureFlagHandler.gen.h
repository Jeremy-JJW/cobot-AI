// SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: LicenseRef-NvidiaProprietary
//
// NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
// property and proprietary rights in and to this material, related
// documentation and any modifications thereto. Any use, reproduction,
// disclosure or distribution of this material and related documentation
// without an express license agreement from NVIDIA CORPORATION or
// its affiliates is strictly prohibited.

// --------- Warning: This is a build system generated file. ----------

#pragma once

#include <omni/core/ITypeFactory.h>
#include <omni/python/PyBind.h>
#include <omni/python/PyString.h>
#include <omni/python/PyVec.h>

#include <sstream>

inline auto bindFeatureFlagState(const py::module& m)
{
    py::enum_<omni::inspect::FeatureFlagState> e(
        m, "FeatureFlagState", R"OMNI_BIND_RAW_(Enumeration representing the potential states of a feature flag.

This enumeration is used to identify how a feature flag is currently set. Not all feature flags will support all
states but any combination can be set.

@note For simplicity any feature flags not specified will be treated as disabled.)OMNI_BIND_RAW_");
    e.value("DISABLED", omni::inspect::FeatureFlagState::Disabled,
            R"OMNI_BIND_RAW_(Indicates that the feature is disabled.)OMNI_BIND_RAW_");
    e.value("ENABLED", omni::inspect::FeatureFlagState::Enabled,
            R"OMNI_BIND_RAW_(Indicates that the feature is fully enabled.)OMNI_BIND_RAW_");
    e.value(
        "DEPRECATING", omni::inspect::FeatureFlagState::Deprecating,
        R"OMNI_BIND_RAW_(Indicates that the feature is enabled but will issue deprecation warnings if used.)OMNI_BIND_RAW_");

    return e;
}

inline auto bindIFeatureFlagHandler(const py::module& m)
{
    // hack around pybind11 issues with C++17
    //   - https://github.com/pybind/pybind11/issues/2234
    //   - https://github.com/pybind/pybind11/issues/2666
    //   - https://github.com/pybind/pybind11/issues/2856
    py::class_<omni::core::Generated<omni::inspect::IFeatureFlagHandler_abi>,
               omni::python::detail::PyObjectPtr<omni::core::Generated<omni::inspect::IFeatureFlagHandler_abi>>,
               omni::core::IObject>
        clsParent(m, "_IFeatureFlagHandler");
    py::class_<omni::inspect::IFeatureFlagHandler, omni::python::detail::PyObjectPtr<omni::inspect::IFeatureFlagHandler>> cls(
        m, "IFeatureFlagHandler", clsParent, R"OMNI_BIND_RAW_(Interface for handling feature flags.

The IFeatureFlagHandler_abi is an interface that provides methods for checking the state of feature flags and
logging messages if the feature flag is not enabled.

This interface provides several methods for checking and logging feature flags, each catering to different
use cases such as handling single feature flags, multiple feature flags, or specific feature flags of a single bundle.)OMNI_BIND_RAW_");
    cls.def(py::init(
        [](const omni::python::detail::PyObjectPtr<omni::core::IObject>& obj)
        {
            auto tmp = omni::core::cast<omni::inspect::IFeatureFlagHandler>(obj.get());
            if (!tmp)
            {
                throw std::runtime_error("invalid type conversion");
            }
            return tmp;
        }));
    cls.def(py::init(
        []()
        {
            auto tmp = omni::core::createType<omni::inspect::IFeatureFlagHandler>();
            if (!tmp)
            {
                throw std::runtime_error("unable to create omni::inspect::IFeatureFlagHandler instantiation");
            }
            return tmp;
        }));
    cls.def("feature_flag_state", &omni::inspect::IFeatureFlagHandler::featureFlagState,
            R"OMNI_BIND_RAW_(Check to see if a named feature flag is enabled.

@param[in] featureFlag Name of the feature flag to check.
@returns True if the feature flag is enabled, false if not.

threadsafety: safe to call from any thread)OMNI_BIND_RAW_",
            py::arg("feature_flag"));
    cls.def("feature_usage_message", &omni::inspect::IFeatureFlagHandler::featureUsageMessage,
            R"OMNI_BIND_RAW_(Check to see if a named feature flag is enabled and log a message if it is not.

@note The message will be logged at the error level if the feature flag is 0 and at the warning level
if the feature flag is 2.

@param[in] featureFlag Name of the feature flag to check.
@param[in] format Format string for the log message.
@param[in] args Variable list of arguments to the formatting string.
@returns An omni::core::Result indicating the success of the operation.

threadsafety: safe to call from any thread)OMNI_BIND_RAW_",
            py::arg("feature_flag"), py::arg("format"));
    cls.def("clear_feature_usage_messages", &omni::inspect::IFeatureFlagHandler::clearFeatureUsageMessages,
            R"OMNI_BIND_RAW_(Clear out any stored feature usage messages.

@returns An omni::core::Result indicating the success of the operation.

threadsafety: safe to call from any thread)OMNI_BIND_RAW_");
    cls.def("feature_usage_message_count", &omni::inspect::IFeatureFlagHandler::featureUsageMessageCount,
            R"OMNI_BIND_RAW_(Return a count of all messages that have been logged against a feature flag.

@param[in] featureFlag Name of the feature flag to check.
@returns A count of the messages that have been logged against the feature flag.

threadsafety: safe to call from any thread)OMNI_BIND_RAW_",
            py::arg("feature_flag"));
    return omni::python::PyBind<omni::inspect::IFeatureFlagHandler>::bind(cls);
}
