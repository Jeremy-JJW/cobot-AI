// Copyright (c) 2023, NVIDIA CORPORATION. All rights reserved.
//
// NVIDIA CORPORATION and its licensors retain all intellectual property
// and proprietary rights in and to this software, related documentation
// and any modifications thereto.  Any use, reproduction, disclosure or
// distribution of this software and related documentation without an express
// license agreement from NVIDIA CORPORATION is strictly prohibited.

#pragma once

#include <atomic>
#include <thread>
#include "Defines.h"
#include <carb/tasking/ITasking.h>
#include "IPtr.h"

namespace omni
{

struct JoinableThread
{
    std::atomic<bool> m_terminate = false;
    std::thread m_thread;

    template<typename ThreadFn>
    void start(ThreadFn fn)
    {
        m_terminate = false;
        m_thread = std::thread([this, fn]()
            {
                while (!m_terminate)
                {
                    fn();
                }
            });
    }

    ~JoinableThread()
    {
        terminate();
    }

    void terminate()
    {
        if (!m_terminate)
        {
            m_terminate = true;
            if (m_thread.joinable())
            {
                m_thread.join();
            }
        }
    }
};

struct JoinableSleepThread
{
    std::atomic<bool> m_terminate = false;
    std::thread m_thread;
    uint32_t m_sleepMS;
    CachedIPtr<carb::tasking::ITasking> m_tasking;

    JoinableSleepThread(uint32_t sleepMS)
        : m_sleepMS(sleepMS)
    {
    }

    template<typename ThreadFn>
    void start(ThreadFn fn)
    {
        m_terminate = false;
        m_thread = std::thread([this, fn]()
            {
                while (!m_terminate)
                {
                    fn();
                    m_tasking->futexWaitFor(m_terminate, false, std::chrono::milliseconds(m_sleepMS));
                }
            });
    }

    ~JoinableSleepThread()
    {
        terminate();
    }

    void terminate()
    {
        if (!m_terminate)
        {
            m_terminate = true;
            m_tasking->futexWakeup(m_terminate, UINT_MAX);
            if (m_thread.joinable())
            {
                m_thread.join();
            }
        }
    }
};

#if CARB_ENABLE_FAIL

// only enable multi thread detection if TEST failing is enabled, otherwise it is not going to report anything anyways

// Simplistic detection of concurrent execution of scoped blocks 
struct MultiThreadDetector
{
    std::atomic_uint32_t threadId;
    MultiThreadDetector()
    {
        threadId = 0;
    }
};

// Scoped instance to detect concurrent execution of a scope.
// Issues an error or breaks in the debugger if the code inside
// the scope is concurrently executed on multiple threads
struct MultiThreadDetectorInstance
{
    MultiThreadDetector& m_detector;
    uint32_t m_threadId;

    MultiThreadDetectorInstance(MultiThreadDetector& detector)
        : m_detector(detector)
    {
        uint32_t newValue = carb::this_thread::getId();
        uint32_t expected = 0;
        if (!m_detector.threadId.compare_exchange_strong(expected, newValue))
        {
            CARB_FAIL_UNLESS(false, "MultiThreadDetector failure : Executing on 0x%x already executed on thread: 0x%x (not aware of fiber switches)", newValue, expected);
            m_threadId = 0;
        }
        else
        {
            m_threadId = newValue;
        }
    }
    ~MultiThreadDetectorInstance()
    {
        uint32_t expected = m_threadId;
        if (!m_detector.threadId.compare_exchange_strong(expected, 0))
        {
            CARB_FAIL_UNLESS(false, "MultiThreadDetector failure at cleanup: was 0x%x, expected: 0x%x", expected, m_threadId);
        }
    }
};

// Scoped instance to detect concurrent execution of a scope.
// Issues an error or breaks in the debugger if a scope with a MultiThreadDetectorInstance
// is executed concurrently
struct MultiThreadDetectorReadInstance
{
    MultiThreadDetector& m_detector;

    MultiThreadDetectorReadInstance(MultiThreadDetector& detector)
        : m_detector(detector)
    {
        CARB_FAIL_UNLESS(m_detector.threadId.load() == 0, "MultiThreadDetectorRead failure : Executing on 0x%x, when no execution was expected", m_detector.threadId.load());
    }

    ~MultiThreadDetectorReadInstance()
    {
        CARB_FAIL_UNLESS(m_detector.threadId.load() == 0, "MultiThreadDetectorRead failure : Executing on 0x%x, when no execution was expected", m_detector.threadId.load());
    }
};

#else // #if CARB_ENABLE_FAIL

// empty implementation
struct MultiThreadDetector {};
struct MultiThreadDetectorInstance
{
    MultiThreadDetectorInstance(MultiThreadDetector& detector) {};
};

#endif // #if !CARB_ENABLE_FAIL

// Small util that allocate indices from a fixed pool of slots and block when no token is available
struct LockedSlotAllocator
{
    LockedSlotAllocator(uint32_t maxSlots)
    {
        for (uint32_t i = 0; i < maxSlots; i++)
        {
            m_freeSlots.push_back(i);
        }
    }

    uint32_t lockSlot()
    {
        while (true)
        {
            std::scoped_lock lock(m_mutex);
            if (m_freeSlots.size())
            {
                uint32_t result = m_freeSlots.back();
                m_freeSlots.pop_back();
                return result;
            }
        }
        return 0;
    }

    void unlockSlot(uint32_t slot)
    {
        std::scoped_lock lock(m_mutex);
        m_freeSlots.push_back(slot);
    }

    // LockLess stack?
    omni::SmallVector<uint32_t, 128> m_freeSlots;
    carb::tasking::MutexWrapper m_mutex;
};

// Scoped util to allocate slots
struct ScopedLockedSlot
{
    ScopedLockedSlot(LockedSlotAllocator& allocator)
        : m_allocator(allocator)
    {
        m_slot = m_allocator.lockSlot();
    }

    ~ScopedLockedSlot()
    {
        m_allocator.unlockSlot(m_slot);
    }

    uint32_t getSlot() const
    {
        return m_slot;
    }

    LockedSlotAllocator& m_allocator;
    uint32_t m_slot;
};

} // namespace omni
