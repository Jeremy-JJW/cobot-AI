# COBOT + AI 应用项目任务计划表

> 用法：每完成一项，就把 `[ ]` 改成 `[x]`。  
> **当前阶段**：阶段 2 已完成 ✅ → 进入阶段 3（Flask 演示版）  
> **最后更新**：2026-05-28

---

## 当前进度摘要

### 已实现功能（你现在能做什么）

| 能力 | 状态 | 说明 |
|------|------|------|
| 固定话术 → JSON 计划 | ✅ 已通 | `rule_parser`，dry-run 与真机均可用 |
| LLM 自由口语 → JSON | ✅ 已通 | 硅基流动 + DeepSeek-V4-Flash，默认 LLM 兜底，真机已验证 |
| 自研链路无 OpenClaw 真机 | ✅ 已通 | `nl_robot.py` 规则+LLM+缓存 交互模式真机彩排通过 |
| 语义经验库（semantic cache） | ✅ 已通 | `semantic_cache.py`，命中后 `source=cache`，跳过 LLM |
| Skill 白名单执行 | ✅ 已通 | 只允许 `skill_registry.json` 内动作 |
| 参数级安全限位 | ✅ 已通 | 距离 ≤200mm、速度 ≤50%、等待 ≤10s |
| 读取当前位姿 | ✅ 已通 | `GetPose` 解析已修复，与示教器一致 |
| Z 轴相对移动（真机） | ✅ 已通 | 已验证抬高 100mm（Z +100）、下降 200mm |
| 交互模式连续指令 | ✅ 已通 | 同一会话内多条指令（连接复用已修复） |
| OpenClaw dry-run 验收 | ✅ 已通 | 消息 1/2 通过，不生成 Python |
| 记录 home 点位 | ✅ 已通 | `positions.json` 中 `home` 已为现场坐标 |
| OpenClaw 真机闭环 | ✅ 已通 | 抬高20毫米 dry-run → 执行 |
| 命名点 A/B 真机运动 | ✅ 已通 | point_a/point_b 已标定，`去 A 点` 已测 |
| 几何级安全/碰撞检测 | ⬜ 未做 | 见第 15 节，首月后迭代 |
| Flask Web 演示版 | ⬜ 未做 | 阶段 3 |

### 已完成章节（概览）

| 章节 | 进度 |
|------|------|
| 1 基础环境 | ✅ 全部完成 |
| 2 本地 dry-run | ✅ 全部完成 |
| 3 LLM 兜底 | ✅ 全部完成 |
| 4 OpenClaw dry-run | ✅ 全部完成 |
| 5 真机连接准备 | ✅ 全部完成 |
| 6 本地真机小步 | ✅ 全部完成 |
| 7 点位标定 | ✅ 全部完成 |
| 8 OpenClaw 真机 | ✅ 全部完成 |
| 9 阶段 1 复盘 | ✅ 全部完成 |
| 10 阶段 2 自研链路 | ✅ 全部完成 |
| 11～12 | 🟡 阶段 3 待做 / 演示前总验收部分完成 |

---

## 0. 项目目标

- [ ] 最终交付一个自研自然语言控臂演示版
- [ ] 客户只看到自研界面或软件，不看到 OpenClaw
- [x] 大模型只负责把人话理解成 JSON
- [x] 机械臂只执行白名单 Skill，不让 AI 临时写 Python 控制代码

主链路：

```text
自然语言
  -> semantic_cache（经验库命中则直接复用）
  -> rule_parser 或 llm_planner
  -> { skill, params }
  -> executor 白名单校验
  -> skills/*.py
  -> robot_session
  -> Dobot 机械臂
```

---

## 1. 基础环境准备

- [x] 确认本机已安装 Python
- [x] 创建或确认项目虚拟环境 `.venv`
- [x] 安装 `requirements.txt` 依赖
- [x] 生成 `.env` 配置文件
- [x] 配置 LLM API 地址、模型和 Key
- [x] 配置真实机械臂 IP：修改 `.env` 中的 `ROBOT_IP`
- [x] 确认 `.env` 不提交、不截图外发、不上传公开平台

验收命令：

```powershell
cd "Z:\Cursor_contents\cobot+AI应用"
.\.venv\Scripts\python nl_robot.py --dry-run "抬高20毫米"
```

---

## 2. 本地规则链路 dry-run 验收

- [x] 启动 dry-run 交互模式
- [x] 测试 `抬高20毫米`
- [x] 测试 `去 A 点`
- [x] 测试 `停止运动`
- [x] 测试 `速度设置为 30%`
- [x] 测试 `向 Z 正方向移动 100 毫米`
- [x] 确认输出中没有连接机械臂
- [x] 确认每条指令都生成合法 `skill + params`
- [x] 确认 `executor` 能补默认参数并通过校验

合格标准：

- [x] `source` 为 `rule`
- [x] 计划保存到 `generated/last_plan.json`
- [x] 不生成新的 Python 控制脚本
- [x] 不连接机械臂

---

## 3. LLM 兜底链路 dry-run 验收

- [x] 配置硅基流动 `OPENAI_BASE_URL`
- [x] 配置模型 `deepseek-ai/DeepSeek-V4-Flash`
- [x] 用非固定话术测试 dry-run（`nl_robot.py` 默认启用 LLM）
- [x] 确认 `source` 为 `llm`
- [x] 确认 LLM 返回合法 JSON
- [x] 收集 5-10 条客户自由口语
- [x] 逐条测试 LLM 能否映射到正确 Skill
- [x] 把高频成功话术补进 `rule_parser.py` 或 prompt

建议测试话术：

```text
帮我把机械臂往上抬二十毫米
让机械臂稍微升高一点
移动到 A 点
把速度调慢到 20%
先停一下
```

---

## 4. OpenClaw dry-run 验收

- [x] 把 `openclaw_消息1-规则与技能.txt` 发给 OpenClaw
- [x] 等 OpenClaw 确认已记住规则
- [x] 把 `openclaw_消息2-验收测试.txt` 发给 OpenClaw
- [x] 确认 OpenClaw 能调用 `nl_robot.py --dry-run`
- [x] 确认 OpenClaw 不生成 Python 文件
- [x] 确认 `向 Z 正方向移动 100 毫米` dry-run 通过
- [x] 确认 `抬高20毫米` dry-run 通过
- [x] 用 OpenClaw 测试自由口语 LLM 兜底
- [x] 记录 OpenClaw 容易误解的话术
- [x] 整理 OpenClaw 阶段验收结论

合格标准：

- [x] OpenClaw 只调用本仓库脚本
- [x] OpenClaw 先 dry-run，不直接真机
- [x] OpenClaw 不写 `dobot_api` 控制代码
- [x] OpenClaw 不新建临时 `.py`

---

## 5. 真机连接准备

- [x] 机械臂上电
- [x] 示教器无报警
- [x] 电脑与机械臂连接到同一网络
- [x] 查到机械臂真实 IP（192.168.5.1）
- [x] 修改 `.env` 中的 `ROBOT_IP`
- [x] PowerShell 能 `ping` 通机械臂 IP
- [x] 示教器切到 `TCP/IP 远程控制`
- [x] 确认 29999、30004 端口未被其他软件占用（测试时已关闭厂商上位机）
- [x] 确认机械臂周围安全，无碰撞风险
- [x] 准备急停方案（示教器急停 + 小步演示）

检查命令：

```powershell
ping 你的机械臂真实IP
```

---

## 6. 本地终端真机小步测试

> 第一次真机测试先不用 OpenClaw，先用本地终端，方便排查。

- [x] 本地终端执行相对 Z 移动（已测 `抬高100毫米`、`下降200毫米`）
- [x] 确认机械臂实际 Z 方向移动正确（100mm 时 Z 从 424.09 → 524.09）
- [x] 确认运动速度安全（默认 30%）
- [x] 确认执行后程序正常结束（连接复用、反馈线程已修复）
- [x] 测试 `停止运动`
- [x] 测试 `下使能`
- [x] 如果失败，记录错误信息（已排查：GetPose 解析、端口占用、反馈线程）
- [x] 如果连接失败，回到第 5 步排查 IP、网络、TCP 模式

第一次真机命令：

```powershell
cd "Z:\Cursor_contents\cobot+AI应用"
.\.venv\Scripts\python nl_robot.py "抬高20毫米"
```

---

## 7. 点位标定与命名点测试

- [x] 读取当前位置
- [x] 记录安全 `home` 点（已写入 `positions.json`）
- [x] 记录安全 `point_a`
- [x] 记录安全 `point_b`
- [x] 检查 `positions.json` 三点均为现场真实安全点
- [x] dry-run 测试 `去 A 点`
- [x] 真机小心测试 `去 A 点`
- [x] dry-run 测试 `从 A 点移动到 B 点`
- [x] 真机小心测试 `从 A 点移动到 B 点`

注意：

- [x] 点位必须是现场真实安全点（home / A / B 均已标定）
- [x] 不要用示例点位直接做大范围真机运动（已知晓风险）
- [x] 点到点默认优先使用 `movj`

---

## 8. OpenClaw 真机闭环测试

> 只有第 6 步本地真机小步成功后，才做本步骤。

- [x] 在 OpenClaw 输入 `抬高20毫米`
- [x] 确认 OpenClaw 先 dry-run
- [x] 确认计划参数正确
- [x] 回复 `执行`
- [x] 确认机械臂实际运动
- [x] 测试 `停止运动`
- [x] 测试 `去 A 点`
- [x] 记录每次成功和失败结果
- [x] 总结 OpenClaw 验证阶段是否通过

阶段 1 通过标准：

- [x] 自然语言能映射到 Skill
- [x] OpenClaw 能自动调用 `nl_robot.py`（dry-run 已验证）
- [x] 本地终端和 OpenClaw 结果一致（真机闭环已验证）
- [x] 真机小动作稳定（Z 轴相对移动已验证）
- [x] 没有生成临时 Python 控制脚本

---

## 9. 阶段 1 复盘与固化

- [x] 整理成功话术表（规则库已覆盖 500+ 常见说法）
- [x] 整理失败话术表
- [x] 整理常见错误及解决方式
- [x] 把稳定话术补进 `使用说明.md`
- [x] 把高频规则补进 `rule_parser.py`
- [x] 扩展 20 个复合运动路线 Skill（仅调用官方 SDK 的 `MovL` / `Arc` / `Circle`）
- [x] 把 LLM 效果好的约束补进 `robot_planner_prompt.txt`
- [x] 同步更新 `openclaw_skill知识.md`
- [x] 确认阶段 1 可以结束

---

## 10. 阶段 2：路线 A 自研替换 OpenClaw

> 用途：验证路线 A 是否可以不依赖 OpenClaw，直接由本项目完成自然语言到机械臂动作的端到端链路。  
> 启动方式：`nl_robot.py` 默认规则 + LLM 兜底；仅规则时加 `--no-llm`。

### 10.1 代码与架构

- [x] 抽出公共 `plan_from_text` 逻辑（`robot_planner.py`）
- [x] 抽出公共 `plan + execute` 逻辑（`robot_runner.py`）
- [x] 让 CLI 和后续 Web API 共用同一套逻辑
- [x] 新增语义经验库（`semantic_cache.py`，`generated/experience_cache.json`）
- [x] `nl_robot.py` 默认启用 LLM 兜底（`--no-llm` 可关闭）
- [x] 不依赖 OpenClaw，直接运行 `nl_robot.py`
- [x] 对比 OpenClaw 与路线 A 的计划结果（dry-run 已对齐）
- [x] 确认无 OpenClaw 进程时仍能端到端执行

### 10.2 运行前确认

- [x] 已关闭或不使用 OpenClaw
- [x] 已进入项目目录：`Z:\Cursor_contents\cobot+AI应用`
- [x] 已确认 `.env` 中 `ROBOT_IP` 为现场机械臂 IP
- [x] 真机测试前已确认示教器为 TCP/IP 远程控制（见第 5 节）
- [x] 真机测试前已确认机械臂周围安全、急停可用（见第 5 节）

### 10.3 规则路径 dry-run

逐条执行：

```powershell
.\.venv\Scripts\python nl_robot.py --dry-run "抬高20毫米"
.\.venv\Scripts\python nl_robot.py --dry-run "下降20毫米"
.\.venv\Scripts\python nl_robot.py --dry-run "去 A 点"
.\.venv\Scripts\python nl_robot.py --dry-run "停止运动"
.\.venv\Scripts\python nl_robot.py --dry-run "速度设置为30%"
```

验收：

- [x] 输出中 `解析来源` 为 `rule`
- [x] 输出中 `Skill` 和参数符合预期
- [x] 不连接机械臂
- [x] 不生成临时 Python 控制脚本

### 10.4 LLM 路径 dry-run

逐条执行：

```powershell
.\.venv\Scripts\python nl_robot.py --dry-run "帮我把机械臂往上抬二十毫米"
.\.venv\Scripts\python nl_robot.py --dry-run "让机械臂稍微升高一点"
.\.venv\Scripts\python nl_robot.py --dry-run "把速度调慢到20%"
```

验收：

- [x] 规则未命中时可进入 LLM 兜底
- [x] 输出中 `解析来源` 为 `llm`
- [x] LLM 返回的是已注册 Skill
- [x] 仍由 `executor.py` 做白名单和参数校验

### 10.5 安全拒绝 dry-run

逐条执行：

```powershell
.\.venv\Scripts\python nl_robot.py --dry-run "抬高500毫米"
.\.venv\Scripts\python nl_robot.py --dry-run "快速撞过去"
.\.venv\Scripts\python nl_robot.py --dry-run "抓取桌上的物体"
```

验收：

- [x] 超出 200mm 的相对移动被拒绝
- [x] 未支持能力不会进入真机执行
- [x] 错误信息可读，现场人员能判断原因

### 10.6 真机验证

基础小步：

```powershell
.\.venv\Scripts\python nl_robot.py "抬高20毫米"
.\.venv\Scripts\python nl_robot.py "下降20毫米"
.\.venv\Scripts\python nl_robot.py "去 A 点"
```

验收：

- [x] 无 OpenClaw 进程时，仍能连接机械臂
- [x] 无 OpenClaw 进程时，仍能执行小步真机动作
- [x] `generated/last_plan.json` 记录最近一次计划
- [x] 动作完成后程序正常退出或交互模式继续可用

真机记录（`nl_robot.py` 交互模式）：

- `帮我把机器人向下面降落50毫米`：Z-50mm，`source=rule`，执行完成
- `帮我把机械臂往上抬二十毫米`：Z+20mm，`source=llm`，执行完成
- `帮我把机械臂往下降100毫米`：Z-100mm，`source=rule`，执行完成
- `让机械臂稍微升高一点`：Z+20mm，`source=llm`，执行完成
- `回到home点`：`move_to_named_point`，`source=rule`，执行完成
- `在桌面上画一个小圆，半径二十毫米就好`：`motion_pattern` circle_xy，`source=llm`，执行完成
- `走一段圆弧，尺寸小一点，25 毫米`：重复执行 3 次，`source=cache` 命中，执行完成
- `做一个 U 型的小轨迹，40 毫米左右`：`motion_pattern` u_shape_xy，`source=llm`，执行完成
- `以 40% 的速度向 Z 正方向移动 50 毫米`：`move_relative_linear` Z+50、40% 速度，执行完成
- `能不能走个简单的三维螺旋，半径大一些`：`motion_pattern` spiral_z，`source=llm`，执行完成

### 10.7 语义经验库验收

- [x] 成功执行后写入 `generated/experience_cache.json`
- [x] 相同或近似语义再次输入时 `解析来源` 为 `cache`
- [x] 缓存计划仍经 `executor.py` 校验后再执行
- [x] 数字、方向、点位不一致时不误命中（如 Z 正 / Z 负）

### 10.8 OpenClaw 与路线 A 对比

对同一批话术分别记录 OpenClaw 阶段结果与路线 A 结果：

- `抬高20毫米`
- `下降20毫米`
- `去 A 点`
- `速度设置为30%`
- `帮我把机械臂往上抬二十毫米`

对比项：

- [x] `skill` 一致或等价
- [x] `params` 一致或等价
- [x] 都经过 `executor.py` 白名单校验
- [x] 路线 A 不生成临时 Python 控制脚本
- [x] 路线 A 不展示 OpenClaw 品牌或窗口

路线 A dry-run 记录：

- `抬高20毫米`：`move_relative_linear`，Z+20，`source=rule`
- `下降20毫米`：`move_relative_linear`，Z-20，`source=rule`
- `去 A 点`：`move_to_named_point`，`point=point_a`，`source=rule`
- `速度设置为30%`：`set_speed`，`speed_percent=30`，`source=rule`
- `帮我把机械臂往上抬二十毫米`：`move_relative_linear`，Z+20，`source=llm`

OpenClaw 阶段已在第 4、8 节记录通过：OpenClaw 只调用本仓库脚本、先 dry-run、不生成临时 Python，`抬高20毫米` 与 `去 A 点` 已通过真机闭环。

### 10.9 阶段 2 通过结论

- [x] `rule_parser` 固定话术稳定
- [x] `llm_planner` 自由口语可用
- [x] `semantic_cache` 经验库可用
- [x] `executor` 安全校验稳定
- [x] CLI 与后续 Web API 可复用同一套 `plan + execute` 逻辑
- [x] 不需要 OpenClaw 也能从自然语言到真机动作
- [x] 对外可称为自研自然语言控臂软件
- [x] 确认路线 A 能覆盖主要演示话术
- [x] 内部彩排自研链路（规则 + LLM + 缓存 真机已验证）

---

## 11. 阶段 3：Flask 产品化演示版

- [ ] 新建最小 Flask 服务
- [ ] 实现 `/api/plan`
- [ ] 实现 `/api/run-once`
- [ ] `/api/plan` 只返回计划，不执行真机
- [ ] `/api/run-once` 完成一句话 plan + execute
- [ ] 新建简单 Web 页面
- [ ] 页面支持输入自然语言
- [ ] 页面显示解析结果
- [ ] 页面支持确认执行
- [ ] 页面支持急停按钮
- [ ] 增加基础执行日志
- [ ] 增加错误提示
- [ ] 增加一页部署说明

产品化原则：

- [ ] UI 简单可用即可
- [ ] 优先保证演示稳定
- [ ] 不在首月追求复杂交互
- [ ] 不在首月扩展太多 Skill

---

## 12. 对外演示前总验收

- [x] 连续 10 次 dry-run 不崩溃
- [ ] 连续 5 次真机小动作成功（已多次 Z 移动，待整理为正式 5 轮）
- [x] 标准话术识别成功率达到 90% 以上（规则路径演示集已通过）
- [x] 越界距离能被拦截（>200mm 会被 `validate_skill_plan` 拒绝）
- [ ] 超出能力范围的话不乱执行（LLM 拒识待更多测试）
- [ ] 急停可用
- [x] 日志可追溯（`generated/last_plan.json`）
- [ ] 部署说明可按步骤跑通
- [ ] 客户界面不出现 OpenClaw 品牌
- [ ] 演示人员熟悉失败兜底说法

---

## 13. 首月暂不承诺的能力

- [ ] 不承诺任意复杂长句
- [ ] 不承诺多动作连续编排
- [ ] 不承诺视觉识别和抓取
- [ ] 不承诺任意坐标都不会遇到奇异点
- [ ] 不承诺 LLM 离线可用
- [ ] 不追求精美 UI
- [ ] 不承诺程序级关节碰撞检测（首月后见第 15 节）
- [ ] 不承诺工作空间边界自动拦截（首月后见第 15 节）

---

## 15. 后续迭代：几何级安全检测与工作空间保护

> 首月演示版（M4）不阻塞；阶段 2～3 稳定后或演示上线后单独排期。详见 `方案规划.md` §3.4、§十五。

### 15.1 设计与配置

- [ ] 新建 `safety_zones.json`（X/Y/Z 等工作空间 min/max）
- [ ] 定义演示模式策略（如仅 Z 小步、命名点优先）
- [ ] 在 `开发说明.md` 补充安全区标定方法
- [ ] 在 `使用说明.md` 补充演示安全边界说明

### 15.2 代码实现

- [ ] 实现 `compute_target_pose()`（相对移动/命名点算出目标位姿）
- [ ] 实现 `check_workspace()` / `validate_target_pose()`
- [ ] 在 `executor.py` 真机执行前调用工作空间校验
- [ ] `move_relative_linear` 执行前先校验目标 Z/X/Y
- [ ] `move_to_named_point` / `move_between_points` 执行前校验目标点
- [ ] `read_pose` 保存点位时校验是否在安全区内
- [ ] 越界时返回可读错误（如「目标 Z 超出上限」）

### 15.3 测试与验收

- [ ] 标定现场安全工作空间范围
- [ ] 测试越界指令被 100% 拦截（dry-run + 真机前校验）
- [ ] 测试演示话术集在安全区内均可通过
- [ ] 测试明显越界话术被拒绝且不真机执行
- [ ] 文档说明：程序边界 + 示教器碰撞检测 + 急停分工

### 15.4 可选增强（二期）

- [ ] Flask `/api/plan` 返回目标位姿与安全检查结果
- [ ] 演示模式开关（仅 Z 小步 / 仅命名点）
- [ ] 记录 Dobot 控制器碰撞/报警码到日志
- [ ] 评估自碰仿真或轨迹 preview 可行性

---

## 14. 当前下一步

**你现在在这里**：阶段 2 已完成 ✅ → 进入阶段 3（Flask 演示版）

阶段 2 已完成项（详见第 10 节）：

- [x] 抽出 `robot_planner.py`、`robot_runner.py`、`semantic_cache.py`，CLI 与后续 Web 共用
- [x] 无 OpenClaw 进程时 dry-run 与真机端到端已验证
- [x] 规则 + LLM + 经验库 自由口语真机彩排通过

下一步按顺序做：

- [ ] 阶段 3：Flask 演示版（第 11 节）
- [ ] **首月后**：几何级安全检测（第 15 节）
