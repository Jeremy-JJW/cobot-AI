# 目录

- [附录C 脚本编程函数说明](#附录c-脚本编程函数说明)
  - [基础语法](#基础语法)
  - [通用说明](#通用说明)
  - [运动指令](#运动指令)
  - [相对运动指令](#相对运动指令)
  - [运动参数](#运动参数)
  - [IO](#io)
  - [末端工具](#末端工具)
  - [TCP&UDP](#tcpudp)
  - [Modbus](#modbus)
  - [总线寄存器](#总线寄存器)
  - [程序控制](#程序控制)
  - [程序中断](#程序中断)
  - [托盘](#托盘)
  - [安全皮肤](#安全皮肤)
- [附录D 远程控制信号时序图](#附录d-远程控制信号时序图)
- [附录E Python编程函数说明](#附录e-python编程函数说明)
  - [基础语法](#基础语法)
  - [通用说明](#通用说明)
  - [运动指令](#运动指令)
  - [相对运动指令](#相对运动指令)
  - [运动参数](#运动参数)
  - [IO](#io)
  - [末端工具](#末端工具)
  - [TCP&UDP](#tcpudp)
  - [Modbus](#modbus)
  - [程序控制](#程序控制)
- [全局速率为20%；](#全局速率为20)
- [可以正常过渡，机器人会在即将到达P1点时判断DI1，如果为ON会以50%的平滑过渡比例过渡到下一条运动指令。](#可以正常过渡机器人会在即将到达p1点时判断di1如果为on会以50的平滑过渡比例过渡到下一条运动指令)
- [平滑过渡参数无效，机器人会在到达P1点后再判断DI1。](#平滑过渡参数无效机器人会在到达p1点后再判断di1)
- [机械臂以默认设置关节运动至P1点。](#机械臂以默认设置关节运动至p1点)
- [机械臂以默认设置关节运动至指定关节角。](#机械臂以默认设置关节运动至指定关节角)
- [机械臂关节运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡比例为50](#机械臂关节运动至指定位姿位姿对应用户坐标系1和工具坐标系1运动加速度与速度均为50平滑过渡比例为50)
- [先定义点位再在运动指令中调用，运行效果和上一条指令相同。](#先定义点位再在运动指令中调用运行效果和上一条指令相同)
- [机械臂以默认设置直线运动至P1点。](#机械臂以默认设置直线运动至p1点)
- [机械臂500m/s的绝对速度直线运动至P1点。](#机械臂500ms的绝对速度直线运动至p1点)
- [机械臂以默认设置直线运动至指定关节角。](#机械臂以默认设置直线运动至指定关节角)
- [机械臂直线运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡半径为5m](#机械臂直线运动至指定位姿位姿对应用户坐标系1和工具坐标系1运动加速度与速度均为50平滑过渡半径为5m)
- [先定义点位再在运动指令中调用，运行效果和上一条指令相同。](#先定义点位再在运动指令中调用运行效果和上一条指令相同)
- [同时携带speed和v，speed生效，同时控制器会打印对应的警告日志。](#同时携带speed和vspeed生效同时控制器会打印对应的警告日志)
- [同时携带cp和r，r生效，同时控制器会打印对应的警告日志。](#同时携带cp和rr生效同时控制器会打印对应的警告日志)
- [机械臂运动至P1，然后经由P2以默认设置圆弧运动至P3。](#机械臂运动至p1然后经由p2以默认设置圆弧运动至p3)
- [机械臂运动至P1，然后经由点[300,200,300,180,0,0]圆弧运动至P3，用户坐标系与工具坐标系都为1，运动加速](#机械臂运动至p1然后经由点30020030018000圆弧运动至p3用户坐标系与工具坐标系都为1运动加速)
- [机械臂运动至P1，然后进行沿着P1，P2，P3确定的整圆运动一圈。](#机械臂运动至p1然后进行沿着p1p2p3确定的整圆运动一圈)
- [机械臂运动至P1，然后沿着P1，点[300,200,300,180,0,0]，P3确定的整圆运动10圈，用户坐标系与工具坐标系都](#机械臂运动至p1然后沿着p1点30020030018000p3确定的整圆运动10圈用户坐标系与工具坐标系都)
- [机械臂以默认设置向P1点关节运动，当运动到距离起点30%的位置时，将DO2设置为开。](#机械臂以默认设置向p1点关节运动当运动到距离起点30的位置时将do2设置为开)
- [机械臂以默认设置向P1点关节运动，当运动到距离终点还有15°的位置时，将DO3设置为关。](#机械臂以默认设置向p1点关节运动当运动到距离终点还有15的位置时将do3设置为关)
- [机械臂以默认设置向P1点直线运动，当运动到距离起点30%的位置时，将DO2设置为开。](#机械臂以默认设置向p1点直线运动当运动到距离起点30的位置时将do2设置为开)
- [机械臂以默认设置向P1点直线运动，当运动到距离终点15mm的位置时，将DO3设置为关。](#机械臂以默认设置向p1点直线运动当运动到距离终点15mm的位置时将do3设置为关)
- [关节运动到track.csv轨迹文件的起始点后，按2倍的原速复现文件中记录的轨迹。](#关节运动到trackcsv轨迹文件的起始点后按2倍的原速复现文件中记录的轨迹)
- [关节运动到track.csv轨迹文件的起始点后，匀速复现文件中记录的轨迹。](#关节运动到trackcsv轨迹文件的起始点后匀速复现文件中记录的轨迹)
- [获取track.csv轨迹文件的起始点并打印。](#获取trackcsv轨迹文件的起始点并打印)
- [将P1在指定用户坐标系下偏移一定距离，然后运动至偏移后的点位。](#将p1在指定用户坐标系下偏移一定距离然后运动至偏移后的点位)
- [将P1在指定工具坐标系下偏移一定距离，然后运动至偏移后的点位。](#将p1在指定工具坐标系下偏移一定距离然后运动至偏移后的点位)
- [机械臂以默认设置沿全局工具坐标系关节运动至指定偏移点。](#机械臂以默认设置沿全局工具坐标系关节运动至指定偏移点)
- [机械臂以默认设置沿全局工具坐标系直线运动至指定偏移点。](#机械臂以默认设置沿全局工具坐标系直线运动至指定偏移点)
- [机械臂以默认设置沿全局用户坐标系关节运动至指定偏移点。](#机械臂以默认设置沿全局用户坐标系关节运动至指定偏移点)
- [机械臂以默认设置沿全局用户坐标系直线运动至指定偏移点。](#机械臂以默认设置沿全局用户坐标系直线运动至指定偏移点)
- [机械臂以默认设置关节运动至偏移角度。](#机械臂以默认设置关节运动至偏移角度)
- [将P1在J1~J6轴上分别偏移一定角度，然后运动至偏移后的点位。](#将p1在j1j6轴上分别偏移一定角度然后运动至偏移后的点位)
- [机械臂从P1运动至P3点经过P2点时以50%平滑过渡。](#机械臂从p1运动至p3点经过p2点时以50平滑过渡)
- [机械臂以20%的速度比例运动至P1点。](#机械臂以20的速度比例运动至p1点)
- [机械臂以50%的加速度比例运动至P1点。](#机械臂以50的加速度比例运动至p1点)
- [机械臂以20%的速度比例运动至P1点。](#机械臂以20的速度比例运动至p1点)
- [机械臂以50%的加速度比例运动至P1点。](#机械臂以50的加速度比例运动至p1点)
- [设置全局运动速率为50%。](#设置全局运动速率为50)
- [设置末端负载重量为1.5kg，偏心坐标为[0, 0, 10]。](#设置末端负载重量为15kg偏心坐标为0-0-10)
- [使用名称为load1的预设负载参数组设置末端负载。](#使用名称为load1的预设负载参数组设置末端负载)
- [切换全局用户坐标系为用户坐标系1。](#切换全局用户坐标系为用户坐标系1)
- [修改用户坐标系1为CalcUser算出的新坐标系，仅工程运行中生效。](#修改用户坐标系1为calcuser算出的新坐标系仅工程运行中生效)
- [下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿基坐标系平移[x=10, y=10, z=10]并旋转[](#下述计算过程可等价为一个初始位姿与用户坐标系1相同的坐标系沿基坐标系平移x10-y10-z10并旋转)
- [下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿用户坐标系1平移[x=10, y=10, z=10]并旋](#下述计算过程可等价为一个初始位姿与用户坐标系1相同的坐标系沿用户坐标系1平移x10-y10-z10并旋)
- [切换全局工具坐标系为工具坐标系1。](#切换全局工具坐标系为工具坐标系1)
- [修改工具坐标系1为CalcTool算出的新坐标系，仅工程运行中生效。](#修改工具坐标系1为calctool算出的新坐标系仅工程运行中生效)
- [下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿法兰坐标系（TCP0）平移[x=10, y=10, z=](#下述计算过程可等价为一个初始位姿与工具坐标系1相同的坐标系沿法兰坐标系tcp0平移x10-y10-z)
- [下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿工具坐标系1平移[x=10, y=10, z=10]并旋](#下述计算过程可等价为一个初始位姿与工具坐标系1相同的坐标系沿工具坐标系1平移x10-y10-z10并旋)
- [机械臂先运动至P1，再返回当前位姿。](#机械臂先运动至p1再返回当前位姿)
- [机械臂运动至P1后再运动至P2，获取P1的位姿。](#机械臂运动至p1后再运动至p2获取p1的位姿)
- [机械臂先运动至P1，再返回当前位姿。](#机械臂先运动至p1再返回当前位姿)
- [机械臂运动至P1后再运动至P2，获取P1的关节角度。](#机械臂运动至p1后再运动至p2获取p1的关节角度)
- [获取已设置的ABZ编码器的当前位置并赋值给变量abz。](#获取已设置的abz编码器的当前位置并赋值给变量abz)
- [检查使用关节运动默认设置是否可达P1，如果可达，则关节运动至P1。](#检查使用关节运动默认设置是否可达p1如果可达则关节运动至p1)
- [检查使用直线运动默认设置是否可达P1，如果可达，则直线运动至P1。](#检查使用直线运动默认设置是否可达p1如果可达则直线运动至p1)
- [开启索引为1的安全墙。](#开启索引为1的安全墙)
- [开启索引为1的安全区域。](#开启索引为1的安全区域)
- [设置碰撞检测等级为3。](#设置碰撞检测等级为3)
- [设置碰撞回退距离为20mm。](#设置碰撞回退距离为20mm)
- [DI1为ON时机械臂以直线运动方式运动至P1点。](#di1为on时机械臂以直线运动方式运动至p1点)
- [当DI1和DI2都为ON时机械臂以直线运动方式运动至P1点。](#当di1和di2都为on时机械臂以直线运动方式运动至p1点)
- [将DO1设置为ON。](#将do1设置为on)
- [将DO1设置为ON,并在50ms后再自动设置为OFF。](#将do1设置为on并在50ms后再自动设置为off)
- [将DO1和DO2设置为ON。](#将do1和do2设置为on)
- [获取DO1的当前状态。](#获取do1的当前状态)
- [获取DO1和DO2的当前状态。](#获取do1和do2的当前状态)
- [末端DI1为ON时机械臂以直线运动方式运动至P1点。](#末端di1为on时机械臂以直线运动方式运动至p1点)
- [将末端DO1设置为ON。](#将末端do1设置为on)
- [获取末端DO1的当前状态。](#获取末端do1的当前状态)
- [重启末端工具电源。](#重启末端工具电源)
- [创建TCP服务端。](#创建tcp服务端)
- [创建TCP客户端。](#创建tcp客户端)
- [开始建立TCP连接，一直等待直到连接建立成功。](#开始建立tcp连接一直等待直到连接建立成功)
- [接收TCP数据，接收的数据分别保存为字符串和table形式。](#接收tcp数据接收的数据分别保存为字符串和table形式)
- [发送TCP数据，数据内容为“test”。](#发送tcp数据数据内容为test)
- [断开与TCP对端的连接。](#断开与tcp对端的连接)
- [创建UDP服务端。](#创建udp服务端)
- [创建UDP客户端。](#创建udp客户端)
- [接收UDP数据，接收的数据分别保存为字符串和table形式。](#接收udp数据接收的数据分别保存为字符串和table形式)
- [发送UDP数据，数据内容为“test”。](#发送udp数据数据内容为test)
- [创建Modbus主站，并和指定从站建立连接。](#创建modbus主站并和指定从站建立连接)
- [创建Modbus主站，并和机器人自带从站建立连接。](#创建modbus主站并和机器人自带从站建立连接)
- [创建Modbus主站并与RS485接口连接的从站建立连接，从站ID为1，波特率为115200。](#创建modbus主站并与rs485接口连接的从站建立连接从站id为1波特率为115200)
- [和Modbus从站断开连接。](#和modbus从站断开连接)
- [从地址0开始连续读取5个地址的值。](#从地址0开始连续读取5个地址的值)
- [从地址2048开始读取一个32位无符号整数。](#从地址2048开始读取一个32位无符号整数)
- [从地址0开始连续读取5个地址的值。](#从地址0开始连续读取5个地址的值)
- [从地址1024开始，连续写5个线圈。](#从地址1024开始连续写5个线圈)
- [从地址2048开始读取一个32位无符号整数。](#从地址2048开始读取一个32位无符号整数)
- [从地址2048开始，写入一个64位双精度浮点数。](#从地址2048开始写入一个64位双精度浮点数)
- [打印字符串Success到控制台。](#打印字符串success到控制台)
- [等待300ms。](#等待300ms)
- [DI1为ON时继续运行。](#di1为on时继续运行)
- [DO1为ON且AI(1)小于7时继续运行。](#do1为on且ai1小于7时继续运行)
- [根据1s内DI1的状态执行不同的业务逻辑。](#根据1s内di1的状态执行不同的业务逻辑)
- [机械臂运行至P1点后暂停运行，通过外部控制继续运行后才会运行至P2点。](#机械臂运行至p1点后暂停运行通过外部控制继续运行后才会运行至p2点)
- [计算机械臂从当前位置经由P1运动至P2的时间，打印至控制台。](#计算机械臂从当前位置经由p1运动至p2的时间打印至控制台)
- [获取系统当前时间。](#获取系统当前时间)
- [计算机械臂移动到P1所花费的时间，单位毫秒。](#计算机械臂移动到p1所花费的时间单位毫秒)

# 附录C 脚本编程函数说明
## 基础语法
## 通用说明
## 运动指令
## 相对运动指令
## 运动参数
## IO
## 末端工具
## TCP&UDP
## Modbus
## 总线寄存器
## 程序控制
## 程序中断
## 托盘
## 安全皮肤
341

#### 基础语法
#### 基本概念
#### 变量与数据类型
#### 运算符
#### 流程控制
#### 函数
#### 数学计算通用函数
#### 字符串处理通用函数
#### 表（数组）操作通用函数
342

#### 基本概念
说明：
如果您想系统学习Lua编程相关知识，请自行在网络上搜索Lua教程。本手册仅列举部分Lua
基础语法，便于您快速查阅。
#### 标识符
标示符用于定义一个变量，函数或者其他用户定义的项。标示符以一个字母 A 到 Z 或 a 到 z 或下
划线 \_ 开头后加上 0 个或多个字母，下划线，数字（0 到 9）。
最好不要使用下划线加大写字母的标示符，因为Lua的保留字也是这样的。
Lua 不允许使用特殊字符如 @, $, 和 % 来定义标示符。
Lua 是一个区分大小写的编程语言，程序中所有标识符的大小写都必须与本手册提供的示例一
致。
#### 关键词
以下列出了 Lua 的保留关键词。保留关键字不能作为常量或变量或其他用户自定义标示符：
```lua
and, break, do, else, elseif, end, false ,for, function, if , in , local, nil, not, or, repeat,
return, then, true, until, while, goto
```
一般约定，以下划线开头连接一串大写字母的名字（比如 \_VERSION）被保留用于 Lua 内部全局
变量。
#### 注释
注释不影响程序的执行，主要用于帮助阅读代码的人理解程序。
#### 单行注释
在单行代码中，两个减号后面的内容都会被视为注释。注释可单独一行，也可跟在代码后面。
#### --单行注释
#### print("Hello World！") --单行注释
#### 多行注释
343

多行注释以“--[[”开始，以“ --]]”结束，两者之间的内容都会被视为注释。
```lua
--[[
#### 多行注释
```
#### 多行注释
```lua
--]]
344
```

#### 变量与数据类型
变量用于存储值，将值作为参数传递或结果返回。变量通过"="进行赋值。
Lua中，使用 local 显式声明为局部变量（当前函数生效），局部变量的作用域为从声明位置开始
到所在函数结束。
未使用 local 显式声明的变量默认为脚本级变量（当前脚本文件生效），脚本级变量的作用域为单
个脚本文件。
此外，您还可以通过DobotStudio Pro的监控 > 全局变量页面设置控制器级别的全局变量，可在
同一个控制器内的不同脚本文件中直接调用。
示例1：主要说明局部变量和脚本级变量作用域的区别。
#### function func() -- 定义函数，运行时打印a，b
```lua
#### local a = 1 -- 局部变量
#### b = 2 -- 脚本级变量
end
```
#### func() -- 执行函数，打印a和b的值分别为1和2
```lua
#### print(a) --> nil(在变量作用域外调用，打印为nil，下同)
print(b) --> 2
示例2：主要说明局部变量可以和脚本级变量同名，但仅在函数内生效。do/end等流程控制（循
```
环、条件判断）的代码块也属于函数。
a = "a"
345

```lua
for i=10,1,-1 do
do
#### local a = 6 -- 局部变量
```
#### print(a) --> 6，语句块内调用的为局部变量a
```lua
end
#### print(a) --> a，语句块外调用的为脚本级变量a
```
示例3：主要说明全局变量和另两种变量作用域的区别，以及全局变量的全局保持功能。
#### -- 工程1的src0.lua文件
#### -- 假设已在全局变量页面添加了两个全局变量g1和g2
#### -- g1非全局保持，取值为10
#### -- g2全局保持，取值为20
```lua
local a = 1
b = 2
```
print(a) --> 1
print(b) --> 2
print(g1) --> 10
print(g2) --> 20
SetGlobalVariable("g1",11) -- 对非全局保持的全局变量赋值
SetGlobalVariable("g2",22) -- 对全局保持的全局变量赋值
print(g1) --> 11
print(g2) --> 22
#### -- 工程2的src0.lua文件
#### -- 工程2在工程1之后运行
print(a) --> nil
print(b) --> nil
```lua
#### print(g1) --> 10(非全局保持的变量恢复成了工程1修改前的值)
#### print(g2) --> 22(全局保持的变量变成了工程1修改后的值)
```
注意：
```lua
每个函数或作用域块（如do...end）内，局部变量（含函数参数）总数不得超过200个。若超
出限制，编译时将报错 "too many local variables"。
```
变量名称可以是任意非数字打头的字母、下划线和数字组成的字符串，Lua保留的关键字除外。
Lua 变量不需要类型定义，只需要为变量赋值，Lua会根据值自动判断变量的类型。在脚本中对已
赋值的变量赋予不同类型的值会改变变量的类型；但在监控 > 全局变量页面设置的变量不可改变
类型，赋值的类型必须和创建变量时选择的类型一致，否则控制器运行脚本时会报错。
Lua支持多种数据类型，较为常见的包括数字（number），布尔值（boolean），字符串
（string）和表（table）。Lua中的数组是table的一种。
346

Lua中还有一种特殊的数据类型为nil，nil 表示空（没有任何有效值），例如打印一个没有赋值的
变量，便会输出一个 nil 值。
#### 数字
Lua中的number为双精度类型的实浮点数，支持各种运算，以下写法均被视为number：
2
### 2.2
### 0.2
2e+1
### 0.2e-1
### 7.8263692594256e-06
#### 布尔值
boolean 类型只有两个可选值：true（真） 和 false（假），Lua 把 false 和 nil 看作是 false，其
他的都为 true，数字 0 也是 true。
#### 字符串
string是由数字、字母、下划线组成的一串字符。string可以使用以下三种方式来表示：
单引号间的一串字符。
双引号间的一串字符。
[[ 与 ]] 间的一串字符。
在对一个数字字符串上进行算术操作时，Lua 会尝试将这个数字字符串转成一个数字。
#### -- 使用单引号定义字符串
```lua
local str1 = 'Hello, Lua!'
#### print(str1) -- 输出: Hello, Lua!
```
#### -- 使用双引号定义字符串
```lua
local str2 = "Hello, Lua!"
#### print(str2) -- 输出: Hello, Lua!
```
#### -- 使用 [[ 与 ]] 定义多行字符串
```lua
local str3 = [[
This is a multi-line
```
string in Lua.
]]
```lua
print(str3)
#### -- 输出:
-- This is a multi-line
-- string in Lua.
```
#### -- Lua 自动将数字字符串转换为数字并进行算术运算
```lua
local numStr = "10"
347
```

```lua
local result = numStr + 20 -- 自动将 "10" 转换为数字 10
#### print(result) -- 输出: 30
```
#### 表
table是一组带索引的数据。
最简单的构造方式是{}，用来创建一个空表，也可以直接初始化表。
table实际为关联型数组，可以用任意类型的值作为索引，但这个值不能是 nil。
table是不固定大小的，可以根据自己需要进行扩容。
通过#符号可以获取表的长度。
#### -- 初始化一个连续索引的表
```lua
local tbl = {[1] = 2, [2] = 6, [3] = 34, [4] = 5}
#### print("tbl 长度 ", #tbl) -- 输出: 4
```
在这个示例中， tbl 的索引 [1] 到 [4] 是连续的，因此 #tbl 正确返回长度 4 。
#### 数组
数组，就是相同数据类型的元素按一定顺序排列的集合，可以是一维数组和多维数组。
在Lua中，数组属于 table 类型，索引键值可以使用整数表示，数组的大小不是固定的。
一维数组：最简单的数组，其逻辑结构是线性表。
多维数组：即数组中包含数组或一维数组的索引键对应一个数组。
在Lua中，数组的索引值默认从 1开始，但也可以指定从 0 或负数开始。使用不存在的索引访问数
组元素时，会返回nil。
```lua
示例1：一维数组可以用for循环出数组中的元素。
#### -- 创建一维数组
local array = {"Lua", "Tutorial"}
#### -- 使用for循环遍历数组，从索引1开始
for i = 1, #array do
```
#### print(array[i]) -- 输出: Lua Tutorial
```lua
end
#### 示例2：包含数字元素的一维数组
```
#### -- 创建一个内部元素为数字的一维数组
```lua
local numbers = {10, 20, 30, 40, 50}
#### -- 使用for循环遍历数组，从索引1开始
```
348

```lua
for i = 1, #numbers do
#### print(numbers[i]) -- 输出: 10 20 30 40 50
end
```
#### 示例3：自定义索引的数组
#### -- 创建数组，并使用负数和正数作为索引
```lua
local array = {}
for i = -2, 2 do
#### array[i] = i \* 2 + 1 -- 为数组赋值
end
#### -- 使用for循环遍历数组，从 -2 到 2 的索引
for i = -2, 2 do
```
#### print(array[i]) -- 输出: -3 -1 1 3 5
```lua
end
#### 示例4：一个三行三列的阵列多维数组
```
#### -- 初始化数组
array = {}
```lua
for i=1,3 do
array[i] = {}
for j=1,3 do
array[i][j] = i\*j
end
end
```
#### -- 访问数组
```lua
for i=1,3 do
for j=1,3 do
```
print(array[i][j]) --打印结果分别为：1 2 3 2 4 6 3 6 9
```lua
end
end
```
349

#### 运算符
#### 算术运算符
#### 指令符号
#### 说明
+
#### 加法运算
-
#### 减法运算
\*
#### 乘法运算
/
#### 浮点除法运算
//
#### 向下取整除法运算
%
#### 取余除法运算
^
#### 指数运算
示例：
a=20
b=5
#### print(a+b) --打印a加b的结果：25
#### print(a-b) --打印a减b的结果：15
#### print(a\*b) --打印a乘b的结果：100
#### print(a/b) --打印a除以b的结果：4
#### print(a//b) --打印a整除b的结果：4
#### print(a%b) --打印a除以b的余数结果：0
#### print(a^b) --打印a的b次幂的结果：3200000
#### 位运算符
#### 指令符号
#### 说明
&
#### 按位与运算
\
#### 按位或运算
~
#### 按位异或运算
<<
#### 按位左移运算
>>
#### 按位右移运算
示例：
#### print(a&b) --打印a和b按位与的结果：4
350

#### print(a|b) --打印a和b按位或的结果：21
#### print(a~b) --打印a异或b的结果：17
#### print(a<>b) --打印a右移b个单位的结果：0
#### 关系运算符
#### 指令符号
#### 说明
==
#### 等于
~=
#### 不等于
<=
#### 小于等于
>=
#### 大于等于
<
#### 小于
>
#### 大于
示例：
#### a=20 --创建变量a
#### b=5 --创建变量b
#### print(a==b) --打印a等于b的对比结果：false
#### print(a~=b) --打印a不等于b的对比结果：true
#### print(a<=b) --打印a小于等于b的对比结果：false
#### print(a>=b) --打印a大于等于b的对比结果：true
#### print(ab) --打印a大于b的对比结果：true
#### 逻辑运算符
#### 指令符
#### 号
#### 说明
and
逻辑与运算符。当两侧均为true时，其结果才为true；只要有一侧为false，其结
#### 果即为false
or
逻辑或运算符，当任一侧结果为true， 其结果即为true；只有两侧均为false，则
#### 结果为false
not
#### 逻辑非运算符，将判断结果直接取反
a=true
b=false
print(a and b) -- 输出: false，a和b必须都为true才返回true
print(a or b) -- 输出: true，只要a或b有一个为true 就返回true
print(not a) -- 输出: false，a为true，取反为false
351

print(not b) -- 输出: true，b为false，取反为true
print(not (20>5)) -- 输出: false，20>5为true，取反为false
352

#### 流程控制
#### 指令符号
#### 说明
```lua
if…then…
else…end
#### if条件判断指令。从上到下依次判断条件是否成立，如果某个判断为true，执
```
#### 行完对应的代码块，后面的条件判断直接忽略，不再执行
```lua
while…
do…end
while循环控制指令。在条件为 true 时，让程序重复地执行某些语句。执行语
```
#### 句前会先检查条件是否为 true
```lua
for…do…
end
#### for循环控制指令，重复执行指定语句，重复次数可在 for 语句中控制
```
repeat…
```lua
until()
#### repeat循环控制指令。重复执行循环，直到 指定的条件为真时为止
#### if条件判断指令
if后的括号内为条件表达式，表达式的结果可以是任何值，Lua 把 false 和 nil 看作是假，其他的
都为真，数字 0 也是真。表达式为真时，执行then语句块；表达式为假时，如果有else语句块，
执行else语句块，否则直接执行end之后的语句。
if语句可嵌套使用，下面是一个典型示例。
a = 100;
b = 200;
```
#### --[ 检查条件 --]
```lua
if(a == 100)
then
#### --[if条件为true时执行以下if条件判断--]
```
353

```lua
if(b == 200)
then
#### --[if条件为true时执行该语句块--]
```
#### print("a 的值为:", a ) -- a 的值为:100
#### print("b 的值为:", b ) -- b 的值为:200
```lua
end
else
#### --[第一个if条件为false时执行以下语句块--]
```
```lua
#### print("a不等于100")
end
#### while循环控制指令
while后的括号内为条件表达式，为真时执行do语句块，然后重新判断条件；为假时直接执行end
```
之后的语句。
示例：
a=10
```lua
while( a < 20 )
do
```
#### print("a 的值为:", a) -- 执行10次，输出值为10到19
a = a+1
```lua
end
#### for循环控制指令
for循环的语法格式如下：
for var=exp1,exp2,exp3 do
```
#### <执行体>
```lua
end
354
```

变量var的起始值为exp1，每次执行一次<执行体>后var递增exp3（exp3的值可以为负，也可不
指定，默认值为1），直至var大于exp2。
示例：
```lua
for i=10,1,-1 do
#### print(i) -- 执行10次，输出值为10到1
end
```
#### repeat循环控制指令
```lua
repeat循环与while循环类似，主要区别在于while是执行要循环的语句前先判断条件，条件为真
时进行循环；repeat是在执行完要循环的语句后再判断条件，条件为假时进行循环。
```
355

示例：
a = 10
repeat
#### print("a的值为:", a) -- 执行5次，输出值为10到15
a = a + 1
```lua
until(a > 15)
356
```

#### 函数
函数是对语句和表达式进行抽象的主要方法，定义格式如下：
```lua
function function\_name(argument1, argument2, argument3..., argumentn)
function\_body
```
return result\_params\_comma\_separated
```lua
end
function\_name： 函数名称。函数可先定义再通过函数名称调用，也可直接在调用时定义，
```
后一种情况可省略函数名称。
argument1, argument2, argument3..., argumentn： 函数参数，多个参数以逗号隔
开。函数也可以不带参数。
function\_body： 函数体，函数中需要执行的代码语句块。
result\_params\_comma\_separated： 函数返回值，Lua语言函数可以返回多个值，每个值
以逗号隔开。函数可以无返回值。
#### 示例 1：不带输入参数和返回值的函数
```lua
function greet()
#### print("Hello, Lua!") -- 函数体
end
```
#### greet() -- 调用函数，输出: Hello, Lua!
#### 示例 2：带输入参数，不带返回值的函数
```lua
function printSquare(number)
print("Square of " .. number .. " is " .. (number \* number)) -- 函数体
end
```
printSquare(5) -- 调用函数，输出: Square of 5 is 25
#### 示例 3：带输入参数和返回值的函数
```lua
function maximum(a)
#### local mi = 1 -- 最大值索引
#### local m = a[mi] -- 最大值
for i, val in ipairs(a) do
if val > m then
mi = i
m = val
end
end
```
#### return m, mi -- 返回最大值和索引
357

```lua
end
local maxVal, maxIndex = maximum({8, 10, 23, 12, 5})
```
print("Max value:", maxVal) -- 输出: Max value: 23
print("Index of max value:", maxIndex) -- 输出: Index of max value: 3
说明：
本手册主要介绍越疆预定义的函数的使用方式，用户也可自行定义函数。如需自定义函数，
请将函数定义写在global.lua中或调用该函数的src\*.lua文件的顶部，否则运行时会报错。
358

#### 数学计算通用函数
lua提供了一些基本的数学函数，作为算术运算符的补充。
```lua
math.abs(X)
返回X的绝对值，例：
```
print("math.abs(-10):", math.abs(-10)) -- 输出: 10
```lua
math.floor(X)
返回不大于X的最大整数值（向下取整），例：
```
print("math.floor(3.7):", math.floor(3.7)) -- 输出: 3
```lua
math.ceil(X)
返回不小于X的最小整数值（向上取整），例：
```
print("math.ceil(3.2):", math.ceil(3.2)) -- 输出: 4
```lua
math.sqrt(X)
返回X的平方根，例：
```
print("math.sqrt(16):", math.sqrt(16)) -- 输出: 4
```lua
math.rad(X)
返回角度X的弧度值（角度转弧度），例：
```
print("math.rad(180):", math.rad(180)) -- 输出: 3.1415926535898
```lua
math.deg(X)
返回弧度X的角度值（弧度转角度），例：
```
print("math.deg(math.pi):", math.deg(math.pi)) -- 输出: 180
359

```lua
math.sin(X)
返回弧度X的正弦值。
```
如果希望使用角度作为参数，可使用math.rad转换，例：
print("math.sin(math.rad(30)):", math.sin(math.rad(30))) -- 输出: 0.5
```lua
math.cos(X)
返回弧度X的余弦值。
```
如果希望使用角度作为参数，可使用math.rad转换，例：
print("math.cos(math.rad(60)):", math.cos(math.rad(60))) -- 输出: 0.5
```lua
math.tan(X)
返回弧度X的正切值。
```
如果希望使用角度作为参数，可使用math.rad转换，例：
print("math.tan(math.rad(45)):", math.tan(math.rad(45))) -- 输出: 1
```lua
math.asin(X)
返回X的反正弦值，返回值为弧度，可使用math.deg转换为角度，例：
```
print("math.deg(math.asin(0.5)):", math.deg(math.asin(0.5))) -- 输出: 30，返回值为角度
```lua
math.acos(X)
返回X的反余弦值，返回值为弧度，可使用math.deg转换为角度，例：
```
print("math.deg(math.acos(0.5)):", math.deg(math.acos(0.5))) -- 输出: 60，返回值为角度
```lua
math.atan(X)
返回X的反正切值，返回值为弧度，可使用math.deg转换为角度，例：
```
print("math.deg(math.atan(1)):", math.deg(math.atan(1))) -- 输出: 45，返回值为角度
360

```lua
math.log(X)
返回X的自然对数，例：
```
print("math.log(10):", math.log(10)) -- 输出: 2.302585092994
```lua
math.exp(X)
返回自然常数e的X次方，例：
```
print("math.exp(1):", math.exp(1)) -- 输出: 2.718281828459
361

#### 字符串处理通用函数
Lua 提供了字符串处理的通用函数，可进行字符串的查找、替换等操作。
```lua
string.sub(s, i, j)
用于截取字符串。
```
#### 必选参数
s：要截取的字符串。
i：开始截取的位置，从1开始。
#### 可选参数
j：结束截取的位置，默认为-1，表示最后一个字符。
#### 返回
截取获得的字符串。
#### 示例
sub1 = string.sub("abcde", 3) --从第3位开始截取：cde
sub2 = string.sub("abcde", 1, 3) --从第1位开始截取到第3位：abc
sub3 = string.sub("abcde", 3, 3) --截取第3位：c
```lua
string.find (s, sub, i, plain)
在指定的字符串中查找子字符串，返回查找到的索引。
```
#### 必选参数
s：被查找的字符串。
sub：要查找的子字符串。
#### 可选参数
i：开始查找的位置，默认为1。
plain：是否使用纯文本模式。指定此参数时必须同时指定参数i。
true：使用纯文本，此时sub会被视为纯文本字符串。
false：默认值，此时sub支持模式匹配机制。
#### 返回
第一个匹配到的子字符串在原字符串中的开始与结束索引。
如果在模式中定义了捕获，捕获到的值会在两个索引之后返回。
查找不到子字符串时返回nil。
#### 示例
362

i,j = string.find("abcde", "cd") --在abcde中查找cd，返回索引：i为3，j为4
```lua
if string.find(str1, str2) ~= nil --如果str1中包含str2，则执行TODO的内容。
then
--TODO
end
```
#### --高级用法
i,j = string.find("abcabc", "ab", 3) --在abcde中查找cd，从第3位开始，返回索引：i为4，j为5
i,j = string.find("123%abc", "%a") --默认支持模式匹配机制，参数%a被解释为通配符（表示任何字符），因
#### 此会匹配到原字符串中的第一个字母a，i和j都为5
i,j = string.find("123%abc", "%a", 1, true) --使用纯文本模式，参数%a被视为纯文本，因此会匹配到原字
符串中的%a，i为4，j为5。
i,j,sub = string.find("abc 10 edf 100", "(%d+)") --查找并捕获第一串数字，返回查找到的索引和捕获的
#### 结果：i为5，j为6，sub为10
```lua
string.match(s, sub, i)
在指定的字符串中查找子字符串，返回模式匹配的捕获结果或子字符串。
```
#### 必选参数
s：被查找的字符串。
sub：要查找的子字符串，支持模式匹配机制。
#### 可选参数
i：开始查找的位置，默认为1。
#### 返回
返回匹配到的子字符串。
如果在模式中定义了捕获，返回所有捕获结果。
查找不到子字符串时返回nil。
#### 示例
sub1 = string.match("abcde", "cd") --在abcde中查找cd，返回查找到的子字符串：cd
sub2 = string.match("abc 10 edf 100", "%a+", 4) --%a+表示连续的字母，从第4位开始查找，返回匹配的
#### 结果为：edf
sub3 = string.match("abc 10 edf 100", "%a+ (%d+) %a+") --捕获两串字母之间的数字，返回捕获的结果：
10
```lua
string.gmatch(s, sub)
在指定的字符串中循环查找子字符串，返回模式匹配的捕获结果或子字符串。
```
363

#### 必选参数
s：被查找的字符串。
sub：要查找的子字符串，支持模式匹配机制。
#### 返回
返回一个迭代器函数，每一次调用这个函数，返回一次匹配结果。
如果找不到匹配的值，迭代函数返回nil。
#### 示例
```lua
for word in string.gmatch("Hello world from dobot", "%a+")
do
```
```lua
print(word)
end
--[[
```
运行效果为逐行打印每个单词：
Hello
world
from
dobot
```lua
--]]
string.gsub(s, find, repl, n)
```
用于替换字符串中的指定部分。
#### 必选参数
s：被操作的字符串。
find：被替换的字符，sub支持模式匹配机制。
repl：要替换的字符。
#### 可选参数
n：最大替换次数。不指定时表示全部替换。
#### 返回
替换后的字符串和替换次数。
#### 示例
str, n = string.gsub("aaaa","a","z"); --所有a替换成z，返回：str为zzzz，n为4
str, n = string.gsub("aaaa","a","z",3); --前3个a替换成z，返回：str为zzza，n为3
str, n = string.gsub("a1b2c3","%d","z"); --把所有数字替换为z，返回：str为azbzcz，n为3
#### 模式匹配机制
364

lua的模式匹配机制类似于正则表达式，可用于string.find, string.gmatch, string.gsub,
string.match，实现模糊匹配。
#### 字符类
字符类是模式匹配的基本单元，用于表示特定字符的集合。主要的字符类如下：
单个字符(除 ^$()%.[]\\*+-? 外): 与该字符自身匹配。
. : 单个点表示与任何字符匹配。
%a : 与任何字母匹配。
%c : 与任何控制符匹配(例如 \n )。
%d : 与任何数字匹配。
%l : 与任何小写字母匹配。
%p : 与任何标点匹配。
%s : 与空白字符（空格）匹配。
%u : 与任何大写字母匹配。
%w : 与任何字母/数字匹配。
%x : 与任何十六进制数匹配。
%x (此处x是非字母非数字字符): 与字符x匹配，主要用来处理特殊字符( ^$()%.[]\*+-? )的匹配
问题, 例如 %% 与 % 匹配。
[数个字符类] : 与任何 [] 中包含的字符类匹配。例如 [%w\_] 与任何字母/数字（ %w ）或下划
线( \_ )匹配。
[^数个字符类] : 与任何不包含在 [] 中的字符类匹配。 例如 [^%s%p] 与任何非空白和非标点字
符匹配。
所有单个字母表示的类别（ %a ， %c ，等）， 若将其字母改为大写，均表示对应的补集。 例
如， %S 表示所有非空格的字符。
#### 模式条目
模式条目是字符类与特殊符号的组合，用于指定字符类匹配的模式。常用的模式条目如下：
单个字符类匹配该类别中任意单个字符；
单个字符类跟一个 \* ， 将匹配零或多个该类的字符。 这个条目总是匹配尽可能长的字符串；
单个字符类跟一个 + ， 将匹配一或多个该类的字符。 这个条目总是匹配尽可能长的字符
串；
单个字符类跟一个 - ， 将匹配零或多个该类的字符。 和 \* 不同， 这个条目总是匹配尽可能
短的字符串；
单个字符类跟一个 ? ， 将匹配零或一个该类的字符。
%n ， n表示1 到 9的整数； 这个条目匹配一个等于n号捕获物（详见后文捕获）的子串。
#### 模式
模式是模式条目的序列。示例如下：
要匹配dd/mm/yyyy格式的日期，可使用模式 %d%d/%d%d/%d%d%d%d 。
要匹配首字母大写的单词，可使用模式 %u%l+ 。
#### 捕获
365

模式可以在内部用小括号括起一个子模式， 这些子模式被称为捕获物。
当匹配成功时，捕获物所匹配到子串会被保存起来，后续返回或用于操作。 捕获物以它们左括号
的次序来编号。 例如，对于模式 (a\*(.)%w(%s\*)) ：
字符串中匹配到 a\*(.)%w(%s\*) 的字符串是1号捕获物 。
匹配到 " . " 的字符是 2 号捕获物。
匹配到 " %s\* " 的字符串是 3 号捕获物。
2号和3号捕获物为1号捕获物的子字符串。
作为一个特例，空的 () 将捕获到对应字符在字符串中的位置。 例如，如果将模式 ()aa() 作用
到字符串 flaaap 上，将产生两个捕获物： 3 和 5 。
#### 其他常用方法
#### 方法
#### 说明
```lua
string.upper (argument)
#### 字符串全部转为大写字母
```
```lua
string.lower (argument)
#### 字符串全部转为小写字母
```
```lua
string.reverse(arg)
#### 字符串反转
string.format(...)
```
#### 返回一个类似printf的格式化字符串
```lua
string.len(arg)
#### 计算字符串长度
```
```lua
string.rep(string, n)
#### 返回字符串string的n个拷贝
```
示例：
str = "Lua"
print(string.upper(str)) --字符串全部转为大写字母，打印结果：LUA
print(string.lower(str)) --字符串全部转为小写字母，打印结果：lua
print(string.reverse(str)) --字符串反转，打印结果：auL
print(string.len("abc")) --计算字符串abc的长度，打印结果：3
```lua
print(string.format("the value is: %d",4)) --打印结果：the value is:4
print(string.rep(str,2)) --字符串复制2次，打印结果：LuaLua
```
#### 其他运算符
#### 指令符号
#### 说明
..
#### 连接两个字符串
#
#### 返回字符串或表的长度
a = "Hello "
b = "World"
c = {1,2,3}
366

#### print(a..b ) --打印a和b连接后的字符串：Hello World
#### print(#b) --打印字符串b的长度：5
#### print(#c) --打印表c的长度:3
367

#### 表（数组）操作通用函数
Lua 提供了表（数组）处理的通用函数，可进行表的插入、排序等操作。下述指令中操作的表必须
是一个连续的一元数组，索引为lua默认的1~n（n为数组长度）。
```lua
table.concat (table, sep, start, end)
将表内各个元素按索引顺序拼接成为一个字符串，支持指定分隔符和开始/结束位置。
```
#### 必选参数
table：要操作的表。
#### 可选参数
sep：分隔符。默认值为无分隔符。
start：起始位置。默认值为1。
```lua
end：结束位置。默认值为数组的长度。
#### 返回
```
拼接获得的字符串。开始位置大于结束位置时，返回空字符串。
#### 示例
fruits = {"banana","orange","apple"}
print(table.concat(fruits)) --使用默认方式连接，打印结果：bananaorangeapple
print(table.concat(fruits,",")) --指定分隔符连接，打印结果：banana,orange,apple
print(table.concat(fruits,",", 2,3)) --指定分隔符和索引连接，打印结果：orange,apple
```lua
table.insert (table, pos, value)
在表内指定位置插入指定的元素。
```
#### 必选参数
table：要操作的表。
value：要插入的元素。
#### 可选参数
pos：插入的位置。默认值为数组的长度加一，即插入到表的末尾。
#### 示例
fruits = {"banana","orange","apple"}
368

#### table.insert(fruits,"mango") --在末尾插入
#### print(fruits[4]) --打印结果：mango
table.insert(fruits,2,"grapes") --在索引2的位置插入
print(fruits[2],",",fruits[3]) --打印结果：grapes,orange
```lua
table.remove(table, pos)
移出表内指定位置上的元素，并返回这个被移除的值。
```
#### 必选参数
table：要操作的表。
#### 可选参数
pos：插入的位置。默认值为数组的长度，即插入到表的末尾。
#### 返回
被移除的值。
#### 示例
fruits = {"banana","orange","apple"}
print(table.remove(fruits)) --移除最后一个元素，打印被移除的元素：apple
print(table.remove(fruits),2) --移除第二个元素，打印被移除的元素：orange
```lua
table.sort(table, comp)
对表内元素进行排序，可指定排序方法。
```
#### 必选参数
table：要操作的表。
#### 可选参数
comp：排序方法。该参数必须是一个函数，符合下述要求：
能够接收两个表内元素作为参数。
需要第一个参数排在第二个参数之前时，返回true，否则返回false。
默认使用lua标准操作符"<"作为排序方法。
#### 示例
fruits = {"banana","orange","apple","grapes"}
```lua
#### print("排序前")
369
```

```lua
for k,v in ipairs(fruits) do
print(v) --打印结果：banana orange apple grapes
end
```
#### --进行升序排序
```lua
table.sort(fruits)
#### print("排序后")
for k,v in ipairs(fruits) do
```
print(v) --打印结果：apple banana grapes orange
```lua
end
#### --进行降序排序
```
```lua
table.sort(fruits,function(a,b)
return a > b
end)
```
```lua
#### print("排序后")
for k,v in ipairs(fruits) do
```
print(v) --打印结果：orange grapes banana apple
```lua
end
370
```

#### 通用说明
#### 运动方式
机械臂支持的运动方式可分为下述几类。
#### 关节运动
机械臂根据当前各关节角度和目标点各关节角度的差值规划各个关节的运动，使各个关节同时完成
运动。关节运动不约束TCP（Tool Center Point）的运动轨迹，一般情况下该轨迹非直线。
关节运动不受奇异位置限制（奇异点位置详见机械臂对应的硬件手册），因此如果对运动轨迹没有
要求，或目标点位在奇异位置附近，建议使用关节运动。
#### 直线运动
机械臂根据当前位姿和目标点的位姿规划运动轨迹，使TCP运动轨迹为直线，且末端姿态在运动过
程中匀速变化。
当运动轨迹经过奇异位置时，下发直线运动指令给机械臂会产生报错，建议重新规划点位或在奇异
位置附近采用关节运动。
#### 弧线运动
机械臂通过当前位置，P1，P2三个不共线的点确定一个圆弧或整圆。运动过程中的机械臂末端姿
态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运动过程中机械臂到达P1点时
的姿态可能与示教姿态不同）。
当运动轨迹经过奇异位置时，下发弧线运动指令给机械臂会产生报错，建议重新规划点位或在奇异
位置附近采用关节运动。
371

#### 点位参数
如无特殊说明，本手册中所有点位参数都支持三种表达方式：
关节变量：使用各个机械臂各个关节的角度（j1~j6）表示目标点位。
关节变量作为直线或弧线运动参数时系统会通过正解运算将其转换为位姿变量使用，但算法会
保证机械臂抵达目标点时的关节角度会与设置的值一致。
{joint = {j1, j2, j3, j4, j5, j6} }
位姿变量：使用笛卡尔坐标（x，y，z）表示目标点位在用户坐标系中的空间位置，使用欧拉
角（rx，ry，rz）表示TCP（Tool Center Point）到达该点时工具坐标系相对于用户坐标系的
旋转角度。
位姿变量作为关节运动的点位参数时系统会通过逆解运算将其转换为关节变量（取离机械臂当
前关节角最近的解）使用。
{pose = {x, y, z, rx, ry, rz} }
越疆机械臂计算欧拉角时的旋转顺序为X->Y->Z，每个轴都是绕固定轴（用户坐标系）旋转，如
下图所示（rx=γ，ry=β，rz=α）。
确定了旋转顺序后，就可以将旋转矩阵（其中cα为cosα，sα为sinα的简写，以此类推）
#### 推导为方程
372

通过该方程计算机械臂末端的姿态。
示教点位：使用控制软件示教获取的点位会保存为如下格式的常量。
```lua
--[[
name：示教点位的名称。
```
joint：示教点位的关节坐标。
tool：示教时使用的工具坐标系索引。
user：示教时使用的用户坐标系索引。
pose：示教点位的位姿变量值。
```lua
--]]
{
name = "name",
joint = {j1, j2, j3, j4, j5, j6},
tool = index,
user = index,
pose = {x, y, z, rx, ry, rz}
```
}
#### 坐标系参数
运动指令可选参数中的user和tool用于指定目标点的用户和工具坐标系，当前仅支持通过索引序号
指定，需要先在控制软件中添加对应坐标系。
系统选择点位坐标系的优先顺序如下：
### 1. 运动指令可选参数指定了坐标系时，使用指定的坐标系。如果点位参数是示教点，则会将示教
点的位姿坐标换算为指定坐标系下的值进行使用。
### 2. 未通过可选参数指定坐标系时:
如果点位是示教点，使用示教点自带的坐标系索引。
如果点位是关节变量或位姿变量，使用运动参数中设置的全局坐标系（详见User和Tool
指令，未调用指令设置时的默认坐标系均为0）。
说明：
开始运行脚本时默认的全局坐标系都会被置为0，与运行脚本前点动面板设置的值无关。
如果目标点位是关节变量，则坐标系参数无效。
#### 速度参数
#### 相对速率
373

可选参数中的a和v用于指定机械臂执行该运动指令时的加速度和速度比例。
#### 机械臂实际运动速度 = 最大速度 x 全局速率 x 指令速率
#### 机械臂实际运动加速度 = 最大加速度 x 指令速率
其中最大速度/加速度受再现参数的控制，可在DobotStudio Pro的运动参数页面查看与修改。
全局速率可通过DobotStudio Pro的速度调节滑轨（上图右上角）或SpeedFactor指令设置。
指令速率为运动指令可选参数携带的比例，未通过可选参数指定运动加速度/速度比例时，默认使
用运动参数中设置的值（详见VelJ，AccJ，VelL，AccL指令，未调用指令设置时的默认值均为
100. 。
  例：
#### AccJ(50) -- 设置关节运动默认加速度为50%
#### VelJ(60) -- 设置关节运动默认速度为60%
#### AccL(70) -- 设置直线运动默认加速度为70%
#### VelL(80) -- 设置直线运动默认速度为80%
```lua
-- 全局速率为20%；
MovJ(P1) -- 以（关节加速度最大值 x 50%）的加速度和（关节速度最大值 x 20% x 60%）的速度关节运动至P1
MovJ(P2,{a = 30, v = 80}) -- 以（关节加速度最大值 x 30%）的加速度和（关节速度最大值 x 20% x 80%）关
```
#### 节运动至P1
MovL(P1) -- 以（笛卡尔加速度最大值 x 70%）的加速度和直线速度（笛卡尔速度最大值 x 20% x 80%）的速度运
#### 动至P1
MovL(P1,{a = 40, v = 90}) -- 以（笛卡尔加速度最大值 x 40%）的加速度和（笛卡尔速度最大值 x 20% x 90
#### %）的速度直线运动至P1
#### 绝对速度
直线和弧线运动指令可选参数中的speed用于指定械臂执行该运动指令时的绝对速度。
374

绝对速度不受全局速率影响，但受再现参数中的最大速度限制（如果机械臂进入了缩减模式，则受
缩减后的最大速度限制），即speed参数设置的目标速度如果大于再现参数中的最大速度，则以最
大速度为准。
例：
MovL(P1,{speed = 1000}) -- 以1000mm/s的绝对速率直线移动至P1
MovL设置了speed为1000，小于再现参数中的最大速度2000，则机械臂会以1000mm/s为目标
速度进行运动，该目标速度与此时的全局速率无关。但如果机械臂处于缩减模式（假设缩减率为
10%），则最大速度变为200，小于1000，此时机械臂会以200mm/s为目标速度进行运动。
speed参数和v参数互斥，若同时存在以speed为准。
#### 平滑过渡参数
机械臂连续运动经过多个点时，可以通过平滑过渡的方式经过中间点，避免机械臂拐弯过于生硬。
如果用户指定的几个路径点基于不同的工具坐标系，则无法平滑过渡。
可选参数中的cp或r用于指定当前运动指令到下一条运动指令之间的平滑过渡比例（cp）或者平滑
过渡半径（r），设置了r时，参数cp会被忽略。
说明：
关节运动相关命令不支持设置平滑过渡半径（r），详见各指令的可选参数。
CP
设置平滑过渡比例时，系统会自动计算过渡曲线的弧度，CP值越大曲线越平滑，如下图所示。CP
过渡曲线会受运动速度/加速度影响，即使点位和CP值都相同，运动速度/加速度不同时的过渡曲
线弧度也会不同。
R
375

设置平滑过渡半径时，系统会以过渡点为圆心，根据指定半径计算过渡曲线。R过渡曲线不受运动
速度/加速度影响，只由点位和过渡半径决定。
如果用户设置的过渡半径过大（超过起始点/终点与过渡点之间的距离），则系统会自动使用起始
点/终点与过渡点之间较短距离的一半作为过渡半径计算过渡曲线。
当两个连续的过渡半径（下图中的r1和r2）重合时，系统会以第一段运动过渡完成后的点作为第二
段运动的起点，然后按照过渡半径过大的逻辑计算实际生效的第二个r。
376

#### 默认值
未通过可选参数指定平缓过渡比例或半径时，默认使用运动参数中设置的平滑过渡比例（详见CP
指令，未调用指令设置时的默认值为0）。
#### 注意事项
平滑过渡会导致机械臂运动不经过中间点，因此设置了平滑过渡时，两条运动指令之间IO信号输
出或功能设置（例如开关安全皮肤）指令会在过渡过程中执行。
如果希望能够在机械臂准确抵达中间点时执行指令，请将前一条指令的平滑过渡参数设置为0。
由于cp和r的实现方式不同，如果在两条需要平滑过渡的运动指令之间插入其他不影响运动的指令
（例如条件判断、IO、功能设置能），cp和r的处理方式也有所差异。
若使用cp方式过渡，大部分指令不会影响过渡，除非指令的处理时间过长（例如Wait指
令）。
```lua
在下面的示例中，在两条运动指令之间插入一条if语句不会影响cp方式的平滑过渡。
-- 可以正常过渡，机器人会在即将到达P1点时判断DI1，如果为ON会以50%的平滑过渡比例过渡到下一条运动指令。
MovL(P1,{cp=50})
if (DI(1)==ON)
then
```
```lua
MovL(P2)
end
```
若使用r方式过渡，仅下述白名单的指令不会影响平滑过渡。插入其他指令都会使r方式的平滑
过渡失效。
RelPointTool, RelPointUser, DOGroup, DO, AO, ToolDO,
SetUser, SetTool, User, Tool, CP, AccJ, AccL, VelL, VelJ,
SetPayload, SetCollisionLevel, SetBackDistance, EnableSafeSkin, SetSafeSkin
```lua
在下面的示例中，在两条运动指令之间插入一条if语句会导致r方式平滑过渡设置无效。
377
```

```lua
-- 平滑过渡参数无效，机器人会在到达P1点后再判断DI1。
MovL(P1,{r=5})
if (DI(1)==ON)
then
```
```lua
MovL(P2)
end
```
#### 停止条件
可选参数中的stopcond用于指定运动停止条件，该参数是一个字符串形式的表达式。若指定了停
止条件，机器人执行运动指令的过程中会实时检查停止条件，并在停止条件满足时结束当前运动，
直接执行下一条指令。
停止条件支持任意符合lua语法的表达式，表达式为true时即视为满足条件，典型应用如下：
#### -- DI1为ON时停止运动
```lua
MovJ(P1,{stopcond="DI(1) == ON"})
#### -- DI1为ON且DO1为ON时停止运动
MovJ(P1,{stopcond="DI(1) == ON and GetDO(1) == ON"})
```
#### -- 保持寄存器地址100存储的值小于10的时候停止运动
```lua
MovJ(P1,{stopcond="GetHoldRegs(id, 100, 1)[1] < 10"})
#### -- 变量var1不等于5的时候停止运动
MovJ(P1,{stopcond="var1 ~= 5"})
```
说明：
指定停止条件会使平滑过渡参数r无效。
#### 指定停止条件后，平滑过渡参数cp可生效，但过渡段（曲线部分）不属于停止条件的生
效范围。
#### 如果停止条件所在指令的前一条指令添加了平滑过渡，也要等到离开过渡段后才会触发
停止条件。
#### IO信号表示方法
系统内部预定义了ON和OFF变量用于表示数字IO是否有信号。
#### ON = 1，表示有信号
#### OFF = 0，表示无信号
参数中的ON|OFF表示该参数取值为ON或OFF，用户也可以使用1或0作为输入。
378

#### 运动指令
#### 指令列表
运动指令用于控制机械臂进行运动，使用前请阅读通用说明。
#### 指令
#### 功能
MovJ
#### 关节运动
MovL
#### 直线运动
Arc
#### 圆弧插补运动
Circle
#### 整圆插补运动
MovJIO
#### 关节运动并输出DO
MovLIO
#### 直线运动并输出DO
ArcIO
#### 圆弧运动并输出DO
MovS
#### 拟合导入的轨迹
GetPathStartPose
#### 获取轨迹的起始点
StartPath
#### 复现录制的运动轨迹
PositiveKin
#### 将关节角度正解为位姿
InverseKin
#### 将位姿逆解为关节角度
MovJ
原型：
```lua
MovJ(point, {user = 1, tool = 0, a = 20, v = 50, cp = 100, stopcond = "expression"})
#### 描述:
```
从当前位置以关节运动方式运动至目标点。
必选参数：
point：目标点。
可选参数：
user：目标点的用户坐标系。
379

tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置关节运动至P1点。
MovJ(P1)
-- 机械臂以默认设置关节运动至指定关节角。
MovJ({joint={0,0,90,0,90,0} })
-- 机械臂关节运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡比例为5
```
0%。
```lua
MovJ({pose={300,200,300,180,0,0} },{user=1,tool=1,a=50,v=50,cp=50})
-- 先定义点位再在运动指令中调用，运行效果和上一条指令相同。
customPoint={pose={300,200,300,180,0,0} }
MovJ(customPoint,{user=1,tool=1,a=50,v=50,cp=50})
-- 机械臂关节运动至P1点，运动过程中DI1为ON时会结束当前运动。
MovJ(P1,{stopcond="DI(1) == ON"})
```
MovL
原型：
MovL(point, {user = 1, tool = 0, a = 20, v = 50, speed = 500, cp = 100, r = 5, stopcond = "exp
ression"})
#### 描述:
从当前位置以直线运动方式运动至目标点。
必选参数：
point：目标点。
可选参数：
user：目标点的用户坐标系。
380

tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置直线运动至P1点。
MovL(P1)
-- 机械臂500m/s的绝对速度直线运动至P1点。
MovL(P1,{speed=500})
-- 机械臂以默认设置直线运动至指定关节角。
MovL({joint={0,0,90,0,90,0} })
-- 机械臂直线运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡半径为5
```
mm。
```lua
MovL({pose={300,200,300,180,0,0} },{user=1,tool=1,a=50,v=50,r=5})
-- 先定义点位再在运动指令中调用，运行效果和上一条指令相同。
customPoint={pose={300,200,300,180,0,0} }
MovL(customPoint,{user=1,tool=1,a=50,v=50,r=5})
-- 同时携带speed和v，speed生效，同时控制器会打印对应的警告日志。
-- 同时携带cp和r，r生效，同时控制器会打印对应的警告日志。
MovL(P1,{v=50,speed=500,cp=60,r=5}) -- 执行时可选参数仅speed和r生效
-- 机械臂直线运动至P1点，运动过程中DI1为ON时会结束当前运动。
```
381

```lua
MovL(P1,{stopcond="DI(1) == ON"})
Arc
```
原型：
Arc(P1, P2, {user = 1, tool = 0, a = 20, v = 50, speed = 500, cp = 100, r = 5, mode=0, stopcon
d = "expression"})
#### 描述:
从当前位置以圆弧插补方式运动至目标点。
需要通过当前点，P1，P2三个点确定一个圆弧，因此当前位置不能在P1和P2确定的直线上。
运动过程中的机械臂末端姿态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运
动过程中机械臂到达P1点时的姿态可能与示教姿态不同）。
必选参数：
P1：圆弧中间点。
P2：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。设置此参数后，参数cp将被忽略。
mode：通过设置姿态控制参数，对插补过程中机器人相对圆弧的姿态进行自适应控制，满足
不同场景的使用需求。不设置该参数时，机器人默认采用mode=0（线性模式）。
382

mode=0（线性模式）：从当前姿态插值到P2目标位姿，忽略P1姿态。该模式下，只能
实现小于180°的姿态变化。适用于对机器人姿态无要求的场合。
mode=1（过中间点模式）：从当前姿态开始，经过中间点位姿，插值到P2目标位姿。
主要用于焊接应用中。
mode=2（固定模式）：从当前姿态开始，TCP保持相对于圆弧切线的方向不变，忽略
P1和P2姿态。该模式下，姿态旋转角度与圆弧角度一致，可实现超过180°的姿态变化。
主要用于涂胶、打磨等应用中。
说明：
#### 当设置为mode=1（过中间点模式）时，为了保证圆弧运动速度的均匀性，示教圆
弧轨迹时，尽可能保证中间点的位置处于实际圆弧的一半。
#### 当设置为mode=1（过中间点模式）时，需要适当调整各点姿态，保证起始点到中
#### 间点的姿态变化与中间点到目标点的姿态变化角度接近。否则所构造的姿态曲线可
能超出机器人的可达范围，运行时会报错。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂运动至P1，然后经由P2以默认设置圆弧运动至P3。
MovJ(P1)
```
```lua
Arc(P2,P3)
-- 机械臂运动至P1，然后经由点{300,200,300,180,0,0}圆弧运动至P3，用户坐标系与工具坐标系都为1，运动加速
```
度与速度均为50%。
```lua
MovJ(P1)
Arc({pose={300,200,300,180,0,0} },P3,{user=1,tool=1,a=50,v=50})
-- 机械臂运动至P1，然后经由P2以默认设置圆弧运动至P3，运动过程中DI1为ON时会结束当前运动。
```
383

```lua
MovJ(P1)
Arc(P2,P3,{stopcond="DI(1) == ON"})
```
Circle
原型：
Circle(P1, P2, Count, {user = 1, tool = 0, a = 20, v = 50, speed = 500, cp = 100, r = 5, mode=0
, stopcond = "expression"})
#### 描述:
从当前位置进行整圆插补运动，运动指定圈数后重新回到当前位置。
需要通过当前点，P1，P2三个点确定一个整圆，因此当前位置不能在P1和P2确定的直线上，且三
个点确定的整圆不能超出机械臂的运动范围。
运动过程中的机械臂末端姿态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运
动过程中机械臂到达P1点时的姿态可能与示教姿态不同）。
必选参数：
P1：整圆定位点1。
P2：整圆定位点2。
Count：进行整圆运动的圈数，取值范围[1, 999]。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
384

speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。设置此参数后，参数cp将被忽略。
mode：通过设置姿态控制参数，对插补过程中机器人相对圆弧的姿态进行自适应控制，满足
不同场景的使用需求。不设置该参数时，机器人默认采用mode=0（线性模式）。
mode=0（线性模式）：从当前姿态插值到P2目标位姿，忽略P1姿态。该模式下，只能
实现小于180°的姿态变化。适用于对机器人姿态无要求的场合。
mode=1（过中间点模式）：从当前姿态开始，经过中间点位姿，插值到P2目标位姿。
主要用于焊接应用中。
mode=2（固定模式）：从当前姿态开始，TCP保持相对于圆弧切线的方向不变，忽略
P1和P2姿态。该模式下，姿态旋转角度与圆弧角度一致，可实现超过180°的姿态变化。
主要用于涂胶、打磨等应用中。
说明：
#### 当设置为mode=1（过中间点模式）时，为了保证圆弧运动速度的均匀性，示教圆
弧轨迹时，尽可能保证中间点的位置处于实际圆弧的一半。
#### 当设置为mode=1（过中间点模式）时，需要适当调整各点姿态，保证起始点到中
#### 间点的姿态变化与中间点到目标点的姿态变化角度接近。否则所构造的姿态曲线可
能超出机器人的可达范围，运行时会报错。
当设置为mode=1（过中间点模式）或mode=2（固定模式）时，可能出现关节6
#### 限位报警，此时需调整起始点关节6的角度，确保关节6绕圆弧一周后仍处于±360°
以内。受限于关节6的范围，无法实现整圆圈数大于1的姿态插值。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
385

```lua
-- 机械臂运动至P1，然后进行沿着P1，P2，P3确定的整圆运动一圈。
MovJ(P1)
```
```lua
Circle(P2,P3,1)
-- 机械臂运动至P1，然后沿着P1，点{300,200,300,180,0,0}，P3确定的整圆运动10圈，用户坐标系与工具坐标系
```
都为1，运动加速度与速度均为50%。
```lua
MovJ(P1)
Circle({pose={300,200,300,180,0,0} },P3,10,{user=1,tool=1,a=50,v=50})
-- 机械臂运动至P1，然后进行沿着P1，P2，P3确定的整圆运动一圈，运动过程中DI1为ON时会结束当前运动。
```
```lua
MovJ(P1)
Circle(P2,P3,1,{stopcond="DI(1) == ON"})
```
MovJIO
原型：
MovJIO(P,{ {Mode,Distance,Index,Status},{Mode,Distance,Index,Status}...}, {user = 1, tool = 0,
a = 20, v = 50, cp = 100})
#### 描述:
从当前位置以关节运动方式运动至目标点，运动时并行设置数字输出端口状态。
必选参数：
P：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：触发模式。0表示百分比触发，1表示距离触发。系统会将各关节角合成一个角度
向量，并计算终点和起点的角度差作为运动的总距离。
Distance：指定百分比/角度。因角度计算使用的是合成角向量，建议使用百分比模式，
效果更直观。
Distance为0时，表示起点即触发。
Distance为正数时，表示离起点的百分比/角度。
Distance为负数时，表示离目标点的百分比/角度。
Mode为0时，Distance表示和总角度的百分比。取值范围：(0,100]。
Mode为1时，Distance表示角度的值。单位：°。
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
可选参数：
user：目标点的用户坐标系。
386

tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置向P1点关节运动，当运动到距离起点30%的位置时，将DO2设置为开。
MovJIO(P1, { {0, 30, 2, 1} })
-- 机械臂以默认设置向P1点关节运动，当运动到距离终点还有15°的位置时，将DO3设置为关。
```
```lua
MovJIO(P1, { {1, -15, 3, 0} })
387
```

```lua
-- 机械臂以默认设置向P1点关节运动，当运动到距离起点30%的位置时，将DO2设置为开，当运动到距离终点20%的位
置时，将DO2设置为关。
```
```lua
MovJIO(P1, { {0, 30, 2, 1}，{0, -20, 2, 0} })
MovLIO
```
原型：
MovLIO(P,{ {Mode,Distance,Index,Status},{Mode,Distance,Index,Status}...},{user = 1, tool = 0,
a = 20, v = 50, speed = 500, cp = 100, r = 5})
#### 描述:
从当前位置以直线运动方式运动至目标点，运动时并行设置数字输出端口状态。
必选参数：
P：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：触发模式。0表示百分比触发，1表示距离触发。
Distance：当Mode为0时，Distance表示起始点与目标点之间距离的百分比；取值范
围：(0,100]。当Mode为1时，Distance表示离起始点或目标点的距离；单位：mm。
Distance为0时，表示起点即触发。
Distance为正数时，表示离起点的百分比/距离。
Distance为负数时，表示离目标点的百分比/距离。
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
388

可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置向P1点直线运动，当运动到距离起点30%的位置时，将DO2设置为开。
MovLIO(P1, { {0, 30, 2, 1} })
-- 机械臂以默认设置向P1点直线运动，当运动到距离终点15mm的位置时，将DO3设置为关。
```
```lua
MovLIO(P1, { {1, -15, 3, 0} })
-- 机械臂以默认设置向P1点直线运动，当运动到距离起点30%的位置时，将DO2设置为开，当运动到距离终点20%的位
```
置时，将DO2设置为关。
```lua
MovLIO(P1, { {0, 30, 2, 1}，{0, -20, 2, 0} })
389
```

ArcIO
原型：
ArcIO(P1,P2{ {Mode,Distance,Index,Status},{Mode,Distance,Index,Status}...},{user = 1, tool = 0
, a = 20, v = 50, speed = 500, cp = 100, r = 5})
#### 描述:
在圆弧插补过程中并行输出指定DO信号。适用于涂胶应用场景，控制涂胶头的提前出胶和提前收
胶（如音响涂胶，轨迹主要为圆弧）。
需要通过当前点，P1，P2三个点确定一个圆弧，因此当前位置不能在P1和P2确定的直线上。
必选参数：
P1：圆弧中间点。
P2：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：设置触发模式。0表示百分比触发，1表示距离触发。
Distance：运行指定的距离。当Mode为0时，Distance表示起始点与目标点之间距离的
百分比；取值范围：(0,100]。当Mode为1时，Distance表示离起始点或目标点的距离；
单位：mm。
Distance为0时，表示起点即触发。
Distance为正数时，表示离起点的百分比/距离。
Distance为负数时，表示离目标点的百分比/距离。
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
390

可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。设置此参数后，参数cp将被忽略。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置经中间点P1向目标点P2进行圆弧运动，当运动到距离起点30%的位置时，将DO3设置为开，当运动
到距离终点20%的位置时，将DO3设置为关。
```
```lua
ArcIO(P1,P2,{ {0, 30, 3, 1},{0, -20, 3, 0} })
MovS
```
原型：
```lua
MovS(PointList,{tool=0,user=0,v=100,speed=100,a=100,freq=1,stopcond=""})
#### 描述:
```
拟合指定的轨迹。调用该指令前需要用户自行运行机械臂到轨迹的起始点。
必选参数：
PointList：点位列表或者轨迹拟合的文件。
点位列表：table 格式，如 {P1,P2,P3, ... }，至少 4个点，至多50个点。
391

拟合轨迹的文件：格式为一个字符串，如"demo.csv"，代表一个文件的名字（含后缀名）。
可选参数：
tool：指定轨迹点位对应的工具坐标系索引，不指定时使用轨迹文件中记录的工具坐标系索
引。
user：指定轨迹点位对应的用户坐标系索引，不指定时使用轨迹文件中记录的用户坐标系索
引。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
freq：滤波系数，该参数的值越小，拟合的轨迹曲线越平滑，但相对原轨迹的变形越严重，请
根据原轨迹的平滑程度设置合适的滤波系数。取值范围：（0,1]，1表示关闭滤波，不带参时
默认值为0.2。CAD输出的轨迹可以设置为1，保证精度；如果是3D相机等曲线，建议打开滤
波，保证曲线的平滑。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
GetPathStartPose
原型：
```lua
GetPathStartPose(string)
#### 描述:
```
一般配合StartPath和MovS指令使用，调用该指令运动到轨迹文件的起始点。轨迹文件不同，点
位数据的类型（示教点/关节/位姿）也可能会不同。
必选参数：
string：轨迹文件名（含后缀名）。
示例：
```lua
-- 获取track.csv轨迹文件的起始点并打印。
local StartPoint = GetPathStartPose("track.csv")
```
```lua
print(StartPoint)
-- StartPoint = {joint={j1,j2,j3,j4,j5,j6},"name" = "",user = userIndex,tool = toolIndex,pose
= {x,y,z,a,b,c}}
-- StartPoint = {joint={j1,j2,j3,j4,j5,j6}}
```
392

```lua
-- StartPoint = {pose = {x,y,z,a,b,c}}
StartPath
```
原型：
```lua
StartPath(string, {multi = 1, isConst = 0, sample = 50， freq = 0.2, user = 0,tool = 0})
#### 描述:
```
复现指定的轨迹文件中录制的轨迹。调用该指令前需要用户自行运行机械臂到轨迹的起始点。
必选参数：
string：轨迹文件名（含后缀名）。
可选参数：
multi：复现时的速度倍数，仅当isConst=0时有效。取值范围：[0.1, 2]，不带参时默认值为
1。
isConst：是否匀速复现。不带参时默认值为0。
1表示匀速复现，机械臂会按照全局速率匀速复现轨迹。
0表示按照轨迹录制时的原速复现，并可以使用multi参数等比缩放运动速度，此时机械
臂的运动速度不受全局速率的影响。
sample：轨迹点位采样间隔，即生成轨迹文件时相邻两个点位的采样时间差。取值范围：[8,
1000]，单位：ms，不带参时默认值为50（控制器录制轨迹文件时的采样间隔）。
freq：滤波系数，该参数的值越小，复现的轨迹曲线越平滑，但相对原轨迹的变形越严重，请
根据原轨迹的平滑程度设置合适的滤波系数。取值范围：（0,1]，1表示关闭滤波，不带参时
默认值为0.2。
user：指定轨迹点位对应的用户坐标系索引，不指定时使用轨迹文件中记录的用户坐标系索
引。
tool：指定轨迹点位对应的工具坐标系索引，不指定时使用轨迹文件中记录的工具坐标系索
引。
示例：
```lua
-- track.csv为用户通过DobotStudio Pro录制的轨迹文件。
-- 关节运动到轨迹文件的起始点后，按2倍的原速复现文件中记录的轨迹。
local StartPoint = GetPathStartPose("track.csv")
```
```lua
MovJ(StartPoint)
StartPath("track.csv", {multi = 2, isConst = 0})
-- track.csv为用户通过DobotStudio Pro录制的轨迹文件。
-- 关节运动到轨迹文件的起始点后，匀速复现文件中记录的轨迹。
local StartPoint = GetPathStartPose("track.csv")
```
```lua
MovJ(StartPoint)
393
```

```lua
StartPath("track.csv", {isConst = 1})
-- customtrack.csv为用户自己生成的轨迹文件，采样间隔为20ms。
-- 关节运动到轨迹文件的起始点后，匀速复现文件中记录的轨迹，滤波系数为1（完全还原录制的轨迹），用户和工
```
具坐标系均为0。
```lua
local StartPoint = GetPathStartPose("customtrack.csv")
MovJ(StartPoint)
StartPath("customtrack.csv", {isConst = 1, sample = 20, freq = 1, user = 0, tool = 0})
```
PositiveKin
#### 原型
```lua
PositiveKin(joint, {user = 1, tool = 0})
#### 描述
```
进行正解运算：给定机械臂各关节角度，计算机械臂末端在给定的笛卡尔坐标系中的坐标值。
#### 必选参数
joint：关节变量，格式为 {joint = {j1, j2, j3, j4, j5, j6} } 。
#### 可选参数
user：用户坐标系索引，不指定时使用全局用户坐标系。
tool：工具坐标系索引，不指定时使用全局工具坐标系。
#### 返回
正解得出的位姿变量，格式为 {pose = {x, y, z, rx, ry, rz} } 。
#### 示例
```lua
-- 关节坐标为{0,0,-90,0,90,0}，计算机械臂末端在全局用户/工具坐标系下的笛卡尔坐标。
PositiveKin({joint={0,0,-90,0,90,0} })
-- 关节坐标为{0,0,-90,0,90,0}，计算机械臂末端在用户坐标系1和工具坐标系1下的笛卡尔坐标。
PositiveKin({joint={0,0,-90,0,90,0} },{user=1,tool=1})
```
InverseKin
#### 原型
```lua
InverseKin(pose, {useJointNear = true, jointNear = joint, user = 1, tool = 0})
394
```

#### 描述
进行逆解运算：给定机械臂末端在给定的笛卡尔坐标系中的坐标值，计算机械臂各关节角度。
由于笛卡尔坐标仅定义了TCP的空间坐标与倾斜角，所以机械臂可以通过多种不同的姿态到达同一
个位姿，意味着一个位姿变量可以对应多个关节变量。为得出唯一的解，系统需要一个指定的关节
坐标，选择最接近该关节坐标的解作为逆解结果。
用户可通过该指令确认某个笛卡尔点位是否可达。
#### 必选参数
pose：位姿变量，格式为 {pose = {x, y, z, rx, ry, rz} } 。
#### 可选参数
useJointNear：布尔值，用于设置JointNear参数是否有效。
true表示根据JointNear参数就近选解。
false或者不带参表示JointNear参数无效，系统根据机械臂当前关节角度就近选解。
如果仅携带此参数而不携带jointNear参数，此参数无效。
jointNear：用于就近选解的关节变量，格式为 {joint = {j1, j2, j3, j4, j5, j6} } 。
user：用户坐标系索引，不指定时使用全局用户坐标系。
tool：工具坐标系索引，不指定时使用全局工具坐标系。
#### 返回
错误码，0表示逆解成功，-1表示逆解失败（无解）。
逆解得出的关节变量，格式为 {joint = {j1, j2, j3, j4, j5, j6} } ，逆解失败时j1~j6全部为
0。
#### 示例
```lua
-- 机械臂末端在全局用户/工具坐标系下的笛卡尔坐标为{300, 200, 300, 180, 0, 0}，计算关节坐标，就近取解。
local errId, jointPoint = InverseKin({ pose = {300, 200, 300, 180, 0, 0} })
-- 机械臂末端在用户坐标系1和工具坐标系1下的笛卡尔坐标为{300, 200, 300, 180, 0, 0}，计算关节坐标，就近
```
取解。
```lua
local errId, jointPoint = InverseKin({ pose = {300, 200, 300, 180, 0, 0} }, {user=1, tool=1})
-- 机械臂末端在全局用户/工具坐标系下的笛卡尔坐标为{300, 200, 300, 180, 0, 0}，计算关节坐标，选择离关
```
节角度{90, 30, -90, 180, 30, 0}最近的解。
```lua
local errId, jointPoint = InverseKin({ pose = {300, 200, 300, 180, 0, 0} }, {useJointNear = tr
ue, jointNear = { joint = {90, 30, -90, 180, 30, 0} } })
```
395

#### 相对运动指令
#### 指令列表
相对运动指令函数用于控制机械臂进行偏移运动，使用前请阅读通用说明。
#### 指令
#### 功能
RelPointUser
#### 点位沿用户坐标系偏移
RelPointTool
#### 点位沿工具坐标系偏移
RelMovJTool
#### 沿工具坐标系进行相对关节运动
RelMovLTool
#### 沿工具坐标系进行相对直线运动
RelMovJUser
#### 沿用户坐标系进行相对关节运动
RelMovLUser
#### 沿用户坐标系进行相对直线运动
RelJointMovJ
#### 关节运动至指定偏移角度
RelJoint
#### 点位偏移指定关节角度
RelPointUser
原型：
```lua
RelPointUser(P, {OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz})
#### 描述:
```
对指定点位沿指定用户坐标系进行偏移，返回偏移后的点位。
必选参数：
P：要进行偏移的指定点。
{OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz}：指定用户坐标系下的偏移量。
x，y，z表示空间偏移量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
如果指定点为示教点，则基于示教点的用户坐标系进行偏移。
如果指定点为关节变量或位姿变量，则基于全局用户坐标系进行偏移。
返回：
如果指定点为示教点，返回偏移后的示教点常量。
如果指定点为关节变量或位姿变量，返回偏移后的位姿变量：{pose = {x, y, z, rx, ry, rz}}。
示例：
396

```lua
-- 将P1在指定用户坐标系下偏移一定距离，然后运动至偏移后的点位。
local Offset={OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz}
local p = RelPointUser(P1, Offset)
```
```lua
MovL(p)
RelPointTool
```
原型：
```lua
RelPointTool(P, {OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz})
#### 描述:
```
对指定点位沿指定工具坐标系进行偏移，返回偏移后的点位。
必选参数：
P：要进行偏移的指定点。
{OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz}：指定工具坐标系下的偏移量。
x，y，z表示空间偏移量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
如果指定点为示教点，则基于示教点的工具坐标系进行偏移。
如果指定点为关节变量或位姿变量，则基于全局工具坐标系进行偏移。
返回：
如果指定点为示教点，返回偏移后的示教点常量。
如果指定点为关节变量或位姿变量，返回偏移后的位姿变量：{pose = {x, y, z, rx, ry, rz}}。
示例：
```lua
-- 将P1在指定工具坐标系下偏移一定距离，然后运动至偏移后的点位。
local Offset={OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz}
local p = RelPointTool(P1, Offset)
```
```lua
MovL(p)
RelMovJTool
```
原型：
RelMovJTool({x, y, z, rx, ry, rz}, {user = 1, tool = 0, a = 20, v = 50, cp = 100, stopcond = "
expression"})
#### 描述:
从当前位置沿指定工具坐标系以关节运动方式进行相对运动。关节运动的轨迹非直线，所有关节会
同时完成运动。
397

必选参数：
{x, y, z, rx, ry, rz}：目标点相对于当前位置在指定工具坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置沿全局工具坐标系关节运动至指定偏移点。
RelMovJTool({10, 10, 10, 0, 0, 0})
```
更多可选参数相关的示例请参考MovJ。
RelMovLTool
原型：
RelMovLTool({x, y, z, rx, ry, rz}, {user = 1, tool = 0, a = 20, v = 50, speed = 500, cp = 100,
r = 5, stopcond = "expression"})
#### 描述:
从当前位置沿指定工具坐标系以直线运动方式进行相对运动。
必选参数：
{x, y, z, rx, ry, rz}：目标点相对于当前位置在指定工具坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
398

speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置沿全局工具坐标系直线运动至指定偏移点。
RelMovLTool({10, 10, 10, 0, 0, 0})
```
更多可选参数相关的示例请参考MovL。
RelMovJUser
原型：
RelMovJUser({x, y, z, rx, ry, rz}, {user = 1, tool = 0, a = 20, v = 50, cp = 100, stopcond = "
expression"})
#### 描述:
从当前位置沿指定用户坐标系以关节运动方式进行相对运动。关节运动的轨迹非直线，所有关节会
同时完成运动。
必选参数：
{x, y, z, rx, ry, rz}：目标点相对于当前位置在指定用户坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
399

示例：
```lua
-- 机械臂以默认设置沿全局用户坐标系关节运动至指定偏移点。
RelMovJUser({10, 10, 10, 0, 0, 0})
```
更多可选参数相关的示例请参考MovJ。
RelMovLUser
原型：
RelMovLUser({x, y, z, rx, ry, rz}, {user = 1, tool = 0, a = 20, v = 50, speed = 500, cp = 100,
r = 5, stopcond = "expression"})
#### 描述:
从当前位置沿指定用户坐标系以直线运动方式进行相对运动。
必选参数：
{x, y, z, rx, ry, rz}：目标点相对于当前位置在指定用户坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
stopcond：停止条件表达式，满足该条件时会结束当前运动去执行下一条指令。
详细介绍请参见通用说明。
示例：
400

```lua
-- 机械臂以默认设置沿全局用户坐标系直线运动至指定偏移点。
RelMovLUser({10, 10, 10, 0, 0, 0})
```
更多可选参数相关的示例请参考MovL。
RelJointMovJ
原型：
RelJointMovJ({Offset1, Offset2, Offset3, Offset4, Offset5, Offset6}, {a = 20, v = 50, cp = 100
})
#### 描述:
从当前位置以关节运动方式运动至指定关节偏移角度。
必选参数：
{Offset1, Offset2, Offset3, Offset4, Offset5, Offset6}：关节坐标系下J1轴 ~ J6轴方向上的偏移
值，单位：°。
可选参数：
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
示例：
```lua
-- 机械臂以默认设置关节运动至偏移角度。
RelJointMovJ({20,20,10,0,10,0})
```
RelJoint
原型：
```lua
RelJoint(P, {Offset1, Offset2, Offset3, Offset4, offset5, offset6})
#### 描述:
```
对指定点位在关节坐标系下增加J1~J6轴偏移量，返回偏移后的关节变量。
必选参数：
P：要进行偏移的指定点。
401

Offset1~Offset6：关节坐标系下J1轴 ~ J6轴方向上的偏移值，单位：°。
返回：
偏移后的关节变量：{joint = {j1, j2, j3, j4, j5, j6}}。
示例：
```lua
-- 将P1在J1~J6轴上分别偏移一定角度，然后运动至偏移后的点位。
local Offset = {Offset1, Offset2, Offset3, Offset4, offset5, offset6}
local p = RelJoint(P1, Offset)
```
```lua
MovJ(p)
402
```

#### 运动参数
#### 指令列表
运动参数函数用于设置或获取机械臂运动相关参数，使用前请阅读通用说明。
#### 指令
#### 功能
CP
#### 设置平滑过渡比例
VelJ
#### 设置关节运动方式的速度比例
AccJ
#### 设置关节运动方式的加速度比例
VelL
#### 设置直线和弧线运动方式的速度比例
AccL
#### 设置直线和弧线运动方式的加速度比例
SpeedFactor
#### 设置机械臂全局运动速率
SetPayload
#### 设置末端负载
User
#### 设置全局用户坐标系
SetUser
#### 修改指定的用户坐标系
CalcUser
#### 计算用户坐标系
Tool
#### 设置全局工具坐标系
SetTool
#### 修改指定的工具坐标系
CalcTool
#### 计算工具坐标系
GetPose
#### 获取机械臂的实时位姿
GetAngle
#### 获取机械臂的实时关节角度
GetABZ
#### 获取编码器当前位置
CheckMovJ
#### 检查关节运动指令的可运行性
CheckMovL
#### 检查直线或弧线运动指令的可运行性
SetSafeWallEnable
#### 开启或关闭指定的安全墙
SetWorkZoneEnable
#### 开启或关闭指定的安全区域
SetCollisionLevel
#### 设置碰撞等级
SetBackDistance
#### 设置碰撞回退距离
CP
403

原型：
```lua
CP(R)
#### 描述:
```
设置平滑过渡比例，即机械臂连续运动经过多个点时，经过中间点是以直角方式过渡还是以曲线方
式过渡。
该命令设置的平滑过渡比例仅在当前工程运行中生效，未设置时默认值为0。
必选参数：
R：平滑过渡比例。取值范围：[0, 100]。
示例：
```lua
-- 机械臂当前位于P1点，以50%平滑过渡比例在P2点前曲线过渡，最终到达P3点。
CP(50)
```
```lua
MovL(P2)
MovL(P3)
```
VelJ
原型：
```lua
VelJ(R)
#### 描述:
```
设置关节运动方式（MovJ/MovJIO/RelMovJTool/RelMovJUser/RelJointMovJ）的速度比例。
该命令设置的速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：速度比例。取值范围：[1,100]。
404

示例：
```lua
-- 机械臂以20%的速度比例运动至P1点。
VelJ(20)
```
```lua
MovJ(P1)
AccJ
```
原型：
```lua
AccJ(R)
#### 描述:
```
设置关节运动方式（MovJ/MovJIO/RelMovJTool/RelMovJUser/RelJointMovJ）的加速度比
例。
该命令设置的加速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：加速度比例。取值范围：[1,100]。
示例：
```lua
-- 机械臂以50%的加速度比例运动至P1点。
AccJ(50)
```
```lua
MovJ(P1)
VelL
```
原型：
```lua
VelL(R)
#### 描述:
```
设置直线和弧线运动方式（MovL/Arc/Circle/MovLIO/RelMovLTool/RelMovLUser）的速度比
例。
该命令设置的速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：速度比例。取值范围：[1,100]。
示例：
405

```lua
-- 机械臂以20%的速度比例运动至P1点。
VelL(20)
```
```lua
MovL(P1)
AccL
```
原型：
```lua
AccL(R)
#### 描述:
```
设置直线和弧线运动方式（MovL/Arc/Circle/MovLIO/RelMovLTool/RelMovLUser）的加速度
比例。
该命令设置的加速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：加速度比例。取值范围：[1,100]。
示例：
```lua
-- 机械臂以50%的加速度比例运动至P1点。
AccL(50)
```
```lua
MovL(P1)
SpeedFactor
```
原型：
```lua
SpeedFactor(ratio)
#### 描述:
```
设置机械臂全局运动速率，详见运动指令中的通用说明。
该命令设置的全局速率仅在当前工程运行中生效，未设置时，沿用运行脚本前控制软件（通过控制
软件运行脚本时）或TCP指令（通过TCP指令运行脚本时）设置的值。
必选参数：
ratio：运动速度比例。取值范围：[1,100]。
示例：
```lua
-- 设置全局运动速率为50%。
406
```

```lua
SpeedFactor(50)
SetPayload
```
原型：
```lua
SetPayload(payload, {x, y, z})
SetPayload(name)
```
#### 描述:
设置末端负载的重量和偏心坐标。支持直接设置和通过预设参数组方式两种方式设置。该命令设置
的参数仅在当前工程运行中生效，覆盖之前的设置；工程停止后恢复为原来的设置。
必选参数1：
payload：末端负载的重量。单位：kg。
{x, y, z}：末端负载的偏心坐标，坐标轴方向参考下图。单位：mm。
示例1：
```lua
-- 设置末端负载重量为1.5kg，偏心坐标为{0, 0, 10}。
SetPayload(1.5, {0, 0, 10})
```
必选参数2：
name：预设负载参数组的名称，需要先在控制软件中保存对应参数组。
407

示例2：
```lua
-- 使用名称为load1的预设负载参数组设置末端负载。
SetPayload("load1")
```
User
原型：
```lua
User(user)
#### 描述:
```
设置全局用户坐标系。如果用户调用其他指令时未通过可选参数指定用户坐标系，系统会使用全局
用户坐标系。
该命令设置的全局用户坐标系仅在当前工程运行中生效，未设置时默认的全局用户坐标系为用户坐
标系0。
必选参数：
user：切换后的用户坐标系，通过索引序号指定，需要先在控制软件中添加对应坐标系。若指定
的坐标系为空或不存在会导致工程停止运行并报错。
示例：
```lua
-- 切换全局用户坐标系为用户坐标系1。
User(1)
```
#### -- 下面的示例说明全局用户坐标系的生效范围
MovL({pose={300,200,300,180,0,0} }) -- 使用全局用户坐标系（1）
MovL({pose={300,200,300,180,0,0} },{user=2}) -- 使用可选参数指定的用户坐标系（2）
MovL(P1) -- P1为示教点，使用示教点自带的用户坐标系。
SetUser
原型：
```lua
SetUser(index,table,type)
#### 描述:
```
修改指定的用户坐标系。
必选参数：
408

index：用户坐标系索引，需要先在控制软件中添加对应坐标系。若指定的坐标系索引不存在
会导致工程停止运行并报错。
table：修改后的用户坐标系，格式为{x, y, z, rx, ry, rz}。涉及到rx、ry、rz旋转值时，建议使
用CalcUser指令获取。
可选参数：
type：是否全局保存。默认值为0。
0：该命令修改的坐标系仅在当前工程运行中生效，工程停止后恢复为原来的值。
1：该命令修改的坐标系全局保存，工程停止后依然保持修改后的值。但当 type=1 （全
#### 局保存）时，用户需要手动切换到对应的坐标系设置界面，再切换回来，才能看到更新后
的值，即需要手动刷新界面。
示例：
```lua
-- 修改用户坐标系1为CalcUser算出的新坐标系，仅工程运行中生效。
newUser = CalcUser(1,1,{10,10,10,10,10,10})
```
```lua
SetUser(1,newUser)
#### User(1) -- 切换全局用户坐标系为1
-- 修改用户坐标系1为CalcUser算出的新坐标系，全局保存。
newUser = CalcUser(1,1,{10,10,10,10,10,10})
```
```lua
SetUser(1,newUser,1)
MovL(P1,{user=1}) -- 使用可选参数指定本条指令的参考用户坐标系为1
```
CalcUser
原型：
```lua
CalcUser(index,matrix\_direction,table)
#### 描述:
```
计算用户坐标系。
必选参数：
index：用户坐标系索引，取值范围：[0,50]，用户坐标系0初始值为基坐标系。
matrix\_direction：计算的方向。
1：左乘，表示index指定的坐标系沿基坐标系偏转table指定的值。
0：右乘，表示index指定的坐标系沿自己偏转table指定的值。
table：用户坐标系偏移值，格式为{x, y, z, rx, ry, rz}。
返回：
计算得出的用户坐标系，格式为{x, y, z, rx, ry, rz}。
示例：
409

```lua
-- 下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿基坐标系平移{x=10, y=10, z=10}并旋转
{rx=10, ry=10, rz=10}后，得到的新坐标系为newUser。
newUser = CalcUser(1,1,{10,10,10,10,10,10})
-- 下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿用户坐标系1平移{x=10, y=10, z=10}并
旋转{rx=10, ry=10, rz=10}后，得到的新坐标系为newUser。
newUser = CalcUser(1,0,{10,10,10,10,10,10})
```
Tool
原型：
```lua
Tool(tool)
#### 描述:
```
切换全局工具坐标系。如果用户调用其他指令时未通过可选参数指定工具坐标系，系统会使用全局
工具坐标系。
该命令设置的全局工具坐标系仅在当前工程运行中生效，未设置时默认的全局工具坐标系为工具坐
标系0。
必选参数：
tool：切换后的工具坐标系，通过索引序号指定，需要先在控制软件中添加对应坐标系。若指定的
坐标系为空或不存在会导致工程停止运行并报错。
示例：
```lua
-- 切换全局工具坐标系为工具坐标系1。
Tool(1)
```
#### -- 下面的示例说明全局工具坐标系的生效范围
MovL({pose={300,200,300,180,0,0} }) -- 使用全局工具坐标系（1）
MovL({pose={300,200,300,180,0,0} },{tool=2}) -- 使用可选参数指定的工具坐标系（2）
MovL(P1) -- P1为示教点，使用示教点自带的工具坐标系。
SetTool
原型：
```lua
SetTool(index,table,type)
#### 描述:
```
修改指定的工具坐标系。
410

必选参数：
index：工具坐标系索引，需要先在控制软件中添加对应坐标系。若指定的坐标系索引不存在
会导致工程停止运行并报错。
table：修改后的工具坐标系，格式为{x, y, z, rx, ry, rz}。涉及到rx、ry、rz旋转值时，建议使
用CalcTool指令获取。
可选参数：
type：是否全局保存。默认值为0。
0：该命令修改的坐标系仅在当前工程运行中生效，工程停止后恢复为原来的值。
1：该命令修改的坐标系全局保存，工程停止后依然保持修改后的值。但当 type=1 （全
#### 局保存）时，用户需要手动切换到对应的坐标系设置界面，再切换回来，才能看到更新后
的值，即需要手动刷新界面。
示例：
```lua
-- 修改工具坐标系1为CalcTool算出的新坐标系，仅工程运行中生效。
newTool = CalcTool(1,1,{10,10,10,10,10,10})
```
```lua
SetTool(1,newTool)
#### Tool(1) -- 切换全局工具坐标系为1
-- 修改工具坐标系1为CalcTool算出的新坐标系，全局生效。
newTool = CalcTool(1,1,{10,10,10,10,10,10})
```
```lua
SetTool(1,newTool,1)
MovL(P1,{tool=1}) -- 使用可选参数指定本条指令的参考工具坐标系为1
```
CalcTool
原型：
```lua
CalcTool(index,matrix\_direction,table)
#### 描述:
```
计算工具坐标系。
必选参数：
index：工具坐标系索引，取值范围：[0,50]，根据坐标系0初始值为法兰坐标系（TCP0）。
matrix\_direction：计算的方向。
1：左乘，表示index指定的坐标系沿法兰坐标系（TCP0）偏转table指定的值。
0：右乘，表示index指定的坐标系沿自己偏转table指定的值。
table：工具坐标系，格式为{x, y, z, rx, ry, rz}。
返回：
计算得出的工具坐标系，格式为{x, y, z, rx, ry, rz}。
411

示例：
```lua
-- 下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿法兰坐标系（TCP0）平移{x=10, y=10, z
=10}并旋转{rx=10, ry=10, rz=10}后，得到的新坐标系为newTool。
newTool = CalcTool(1,1,{10,10,10,10,10,10})
-- 下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿工具坐标系1平移{x=10, y=10, z=10}并
旋转{rx=10, ry=10, rz=10}后，得到的新坐标系为newTool。
newTool = CalcTool(1,0,{10,10,10,10,10,10})
```
GetPose
原型：
```lua
GetPose(user\_index, tool\_index)
#### 描述:
```
获取机械臂的实时位姿。
若在两条运动指令之间调用该指令，获取的点位会受到平滑过渡设置的影响：
若关闭了平滑过渡功能（cp或r为0），可准确获取到目标点。
若开启了平滑过渡功能，会获取到过渡曲线上的某个点位。
可选参数：
user\_index：位姿对应的用户坐标系索引，需要先在控制软件中添加对应坐标系。不设置时
使用全局用户坐标系。
tool\_index：位姿对应的工具坐标系索引，需要先在控制软件中添加对应坐标系。不设置时使
用全局工具坐标系。
返回：
#### 机械臂当前位姿：{pose = {x, y, z, rx, ry, rz}}
示例：
```lua
-- 机械臂先运动至P1，再返回当前位姿。
local currentPose = GetPose()
```
```lua
MovJ(P1)
MovJ(currentPose)
-- 机械臂运动至P1后再运动至P2，获取P1的位姿。
MovJ(P1,{cp=0})
#### local currentPose = GetPose() --获取P1的位姿
```
```lua
MovJ(P2)
412
```

```lua
-- 获取当前位姿在用户坐标系1和工具坐标系1下的位姿。
local currentPose = GetPose(1,1)
```
GetAngle
原型：
```lua
GetAngle()
#### 描述:
```
获取机械臂的实时关节角度。
若在两条运动指令之间调用该指令，获取的关节角会受到平滑过渡设置的影响：
若关闭了平滑过渡功能（cp或r为0），可准确获取到目标点对应的关节角。
若开启了平滑过渡功能，会获取到过渡曲线上某个点位对应的关节角。
返回：
机械臂当前的关节角度：{joint = {j1, j2, j3, j4, j5, j6} }。
示例：
```lua
-- 机械臂先运动至P1，再返回当前位姿。
local currentAngle = GetAngle()
```
```lua
MovJ(P1)
MovJ(currentAngle)
-- 机械臂运动至P1后再运动至P2，获取P1的关节角度。
MovJ(P1,{cp=0})
local currentAngle = GetAngle() --获取P1的关节角度
```
```lua
MovJ(P2)
GetABZ
```
原型：
```lua
GetABZ()
#### 描述:
```
获取编码器当前位置。
返回值：
413

编码器的当前位置。
示例：
```lua
-- 获取已设置的ABZ编码器的当前位置并赋值给变量abz。
local abz = GetABZ()
```
CheckMovJ
原型：
```lua
CheckMovJ(P, {user = 1, tool = 0, a = 20, v = 50, cp = 100})
#### 描述:
```
检查当前点到目标点关节运动的可运行性。系统会算出整个运动轨迹，检查轨迹中是否有不可达的
点位。
必选参数：
P：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
返回：
检查结果。
#### 0：无错误
#### 16：终点接近肩部奇异
#### 17：终点逆解无解
#### 18：终点逆解限位
#### 22：手势切换错误
#### 26：终点接近腕部奇异
#### 27：终点接近肘部奇异
#### 29：速度参数错误
#### 30：全参逆解求解失败
#### 32：轨迹有肩部奇异点
#### 33：轨迹存在逆解无解点
414

#### 34：轨迹存在逆解限位点
#### 35：轨迹有腕部奇异点
#### 36：轨迹有轴部奇异点
#### 37：轨迹存在关节跳变点
示例：
```lua
-- 检查使用关节运动默认设置是否可达P1，如果可达，则关节运动至P1。
local status=CheckMovJ(P1)
if(status==0)
then
```
```lua
MovJ(P1)
status=CheckMovJ(P2) -- 从P1到P2的可行性检查要等机械臂运动到P1后再执行
if(status==0)
then
```
```lua
MovJ(P2)
end
end
```
CheckMovL
原型：
```lua
CheckMovL(P, {user = 1, tool = 0, a = 20, v = 50, cp = 100, r = 5})
#### 描述:
```
检查当前点到目标点直线运动的可运行性。系统会算出整个运动轨迹，检查轨迹中是否有不可达的
点位。
必选参数：
P：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
415

r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
返回：
检查结果。
#### 0：无错误
#### 16：终点接近肩部奇异
#### 17：终点逆解无解
#### 18：终点逆解限位
#### 22：手势切换错误
#### 26：终点接近腕部奇异
#### 27：终点接近肘部奇异
#### 29：速度参数错误
#### 30：全参逆解求解失败
#### 32：轨迹有肩部奇异点
#### 33：轨迹存在逆解无解点
#### 34：轨迹存在逆解限位点
#### 35：轨迹有腕部奇异点
#### 36：轨迹有轴部奇异点
#### 37：轨迹存在关节跳变点
示例：
```lua
-- 检查使用直线运动默认设置是否可达P1，如果可达，则直线运动至P1。
local status=CheckMovL(P1)
if(status==0)
then
```
```lua
MovL(P1)
status=CheckMovL(P2) -- 从P1到P2的可行性检查要等机械臂运动到P1后再执行
if(status==0)
then
```
```lua
MovL(P2)
end
end
```
SetSafeWallEnable
原型：
```lua
SetSafeWallEnable(index,value)
#### 描述:
```
416

开启或关闭指定的安全墙。该命令设置的安全墙状态仅在当前工程运行中生效，工程停止后恢复为
原来的值。
必选参数：
index：要设置的安全墙索引，需要先在控制软件中添加对应的安全墙。取值范围：[1,8]。
value：
true：开启安全墙。
false：关闭安全墙。
示例：
```lua
-- 开启索引为1的安全墙。
SetSafeWallEnable(1,true)
```
SetWorkZoneEnable
原型：
```lua
SetWorkZoneEnable(index,value)
#### 描述:
```
开启或关闭指定的安全区域。该命令设置的安全区域状态仅在当前工程运行中生效，工程停止后恢
复为原来的值。
必选参数：
index：要设置的安全区域索引，需要先在控制软件中添加对应的安全区域。取值范围：
[1,6]。
value：
true：开启安全区域。
false：关闭安全区域。
示例：
```lua
-- 开启索引为1的安全区域。
SetWorkZoneEnable(1,true)
```
SetCollisionLevel
原型：
```lua
SetCollisionLevel(level)
#### 描述:
```
417

设置碰撞检测等级。对于CRA机型，该命令设置的是安全限制的TCP力和功率值。该命令设置的值
仅在当前工程运行中生效，工程停止后恢复为原来的值。
必选参数：
level：碰撞检测等级，0表示关闭碰撞检测，1~5数字越大灵敏度越高。
示例：
```lua
-- 设置碰撞检测等级为3。
SetCollisionLevel(3)
```
SetBackDistance
原型：
```lua
SetBackDistance(distance)
#### 描述:
```
设置机械臂检测到碰撞后原路回退的距离。该命令设置的值仅在当前工程运行中生效，工程停止后
恢复为原来的值。
必选参数：
distance：碰撞回退的距离，取值范围：[0,50]，单位：mm。
示例：
```lua
-- 设置碰撞回退距离为20mm。
SetBackDistance(20)
```
418

IO
#### 指令列表
IO指令用于进行机械臂系统IO的读写和相关参数的设置。
#### 指令
#### 功能
DI
#### 获取DI端口的状态
DIGroup
#### 获取多个DI端口的状态
DIGroupDEC
#### 获取多个DI端口的状态，返回值为十进制数
DO
#### 设置数字输出端口状态
DOGroup
#### 设置多个数字输出端口状态
DOGroupDEC
#### 通过赋值十进制设置多个数字输出端口状态
GetDO
#### 获取数字输出端口当前状态
GetDOGroup
#### 获取多个数字输出端口当前状态
GetDOGroupDEC
#### 获取多个数字输出端口当前状态，返回值为十进制数
AI
#### 获取AI端口的值
AO
#### 设置模拟输出端口的值
GetAO
#### 获取模拟输出端口当前的值
DI
原型：
```lua
DI(index)
#### 描述:
```
读取数字输入端口状态。
必选参数：
index：DI端子的编号。
返回：
对应的DI端子的状态（ON/OFF）。
419

示例：
```lua
-- DI1为ON时机械臂以直线运动方式运动至P1点。
if (DI(1)==ON)
then
```
```lua
MovL(P1)
end
```
DIGroup
原型：
```lua
DIGroup(index1,...,indexN)
#### 描述:
```
读取多个数字输入端口状态。
必选参数：
index：DI端子的编号，可输入多个，以逗号分隔。
返回：
对应的DI端子的状态（ON/OFF），以数组形式返回。
示例：
```lua
-- 当DI1和DI2都为ON时机械臂以直线运动方式运动至P1点。
local digroup = DIGroup(1,2)
if (digroup[1]&digroup[2]==ON)
then
```
```lua
MovL(P1)
end
```
DIGroupDEC
原型：
```lua
DIGroupDEC(index1,...,indexN)
#### 描述:
```
读取多个数字输入端口状态，返回值为十进制数。
必选参数：
index：DI端子的编号，以逗号分隔；最多可输入24个。
420

返回：
对应的DI端子的状态，以十进制形式返回。
说明：
返回值按照低位优先的方式计算，假设按照1，3，5的顺序定义了一个组DI，则转化成二
进制数是按照5，3，1的顺序保存。
输入table变量的顺序有先后区分，DIGroupDEC({1,2,3})和DIGroupDEC({1,3,2})的结果
是不同的。如DI（1,2,3)输出0(DI3)、1(DI2)、1(DI1)，那么DIGroupDEC({1,2,3})返回
结果为3(即DI3=0,DI2=1,DI1=1)；DIGroupDEC({1,3,2})返回结果为5(即
DI2=1,DI3=0,DI1=1)。
假设DI1~DI6的信号状态如下表所示：
#### index（DI编号）
1
2
3
4
5
6
#### DI信号状态
ON
OFF
ON
OFF
ON
OFF
#### 对应二进制
1
0
1
0
1
0
示例1：
GDI={1,3,6}
```lua
result=DIGroupDEC(GDI)
----------------------
```
### 1.先读取{1,3,6}的信号状态为ON、ON、OFF
### 2.返回十进制时，按照低位优先的顺序进行保存；也就是按照DI6、DI3、DI1对应的二进制进行保存。即实际保存的二
#### 进制是011（DI6=0,DI3=1,DI1=1）
### 3.将二进制011转化为十进制，那么如上指令执行完成后返回值为3
示例2：
GDI={1,6,3}
```lua
result=DIGroupDEC(GDI)
----------------------
```
### 1.先读取{1,6,3}的信号状态为ON、OFF、ON
### 2.返回十进制时，按照低位优先的顺序进行保存；也就是按照DI3、DI6、DI1对应的二进制进行保存。即实际保存 的
#### 二进制是101（DI3=1,DI6=0,DI1=1）
### 3.将二进制101转化为十进制，那么如上指令执行完成后返回值为5
DO
原型：
```lua
DO(index,ON|OFF,time\_ms)
421
```

#### 描述:
设置数字输出端口状态。
必选参数：
index：DO端子的编号。
ON|OFF：要设置的DO端口状态。
可选参数：
time\_ms： 持续输出时间，单位ms，取值范围：[25, 60000]。如果设置了该参数，系统会在指定
时间后对DO自动取反。取反为异步动作，不会阻塞指令队列，系统执行了DO输出后就会执行下
一条指令。
示例：
```lua
-- 将DO1设置为ON。
DO(1,ON)
-- 将DO1设置为ON,并在50ms后再自动设置为OFF。
```
```lua
DO(1,ON,50)
DOGroup
```
原型：
```lua
DOGroup({index1,ON|OFF},..,{indexN,ON|OFF})
#### 描述:
```
设置多个数字输出端口状态。
必选参数：
index：DO端子的编号。
ON|OFF：要设置的DO端口状态。
可设置多组，每组以大括号包裹，组间以逗号隔开。
示例：
```lua
-- 将DO1和DO2设置为ON。
DOGroup({1,ON},{2,ON})
```
DOGroupDEC
原型：
422

```lua
DOGroupDEC({index1,index2,...,indeN},value)
#### 描述:
```
通过赋值十进制设置多个数字输出端口状态。
必选参数：
index：DO端子的编号，以逗号分隔；最多可输入24个。
value：十进制值，赋值顺序按照低位优先的方式。
示例1：
GDO={1,2,3,4,5}
```lua
DOGroupDEC(GDO,18)
------------------
```
### 1.先将十进制数18转化为二进制10010
### 2.按低位优先进行赋值，如下所示：
index: 1 2 3 4 5
value: 0 1 0 0 1
### 3.如上指令执行完成后将DO2和DO5切换到ON状态
示例2：
GDO={5,4,3,2,1}
```lua
DOGroupDEC(GDO,18)
------------------
```
### 1.先将十进制数18转化为二进制10010
### 2.按低位优先进行赋值，如下所示：
index: 5 4 3 2 1
value: 0 1 0 0 1
### 3.如上指令执行完成后将DO4和DO1切换到ON状态
GetDO
原型：
```lua
GetDO(index)
#### 描述:
```
获取数字输出端口当前状态。
必选参数：
index：DO端子的编号。
#### 返回
423

对应的DI端子的状态（ON/OFF）。
示例：
```lua
-- 获取DO1的当前状态。
local do1 = GetDO(1)
```
GetDOGroup
原型：
```lua
GetDOGroup(index1,...,indexN)
#### 描述:
```
获取多个数字输出端口当前状态。
必选参数：
index：DO端子的编号，可输入多个，以逗号分隔。
返回：
对应的DO端子的状态（ON/OFF），以数组形式返回。
示例：
```lua
-- 获取DO1和DO2的当前状态。
local dogroup = GetDOGroup(1,2)
local do1 = dogroup[1]
local do2 = dogroup[2]
```
GetDOGroupDEC
原型：
```lua
GetDOGroupDEC(index1,...,indexN)
#### 描述:
```
获取多个数字输出端口当前状态，返回值为十进制数。
必选参数：
index：DO端子的编号，以逗号分隔；最多可输入24个。
返回：
424

对应的DO端子的状态，以十进制形式返回。
示例：
```lua
-- 获取DO1和DO2的当前状态。
local dogroup = GetDOGroupDEC(1,2)
local do1 = dogroup[1]
local do2 = dogroup[2]
```
AI
原型：
```lua
AI(index)
#### 描述:
```
读取模拟输入端口的值。值的含义（电压/电流）可在DobotStudio Pro的监控 > 控制柜AI/AO页
面查看和修改。
必选参数：
index：AI端子的编号。
返回：
对应的AI端子的值。
示例：
#### -- 读取AI1的值
```lua
local ai1 = AI(1)
AO
```
原型：
```lua
AO(index,value)
#### 描述:
```
设置模拟输出端口的值。值的含义（电压/电流）可在DobotStudio Pro的监控 > 控制柜AI/AO页
面查看和修改。
必选参数：
index：AO端子的编号。
425

value：要设置的值，电压取值范围：[0,10]，单位：V；电流取值范围：[4,20]，单位：
mA。
示例：
```lua
-- 将AO1的输出值设置为2。
AO(1,2)
```
GetAO
原型：
```lua
GetAO(index)
#### 描述:
```
获取模拟输出端口当前的值。值的含义（电压/电流）可在DobotStudio Pro的监控 > 控制柜
AI/AO页面查看和修改。
必选参数：
index：AO端子的编号。
#### 返回
对应的AO端子的值。
示例：
```lua
-- 获取AO1的当前状态。
local ao1 = GetAO(1)
```
426

#### 末端工具
#### 指令列表
末端工具指令用于进行机械臂系统末端IO的读写和相关参数的设置。
#### 指令
#### 功能
ToolDI
#### 读取末端数字输入端口状态
ToolDO
```lua
#### 设置末端数字输出端口状态(队列指令)
GetToolDO
```
#### 获取末端数字输出端口当前状态
ToolAI
#### 读取末端模拟输入端口的值
SetToolMode
#### 设置末端复用端子通信模式
GetToolMode
#### 获取末端复用端子通信模式
SetToolPower
#### 设置末端工具供电状态
SetTool485
#### 设置末端工具的RS485接口对应的数据格式
ToolDI
原型：
```lua
ToolDI(index)
描述：
```
读取末端数字输入端口状态。
必选参数：
index：末端DI端子的编号。
返回：
对应的DI端子的状态（ON/OFF）。
示例：
```lua
-- 末端DI1为ON时机械臂以直线运动方式运动至P1点。
if (ToolDI(1)==ON)
then
```
```lua
MovL(P1)
end
```
427

ToolDO
原型：
```lua
ToolDO(index,ON|OFF)
描述：
```
设置末端数字输出端口状态。
必选参数：
index：末端DO端子的编号。
ON|OFF：要设置的DO端口状态。
示例：
```lua
-- 将末端DO1设置为ON。
ToolDO(1,ON)
```
GetToolDO
原型：
```lua
GetToolDO(index)
描述：
```
获取末端数字输出端口当前状态。
必选参数：
index：末端DO端子的编号。
#### 返回
对应的末端DO端子的状态（ON/OFF）。
示例：
```lua
-- 获取末端DO1的当前状态。
GetToolDO(1)
```
ToolAI
原型：
428

```lua
ToolAI(index)
描述：
```
读取末端模拟输入端口的值。
使用前需要通过SetToolMode将端子设置为模拟输入模式。
说明：
无末端AI接口的机械臂调用此接口无效果。
必选参数：
index：末端AI端子的编号。
返回：
对应的AI端子的值。
示例：
```lua
-- 读取末端AI1的值并赋值给变量test。
test = ToolAI(1)
```
SetToolMode
原型：
```lua
SetToolMode(mode,type,identify)
描述：
```
对于机械臂末端AI1和AI2接口与485接口复用端子的机型，可通过此接口设置末端复用端子的模
式。
默认模式为485模式。
说明：
不支持末端模式切换的机械臂调用此接口无效果。
必选参数：
429

mode：复用端子的模式。
1：485模式。
2：模拟输入模式。
type：
当mode为1时，该参数无效。
当mode为2时，该参数用于设置模拟输入的模式。
个位表示AI1的模式，十位表示AI2的模式，十位为0时可仅输入个位。
取值：
0：0~10V电压输入模式。
1：电流采集模式。
2：0~5V电压输入模式。
样例：
0：AI1与AI2均为0~10V电压输入模式。
1：AI2是0~10V电压输入模式，AI1是电流采集模式。
11：AI2和AI1都是电流采集模式。
12：AI2是电流采集模式，AI1是0~5V电压输入模式。
20：AI2是0~5V电压输入模式，AI1是0~10V电压输入模式。
可选参数：
```lua
identify：当机械臂有多个末端航插时，用于指定航插。默认值为1。
1表示航插1。
```
2表示航插2。
示例：
```lua
-- 设置末端复用端子为485模式。
SetToolMode(1,0)
-- 设置CR20A末端航插2的复用端子为485模式。
```
```lua
SetToolMode(1,0,2)
-- 设置末端复用端子为模拟输入模式，两路都是0~10V电压输入模式。
```
```lua
SetToolMode(2,0)
-- 设置末端复用端子为模拟输入模式，AI1是0~10V电压输入模式，AI2是电流模式。
```
430

```lua
SetToolMode(2,10)
GetToolMode
```
原型：
```lua
GetToolMode(identify)
描述：
```
获取末端复用端子的当前模式。
可选参数：
```lua
identify：当机械臂有多个末端航插时，用于指定航插。默认值为1。
1表示航插1。
```
2表示航插2。
返回：
mode：复用端子的模式。
type：模拟输入的模式。
详见SetToolMode的同名参数。
示例：
```lua
-- 获取末端复用端子的模式。
local mode,type = GetToolMode()
-- 获取CR20A末端航插2复用端子的模式。
local mode,type = GetToolMode(2)
```
SetToolPower
原型：
```lua
SetToolPower(status)
描述：
```
设置末端工具供电状态，一般用于重启末端电源，例如对末端夹爪重新上电初始化。
如需连续调用该接口，建议至少间隔4ms以上。
431

说明：
关闭末端电源后末端DO也会失效。
必选参数：
status：末端工具供电状态。
0：关闭电源。
1：打开电源。
示例：
```lua
-- 重启末端工具电源。
SetToolPower(0)
```
```lua
Wait(5)
SetToolPower(1)
```
SetTool485
原型：
```lua
SetTool485(baud,parity,stopbit,identify)
描述：
```
设置末端工具的RS485接口对应的数据格式。
说明：
无末端485接口的机械臂调用此接口无效果。
必选参数：
baud：RS485接口的波特率。
可选参数：
parity：是否有奇偶校验位。
"O"：奇校验。
"E"：偶校验。
"N"：无奇偶校验位。
默认值为“N”。
stopbit：停止位长度。取值范围：0.5，1，1.5，2。默认值为1。
432

误差±0.1以内会自动就近选择（0.4001取值0.5 ，0.3999 报错，1.09999 取值 1）。
```lua
identify：当机械臂有多个末端航插时，用于指定航插，1表示航插1，2表示航插2。
示例：
-- 将末端工具的RS485接口对应的波特率设置为115200Hz，无奇偶校验位，停止位长度为1。
```
```lua
SetTool485(115200,"N",1)
433
```

TCP&UDP
#### 指令列表
TCP&UDP函数用于进行TCP或UDP通信。
#### 指令
#### 功能
TCPCreate
#### 创建TCP网络对象
TCPStart
#### 建立TCP连接
TCPRead
#### 接收TCP对端发送的数据
TCPWrite
#### 发送数据给TCP对端
TCPDestroy
#### 断开TCP连接并销毁socket对象
UDPCreate
#### 创建UDP网络对象
UDPRead
#### 接收UDP对端发送的数据
UDPWrite
#### 发送数据给UDP对端
TCPCreate
原型：
```lua
TCPCreate(isServer, IP, port)
#### 描述:
```
创建TCP网络对象。
必选参数：
isServer：是否创建服务端。
true：表示创建服务端。
false：表示创建客户端。
IP：服务端IP地址。需与客户端IP地址在同一网段，且不冲突。
创建服务端时为机械臂的IP地址。
创建客户端时为对端的地址。
port：服务端端口。
请勿使用下述已被系统占用的端口，会导致服务端创建失败。
434

7, 13, 22, 37, 139, 445, 502, 503（0~1024之间的端口是linux系统自定义端口，被占用可能
#### 性高，请尽量避免使用）,
1501, 1502, 1503, 4840, 8172, 9527,
11740, 22000, 22001, 29999, 30004, 30005, 30006,
60000~65504, 65506, 65511~65515, 65521, 65522。
返回：
err： 0表示创建TCP网络对象成功 ，1表示创建TCP网络对象失败。
socket：创建的socket对象。
示例1：
```lua
-- 创建TCP服务端。
local ip="192.168.5.1" -- 机械臂的IP地址作为服务端的IP地址
#### local port=6001 -- 服务端端口
local err=0
local socket=0
err, socket = TCPCreate(true, ip, port)
```
示例2：
```lua
-- 创建TCP客户端。
local ip="192.168.5.25" -- 外部设备如相机的IP地址作为服务端的IP地址
#### local port=6001 -- 服务端端口
local err=0
local socket=0
err, socket = TCPCreate(false, ip, port)
```
TCPStart
原型：
```lua
TCPStart(socket, timeout)
#### 描述:
```
建立TCP连接。
机械臂作为服务端时，等待客户端来连接。
机械臂作为客户端时，主动连接服务端。
必选参数：
socket：已创建的socket对象。
timeout：等待超时时间，单位：秒。
如果设置为0，会一直等待到连接建立成功。
435

如果不为0，则超过设定的时间后返回连接失败。
返回：
连接结果。
0：连接成功。
1：输入参数错误。
2：socket对象不存在。
3：设置超时时间错误。
4：连接失败。
示例：
```lua
-- 开始建立TCP连接，一直等待直到连接建立成功。
err = TCPStart(socket, 0) -- socket为TCPCreate成功返回的socket对象
```
TCPRead
原型：
```lua
TCPRead(socket, timeout, type)
#### 描述:
```
接收TCP对端发送的数据。
必选参数：
socket：已创建的socket对象。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为小于等于0的值，会一直等待直到读取完数据再往下执行。
如果设为大于0的值，则超过设定的时间后会直接往下执行。
type：返回值类型。
如果不设置或设置为"table"，则RecBuf缓存格式为table形式。
如果设置为“string”，则RecBuf缓存为字符串。
返回：
err： 0表示接收数据成功 ，1表示接收数据失败。
Recbuf：接收数据缓存区。
示例：
```lua
-- 接收TCP数据，永不超时，接收的数据保存为table形式。
#### -- socket为TCPCreate成功返回的socket对象
err, RecBuf = TCPRead(socket) -- RecBuf数据类型为table
```
436

```lua
-- 接收TCP数据，超时时间为5秒，接收的数据保存为table形式。
#### -- socket为TCPCreate成功返回的socket对象
err, RecBuf = TCPRead(socket,5) -- RecBuf数据类型为table
-- 接收TCP数据，永不超时，接收的数据保存为字符串。
```
#### -- socket为TCPCreate成功返回的socket对象
err, RecBuf = TCPRead(socket,0,"string") -- RecBuf数据类型为字符串
TCPWrite
原型：
```lua
TCPWrite(socket, buf, timeout)
#### 描述:
```
发送数据给TCP对端。
必选参数：
socket：已创建的socket对象。
buf：要发送的数据。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为0，会一直等待直到对端接收完数据再往下执行。
如果不为0，则超过设定的时间后会直接往下执行。
返回：
发送结果。
0：发送成功。
1：发送失败。
示例：
```lua
-- 发送TCP数据，数据内容为“test”,永不超时。
TCPWrite(socket, "test") -- socket为TCPCreate成功返回的socket对象
-- 发送TCP数据，数据内容为“test”,超时时间为5秒。
```
TCPWrite(socket, "test", 5) -- socket为TCPCreate成功返回的socket对象
TCPDestroy
437

原型：
```lua
TCPDestroy(socket)
#### 描述:
```
断开TCP连接并销毁socket对象。
必选参数：
socket：已创建的socket对象。
返回：
执行结果。
0：执行成功。
1：执行失败。
示例：
```lua
-- 断开与TCP对端的连接。
TCPDestroy(socket) -- socket为TCPCreate成功返回的socket对象
```
UDPCreate
原型：
```lua
UDPCreate(isServer, IP, port)
#### 描述:
```
创建UDP网络对象，仅可创建一个。
必选参数：
isServer：是否创建服务端。
true：表示创建服务端。
false：表示创建客户端。
IP：创建服务端和客户端时都填写为对端IP地址。需与机械臂IP地址在同一网段，且不冲突。
port：
#### 创建服务端时，表示本端和对端都使用该端口。请勿使用已被系统占用的端口，详见
TCPCreate的参数说明。
创建客户端时，表示对端的端口。此时本端发送数据时会使用随机端口。
返回：
err： 0表示创建UDP网络对象成功 ，1表示创建UDP网络对象失败。
438

socket：创建的socket对象。
示例1：
```lua
-- 创建UDP服务端。
local ip="192.168.5.25" -- 外部设备如相机的IP地址作为对端的IP地址
#### local port=6001 -- 本端和对端都使用该端口
local err=0
local socket=0
err, socket = UDPCreate(true, ip, port)
```
示例2：
```lua
-- 创建UDP客户端。
local ip="192.168.5.25" -- 外部设备如相机的IP地址作为对端的IP地址
#### local port=6001 -- 对端端口
local err=0
local socket=0
err, socket = UDPCreate(false, ip, port)
```
UDPRead
原型：
```lua
UDPRead(socket, timeout, type)
#### 描述:
```
接收UDP对端发送的数据。
必选参数：
socket：已创建的socket对象。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为小于等于0的值，会一直等待直到读取完数据再往下执行。
如果设为大于0的值，则超过设定的时间后会直接往下执行。
type：返回值类型。
如果不设置或设置为"table"，则RecBuf缓存格式为table形式。
如果设置为“string”，则RecBuf缓存为字符串。
返回：
err： 0表示接收数据成功 ，1表示接收数据失败。
Recbuf：接收数据缓存区。
示例：
439

```lua
-- 接收UDP数据，接收的数据分别保存为字符串和table形式。
#### -- socket为UDPCreate成功返回的socket对象
err, RecBuf = UDPRead(socket,0,"string") -- RecBuf数据类型为字符串
err, RecBuf = UDPRead(socket,0) -- RecBuf数据类型为table
```
UDPWrite
原型：
```lua
UDPWrite(socket, buf, timeout)
#### 描述:
```
发送数据给UDP对端。
必选参数：
socket：已创建的socket对象。
buf：要发送的数据。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为0，会一直等待直到对端接收完数据再往下执行。
如果不为0，则超过设定的时间后会直接往下执行。
返回：
发送结果。
0：发送成功。
1：发送失败。
示例：
```lua
-- 发送UDP数据，数据内容为“test”。
UDPWrite(socket, "test") -- socket为UDPCreate成功返回的socket对象
```
440

Modbus
#### 指令列表
Modbus函数用于建立Modbus主站与从站进行通讯，寄存器地址的取值范围与定义请参考对应从
站的Modbus寄存器地址定义说明。
#### 指令
#### 功能
ModbusCreate
#### 创建Modbus主站
ModbusRTUCreate
#### 创建基于RS485接口的Modbus主站
ModbusClose
#### 和Modbus从站断开连接
GetInBits
#### 读取触点寄存器
GetInRegs
#### 读取输入寄存器
GetCoils
#### 读取线圈寄存器
SetCoils
#### 写入线圈寄存器
GetHoldRegs
#### 读取保持寄存器
SetHoldRegs
#### 写入保存寄存器
各类寄存器对应的Modbus功能码遵循标准Modbus协议：
#### 寄存器类型
#### 读取寄存器
#### 写单个寄存器
#### 写多个寄存器
#### 线圈寄存器
01
05
0F
#### 触点寄存器
02
-
-
#### 输入寄存器
04
-
-
#### 保持寄存器
03
06
10
ModbusCreate
原型：
```lua
ModbusCreate(IP, port, slave\_id, isRTU)
#### 描述:
```
创建基于TCP/IP协议的Modbus主站，并和从站建立连接。最多支持同时连接15个设备。
441

连接机器人自带的从站时IP设置为机器人的IP（默认192.168.5.1，可修改），端口设置为
502（map1）或1502（map2），详见附录A Modbus寄存器定义。
连接第三方从站时，IP和端口为第三方从站的地址，读写寄存器时的寄存器地址的取值范围与定义
请参考对应从站的Modbus寄存器地址定义说明。
必选参数：
IP：从站IP地址。
port：从站端口。
可选参数：
slave\_id：从站ID。
isRTU：布尔值。
isRTU为false时，建立基于控制柜网口的Modbus TCP通讯。
isRTU为true时，建立基于本体末端RS485的Modbus RTU通讯，只能使用60000端口。
注意：
#### 此参数决定了连接建立后传输数据使用的协议格式，并不影响连接结果。因此，如
果创建主站时该参数设置错误，依然可以创建成功，但后续通讯时会导致异常。
返回：
err：
0：创建Modbus主站成功 。
1：创建的主站已达到最大数量，创建新主站失败。
2：初始化主站失败，建议检查IP，端口，网络是否正常等。
3：连接从站失败，建议排查从站是否正常建立，网络是否正常等。
id：返回的主站索引，后续调用其他Modbus指令时使用。取值范围：[0, 14]。
示例：
```lua
-- 创建ModbusTCP主站，并和机器人自带从站建立连接。
-- IP为机器人的IP，端口为502，不指定从站ID。
local ip="192.168.5.1"
local port=502
local err=0
local id=0
err, id = ModbusCreate(ip, port)
-- 创建ModbusTCP主站，并和指定从站建立连接。
```
#### -- 从站IP为192.168.5.123，端口为503，从站ID为1
```lua
local ip="192.168.5.123"
local port=503
local err=0
local id=0
```
442

```lua
err, id = ModbusCreate(ip, port, 1)
-- 创建ModbusRTU-over-TCP主站，并和指定从站建立连接。
-- 从站IP为192.168.5.123，端口为503，从站ID为1。
local ip="192.168.5.123"
local port=503
local err=0
local id=0
err, id = ModbusCreate(ip, port, 1, true)
```
ModbusRTUCreate
原型：
```lua
ModbusRTUCreate(slave\_id, baud, parity, data\_bit, stop\_bit)
#### 描述:
```
创建基于控制柜RS485接口的Modbus主站，并和从站建立连接。最多支持同时连接15个设备。
必选参数：
slave\_id：从站ID。
baud：RS485接口的波特率。
可选参数：
parity：是否有奇偶校验位。"O"表示奇校验，"E"表示偶校验，"N"表示无奇偶校验位。不带
参时默认值为“E”。
data\_bit：数据位长度。取值范围：8。不带参时默认值为8。
stopbit：停止位长度。取值范围：1，2。不带参时默认值为1。
返回：
err： 0表示创建Modbus主站成功 ，1表示创建Modbus主站失败。
id：返回的主站索引，后续调用其他Modbus指令时使用。
示例：
```lua
-- 创建Modbus主站并与RS485接口连接的从站建立连接，从站ID为1，波特率为115200。
err, id = ModbusRTUCreate(1, 115200)
-- 创建Modbus主站并与RS485接口连接的从站建立连接，从站ID为1，波特率为115200，无奇偶校验位，数据位长度
```
为1，停止位长度为2。
```lua
err, id = ModbusRTUCreate(1, 115200, "N", 8, 2)
ModbusClose
```
443

原型：
```lua
ModbusClose(id)
#### 描述:
```
和Modbus从站断开连接，释放主站。
必选参数：
id：已创建的主站索引。
返回：
操作结果。
0：断开连接成功。
1：断开连接失败。
示例：
```lua
-- 和Modbus从站断开连接。
ModbusClose(id)
```
GetInBits
原型：
```lua
GetInBits(id, addr, count)
#### 描述:
```
读取Modbus从站触点寄存器地址的值。对应Modbus功能码02。
必选参数：
id：已创建的主站索引。
addr：触点寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续读取触点寄存器的数量。取值范围：[1, 2000]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
返回：
触点寄存器地址的值，存储在table中。table中第一个值对应触点寄存器起始地址的值。
说明：
如果输入的地址不是整型数字或字符串别名时，提示指令参数类型错误。
444

#### 如果地址输入的字符串没有对应的寄存器别名时，后台日志报错，提示寄存器没有对应
的地址，返回-1。
#### 如果输入的地址与对应的寄存器地址范围不一致时，后台日志报错，提示非法的地址，
返回-1。线圈寄存器地址为1-9999、触点寄存器地址为10001-19999、输入寄存器地址
为30001-39999、保持寄存器地址为40001-49999。
示例：
```lua
-- 从地址0开始连续读取5个地址的值。
inBits = GetInBits(id,0,5)
```
GetInRegs
原型：
```lua
GetInRegs(id, addr, count, type)
#### 描述:
```
按照指定的数据类型，读取Modbus从站输入寄存器地址的值。对应Modbus功能码04。
必选参数：
id：已创建的主站索引。
addr：输入寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续读取输入寄存器的数量。取值范围：[1, 125]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
返回：
输入寄存器地址的值，存储在table中。table中第一个值对应输入寄存器起始地址的值。
示例：
```lua
-- 从地址2048开始读取一个16位无符号整数。
data = GetInRegs(id, 2048, 1)
```
445

```lua
-- 从地址2048开始读取一个32位无符号整数。
data = GetInRegs(id, 2048, 2, "U32")
-- 从地址2048开始读取两个32位单精度浮点数。
data = GetInRegs(id, 2048, 4, "F32")
```
GetCoils
原型：
```lua
GetCoils(id, addr, count)
#### 描述:
```
读取Modbus从站线圈寄存器地址的值。对应Modbus功能码01。
必选参数：
id：已创建的主站索引。
addr：线圈寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续读取线圈寄存器的数量。取值范围：[1, 2000]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
返回：
线圈寄存器地址的值，存储在table中。table中第一个值对应线圈寄存器起始地址的值。
示例：
```lua
-- 从地址0开始连续读取5个地址的值。
Coils = GetCoils(id,0,5)
```
SetCoils
原型：
```lua
SetCoils(id, addr, count, table)
#### 描述:
```
将指定的值写入线圈寄存器指定的地址。对应Modbus功能码05（写单个）和0F（写多个）。
必选参数：
id：已创建的主站索引。
446

addr：线圈寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续写入线圈寄存器的数量。取值范围：[1, 1968]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
table：要写入线圈寄存器的值，存储在table中。table中第一个值对应线圈寄存器起始地址
的值。
返回：
返回值为0或-1，0表示设置成功，-1表示设置失败。
示例：
```lua
-- 从地址1024开始，连续写5个线圈。
local coils = {0,1,1,1,0}
```
```lua
SetCoils(id, 1024, #coils, coils)
GetHoldRegs
```
原型：
```lua
GetHoldRegs(id, addr, count, type)
#### 描述:
```
按照指定的数据类型，读取Modbus从站保持寄存器地址的值。对应Modbus功能码03。
必选参数：
id：已创建的主站索引。
addr：保持寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续读取保持寄存器的数量。取值范围：[1, 125]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
返回：
保持寄存器地址的值，存储在table中。table中第一个值对应保持寄存器起始地址的值。
447

示例：
```lua
-- 从地址2048开始读取一个16位无符号整数。
data = GetHoldRegs(id, 2048, 1)
-- 从地址2048开始读取一个32位无符号整数。
data = GetHoldRegs(id, 2048, 2, "U32")
-- 从地址2048开始读取两个32位单精度浮点数。
data = GetHoldRegs(id, 2048, 4, "F32")
```
SetHoldRegs
原型：
```lua
SetHoldRegs(id, addr, count, table, type)
#### 描述:
```
按照指定的数据类型，写入指定的值到保持寄存器指定的地址。对应Modbus功能码06（写单
个）和10（写多个）。
必选参数：
id：已创建的主站索引。
addr：保持寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：连续写入保持寄存器的数量。取值范围：[1, 123]（ModbusTCP协议限制，实际取
值范围请根据从站寄存器数量或协议规范确定）。
table：要写入保持寄存器的值，存储在table中。table中第一个值对应保持寄存器起始地址
的值。
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
返回：
返回值为0或-1，0表示设置成功，-1表示设置失败。
448

示例：
```lua
-- 从地址2048开始，写入一个16位无符号整数。
local data = {12}
```
```lua
SetHoldRegs(id, 2048, 1, data)
-- 从地址2048开始，写入一个64位双精度浮点数。
local data = {95.32105}
```
```lua
SetHoldRegs(id, 2048, 4, data, "F64")
-- 从地址2048开始，写入两个64位双精度浮点数。
local data = {95.32105, 104.24411}
```
```lua
SetHoldRegs(id, 2048, 8, data, "F64")
449
```

#### 总线寄存器
#### 指令列表
总线寄存器指令用于读写Profinet或Ethernet/IP总线寄存器。
说明：
Magician E6不支持这组指令。
#### 指令
#### 功能
GetInputBool
#### 获取输⼊寄存器指定地址的bool值
GetInputInt
#### 获取输⼊寄存器指定地址的int值
GetInputFloat
#### 获取输⼊寄存器指定地址的float值
GetOutputBool
#### 获取输出寄存器指定地址的bool值
GetOutputInt
#### 获取输出寄存器指定地址的int值
GetOutputFloat
#### 获取输出寄存器指定地址的float值
SetOutputBool
#### 设置输出寄存器指定地址的bool值
SetOutputInt
#### 设置输出寄存器指定地址的int值
SetOutputFloat
#### 设置输出寄存器指定地址的float值
GetInputBool
原型：
```lua
GetInputBool(address)
#### 描述:
```
获取输⼊寄存器指定地址的bool类型的数值。
必选参数：
address：寄存器地址，取值范围[0-63]。
返回：
指定的寄存器地址的值，为0或1。
450

示例：
```lua
-- 当输入寄存器0的值为1时，执行后续操作。
if(GetInputBool(0)==1)
then
```
#### -- 执行后续操作
```lua
end
GetInputInt
```
原型：
```lua
GetInputInt(address)
#### 描述:
```
获取输⼊寄存器指定地址的int类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
返回：
指定的寄存器地址的值，为整型数（int32）。
示例：
```lua
-- 读取输入寄存器1的值并赋值给变量regInt。
local regInt = GetInputInt(1)
```
GetInputFloat
原型：
```lua
GetInputFloat(address)
#### 描述:
```
获取输⼊寄存器指定地址的float类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
返回：
指定的寄存器地址的值，为单精度浮点数（float）。
451

示例：
```lua
-- 读取输入寄存器2的值并赋值给变量regFloat。
local regFloat = GetInputFloat(2)
```
GetOutputBool
原型：
```lua
GetOutputBool(address)
#### 描述:
```
获取输出寄存器指定地址的bool类型的数值。
必选参数：
address：寄存器地址，取值范围[0-63]。
返回：
指定的寄存器地址的值，为0或1。
示例：
```lua
-- 当输出寄存器0的值为1时，执行后续操作。
if(GetOutputBool(0)==1)
then
```
#### -- 执行后续操作
```lua
end
GetOutputInt
```
原型：
```lua
GetOutputInt(address)
#### 描述:
```
获取输出寄存器指定地址的int类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
返回：
指定的寄存器地址的值，为整型数（int32）。
452

示例：
```lua
-- 读取输出寄存器1的值并赋值给变量regInt。
local regInt = GetOutputInt(1)
```
GetOutputFloat
原型：
```lua
GetOutputFloat(address)
#### 描述:
```
获取输出寄存器指定地址的float类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
返回：
指定的寄存器地址的值，为单精度浮点数（float）。
示例：
```lua
-- 读取输出寄存器2的值并赋值给变量regFloat。
local regFloat = GetOutputFloat(2)
```
SetOutputBool
原型：
```lua
SetOutputBool(address, value)
#### 描述:
```
设置输出寄存器指定地址的bool类型的数值。
必选参数：
address：寄存器地址，取值范围[0-63]。
value：要设置的值，支持布尔值或0/1。
示例：
```lua
-- 设置输出寄存器0的值为假。
SetOutputBool(0,0)
```
453

SetOutputInt
原型：
```lua
SetOutputInt(address, value)
#### 描述:
```
设置输出寄存器指定地址的int类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
value：要设置的值，支持整型数（int32）。
示例：
```lua
-- 设置输出寄存器1的值为123。
SetOutputInt(1,123)
```
SetOutputFloat
原型：
```lua
SetOutputFloat(address, value)
#### 描述:
```
设置输出寄存器指定地址的float类型的数值。
必选参数：
address：寄存器地址，取值范围[0-23]。
value：要设置的值，支持单精度浮点数（float）。
受浮点数的存储机制（IEEE754）限制，单精度浮点数可保存的有效数字约6到7位（与小数点
位置无关）。有效数字大于6位的值保存为单精度浮点数可能会产生偏差，有效数字位数越多
可能产生的偏差越大。
示例：
```lua
-- 设置输出寄存器2的值为12.3。
SetOutputFloat(2,12.3)
```
454

#### 程序控制
#### 指令列表
程序控制函数为程序运行控制相关的通用函数。
#### 指令
#### 功能
Print
#### 打印调试信息到控制台
Log
#### 输出自定义日志
Wait
#### 等待指定时间或满足指定条件后再继续执行下一条命令
Pause
#### 暂停脚本运行
ResetElapsedTime
#### 开始计时
ElapsedTime
#### 结束计时
Systime
#### 获取当前系统时间
Popup
#### 设置脚本运行时的弹窗
Print
原型：
```lua
Print(value)
#### 描述:
```
打印调试信息到控制台（命令名也可写成 print ）。
说明：
变量打印出来的格式和本文档中描述的格式可能有所差异，但表示的是同一种数据格式，参
考本文档中描述的格式理解和使用即可。
```lua
-- 例如，变量的格式为 {pose={x,y,z,rx,ry,rz}}，打印出来的格式则类似于：
table:0x123abc{
[pose] => table:0x123abc{
[1] => x
[2] => y
[3] => z
```
455

[4] => rx
[5] => ry
[6] => rz
}
}
必选参数：
value：待打印的数据。
示例：
```lua
-- 打印字符串Success到控制台。
Print('Success')
```
Log
原型：
```lua
Log(value)
#### 描述:
```
输出自定义级别的日志信息，可在控制软件日志页面中查看及导出。
必选参数：
value：日志信息。
示例：
456

```lua
-- 输出内容为test的日志信息。
Log('test')
```
Wait
原型：
```lua
Wait(time\_ms)
Wait(check\_str)
```
```lua
Wait(check\_str, timeout\_ms)
#### 描述:
```
机械臂完成上一条指令后，等待指定时间或满足指定条件后再继续执行下一条命令。等待时间的最
大值为2147483647ms，设置的参数超过最大值会导致指令无效。
必选参数：
time\_ms：参数值为integer类型时，表示指定等待时间，小于等于0时表示不等待。单位：
ms。
check\_str：参数值为string类型时，表示判断逻辑，逻辑为true后再继续执行下一条指令。
可选参数：
timeout\_ms：超时时间，单位：ms。
判断逻辑一直为false且等待超过该时间时，系统会继续执行下一条指令并返回false。
小于等于0时表示不等待，立刻超时。
不设置该参数时，表示不会超时，会一直等待直到判断逻辑为true。
返回：
满足条件并继续运行时，返回true；
不满足条件，因超时导致继续运行时，返回false。
示例：
```lua
-- 等待300ms。
Wait(300)
-- DI1为ON时继续运行。
Wait("DI(1) == ON")
-- DO1为ON且AI(1)小于7时继续运行。
Wait("GetDO(1) == ON and AI(1) < 7")
-- 根据1s内DI1的状态执行不同的业务逻辑。
```
457

```lua
flag=Wait("DI(1) == ON", 1000)
if(flag==true)
then
```
#### -- DI1状态为ON
else
#### -- DI1状态为OFF且等待超过1s
```lua
end
Pause
```
原型：
```lua
Pause()
#### 描述:
```
暂停脚本运行。需要通过控制软件或者远程控制操作才可继续运行。
示例：
```lua
-- 机械臂运行至P1点后暂停运行，通过外部控制继续运行后才会运行至P2点。
MovJ(P1)
```
```lua
Pause()
MovJ(P2)
```
ResetElapsedTime
原型：
```lua
ResetElapsedTime()
#### 描述:
```
待此指令前所有指令执行完成后开始计时，需配合ElapsedTime()一起使用，可用于计算运行时
间。
示例：
请参考ElapsedTime的示例。
ElapsedTime
原型：
```lua
ElapsedTime()
458
```

#### 描述:
结束计时，返回时间差，需配合ResetElapsedTime()一起使用。
#### 返回:
从开始计时到结束计时的时间差，单位毫秒。
最大可统计4294967295ms（约49.7天），超过此时间后会从0开始重新计数。
示例：
```lua
-- 计算机械臂在P1和P2间直线运动来回10次的时间，打印至控制台。
MovJ(P2)
```
```lua
ResetElapsedTime()
for i=1,10 do
```
```lua
MovL(P1)
MovL(P2)
end
```
```lua
print (ElapsedTime())
Systime
```
原型：
```lua
Systime()
#### 描述:
```
获取当前系统时间。
#### 返回:
系统当前时间的Unix时间戳，单位转换为了毫秒，即从格林威治时间1970年1月1日零时到当前时
间的毫秒数，一般用于计算时间差。
如需获取当地时间，请根据当地时区使用获取的格林威治时间换算获得。
示例：
```lua
-- 获取系统当前时间。
local time1 = Systime()
```
print(time1) -- > 1686304295963,换算为北京时间为2023-06-09 17:51:35（加963毫秒）
```lua
local time2 = Systime()
print(time2) -- > 1686304421968,换算为北京时间为2023-06-09 17:53:41（加968毫秒）
-- 计算机械臂移动到P1所花费的时间，单位毫秒。
local time1 = Systime()
```
```lua
MovL(P1)
local time2 = Systime()
```
459

```lua
print(time2-time1)
Popup
```
原型：
```lua
Popup(message, title, type, logControl)
#### 描述:
```
用于在脚本运行时弹出用户可自定义信息的窗口。DobotStudio Pro只会同时显示一个弹窗，内
容为最新的弹窗信息。
如果工程运行时未打开DobotStudio Pro（例如通过IO或Modbus启动工程），则不会出现弹
窗，但警告和错误类的弹窗指令依旧会导致工程暂停。
必选参数：
message：要显示的信息内容。支持包括字符串在内的各种lua变量。如果是字符串，长度不
能超过128；如果是其他变量类型，会被转换为字符，转换后的字符大小不能超过512字节。
title：要显示的弹窗标题。仅支持字符串，长度不能超过128。
type：消息类型。
0：信息。该类型弹窗不影响工程运行。单击停止可停止工程，单击确定直接关闭弹窗。
弹窗出现后，用户也可通过IO或Modbus停止工程，工程停止后弹窗自动消失。
#### 1：警告。该类型弹窗会暂停工程。单击停止可停止工程，单击继续可继续运行工程。弹
窗出现后，用户也可通过IO或Modbus停止/继续工程，工程停止/继续后弹窗自动消失。
#### 2：错误。该类型弹窗会暂停工程。单击停止可停止工程，单击继续可继续运行工程。弹
窗出现后，用户也可通过IO或Modbus停止/继续工程，工程停止/继续后弹窗自动消失。
460

logControl：弹窗内容是否写入日志。
0：不写入日志。
1：写入对应类型的日志（其中“信息”类型的弹窗消息对应“自定义”类型的日志）。
日志内容为“弹窗标题：弹窗内容”。
示例1：
```lua
-- 警告类弹窗，弹窗内容为字符串，写入日志。
#### Popup("请放置工件后单击继续", "未检测到工件", 1, 1)
```
示例2：
```lua
-- 提示类弹窗，弹窗内容为变量，不写入日志。
#### Popup(a, "变量a的值为", 0, 0)
```
461

#### 程序中断
#### 指令列表
在程序运行过程中，当满足特定的条件（中断条件）时，机器人转向执行特定的指令（中断程
序），执行完成后回到主程序继续运行。
#### 指令
#### 功能
Trap
#### 创建一个新的中断
DestroyTrap
#### 注销中断
StopMove
#### 立即停止当前运动
StartMove
#### 中断后返回中断的位置，继续运动
Halt
#### 停止脚本运行
Trap
原型：
```lua
Trap("name", "condition", function)
#### 描述:
```
创建一个新的中断。最多支持创建5个中断，不支持中断嵌套，按触发先后顺序进行响应（即如果
在中断程序的执行过程中，又再次触发了中断条件；那么机器人不会立即执行第二次中断，而是先
把上一次的中断执行完成后，再执行第二次中断）。
必选参数：
name：中断名称，不可重复。
condition：中断条件（条件语句表达式），中断条件被触发后执行中断动作function。
function：中断动作，满足中断条件后，执行已定义函数或自定义函数。
示例：
```lua
Trap("trap1", "DI(1)==1", function)
说明：
```
#### 可点击Trap指令右侧的
#### 图标弹出如下配置弹窗，在弹窗内配置中断名称、中断条件和中
462

断动作；配置完成后，点击确定完成。
中断名称：输入中断名称，不可重复。
中断条件：设置触发中断的条件，中断条件被触发后执行设置的中断动作。点击 + 可新增中
#### 断条件，最多支持3条中断条件。中断条件之间的关系可以是并且、或者（第一行的语句
为“当”，第二行可以选择 “并且”、“或者”。第三行不可选，跟随第二行的逻辑条件统
一变化）。中断条件支持的类型包括“DI、GetDO、ToolDI、GetToolDO、AI、GetAO、
全局变量、表达式”。
中断动作：满足中断条件后，执行已定义函数或手动输入的函数。
已定义函数：直接输入函数名。
手动输入：输入自定义的函数。
DestroyTrap
原型：
```lua
DestroyTrap("name")
#### 描述:
```
注销中断。
必选参数：
#### name：待注销的中断名称
463

示例：
```lua
DestroyTrap("trap1")
StopMove
```
原型：
```lua
StopMove(type)
#### 描述:
```
立即停止当前运动。
必选参数：
type：停止运动的方式。0表示正常停止（默认值），1表示快速停止。
StartMove
原型：
```lua
StartMove()
#### 描述:
```
中断后返回中断的位置，继续执行被StopMove停止的运动。若用户在中断函数中调用了力控相关
指令，需调用FCOff关闭力控功能后才能退出中断。即需在StartMove前加上FCOff指令，否则会
报错。
Halt
原型：
```lua
Halt()
#### 描述:
```
停止脚本运行。
示例：
```lua
-- 当变量count=100时，停止脚本运行。
if(count==100)
then
```
464

```lua
Halt()
end
```
#### 中断指令示例
#### -- 定义中断动作test\_aa
```lua
function test\_aa()
StopMove(0)
MovJ({joint={0,0,0,0,0,0}})
```
```lua
StartMove()
end
--中断条件触发后，机器人停止向P1点运动；改为向零点运动。中断结束后机器人返回到中断时的停止位置，继续向P
```
1点运动，最后运动到P2点。
```lua
Trap("trap1", "DI(9)==1", test\_aa)
MovJ(P1)
```
```lua
MovJ(P2)
465
```

#### 托盘
#### 指令列表
托盘是按排列规律放置批量物料的承载装置，常用于自动化上下料环节。托盘中通常阵列分布了许
多凹槽，每个凹槽都可以放置一个物料。使用托盘指令可以通过示教少量的点创建一个完整的托盘
点位阵列，并可以获取已创建托盘中的具体点位，快速实现机器人自动上下料。
#### 指令
#### 功能
CreateTray
#### 创建托盘
GetTrayPoint
#### 获取托盘点
CreateTray
原型：
CreateTray(Trayname, {Count}, {P1,P2}) -- 一维托盘
CreateTray(Trayname, {row,col}, {P1,P2,P3,P4}) -- 二维托盘
CreateTray(Trayname, {row,col,layer}, {P1,P2,P3,P4,P5,P6,P7,P8}) -- 三维托盘
#### 描述:
创建托盘，支持创建一维、二维和三维的托盘。
最多可创建20个托盘，创建同名的托盘时会覆盖已有的托盘，不会增加托盘数量。
必选参数：
Trayname：托盘名称，最长32字节的字符串，不允许为纯数字或者纯空格。
后两个参数为table变量，根据要创建的托盘维度不同，table内的值的数量不同，下文分别进行介
绍。
创建一维托盘：一维托盘是在一条直线上等距分布的一组点。
{Count}：Count表示点位数量，取值范围：[2, 50]，输入非整数会自动向下取整。
{P1,P2}：P1和P2分别为一位托盘的2个端点，支持示教点和位姿变量。
创建二维托盘：二维托盘是在一个平面上阵列分布的一组点。
466

{row,col}：row表示行方向（P1到P2方向）上点位的数量，col表示列方向（P1到P4方
向）上点位的数量，取值范围都与一维托盘的Count相同。
{P1,P2,P3,P4}：P1、P2、P3、P4分别为二维托盘的4个顶点，支持示教点和位姿变量。
创建三维托盘：三位托盘是在空间上立体分布的一组点，可视为竖向排布的多个二维托盘。
{row,col,layer}：row表示行方向（P1到P2方向）上点位的数量，col表示列方向（P1到
P4方向）上点位的数量，layer表示层数（P1到P5方向）。
{P1,P2,P3,P4,P5,P6,P7,P8}：P1~P8分别为三维托盘的8个顶点，支持示教点和位姿变
量。
注意：
如果使用了末端工具，请确保示教点位时选择了末端工具对应的工具坐标系。
示例：
```lua
-- 创建名称为t1的5个点的一维托盘。
CreateTray("t1", {5}, {P1,P2})
-- 创建名称为t2的4x5的二维托盘。
```
```lua
CreateTray("t2", {4,5}, {P1,P2,P3,P4})
-- 创建名称为t3的4x5x6的三维托盘。
```
```lua
CreateTray("t2", {4,5,6}, {P1,P2,P3,P4,P5,P6,P7,P8})
GetTrayPoint
```
467

原型：
```lua
GetTrayPoint(Trayname, index)
#### 描述:
```
获取指定托盘指定序号的点位。点位序号和创建托盘时传入的点位顺序有关。
一维托盘：P1点序号为1，P2点序号与点位数量相同，以此类推。
二维托盘：下图以3x3的托盘为例说明示教点与点位序号的关系。
三维托盘：参考二维托盘，第二层的第一个点的序号为第一层最后一个点的序号加一，以此类
推。
必选参数：
Trayname：已创建的托盘名称，最长32字节的字符串。
index：要获取的点位的序号。
返回：
对应序号的点位坐标。
如果创建托盘时使用的点为示教点，则返回的点位格式也是示教点。
如果创建托盘时使用的点为位姿变量，则返回的点位格式也是位姿变量。
示例：
```lua
-- 获取名称为t1的托盘的序号为3的点位。
GetTrayPoint("t1",3)
```
468

#### 安全皮肤
#### 指令列表
安全皮肤指令用于设置安全皮肤相关功能。
#### 指令
#### 功能
EnableSafeSkin
#### 安全皮肤开关
SetSafeSkin
#### 设置安全皮肤各个部位的灵敏度
EnableSafeSkin
原型：
```lua
EnableSafeSkin(ON|OFF)
#### 描述:
```
安全皮肤开关。
必选参数：
ON|OFF：ON表示开启安全皮肤，OFF表示关闭安全皮肤。
返回：
0：操作失败，未检测到安全皮肤。
1：操作成功。
示例：
```lua
-- 开启安全皮肤。
EnableSafeSkin(ON)
```
SetSafeSkin
原型：
```lua
SetSafeSkin(part,status)
#### 描述:
```
设置安全皮肤各个部位的灵敏度。
469

必选参数：
part：要设置的部位，取值范围 [3, 6]。
3：小臂。
4：J4关节。
5：J5关节。
6：J:6关节。
#### status：要设置的灵敏度，取值范围 [0, 3]
0：关闭。
1：低灵敏度（接近距离≤5cm）。
2：中灵敏度（接近距离≤10cm）。
3：高灵敏度（接近距离≤15cm）。
示例：
```lua
-- 将小臂的安全皮肤灵敏度设置为关闭。
SetSafeSkin(3,0)
```
470

# 附录D 远程控制信号时序图
使用远程信号控制机械臂运行工程的典型时序图如下。
机械臂未使能时工程控制相关信号不生效。
若配置了防护停止输入且未配置防护停止重置输入（安全IO），其典型时序图如下。
防护停止状态生效时工程控制相关信号不生效；机械臂运行工程时触发防护停止状态会触发工程暂
停，复位后会继续运行工程。
471

472

# 附录E Python编程函数说明
## 基础语法
## 通用说明
## 运动指令
## 相对运动指令
## 运动参数
## IO
## 末端工具
## TCP&UDP
## Modbus
## 程序控制
473

#### 基础语法
说明：
本手册的读者应当已具备一定的Python编程基础。本手册仅列举部分Python基础语法，便于
您快速查阅。
Python语言对大小写敏感，程序中所有标识符（变量名、函数名等）的大小写都必须与本手册提
供的示例一致。
#### 变量
变量用于存储值，将值作为参数传递或结果返回。Python中变量无需声明，赋值后即可使用。
变量通过等号进行赋值，双等号用于判断左右两边的表达式是否相等。
除Python本身的作用域规则外，您还可以通过DobotStudio Pro的监控 > 全局变量页面设置控制
器级别的全局变量，可在同一个控制器内的不同工程中直接调用。
下方示例用于说明控制器全局变量的作用域和全局保持功能。
'''
#### 工程1的main.py文件
#### 假设已在全局变量页面添加了两个全局变量g1和g2
#### g1非全局保持，取值为10
#### g2全局保持，取值为20
'''
474

a = 1
print(a) --> 1
print(g1) --> 10
print(g2) --> 20
#### g1=11 # 对非全局保持的全局变量赋值
#### g2=22 # 对全局保持的全局变量赋值
print(g1) --> 11
print(g2) --> 22
'''
#### 工程2的main.py文件
#### 工程2在工程1之后运行
'''
#### print(a) # 变量不存在
#### print(b) # 变量不存在
```lua
#### print(g1) # 10(非全局保持的变量恢复成了工程1修改前的值)
#### print(g2) # 22(全局保持的变量变成了工程1修改后的值)
```
Python支持多种数据类型，Dobot设计API时常用的包括数字（Number），布尔值（bool），
字符串（String），列表（List）和字典（Dictionary）。
#### 数字
Python中的数字支持 int（长整数）、float（双精度浮点数）、bool（布尔值）和complex（复
数）。
var1 = 1
var2 = 10
#### 布尔值
布尔类型只有两个可选值：True（真） 和 False（假）。在Python中，True和数字1相等，False
和数字0相同，可用于数值运算。此外所有非零的数字和非空的字符串、列表、字典等数据类型都
被视为 True，只有0、空字符串、空列表、空字典等被视为 False。
print(2 < 3) # True
print(2 == 3) # False
a = True
b = False
print(a and b) # False
print(a or b) # True
print(not a) # False
#### 字符串
Python中的字符串用单引号或双引号括起来，可通过加号连接。
475

str1 = 'Dobot'
str2 = " Robot"
print(str1 + str2) # Dobot Robot
字符串可通过下标索引，索引值以 0 为开始值，-1 为从末尾的开始位置。截取字符串的语法格式
为 变量[头下标:尾下标] 。字符串不可被改变，您可以赋值一个新的字符串给变量，但不可通过索引
的方式修改字符串中的某个字符。
str1 = 'Dobot'
print(str1[0]) # D
print(str1[-1]) # t
print(str1[0:2]) # Dob
#### 列表
列表是写在方括号之间，用逗号隔开的元素列表。列表和字符串一样，可通过加号连接，可通过下
标索引。与字符串不同的是，列表可被改变，您可以通过索引单独改变列表中的某个元素。
list1 = [1, 2, 3]
list2 = ["a", "b", "c"]
print(list1 + list2) # [1, 2, 3, "a", "b", "c"]
print(list1[0]) # 1
list1[0] = "a"
print(list1) # ["a", 2, 3]
#### del list[2] # 删除列表中第三个元素
#### 字典
字典是写在大括号之间，用逗号隔开的”键(key) : 值(value)“的集合。字典通过键进行索引，因
此键必须是唯一的。字典可通过键进行增删改查。
dict1 = {"user" : 1, "tool" : 0, "a" : 20, "v" : 50, "cp" : 100}
print(dict1["user"]) # 1
#### dict1["tool"] = 1 # 将字典中tool的值改为1
dict1["r"] = 5 # 如果是字典中不存在的键，则会向字典中添加新的键值对。
#### del dict1["r"] # 删除指定的键值对
#### 流程控制
```lua
#### if条件判断指令
If指令可根据条件判断的结果执行不同的代码块。
```
476

```lua
Python中if语句的标准形式如下，其中elif和else不是必须的：
if condition\_1:
```
statement\_block\_1
```lua
elif condition\_2:
statement\_block\_2
```
else:
statement\_block\_3
如果 "condition\_1" 为 True 将执行 "statement\_block\_1" 语句块
如果 "condition\_1" 为False，将判断 "condition\_2"
如果"condition\_2" 为 True 将执行 "statement\_block\_2" 语句块
如果 "condition\_2" 为False，将执行"statement\_block\_3"语句块
```lua
if语句可嵌套使用，下面是一个典型示例。
a = 100;
b = 200;
```
#### # 检查条件
```lua
if a == 100:
#### # if条件为True时执行以下if条件判断
if b == 200:
#### # if条件为True时执行该语句块
```
#### print("a 的值为:", a ) # a 的值为:100
#### print("b 的值为:", b ) # b 的值为:200
else:
```lua
#### # 第一个if条件为False时执行以下语句块
#### print("a不等于100")
#### while循环指令
```
477

```lua
while指令可根据条件循环执行代码块。
#### Python中while语句的标准形式如下:
while condition：
```
statement\_block
如果"condition"为 True 将执行 "statement\_block" 语句块，然后重新判断"condition"
如果"condition"为False将跳过 "statement\_block" 语句块，直接执行后续语句
示例：
a=10
```lua
while a < 20
#### print("a 的值为:", a) # 执行10次，输出值为10到19
a = a+1
#### for循环指令
for指令可以遍历任何可迭代对象，如一个列表或者一个字符串，根据遍历的次数循环执行代码
```
块。
478

```lua
#### Python中for语句的标准形式如下:
for variable in sequence:
```
statement\_block
sequence为可迭代对象，variable为用于遍历可迭代对象的变量。
### 1. 系统首先将“sequence”中的第一个元素赋值给“variable”，然后执行一
次“statement\_block”语句块。
### 2. 如果“sequence”中还有下一个元素，系统将下一个元素赋值给“variable”，再执行一
次“statement\_block”语句块。
### 3. 重复以上步骤直至“sequence”中全部元素都已被遍历过。
示例：
joint = [j1,j2,j3,j4,j5,j6]
```lua
for angles in joint:
#### print(angles) # 执行6次，输出j1~j6的值
```
479

#### 通用说明
#### 运动方式
机械臂支持的运动方式可分为下述几类。
#### 关节运动
机械臂根据当前各关节角度和目标点各关节角度的差值规划各个关节的运动，使各个关节同时完成
运动。关节运动不约束TCP（Tool Center Point）的运动轨迹，一般情况下该轨迹非直线。
关节运动不受奇异位置限制（奇异点位置详见机械臂对应的硬件手册），因此如果对运动轨迹没有
要求，或目标点位在奇异位置附近，建议使用关节运动。
#### 直线运动
机械臂根据当前位姿和目标点的位姿规划运动轨迹，使TCP运动轨迹为直线，且末端姿态在运动过
程中匀速变化。
当运动轨迹经过奇异位置时，下发直线运动指令给机械臂会产生报错，建议重新规划点位或在奇异
位置附近采用关节运动。
#### 弧线运动
机械臂通过当前位置，P1，P2三个不共线的点确定一个圆弧或整圆。运动过程中的机械臂末端姿
态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运动过程中机械臂到达P1点时
的姿态可能与示教姿态不同）。
当运动轨迹经过奇异位置时，下发弧线运动指令给机械臂会产生报错，建议重新规划点位或在奇异
位置附近采用关节运动。
480

#### 点位参数
如无特殊说明，本手册中所有点位参数都支持三种表达方式：
关节变量：使用各个机械臂各个关节的角度（j1~j6）表示目标点位。
关节变量作为直线或弧线运动参数时系统会通过正解运算将其转换为位姿变量使用，但算法会
保证机械臂抵达目标点时的关节角度会与设置的值一致。
{"joint" : [j1, j2, j3, j4, j5, j6]}
位姿变量：使用笛卡尔坐标（x，y，z）表示目标点位在用户坐标系中的空间位置，使用欧拉
角（rx，ry，rz）表示TCP（Tool Center Point）到达该点时工具坐标系相对于用户坐标系的
旋转角度。
位姿变量作为关节运动的点位参数时系统会通过逆解运算将其转换为关节变量（取离机械臂当
前关节角最近的解）使用。
{"pose" : [x, y, z, rx, ry, rz]}
越疆机械臂计算欧拉角时的旋转顺序为X->Y->Z，每个轴都是绕固定轴（用户坐标系）旋转，如
下图所示（rx=γ，ry=β，rz=α）。
确定了旋转顺序后，就可以将旋转矩阵（其中cα为cosα，sα为sinα的简写，以此类推）
#### 推导为方程
481

通过该方程计算机械臂末端的姿态。
示教点位：使用控制软件示教获取的点位会保存为如下格式的常量。
'''
name：示教点位的名称。
joint：示教点位的关节坐标。
tool：示教时使用的工具坐标系索引。
user：示教时使用的用户坐标系索引。
pose：示教点位的位姿变量值。
'''
{
"name" : "name",
"tool" : index,
"user" : index,
"joint" : [j1, j2, j3, j4, j5, j6],
"pose" : [x, y, z, rx, ry, rz]
}
#### 坐标系参数
运动指令可选参数中的user和tool用于指定目标点的用户和工具坐标系，当前仅支持通过索引序号
指定，需要先在控制软件中添加对应坐标系。
系统选择点位坐标系的优先顺序如下：
### 1. 运动指令可选参数指定了坐标系时，使用指定的坐标系。如果点位参数是示教点，则会将示教
点的位姿坐标换算为指定坐标系下的值进行使用。
### 2. 未通过可选参数指定坐标系时，如果点位是示教点，使用示教点自带的坐标系索引。
### 3. 未通过可选参数指定坐标系时，如果点位是关节变量或位姿变量，使用运动参数中设置的全局
坐标系（详见User和Tool指令，未调用指令设置时的默认坐标系均为0）。
说明：
开始运行脚本时默认的全局坐标系都会被置为0，与运行脚本前点动面板设置的值无关。
调用关节运动指令（MovJ/MovJIO/RelMovJTool/RelMovJUser）时，如果点位是关节
变量，则坐标系参数无效。
#### 速度参数
#### 相对速率
482

可选参数中的a和v用于指定机械臂执行该运动指令时的加速度和速度比例。
#### 机械臂实际运动速度 = 最大速度 x 全局速率 x 指令速率
#### 机械臂实际运动加速度 = 最大加速度 x 指令速率
其中最大速度/加速度受再现参数的控制，可在DobotStudio Pro的运动参数页面查看与修改。
全局速率可通过DobotStudio Pro的速度调节滑轨（上图右上角）或SpeedFactor指令设置。
指令速率为运动指令可选参数携带的比例，未通过可选参数指定运动加速度/速度比例时，默认使
用运动参数中设置的值（详见VelJ，AccJ，VelL，AccL指令，未调用指令设置时的默认值均为
100. 。
  例：
#### AccJ(50) # 设置关节运动默认加速度为50%
#### VelJ(60) # 设置关节运动默认速度为60%
#### AccL(70) # 设置直线运动默认加速度为70%
#### VelL(80) # 设置直线运动默认速度为80%
# 全局速率为20%；
MovJ(P1) # 以（关节加速度最大值 x 50%）的加速度和（关节速度最大值 x 20% x 60%）的速度关节运动至P1
MovJ(P2,{"a":30, "v":80}) # 以（关节加速度最大值 x 30%）的加速度和（关节速度最大值 x 20% x 80%）关
#### 节运动至P1
MovL(P1) # 以（笛卡尔加速度最大值 x 70%）的加速度和直线速度（笛卡尔速度最大值 x 20% x 80%）的速度运动
#### 至P1
MovL(P1,{"a":40, "v":90}) # 以（笛卡尔加速度最大值 x 40%）的加速度和（笛卡尔速度最大值 x 20% x 90%
#### ）的速度直线运动至P1
483

#### 绝对速度
直线和弧线运动指令可选参数中的speed用于指定械臂执行该运动指令时的绝对速度。
绝对速度不受全局速率影响，但受再现参数中的最大速度限制（如果机械臂进入了缩减模式，则受
缩减后的最大速度限制），即speed参数设置的目标速度如果大于再现参数中的最大速度，则以最
大速度为准。
例：
MovL(P1,{"speed":1000}) # 以1000的绝对速率直线移动至P1
MovL设置了speed为1000，小于再现参数中的最大速度2000，则机械臂会以1000mm/s为目标
速度进行运动，该目标速度与此时的全局速率无关。但如果机械臂处于缩减模式（假设缩减率为
10%），则最大速度变为200，小于1000，此时机械臂会以200mm/s为目标速度进行运动。
speed参数和v参数互斥，若同时存在以speed为准。
#### 平滑过渡参数
机械臂连续运动经过多个点时，可以通过平滑过渡的方式经过中间点，避免机械臂拐弯过于生硬。
如果用户指定的几个路径点基于不同的工具坐标系，则无法平滑过渡。
可选参数中的cp或r用于指定当前运动指令到下一条运动指令之间的平滑过渡比例（cp）或者平滑
过渡半径（r），两者互斥，若同时存在以r为准。
说明：
关节运动相关命令不支持设置平滑过渡半径（r），详见各指令的可选参数。
CP
设置平滑过渡比例时，系统会自动计算过渡曲线的弧度，CP值越大曲线越平滑，如下图所示。CP
过渡曲线会受运动速度/加速度影响，即使点位和CP值都相同，运动速度/加速度不同时的过渡曲
线弧度也会不同。
484

R
设置平滑过渡半径时，系统会以过渡点为圆心，根据指定半径计算过渡曲线。R过渡曲线不受运动
速度/加速度影响，只由点位和过渡半径决定。
如果用户设置的过渡半径过大（超过起始点/终点与过渡点之间的距离），则系统会自动使用起始
点/终点与过渡点之间较短距离的一半作为过渡半径计算过渡曲线。
未通过可选参数指定平缓过渡比例或半径时，默认使用运动参数中设置的平滑过渡比例（详见CP
指令，未调用指令设置时的默认值为0）。
485

说明：
平滑过渡会导致机械臂运动不经过中间点，因此设置了平滑过渡时，两条运动指令之间IO信
号输出或功能设置（例如开关安全皮肤）指令会在过渡过程中执行。
如果希望能够在机械臂准确抵达中间点时执行指令，请将前一条指令的平滑过渡参数设置为
0。
由于cp和r的实现方式不同，如果在两条需要平滑过渡的运动指令之间插入其他不影响运动的指令
（例如条件判断、IO、功能设置能），cp和r的处理方式也有所差异。
若使用cp方式过渡，大部分指令不会影响过渡，除非指令的处理时间过长（例如Wait指
令）。
```lua
在下面的示例中，在两条运动指令之间插入一条if语句不会影响cp方式的平滑过渡。
# 可以正常过渡，机器人会在即将到达P1点时判断DI1，如果为ON会以50%的平滑过渡比例过渡到下一条运动指令。
```
```lua
MovL(P1,{"cp":50})
if DI(1)==ON:
```
```lua
MovL(P2)
若使用r方式过渡，仅下述白名单的指令不会影响平滑过渡。插入其他指令都会使r方式的平滑
```
过渡失效。
RelPointTool, RelPointUser, DOGroup, DO, AO, ToolDO,
SetUser, SetTool, User, Tool, CP, AccJ, AccL, VelL, VelJ,
SetPayload, SetCollisionLevel, SetBackDistance, EnableSafeSkin, SetSafeSkin
```lua
在下面的示例中，在两条运动指令之间插入一条if语句会导致r方式平滑过渡设置无效。
# 平滑过渡参数无效，机器人会在到达P1点后再判断DI1。
```
```lua
MovL(P1,{"r":5})
if DI(1)==ON:
```
```lua
MovL(P2)
#### IO信号表示方法
```
系统内部预定义了ON和OFF变量用于表示数字IO是否有信号。
#### ON = 1，表示有信号
#### OFF = 0，表示无信号
参数中的ON|OFF表示该参数取值为ON或OFF，用户也可以使用1或0作为输入。
486

#### 运动指令
#### 指令列表
运动指令用于控制机械臂进行运动，使用前请阅读通用说明。
#### 指令
#### 功能
MovJ
#### 关节运动
MovL
#### 直线运动
Arc
#### 圆弧插补运动
Circle
#### 整圆插补运动
MovJIO
#### 关节运动并输出DO
MovLIO
#### 直线运动并输出DO
ArcIO
#### 圆弧运动并输出DO
StartPath
#### 复现录制的运动轨迹
GetPathStartPose
#### 获取轨迹的起始点
PositiveKin
#### 将关节角度正解为位姿
InverseKin
#### 将位姿逆解为关节角度
MovJ
原型：
```lua
MovJ(point, {"user":1, "tool":0, "a":20, "v":50, "cp":100})
#### 描述:
```
从当前位置以关节运动方式运动至目标点。
必选参数：
point：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
487

v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置关节运动至P1点。
```lua
MovJ(P1)
# 机械臂以默认设置关节运动至指定关节角。
```
```lua
MovJ({"joint":[0,0,90,0,90,0]})
# 机械臂关节运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡比例为50
```
%。
```lua
MovJ({"pose":[300,200,300,180,0,0]},{"user":1, "tool":1, "a":50, "v":50, "cp":50})
# 先定义点位再在运动指令中调用，运行效果和上一条指令相同。
customPoint={"pose":[300,200,300,180,0,0]}
```
```lua
MovJ(customPoint,{"user":1, "tool":1, "a":50, "v":50, "cp":50})
MovL
```
原型：
```lua
MovL(point, {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
```
从当前位置以直线运动方式运动至目标点。
必选参数：
point：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
488

设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置直线运动至P1点。
```lua
MovL(P1)
# 机械臂500m/s的绝对速度直线运动至P1点。
```
```lua
MovL(P1,{"speed":500})
# 机械臂以默认设置直线运动至指定关节角。
```
```lua
MovL({"joint":[0,0,90,0,90,0]})
# 机械臂直线运动至指定位姿，位姿对应用户坐标系1和工具坐标系1，运动加速度与速度均为50%，平滑过渡半径为5m
```
m。
```lua
MovL({"pose":[300,200,300,180,0,0]},{"user":1, "tool":1, "a":50, "v":50, "r":5})
# 先定义点位再在运动指令中调用，运行效果和上一条指令相同。
customPoint={"pose":[300,200,300,180,0,0]}
```
```lua
MovL(customPoint,{"user":1, "tool":1, "a":50, "v":50, "r":5})
# 同时携带speed和v，speed生效，同时控制器会打印对应的警告日志。
```
# 同时携带cp和r，r生效，同时控制器会打印对应的警告日志。
MovL(P1,{"v":50,"speed":500,"cp":60,"r":5}) # 执行时可选参数仅speed和r生效
Arc
原型：
```lua
Arc(P1, P2, {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
```
从当前位置以圆弧插补方式运动至目标点。
需要通过当前位置，P1，P2三个点确定一个圆弧，因此当前位置不能在P1和P2确定的直线上。
489

运动过程中的机械臂末端姿态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运
动过程中机械臂到达P1点时的姿态可能与示教姿态不同）。
必选参数：
P1：圆弧中间点。
P2：目标点。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
示例：
# 机械臂运动至P1，然后经由P2以默认设置圆弧运动至P3。
```lua
MovJ(P1)
Arc(P2,P3)
```
# 机械臂运动至P1，然后经由点[300,200,300,180,0,0]圆弧运动至P3，用户坐标系与工具坐标系都为1，运动加速
度与速度均为50%。
```lua
MovJ(P1)
Arc({pose:[300,200,300,180,0,0]},P3,{"user":1, "tool":1, "a":50, "v":50})
```
490

Circle
原型：
```lua
Circle(P1, P2, Count, {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
```
从当前位置进行整圆插补运动，运动指定圈数后重新回到当前位置。
需要通过当前位置，P1，P2三个点确定一个整圆，因此当前位置不能在P1和P2确定的直线上，且
三个点确定的整圆不能超出机械臂的运动范围。
运动过程中的机械臂末端姿态通过当前点和P2点的姿态插补算出，P1点的姿态不参与运算（即运
动过程中机械臂到达P1点时的姿态可能与示教姿态不同）。
必选参数：
P1：整圆定位点1。
P2：整圆定位点2。
Count：进行整圆运动的圈数，取值范围[1, 999]。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
491

设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
示例：
# 机械臂运动至P1，然后进行沿着P1，P2，P3确定的整圆运动一圈。
```lua
MovJ(P1)
Circle(P2,P3,1)
```
# 机械臂运动至P1，然后沿着P1，点[300,200,300,180,0,0]，P3确定的整圆运动10圈，用户坐标系与工具坐标系都
为1，运动加速度与速度均为50%。
```lua
MovJ(P1)
Circle({pose:[300,200,300,180,0,0]},P3,10,{"user":1, "tool":1, "a":50, "v":50})
```
MovJIO
原型：
MovJIO(P,[[Mode,Distance,Index,Status],[Mode,Distance,Index,Status]...], {"user":1, "tool":0,
"a":20, "v":50, "cp":100})
#### 描述:
从当前位置以关节运动方式运动至目标点，运动时并行设置数字输出端口状态。
必选参数：
P：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：触发模式。0表示百分比触发，1表示距离触发。系统会将各关节角合成一个角度
向量，并计算终点和起点的角度差作为运动的总距离。
Distance：指定百分比/角度。因角度计算使用的是合成角向量，建议使用百分比模式，
效果更直观。
Distance为正数时，表示离起点的百分比/角度。
Distance为负数时，表示离目标点的百分比/角度。
Mode为0时，Distance表示和总角度的百分比。取值范围：(0,100]。
Mode为1时，Distance表示角度的值。单位：°。
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
492

a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置向P1点关节运动，当运动到距离起点30%的位置时，将DO2设置为开。
```lua
MovJIO(P1, [[0, 30, 2, 1]])
# 机械臂以默认设置向P1点关节运动，当运动到距离终点还有15°的位置时，将DO3设置为关。
```
```lua
MovJIO(P1,[[1, -15, 3, 0]])
493
```

MovLIO
原型：
MovLIO(P,[[Mode,Distance,Index,Status],[Mode,Distance,Index,Status]...],{"user":1, "tool":0, "
a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
从当前位置以直线运动方式运动至目标点，运动时并行设置数字输出端口状态。
必选参数：
P：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：触发模式。0表示百分比触发，1表示距离触发。
Distance：指定百分比/距离。
Distance为正数时，表示离起点的百分比/距离。
Distance为负数时，表示离目标点的百分比/距离。
Mode为0时，Distance表示和总距离的百分比。取值范围：(0,100]。
#### Mode为1时，Distance表示距离的值。单位：mm
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
494

a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置向P1点直线运动，当运动到距离起点30%的位置时，将DO2设置为开。
```lua
MovLIO(P1, [[0, 30, 2, 1]])
# 机械臂以默认设置向P1点直线运动，当运动到距离终点15mm的位置时，将DO3设置为关。
```
```lua
MovLIO(P1, [[1, -15, 3, 0]])
ArcIO
```
原型：
ArcIO(P1, P2, [[Mode,Distance,Index,Status],[Mode,Distance,Index,Status]...], {"user":1, "tool"
:0, "a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
在圆弧插补过程中并行输出指定DO信号。适用于涂胶应用场景，控制涂胶头的提前出胶和提前收
胶（如音响涂胶，轨迹主要为圆弧）。
495

需要通过当前点，P1，P2三个点确定一个圆弧，因此当前位置不能在P1和P2确定的直线上。
必选参数：
P1：圆弧中间点。
P2：目标点。
并行数字输出参数：设置当机械臂运动到指定距离或百分比时，触发指定DO。可设置多组，
每组含有以下参数：
Mode：设置触发模式。0表示百分比触发，1表示距离触发。
Distance：运行指定的距离。当Mode为0时，Distance表示起始点与目标点之间距离的
百分比；取值范围：(0,100]。当Mode为1时，Distance表示离起始点或目标点的距离；
单位：mm。
Distance为0时，表示起点即触发。
Distance为正数时，表示离起点的百分比/距离。
Distance为负数时，表示离目标点的百分比/距离。
Index：DO端子的编号。
Status：要设置的DO状态，0和OFF表示无信号，1和ON表示有信号。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。设置此参数后，参数cp将被忽略。
平滑过渡会改变机械臂运动轨迹，对DO输出的时机造成影响，请谨慎使用。
详细介绍请参见通用说明。
496

StartPath
原型：
```lua
StartPath(string, {"multi":1, "isConst":0, "sample":50， "freq":0.2, "user":0, "tool":0})
#### 描述:
```
复现指定的轨迹文件中录制的轨迹。调用该指令前需要用户自行运行机械臂到轨迹的起始点。
必选参数：
string：轨迹文件名（含后缀名）。
可选参数：
multi：复现时的速度倍数，仅当isConst=0时有效。取值范围：[0.1, 2]，不带参时默认值为
1。
isConst：是否匀速复现。不带参时默认值为0。
1表示匀速复现，机械臂会按照全局速率匀速复现轨迹。
0表示按照轨迹录制时的原速复现，并可以使用multi参数等比缩放运动速度，此时机械
臂的运动速度不受全局速率的影响。
sample：轨迹点位采样间隔，即生成轨迹文件时相邻两个点位的采样时间差。取值范围：[8,
1000]，单位：ms，不带参时默认值为50（控制器录制轨迹文件时的采样间隔）。
freq：滤波系数，该参数的值越小，复现的轨迹曲线约平滑，但相对原轨迹的变形越严重，请
根据原轨迹的平滑程度设置合适的滤波系数。取值范围：（0,1]，1表示关闭滤波，不带参时
默认值为0.2。
user：指定轨迹点位对应的用户坐标系索引，不指定时使用轨迹文件中记录的用户坐标系索
引。
tool：指定轨迹点位对应的工具坐标系索引，不指定时使用轨迹文件中记录的工具坐标系索
引。
示例：
# 关节运动到track.csv轨迹文件的起始点后，按2倍的原速复现文件中记录的轨迹。
```lua
StartPoint = GetPathStartPose("track.csv")
MovJ(StartPoint)
```
```lua
StartPath("track.csv", {"multi":2, "isConst":0})
# 关节运动到track.csv轨迹文件的起始点后，匀速复现文件中记录的轨迹。
StartPoint = GetPathStartPose("track.csv")
```
```lua
MovJ(StartPoint)
StartPath("track.csv", {"isConst":1})
```
GetPathStartPose
497

原型：
```lua
GetPathStartPose(string)
#### 描述:
```
获取轨迹的起始点。根据轨迹文件不同，点位数据的类型（示教点/关节/位姿）也可能会不同。
必选参数：
string：轨迹文件名（含后缀名）。
示例：
# 获取track.csv轨迹文件的起始点并打印。
```lua
StartPoint = GetPathStartPose("track.csv")
print(StartPoint)
```
PositiveKin
#### 原型
```lua
PositiveKin(joint, {"user":1, "tool":0})
#### 描述
```
进行正解运算：给定机械臂各关节角度，计算机械臂末端在给定的笛卡尔坐标系中的坐标值。
#### 必选参数
joint：关节变量，格式为 {"joint":[j1, j2, j3, j4, j5, j6]} 。
#### 可选参数
user：用户坐标系索引，不指定时使用全局用户坐标系。
tool：工具坐标系索引，不指定时使用全局工具坐标系。
#### 返回
正解得出的位姿变量，格式为 {"pose":[x, y, z, rx, ry, rz]} 。
#### 示例
```lua
PositiveKin({"joint":[0,0,-90,0,90,0]},{"user":1, "tool":1})
关节坐标为[0,0,-90,0,90,0]，计算机械臂末端在用户坐标系1和关节坐标系1下的笛卡尔坐标。
```
InverseKin
498

#### 原型
```lua
InverseKin(pose, {"useJointNear":True, "jointNear":joint, "user":1, "tool":0})
#### 描述
```
进行逆解运算：给定机械臂末端在给定的笛卡尔坐标系中的坐标值，计算机械臂各关节角度。
由于笛卡尔坐标仅定义了TCP的空间坐标与倾斜角，所以机械臂可以通过多种不同的姿态到达同一
个位姿，意味着一个位姿变量可以对应多个关节变量。为得出唯一的解，系统需要一个指定的关节
坐标，选择最接近该关节坐标的解作为逆解结果。
#### 必选参数
pose：位姿变量，格式为 {"pose":[x, y, z, rx, ry, rz]}
#### 可选参数
useJointNear：布尔值，用于设置JointNear参数是否有效。
True表示根据JointNear参数就近选解。
False或者不带参表示JointNear参数无效，系统根据机械臂当前关节角度就近选解。
如果仅携带此参数而不携带jointNear参数，此参数无效。
jointNear：用于就近选解的关节变量，格式为 {"joint":[j1, j2, j3, j4, j5, j6]} 。
user：用户坐标系索引，不指定时使用全局用户坐标系。
tool：工具坐标系索引，不指定时使用全局工具坐标系。
#### 返回
错误码，0表示逆解成功，-1表示逆解失败（无解）。
逆解得出的关节变量，格式为 {"joint":[j1, j2, j3, j4, j5, j6]} ，逆解失败时j1~j6全部为
0。
#### 示例
errId, jointPoint = InverseKin({"pose":[300, 200, 300, 180, 0, 0]}, {"useJointNear" :True, "jo
intNear":{"joint":[90, 30, -90, 180, 30, 0]} })
机械臂末端在全局用户坐标系和全局关节坐标系下的笛卡尔坐标为[300, 200, 300, 180, 0, 0]，计
算关节坐标，选择离关节角度[90, 30, -90, 180, 30, 0]最近的解。
499

#### 相对运动指令
#### 指令列表
相对运动指令函数用于控制机械臂进行偏移运动，使用前请阅读通用说明。
#### 指令
#### 功能
RelPointUser
#### 点位沿用户坐标系偏移
RelPointTool
#### 点位沿工具坐标系偏移
RelMovJTool
#### 沿工具坐标系进行相对关节运动
RelMovLTool
#### 沿工具坐标系进行相对直线运动
RelMovJUser
#### 沿用户坐标系进行相对关节运动
RelMovLUser
#### 沿用户坐标系进行相对直线运动
RelJointMovJ
#### 关节运动至指定偏移角度
RelJoint
#### 点位偏移指定关节角度
RelPointUser
原型：
```lua
RelPointUser(P, [OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz])
#### 描述:
```
对指定点位沿指定用户坐标系进行偏移，返回偏移后的点位。
必选参数：
P：要进行偏移的指定点。
[OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz]：指定用户坐标系下的偏移量。
x，y，z表示空间偏移量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
如果指定点为示教点，则基于示教点的用户坐标系进行偏移。
如果指定点为关节变量或位姿变量，则基于全局用户坐标系进行偏移。
返回：
如果指定点为示教点，返回偏移后的示教点常量。
如果指定点为关节变量或位姿变量，返回偏移后的位姿变量：{pose = {x, y, z, rx, ry, rz}}。
示例：
500

# 将P1在指定用户坐标系下偏移一定距离，然后运动至偏移后的点位。
Offset=[OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz]
```lua
p = RelPointUser(P1, Offset)
MovL(p)
```
RelPointTool
原型：
```lua
RelPointTool(P, [OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz])
#### 描述:
```
对指定点位沿指定工具坐标系进行偏移，返回偏移后的点位。
必选参数：
P：要进行偏移的指定点。
[OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz]：指定工具坐标系下的偏移量。
x，y，z表示空间偏移量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
如果指定点为示教点，则基于示教点的工具坐标系进行偏移。
如果指定点为关节变量或位姿变量，则基于全局工具坐标系进行偏移。
返回：
如果指定点为示教点，返回偏移后的示教点常量。
如果指定点为关节变量或位姿变量，返回偏移后的位姿变量：{pose = {x, y, z, rx, ry, rz}}。
示例：
# 将P1在指定工具坐标系下偏移一定距离，然后运动至偏移后的点位。
Offset=[OffsetX, OffsetY, OffsetZ, OffsetRx, OffsetRy, OffsetRz]
```lua
p = RelPointTool(P1, Offset)
MovL(p)
```
RelMovJTool
原型：
```lua
RelMovJTool([x, y, z, rx, ry, rz], {"user":1, "tool":0, "a":20, "v":50, "cp":100})
#### 描述:
```
从当前位置沿指定工具坐标系以关节运动方式进行相对运动。关节运动的轨迹非直线，所有关节会
同时完成运动。
必选参数：
501

[x, y, z, rx, ry, rz]：目标点相对于当前位置在指定工具坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置沿全局工具坐标系关节运动至指定偏移点。
```lua
RelMovJTool([10, 10, 10, 0, 0, 0])
RelMovLTool
```
原型：
RelMovLTool([x, y, z, rx, ry, rz], {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100,
"r":5})
#### 描述:
从当前位置沿指定工具坐标系以直线运动方式进行相对运动。
必选参数：
[x, y, z, rx, ry, rz]：目标点相对于当前位置在指定工具坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
#### a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
502

cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置沿全局工具坐标系直线运动至指定偏移点。
```lua
RelMovLTool([10, 10, 10, 0, 0, 0])
RelMovJUser
```
原型：
```lua
RelMovJUser([x, y, z, rx, ry, rz], {"user":1, "tool":0, "a":20, "v":50, "cp":100})
#### 描述:
```
从当前位置沿指定用户坐标系以关节运动方式进行相对运动。关节运动的轨迹非直线，所有关节会
同时完成运动。
必选参数：
[x, y, z, rx, ry, rz]：目标点相对于当前位置在指定用户坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例，与r互斥。取值范围：[0,100]。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置沿全局用户坐标系关节运动至指定偏移点。
```lua
RelMovJUser([10, 10, 10, 0, 0, 0])
RelMovLUser
```
原型：
503

RelMovLUser([x, y, z, rx, ry, rz], {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100,
"r":5})
#### 描述:
从当前位置沿指定用户坐标系以直线运动方式进行相对运动。
必选参数：
[x, y, z, rx, ry, rz]：目标点相对于当前位置在指定用户坐标系下的偏移量。x，y，z表示空间偏移
量，单位：mm；rx，ry，rz表示角度偏移量，单位：°。
可选参数：
user：目标点的用户坐标系。
tool：目标点的工具坐标系。
#### a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置沿全局用户坐标系直线运动至指定偏移点。
```lua
RelMovLUser([10, 10, 10, 0, 0, 0])
RelJointMovJ
```
原型：
RelJointMovJ([Offset1, Offset2, Offset3, Offset4, Offset5, Offset6], {"a":20, "v":50, "cp":100
})
#### 描述:
504

从当前位置以关节运动方式运动至指定关节偏移角度。
必选参数：
[Offset1, Offset2, Offset3, Offset4, Offset5, Offset6]：关节坐标系下J1轴 ~ J6轴方向上的偏移
值，单位：°。
可选参数：
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
示例：
# 机械臂以默认设置关节运动至偏移角度。
```lua
RelJointMovJ([20,20,10,0,10,0])
RelJoint
```
原型：
```lua
RelJoint(P, [Offset1, Offset2, Offset3, Offset4, offset5, offset6])
#### 描述:
```
对指定点位在关节坐标系下增加J1~J6轴偏移量，返回偏移后的关节变量。
必选参数：
P：要进行偏移的指定点。
Offset1~Offset6：关节坐标系下J1轴 ~ J6轴方向上的偏移值，单位：°。
返回：
偏移后的关节变量： {"joint": [j1, j2, j3, j4, j5, j6]} 。
示例：
# 将P1在J1~J6轴上分别偏移一定角度，然后运动至偏移后的点位。
Offset = [Offset1, Offset2, Offset3, Offset4, offset5, offset6]
```lua
p = RelJoint(P1, Offset)
MovJ(p)
```
505

#### 运动参数
#### 指令列表
运动参数函数用于设置或获取机械臂运动相关参数，使用前请阅读通用说明。
#### 指令
#### 功能
CP
#### 设置平滑过渡比例
VelJ
#### 设置关节运动方式的速度比例
AccJ
#### 设置关节运动方式的加速度比例
VelL
#### 设置直线和弧线运动方式的速度比例
AccL
#### 设置直线和弧线运动方式的加速度比例
SpeedFactor
#### 设置机械臂全局运动速率
SetPayload
#### 设置末端负载
User
#### 设置全局用户坐标系
SetUser
#### 修改指定的用户坐标系
CalcUser
#### 计算用户坐标系
Tool
#### 设置全局工具坐标系
SetTool
#### 修改指定的工具坐标系
CalcTool
#### 计算工具坐标系
GetPose
#### 获取机械臂的实时位姿
GetAngle
#### 获取机械臂的实时关节角度
GetABZ
#### 获取编码器当前位置
CheckMovJ
#### 检查关节运动指令的可运行性
CheckMovL
#### 检查直线或弧线运动指令的可运行性
SetSafeWallEnable
#### 开启或关闭指定的安全墙
SetWorkZoneEnable
#### 开启或关闭指定的安全区域
SetCollisionLevel
#### 设置碰撞等级
SetBackDistance
#### 设置碰撞回退距离
CP
506

原型：
```lua
CP(R)
#### 描述:
```
设置平滑过渡比例，即机械臂连续运动经过多个点时，经过中间点是以直角方式过渡还是以曲线方
式过渡。
该命令设置的平滑过渡比例仅在当前工程运行中生效，未设置时默认值为0。
必选参数：
R：平滑过渡比例。取值范围：[0, 100]。
示例：
# 机械臂从P1运动至P3点经过P2点时以50%平滑过渡。
```lua
CP(50)
MovL(P1)
```
```lua
MovL(P2)
MovL(P3)
```
VelJ
原型：
```lua
VelJ(R)
#### 描述:
```
设置关节运动方式（MovJ/MovJIO/RelMovJTool/RelMovJUser/RelJointMovJ）的速度比例。
该命令设置的速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
507

R：速度比例。取值范围：[1,100]。
示例：
# 机械臂以20%的速度比例运动至P1点。
```lua
VelJ(20)
MovJ(P1)
```
AccJ
原型：
```lua
AccJ(R)
#### 描述:
```
设置关节运动方式（MovJ/MovJIO/RelMovJTool/RelMovJUser/RelJointMovJ）的加速度比
例。
该命令设置的加速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：加速度比例。取值范围：[1,100]。
示例：
# 机械臂以50%的加速度比例运动至P1点。
```lua
AccJ(50)
MovJ(P1)
```
VelL
原型：
```lua
VelL(R)
#### 描述:
```
设置直线和弧线运动方式（MovL/Arc/Circle/MovLIO/RelMovLTool/RelMovLUser）的速度比
例。
该命令设置的速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：速度比例。取值范围：[1,100]。
508

示例：
# 机械臂以20%的速度比例运动至P1点。
```lua
VelL(20)
MovL(P1)
```
AccL
原型：
```lua
AccL(R)
#### 描述:
```
设置直线和弧线运动方式（MovL/Arc/Circle/MovLIO/RelMovLTool/RelMovLUser）的加速度
比例。
该命令设置的加速度比例仅在当前工程运行中生效，未设置时默认值为100。
必选参数：
R：加速度比例。取值范围：[1,100]。
示例：
# 机械臂以50%的加速度比例运动至P1点。
```lua
AccL(50)
MovL(P1)
```
SpeedFactor
原型：
```lua
SpeedFactor(ratio)
#### 描述:
```
设置机械臂全局运动速率，详见通用说明。
该命令设置的全局速率仅在当前工程运行中生效，未设置时，沿用运行脚本前控制软件（通过控制
软件运行脚本时）或TCP指令（通过TCP指令运行脚本时）设置的值。
必选参数：
ratio：运动速度比例。取值范围：[1,100]。
示例：
509

# 设置全局运动速率为50%。
```lua
SpeedFactor(50)
SetPayload
```
原型：
```lua
SetPayload(payload, [x, y, z])
SetPayload(name)
```
#### 描述:
设置末端负载的重量和偏心坐标。支持直接设置和通过预设参数组方式两种方式设置。该命令设置
的参数仅在当前工程运行中生效，覆盖之前的设置；工程停止后恢复为原来的设置。
必选参数1：
payload：末端负载的重量。单位：kg。
[x, y, z]：末端负载的偏心坐标，坐标轴方向参考下图。单位：mm。
示例1：
# 设置末端负载重量为1.5kg，偏心坐标为[0, 0, 10]。
```lua
SetPayload(1.5, [0, 0, 10])
必选参数2：
```
name：预设负载参数组的名称，需要先在控制软件中保存对应参数组。
510

示例2：
# 使用名称为load1的预设负载参数组设置末端负载。
```lua
SetPayload("load1")
User
```
原型：
```lua
User(user)
#### 描述:
```
设置全局用户坐标系。如果用户调用其他指令时未通过可选参数指定用户坐标系，系统会使用全局
用户坐标系。
该命令设置的全局用户坐标系仅在当前工程运行中生效，未设置时默认的全局用户坐标系为用户坐
标系0。
必选参数：
user：切换后的用户坐标系，通过索引序号指定，需要先在控制软件中添加对应坐标系。若指定
的坐标系索引不存在会导致工程停止运行并报错。
示例：
# 切换全局用户坐标系为用户坐标系1。
```lua
User(1)
SetUser
```
原型：
```lua
SetUser(index,table,type)
#### 描述:
```
修改指定的用户坐标系。
必选参数：
index：用户坐标系索引，需要先在控制软件中添加对应坐标系。若指定的坐标系索引不存在
会导致工程停止运行并报错。
table：修改后的用户坐标系，格式为[x, y, z, rx, ry, rz]，建议使用CalcUser指令获取。
可选参数：
511

type：是否全局保存。
0：该命令修改的坐标系仅在当前工程运行中生效，工程停止后恢复为原来的值。
1：该命令修改的坐标系全局保存，工程停止后依然保持修改后的值。但当 type=1 （全
#### 局保存）时，用户需要手动切换到对应的坐标系设置界面，再切换回来，才能看到更新后
的值，即需要手动刷新界面。
示例：
# 修改用户坐标系1为CalcUser算出的新坐标系，仅工程运行中生效。
```lua
newUser = CalcUser(1,1,[10,10,10,10,10,10])
SetUser(1,newUser,0)
```
CalcUser
原型：
```lua
CalcUser(index,matrix\_direction,table)
#### 描述:
```
计算用户坐标系。
必选参数：
index：用户坐标系索引，取值范围：[0,50]，用户坐标系0初始值为基坐标系。
matrix\_direction：计算的方向。
1：左乘，表示index指定的坐标系沿基坐标系偏转table指定的值。
0：右乘，表示index指定的坐标系沿自己偏转table指定的值。
table：用户坐标系偏移值，格式为[x, y, z, rx, ry, rz]。
返回：
计算得出的用户坐标系，格式为[x, y, z, rx, ry, rz]。
示例：
# 下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿基坐标系平移[x=10, y=10, z=10]并旋转[
rx=10, ry=10, rz=10]后，得到的新坐标系为newUser。
```lua
newUser = CalcUser(1,1,[10,10,10,10,10,10])
# 下述计算过程可等价为：一个初始位姿与用户坐标系1相同的坐标系，沿用户坐标系1平移[x=10, y=10, z=10]并旋
转[rx=10, ry=10, rz=10]后，得到的新坐标系为newUser。
newUser = CalcUser(1,0,[10,10,10,10,10,10])
```
Tool
512

原型：
```lua
Tool(tool)
#### 描述:
```
切换全局工具坐标系。如果用户调用其他指令时未通过可选参数指定工具坐标系，系统会使用全局
工具坐标系。
该命令设置的全局工具坐标系仅在当前工程运行中生效，未设置时默认的全局工具坐标系为工具坐
标系0。
必选参数：
tool：切换后的工具坐标系，通过索引序号指定，需要先在控制软件中添加对应坐标系。若指定的
坐标系索引不存在会导致工程停止运行并报错。
示例：
# 切换全局工具坐标系为工具坐标系1。
```lua
Tool(1)
SetTool
```
原型：
```lua
SetTool(index,table,type)
#### 描述:
```
修改指定的工具坐标系。
必选参数：
index：工具坐标系索引，需要先在控制软件中添加对应坐标系。若指定的坐标系索引不存在
会导致工程停止运行并报错。
table：修改后的工具坐标系，格式为[x, y, z, rx, ry, rz]，建议使用CalcTool指令获取。
可选参数：
type：是否全局保存。
0：该命令修改的坐标系仅在当前工程运行中生效，工程停止后恢复为原来的值。
1：该命令修改的坐标系全局保存，工程停止后依然保持修改后的值。但当 type=1 （全
#### 局保存）时，用户需要手动切换到对应的坐标系设置界面，再切换回来，才能看到更新后
的值，即需要手动刷新界面。
示例：
513

# 修改工具坐标系1为CalcTool算出的新坐标系，仅工程运行中生效。
```lua
newTool = CalcTool(1,1,[10,10,10,10,10,10])
SetTool(1,newTool,0)
```
CalcTool
原型：
```lua
CalcTool(index,matrix\_direction,table)
#### 描述:
```
计算工具坐标系。
必选参数：
index：工具坐标系索引，取值范围：[0,50]，根据坐标系0初始值为法兰坐标系（TCP0）。
matrix\_direction：计算的方向。
1：左乘，表示index指定的坐标系沿法兰坐标系（TCP0）偏转table指定的值。
0：右乘，表示index指定的坐标系沿自己偏转table指定的值。
table：工具坐标系，格式为[x, y, z, rx, ry, rz]。
返回：
计算得出的工具坐标系，格式为[x, y, z, rx, ry, rz]。
示例：
# 下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿法兰坐标系（TCP0）平移[x=10, y=10, z=
10]并旋转[rx=10, ry=10, rz=10]后，得到的新坐标系为newTool。
```lua
newTool = CalcTool(1,1,[10,10,10,10,10,10])
# 下述计算过程可等价为：一个初始位姿与工具坐标系1相同的坐标系，沿工具坐标系1平移[x=10, y=10, z=10]并旋
转[rx=10, ry=10, rz=10]后，得到的新坐标系为newTool。
newTool = CalcTool(1,0,[10,10,10,10,10,10])
```
GetPose
原型：
```lua
GetPose(user\_index, tool\_index)
#### 描述:
```
获取机械臂的实时位姿。
514

若在两条运动指令之间调用该指令，获取的点位会受到平滑过渡设置的影响：
若关闭了平滑过渡功能（cp或r为0），可准确获取到目标点。
若开启了平滑过渡功能，会获取到过渡曲线上的某个点位。
可选参数：
user\_index：位姿对应的用户坐标系索引，需要先在控制软件中添加对应坐标系。不设置时
使用全局用户坐标系。
tool\_index：位姿对应的工具坐标系索引，需要先在控制软件中添加对应坐标系。不设置时使
用全局工具坐标系。
返回：
机械臂当前位姿： {"pose":[x, y, z, rx, ry, rz]} 。
示例：
# 机械臂先运动至P1，再返回当前位姿。
```lua
currentPose = GetPose()
MovJ(P1)
```
```lua
MovJ(currentPose)
# 机械臂运动至P1后再运动至P2，获取P1的位姿。
```
```lua
MovJ(P1,{"cp":0})
#### currentPose = GetPose() #获取P1的位姿
```
```lua
MovJ(P2)
GetAngle
```
原型：
```lua
GetAngle()
#### 描述:
```
获取机械臂的实时关节角度。
若在两条运动指令之间调用该指令，获取的关节角会受到平滑过渡设置的影响：
若关闭了平滑过渡功能（cp或r为0），可准确获取到目标点对应的关节角。
若开启了平滑过渡功能，会获取到过渡曲线上某个点位对应的关节角。
返回：
机械臂当前的关节角度： {"joint":[j1, j2, j3, j4, j5, j6]} 。
示例：
# 机械臂先运动至P1，再返回当前位姿。
515

```lua
currentAngle = GetAngle()
MovJ(P1)
```
```lua
MovJ(currentAngle)
# 机械臂运动至P1后再运动至P2，获取P1的关节角度。
```
```lua
MovJ(P1,{"cp":0})
#### currentAngle = GetAngle() #获取P1的关节角度
```
```lua
MovJ(P2)
GetABZ
```
原型：
```lua
GetABZ()
#### 描述:
```
获取编码器当前位置。
返回值：
编码器的当前位置。
示例：
# 获取已设置的ABZ编码器的当前位置并赋值给变量abz。
```lua
abz = GetABZ()
CheckMovJ
```
原型：
```lua
CheckMovJ(P, {"user":1, "tool":0, "a":20, "v":50, "cp":100})
#### 描述:
```
检查当前点到目标点关节运动的可运行性。系统会算出整个运动轨迹，检查轨迹中是否有不可达的
点位。
必选参数：
P：目标点。
可选参数：
user：目标点的用户坐标系。
516

tool：目标点的工具坐标系。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
cp：平滑过渡比例。取值范围：[0,100]。
详细介绍请参见通用说明。
返回：
检查结果。
#### 0：无错误
#### 16：终点接近肩部奇异
#### 17：终点逆解无解
#### 18：终点逆解限位
#### 22：手势切换错误
#### 26：终点接近腕部奇异
#### 27：终点接近肘部奇异
#### 29：速度参数错误
#### 30：全参逆解求解失败
#### 32：轨迹有肩部奇异点
#### 33：轨迹存在逆解无解点
#### 34：轨迹存在逆解限位点
#### 35：轨迹有腕部奇异点
#### 36：轨迹有轴部奇异点
#### 37：轨迹存在关节跳变点
示例：
# 检查使用关节运动默认设置是否可达P1，如果可达，则关节运动至P1。
```lua
status=CheckMovJ(P1)
if status==0:
```
```lua
MovJ(P1)
status=CheckMovJ(P2) # 从P1到P2的可行性检查要等机械臂运动到P1后再执行
if(status==0)
```
```lua
MovJ(P2)
CheckMovL
```
原型：
```lua
CheckMovL(P, {"user":1, "tool":0, "a":20, "v":50, "speed":500, "cp":100, "r":5})
#### 描述:
```
检查当前点到目标点直线运动的可运行性。系统会算出整个运动轨迹，检查轨迹中是否有不可达的
点位。
517

必选参数：
P：目标点。
可选参数：
user：目标点的用户坐标系。目标点为关节变量时，坐标系参数无效。
tool：目标点的工具坐标系。目标点为关节变量时，坐标系参数无效。
a：执行该条指令时的机械臂运动加速度比例。取值范围：(0,100]。
v：执行该条指令时的机械臂运动速度比例。取值范围：(0,100]。
speed：执行该条指令时的机械臂运动目标速度，取值范围：[1, 最大运动速度]，单位：
mm/s。
设置此参数后，参数v将被忽略。
cp：平滑过渡比例。取值范围：[0,100]。
r：平滑过渡半径，取值范围：[0,100]，单位：mm。
设置此参数后，参数cp将被忽略。
详细介绍请参见通用说明。
返回：
检查结果。
#### 0：无错误
#### 16：终点接近肩部奇异
#### 17：终点逆解无解
#### 18：终点逆解限位
#### 22：手势切换错误
#### 26：终点接近腕部奇异
#### 27：终点接近肘部奇异
#### 29：速度参数错误
#### 30：全参逆解求解失败
#### 32：轨迹有肩部奇异点
#### 33：轨迹存在逆解无解点
#### 34：轨迹存在逆解限位点
#### 35：轨迹有腕部奇异点
#### 36：轨迹有轴部奇异点
#### 37：轨迹存在关节跳变点
示例：
# 检查使用直线运动默认设置是否可达P1，如果可达，则直线运动至P1。
```lua
status=CheckMovL(P1)
518
```

```lua
if(status==0)
MovL(P1)
status=CheckMovL(P2) # 从P1到P2的可行性检查要等机械臂运动到P1后再执行
if(status==0)
```
```lua
MovL(P2)
SetSafeWallEnable
```
原型：
```lua
SetSafeWallEnable(index,value)
#### 描述:
```
开启或关闭指定的安全墙。该命令设置的安全墙状态仅在当前工程运行中生效，工程停止后恢复为
原来的值。
必选参数：
index：要设置的安全墙索引，需要先在控制软件中添加对应的安全墙。取值范围：[1,8]。
value：
True：开启安全墙。
False：关闭安全墙。
示例：
# 开启索引为1的安全墙。
```lua
SetSafeWallEnable(1,True)
SetWorkZoneEnable
```
原型：
```lua
SetWorkZoneEnable(index,value)
#### 描述:
```
开启或关闭指定的安全区域。该命令设置的安全区域状态仅在当前工程运行中生效，工程停止后恢
复为原来的值。
必选参数：
index：要设置的安全区域索引，需要先在控制软件中添加对应的安全区域。取值范围：
[1,6]。
value：
True：开启安全区域。
519

False：关闭安全区域。
示例：
# 开启索引为1的安全区域。
```lua
SetWorkZoneEnable(1,True)
SetCollisionLevel
```
原型：
```lua
SetCollisionLevel(level)
#### 描述:
```
设置碰撞检测等级。该命令设置的值仅在当前工程运行中生效，工程停止后恢复为原来的值。
必选参数：
level：碰撞检测等级，0表示关闭碰撞检测，1~5数字越大灵敏度越高。
示例：
# 设置碰撞检测等级为3。
```lua
SetCollisionLevel(3)
SetBackDistance
```
原型：
```lua
SetBackDistance(distance)
#### 描述:
```
设置机械臂检测到碰撞后原路回退的距离。该命令设置的值仅在当前工程运行中生效，工程停止后
恢复为原来的值。
必选参数：
distance：碰撞回退的距离，取值范围：[0,50]，单位：mm。
示例：
# 设置碰撞回退距离为20mm。
```lua
SetBackDistance(20)
520
```

IO
#### 指令列表
IO指令用于进行机械臂系统IO的读写和相关参数的设置。
#### 指令
#### 功能
DI
#### 获取DI端口的状态
DIGroup
#### 获取多个DI端口的状态
DIGroupDEC
#### 获取多个DI端口的状态，返回值为十进制数
DO
#### 设置数字输出端口状态
DOGroup
#### 设置多个数字输出端口状态
DOGroupDEC
#### 通过赋值十进制设置多个数字输出端口状态
GetDO
#### 获取数字输出端口当前状态
GetDOGroup
#### 获取多个数字输出端口当前状态
GetDOGroupDEC
#### 获取多个数字输出端口当前状态，返回值为十进制数
DI
原型：
```lua
DI(index)
#### 描述:
```
读取数字输入端口状态。
必选参数：
index：DI端子的编号。
返回：
对应的DI端子的状态（ON/OFF）。
示例：
# DI1为ON时机械臂以直线运动方式运动至P1点。
```lua
if DI(1)==ON:
MovL(P1)
```
521

DIGroup
原型：
```lua
DIGroup(index1,...,indexn)
#### 描述:
```
读取多个数字输入端口状态。
必选参数：
index：DI端子的编号，可输入多个，以逗号分隔。
返回：
对应的DI端子的状态（ON/OFF），以数组形式返回。
示例：
# 当DI1和DI2都为ON时机械臂以直线运动方式运动至P1点。
```lua
digroup = DIGroup(1,2)
if digroup[1]&digroup[2]==ON:
```
```lua
MovL(P1)
DIGroupDEC
```
原型：
```lua
DIGroupDEC(index1,...,indexN)
#### 描述:
```
读取多个数字输入端口状态，返回值为十进制数。
必选参数：
index：DI端子的编号，以逗号分隔；最多可输入24个。
返回：
对应的DI端子的状态，以十进制形式返回。
说明：
返回值按照低位优先的方式计算，假设按照1，3，5的顺序定义了一个组DI，则转化成二
进制数是按照5，3，1的顺序保存。
522

输入table变量的顺序有先后区分，DIGroupDEC([1,2,3])和DIGroupDEC([1,3,2])的结果
是不同的。如DI（1,2,3)输出0(DI3)、1(DI2)、1(DI1)，那么DIGroupDEC([1,2,3])返回
结果为3(即DI3=0,DI2=1,DI1=1)；DIGroupDEC([1,3,2])返回结果为5(即
DI2=1,DI3=0,DI1=1)。
假设DI1~DI6的信号状态如下表所示：
#### index（DI编号）
1
2
3
4
5
6
#### DI信号状态
ON
OFF
ON
OFF
ON
OFF
#### 对应二进制
1
0
1
0
1
0
示例1：
GDI=[1,3,6]
```lua
result=DIGroupDEC(GDI)
----------------------
```
### 1.先读取{1,3,6}的信号状态为ON、ON、OFF
### 2.返回十进制时，按照低位优先的顺序进行保存；也就是按照DI6、DI3、DI1对应的二进制进行保存。即实际保存的二
#### 进制是011（DI6=0,DI3=1,DI1=1）
### 3.将二进制011转化为十进制，那么如上指令执行完成后返回值为3
示例2：
GDI=[1,6,3]
```lua
result=DIGroupDEC(GDI)
----------------------
```
### 1.先读取{1,6,3}的信号状态为ON、OFF、ON
### 2.返回十进制时，按照低位优先的顺序进行保存；也就是按照DI3、DI6、DI1对应的二进制进行保存。即实际保存 的
#### 二进制是101（DI3=1,DI6=0,DI1=1）
### 3.将二进制101转化为十进制，那么如上指令执行完成后返回值为5
DO
原型：
```lua
DO(index,ON|OFF,time\_ms)
#### 描述:
```
设置数字输出端口状态。
必选参数：
index：DO端子的编号。
ON|OFF：要设置的DO端口状态。
可选参数：
523

time\_ms： 持续输出时间，单位ms，取值范围：[25, 60000]。如果设置了该参数，系统会在指定
时间后对DO自动取反。取反为异步动作，不会阻塞指令队列，系统执行了DO输出后就会执行下
一条指令。
示例：
# 将DO1设置为ON。
```lua
DO(1,ON)
# 将DO1设置为ON,并在50ms后再自动设置为OFF。
```
```lua
DO(1,ON,50)
DOGroup
```
原型：
```lua
DOGroup([index1,ON|OFF],..,[indexN,ON|OFF])
#### 描述:
```
设置多个数字输出端口状态。
必选参数：
index：DO端子的编号。
ON|OFF：要设置的DO端口状态。
可设置多组，每组以大括号包裹，组间以逗号隔开。
示例：
# 将DO1和DO2设置为ON。
```lua
DOGroup([1,ON],[2,ON])
DOGroupDEC
```
原型：
```lua
DOGroupDEC([index1,index2,...,indeN],value)
#### 描述:
```
通过赋值十进制设置多个数字输出端口状态。
必选参数：
524

index：DO端子的编号，以逗号分隔；最多可输入24个。
value：十进制值，赋值顺序按照低位优先的方式。
示例1：
GDO=[1,2,3,4,5]
```lua
DOGroupDEC(GDO,18)
------------------
```
### 1.先将十进制数18转化为二进制10010
### 2.按低位优先进行赋值，如下所示：
index: 1 2 3 4 5
value: 0 1 0 0 1
### 3.如上指令执行完成后将DO2和DO5切换到ON状态
示例2：
GDO=[5,4,3,2,1]
```lua
DOGroupDEC(GDO,18)
------------------
```
### 1.先将十进制数18转化为二进制10010
### 2.按低位优先进行赋值，如下所示：
index: 5 4 3 2 1
value: 0 1 0 0 1
### 3.如上指令执行完成后将DO4和DO1切换到ON状态
GetDO
原型：
```lua
GetDO(index)
#### 描述:
```
获取数字输出端口当前状态。
必选参数：
index：DO端子的编号。
#### 返回
对应的DI端子的状态（ON/OFF）。
示例：
# 获取DO1的当前状态。
```lua
GetDO(1)
525
```

GetDOGroup
原型：
```lua
GetDOGroup(index1,...,indexN)
#### 描述:
```
获取多个数字输出端口当前状态。
必选参数：
index：DO端子的编号，可输入多个，以逗号分隔。
返回：
对应的DO端子的状态（ON/OFF），以数组形式返回。
示例：
# 获取DO1和DO2的当前状态。
```lua
GetDOGroup(1,2)
GetDOGroupDEC
```
原型：
```lua
GetDOGroupDEC(index1,...,indexN)
#### 描述:
```
获取多个数字输出端口当前状态，返回值为十进制数。
必选参数：
index：DO端子的编号，以逗号分隔；最多可输入24个。
返回：
对应的DO端子的状态，以十进制形式返回。
示例：
```lua
-- 获取DO1和DO2的当前状态。
local dogroup = GetDOGroupDEC(1,2)
local do1 = dogroup[1]
local do2 = dogroup[2]
```
526

#### 末端工具
#### 指令列表
末端工具指令用于进行机械臂系统末端IO的读写和相关参数的设置。
#### 指令
#### 功能
ToolDI
#### 读取末端数字输入端口状态
ToolDO
```lua
#### 设置末端数字输出端口状态(队列指令)
GetToolDO
```
#### 获取末端数字输出端口当前状态
SetToolPower
#### 设置末端工具供电状态
ToolDI
原型：
```lua
ToolDI(index)
#### 描述:
```
读取末端数字输入端口状态。
必选参数：
index：末端DI端子的编号。
返回：
对应的DI端子的状态（ON/OFF）。
示例：
# 末端DI1为ON时机械臂以直线运动方式运动至P1点。
```lua
if ToolDI(1)==ON:
MovL(P1)
```
ToolDO
原型：
```lua
ToolDO(index,ON|OFF)
527
```

#### 描述:
设置末端数字输出端口状态。
必选参数：
index：末端DO端子的编号。
ON|OFF：要设置的DO端口状态。
示例：
# 将末端DO1设置为ON。
```lua
ToolDO(1,ON)
GetToolDO
```
原型：
```lua
GetToolDO(index)
#### 描述:
```
获取末端数字输出端口当前状态。
必选参数：
index：末端DO端子的编号。
#### 返回
对应的末端DO端子的状态（ON/OFF）。
示例：
# 获取末端DO1的当前状态。
```lua
GetToolDO(1)
SetToolPower
```
原型：
```lua
SetToolPower(status)
#### 描述:
```
设置末端工具供电状态，一般用于重启末端电源，例如对末端夹爪重新上电初始化。如需连续调用
该接口，建议至少间隔4ms以上。
528

必选参数：
status：末端工具供电状态。
0：关闭电源。
1：打开电源。
示例：
# 重启末端工具电源。
```lua
SetToolPower(0)
Wait(5)
```
```lua
SetToolPower(1)
529
```

TCP&UDP
#### 指令列表
TCP&UDP函数用于进行TCP或UDP通信。
#### 指令
#### 功能
TCPCreate
#### 创建TCP网络对象
TCPStart
#### 建立TCP连接
TCPRead
#### 接收TCP对端发送的数据
TCPWrite
#### 发送数据给TCP对端
TCPDestroy
#### 断开TCP连接并销毁socket对象
UDPCreate
#### 创建UDP网络对象
UDPRead
#### 接收UDP对端发送的数据
UDPWrite
#### 发送数据给UDP对端
TCPCreate
原型：
```lua
TCPCreate(isServer, IP, port)
#### 描述:
```
创建TCP网络对象。
必选参数：
isServer：是否创建服务端。
True：表示创建服务端。
False：表示创建客户端。
IP：服务端IP地址。需与客户端IP地址在同一网段，且不冲突。
创建服务端时为机械臂的IP地址。
创建客户端时为对端的地址。
port：服务端端口。
请勿使用下述已被系统占用的端口，会导致服务端创建失败。
530

7, 13, 22, 37, 139, 445, 502, 503（0~1024之间的端口是linux系统自定义端口，被占用可能
#### 性高，请尽量避免使用）,
1501, 1502, 1503, 4840, 8172, 9527,
11740, 22000, 22001, 29999, 30004, 30005, 30006,
60000~65504, 65506, 65511~65515, 65521, 65522。
返回：
err： 0表示创建TCP网络对象成功 ，1表示创建TCP网络对象失败。
socket：创建的socket对象。
示例1：
# 创建TCP服务端。
#### ip="192.168.5.1" # 机械臂的IP地址作为服务端的IP地址
#### port=6001 # 服务端端口
err=0
socket=0
```lua
err, socket = TCPCreate(True, ip, port)
示例2：
```
# 创建TCP客户端。
ip="192.168.5.25" # 外部设备如相机的IP地址作为服务端的IP地址
#### port=6001 # 服务端端口
err=0
socket=0
```lua
err, socket = TCPCreate(False, ip, port)
TCPStart
```
原型：
```lua
TCPStart(socket, timeout)
#### 描述:
```
建立TCP连接。
机械臂作为服务端时，等待客户端来连接。
机械臂作为客户端时，主动连接服务端。
必选参数：
socket：已创建的socket对象。
timeout：等待超时时间，单位：秒。
如果设置为0，会一直等待到连接建立成功。
531

如果不为0，则超过设定的时间后返回连接失败。
返回：
连接结果。
0：连接成功。
1：输入参数错误。
2：socket对象不存在。
3：设置超时时间错误。
4：连接失败。
示例：
# 开始建立TCP连接，一直等待直到连接建立成功。
err = TCPStart(socket, 0) # socket为TCPCreate成功返回的socket对象
TCPRead
原型：
```lua
TCPRead(socket, timeout, type)
#### 描述:
```
接收TCP对端发送的数据。
必选参数：
socket：已创建的socket对象。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为小于等于0的值，会一直等待直到读取完数据再往下执行。
如果设为大于0的值，则超过设定的时间后会直接往下执行。
type：返回值类型。
如果不设置或设置为"table"，则RecBuf缓存格式为列表形式。
如果设置为“string”，则RecBuf缓存为字符串。
返回：
err： 0表示接收数据成功 ，1表示接收数据失败。
Recbuf：接收数据缓存区。
示例：
# 接收TCP数据，接收的数据分别保存为字符串和table形式。
#### # socket为TCPCreate成功返回的socket对象
err, RecBuf = TCPRead(socket,0,"string") # RecBuf数据类型为字符串
532

err, RecBuf = TCPRead(socket,0) # RecBuf数据类型为列表
TCPWrite
原型：
```lua
TCPWrite(socket, buf, timeout)
#### 描述:
```
发送数据给TCP对端。
必选参数：
socket：已创建的socket对象。
buf：要发送的数据。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为0，会一直等待直到对端接收完数据再往下执行。
如果不为0，则超过设定的时间后会直接往下执行。
返回：
发送结果。
0：发送成功。
1：发送失败。
示例：
# 发送TCP数据，数据内容为“test”。
TCPWrite(socket, "test") # socket为TCPCreate成功返回的socket对象
TCPDestroy
原型：
```lua
TCPDestroy(socket)
#### 描述:
```
断开TCP连接并销毁socket对象。
必选参数：
533

socket：已创建的socket对象。
返回：
执行结果。
0：执行成功。
1：执行失败。
示例：
# 断开与TCP对端的连接。
TCPDestroy(socket) # socket为TCPCreate成功返回的socket对象
UDPCreate
原型：
```lua
UDPCreate(isServer, IP, port)
#### 描述:
```
创建UDP网络对象，仅可创建一个。
必选参数：
isServer：是否创建服务端。
True：表示创建服务端。
False：表示创建客户端。
IP：创建服务端和客户端时都填写为对端IP地址。需与机械臂IP地址在同一网段，且不冲突。
port：
#### 创建服务端时，表示本端和对端都使用该端口。请勿使用已被系统占用的端口，详见
TCPCreate的参数说明。
创建客户端时，表示对端的端口。此时本端发送数据时会使用随机端口。
返回：
err： 0表示创建UDP网络对象成功 ，1表示创建UDP网络对象失败。
socket：创建的socket对象。
示例1：
# 创建UDP服务端。
```lua
local ip="192.168.5.25" # 外部设备如相机的IP地址作为对端的IP地址
#### local port=6001 # 本端和对端都使用该端口
local err=0
local socket=0
err, socket = UDPCreate(True, ip, port)
```
534

示例2：
# 创建UDP客户端。
```lua
local ip="192.168.5.25" # 外部设备如相机的IP地址作为对端的IP地址
#### local port=6001 # 对端端口
local err=0
local socket=0
err, socket = UDPCreate(False, ip, port)
```
UDPRead
原型：
```lua
UDPRead(socket, timeout, type)
#### 描述:
```
接收UDP对端发送的数据。
必选参数：
socket：已创建的socket对象。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为小于等于0的值，会一直等待直到读取完数据再往下执行。
如果设为大于0的值，则超过设定的时间后会直接往下执行。
type：返回值类型。
如果不设置或设置为"table"，则RecBuf缓存格式为列表形式。
如果设置为“string”，则RecBuf缓存为字符串。
返回：
err： 0表示接收数据成功 ，1表示接收数据失败。
Recbuf：接收数据缓存区。
示例：
# 接收UDP数据，接收的数据分别保存为字符串和table形式。
#### # socket为UDPCreate成功返回的socket对象
err, RecBuf = UDPRead(socket,0,"string") # RecBuf数据类型为字符串
err, RecBuf = UDPRead(socket, 0) # RecBuf数据类型为列表
UDPWrite
原型：
535

```lua
UDPWrite(socket, buf, timeout)
#### 描述:
```
发送数据给UDP对端。
必选参数：
socket：已创建的socket对象。
buf：要发送的数据。
可选参数：
timeout：等待超时时间，单位：秒。
如果不设置或设置为0，会一直等待直到对端接收完数据再往下执行。
如果不为0，则超过设定的时间后会直接往下执行。
返回：
发送结果。
0：发送成功。
1：发送失败。
示例：
# 发送UDP数据，数据内容为“test”。
UDPWrite(socket, "test") # socket为UDPCreate成功返回的socket对象
536

Modbus
#### 指令列表
Modbus函数用于建立Modbus主站与从站进行通讯，寄存器地址的取值范围与定义请参考对应从
站的Modbus寄存器地址定义说明。
#### 指令
#### 功能
ModbusCreate
#### 创建Modbus主站
ModbusRTUCreate
#### 创建基于RS485接口的Modbus主站
ModbusClose
#### 和Modbus从站断开连接
GetInBits
#### 读取触点寄存器
GetInRegs
#### 读取输入寄存器
GetCoils
#### 读取线圈寄存器
SetCoils
#### 写入线圈寄存器
GetHoldRegs
#### 读取保持寄存器
SetHoldRegs
#### 写入保存寄存器
各类寄存器对应的Modbus功能码遵循标准Modbus协议：
#### 寄存器类型
#### 读取寄存器
#### 写单个寄存器
#### 写多个寄存器
#### 线圈寄存器
01
05
0F
#### 触点寄存器
02
-
-
#### 输入寄存器
04
-
-
#### 保持寄存器
03
06
10
ModbusCreate
原型：
```lua
ModbusCreate(IP, port, slave\_id, isRTU)
#### 描述:
```
创建Modbus主站，并和从站建立连接。最多支持同时连接15个设备。
537

连接机器人自带的从站时IP设置为机器人的IP（默认192.168.5.1，可修改），端口设置为
502（map1）或1502（map2），详见附录A Modbus寄存器定义。
连接第三方从站时，IP和端口为第三方从站的地址，读写寄存器时的寄存器地址的取值范围与定义
请参考对应从站的Modbus寄存器地址定义说明。
必选参数：
IP：从站IP地址。
port：从站端口。
可选参数：
slave\_id：从站ID。
isRTU：布尔值。
isRTU为false时，建立基于控制柜网口的Modbus TCP通讯。
isRTU为true时，建立基于本体末端RS485的Modbus RTU通讯，只能使用60000端口。
注意：
#### 此参数决定了连接建立后传输数据使用的协议格式，并不影响连接结果。因此，如
果创建主站时该参数设置错误，依然可以创建成功，但后续通讯时会导致异常。
返回：
err：
0：创建Modbus主站成功 。
1：创建的主站已达到最大数量，创建新主站失败。
2：初始化主站失败，建议检查IP，端口，网络是否正常等。
3：连接从站失败，建议排查从站是否正常建立，网络是否正常等。
id：返回的主站索引，后续调用其他Modbus指令时使用。取值范围：[0, 14]。
示例：
# 创建Modbus主站，并和指定从站建立连接。
ip="192.168.5.123"
port=503
err=0
id=0
```lua
err, id = ModbusCreate(ip, port, 1)
# 创建Modbus主站，并和机器人自带从站建立连接。
ip="192.168.5.1"
port=502
err=0
id=0
err, id = ModbusCreate(ip, port)
```
538

ModbusRTUCreate
原型：
```lua
ModbusRTUCreate(slave\_id, baud, parity, data\_bit, stop\_bit)
#### 描述:
```
创建基于控制柜RS485接口的Modbus主站，并和从站建立连接。最多支持同时连接15个设备。
必选参数：
slave\_id：从站ID。
baud：RS485接口的波特率。
可选参数：
parity：是否有奇偶校验位。
"O"：奇校验。
"E"：偶校验。
"N"：无奇偶校验位。
不带参时默认值为“E”。
data\_bit：数据位长度。取值范围：8。不带参时默认值为8。
stopbit：停止位长度。取值范围：1，2。不带参时默认值为1。
返回：
err： 0表示创建Modbus主站成功 ，1表示创建Modbus主站失败。
id：返回的主站索引，后续调用其他Modbus指令时使用。
示例：
# 创建Modbus主站并与RS485接口连接的从站建立连接，从站ID为1，波特率为115200。
```lua
err, id = ModbusRTUCreate(1, 115200)
ModbusClose
```
原型：
```lua
ModbusClose(id)
#### 描述:
```
和Modbus从站断开连接，释放主站。
必选参数：
id：已创建的主站索引。
539

返回：
#### 操作结果
0：断开连接成功。
1：断开连接失败。
示例：
# 和Modbus从站断开连接。
```lua
ModbusClose(id)
GetInBits
```
原型：
```lua
GetInBits(id, addr, count)
#### 描述:
```
读取Modbus从站触点寄存器地址的值。对应Modbus功能码02。
必选参数：
id：已创建的主站索引。
addr：触点寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续读取触点寄存器的数量。取值范围： [1, 2000]（ModbusTCP协议协议限制，
#### 实际取值范围请根据从站寄存器数量或协议规范确定）
返回：
读取到的触点寄存器地址的值，存储在table中。table中第一个值对应触点寄存器起始地址的值。
说明：
如果输入的地址不是整型数字或字符串别名时，提示指令参数类型错误。
#### 如果地址输入的字符串没有对应的寄存器别名时，后台日志报错，提示寄存器没有对应
的地址，返回-1。
#### 如果输入的地址与对应的寄存器地址范围不一致时，后台日志报错，提示非法的地址，
返回-1。线圈寄存器地址为1-9999、触点寄存器地址为10001-19999、输入寄存器地址
为30001-39999、保持寄存器地址为40001-49999。
示例：
# 从地址0开始连续读取5个地址的值。
```lua
inBits = GetInBits(id,0,5)
540
```

GetInRegs
原型：
```lua
GetInRegs(id, addr, count, type)
#### 描述:
```
按照指定的数据类型，读取Modbus从站输入寄存器地址的值。对应Modbus功能码04。
必选参数：
id：已创建的主站索引。
addr：输入寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续读取输入寄存器的数量。取值范围：[1, 125]（ModbusTCP协议协议限制，
#### 实际取值范围请根据从站寄存器数量或协议规范确定）
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
返回：
读取到的输入寄存器地址的值，存储在table中。table中第一个值对应输入寄存器起始地址的值。
示例：
# 从地址2048开始读取一个32位无符号整数。
```lua
data = GetInRegs(id, 2048, 2, "U32")
GetCoils
```
原型：
```lua
GetCoils(id, addr, count)
#### 描述:
```
读取Modbus从站线圈寄存器地址的值。对应Modbus功能码01。
必选参数：
541

id：已创建的主站索引。
addr：线圈寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续读取线圈寄存器的数量。取值范围：[1, 2000]（ModbusTCP协议协议限制，
实际取值范围请根据从站寄存器数量或协议规范确定）。
返回：
读取到的线圈寄存器地址的值，存储在table中。table中第一个值对应线圈寄存器起始地址的值。
示例：
# 从地址0开始连续读取5个地址的值。
```lua
Coils = GetCoils(id,0,5)
SetCoils
```
原型：
```lua
SetCoils(id, addr, count, table)
#### 描述:
```
将指定的值写入线圈寄存器指定的地址。对应Modbus功能码05（写单个）和0F（写多个）。
必选参数：
id：已创建的主站索引。
addr：线圈寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续写入线圈寄存器的数量。取值范围：[1, 1968]（ModbusTCP协议协议限制，
#### 实际取值范围请根据从站寄存器数量或协议规范确定）.
table：要写入线圈寄存器的值，存储在table中。table中第一个值对应线圈寄存器起始地址
的值。
示例：
# 从地址1024开始，连续写5个线圈。
```lua
local Coils = {0,1,1,1,0}
SetCoils(id, 1024, #coils, Coils)
```
GetHoldRegs
原型：
```lua
GetHoldRegs(id, addr, count, type)
542
```

#### 描述:
按照指定的数据类型，读取Modbus从站保持寄存器地址的值。对应Modbus功能码03。
必选参数：
id：已创建的主站索引。
addr：保持寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续读取保持寄存器的数量。取值范围：[1, 125]（ModbusTCP协议协议限制，
实际取值范围请根据从站寄存器数量或协议规范确定）。
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
返回：
读取到的保持寄存器地址的值，存储在table中。table中第一个值对应保持寄存器起始地址的值。
示例：
# 从地址2048开始读取一个32位无符号整数。
```lua
data = GetHoldRegs(id, 2048, 2, "U32")
SetHoldRegs
```
原型：
```lua
SetHoldRegs(id, addr, count, table, type)
#### 描述:
```
按照指定的数据类型，写入指定的值到保持寄存器指定的地址。对应Modbus功能码06（写单
个）和10（写多个）。
必选参数：
id：已创建的主站索引。
addr：保持寄存器的起始地址，可输入整型数字，也可输入对应寄存器的别名（别名只支持
字符串）。
count：要连续写入保持寄存器的数量。取值范围：[1, 123]（ModbusTCP协议协议限制，
实际取值范围请根据从站寄存器数量或协议规范确定）。
543

table：要写入保持寄存器的值，存储在table中。table中第一个值对应保持寄存器起始地址
的值。
可选参数：
type：数据类型。
如果为空，默认为U16。
U16：16位无符号整数（2个字节，占用1个寄存器）。
U32：32位无符号整数（4个字节，占用2个寄存器）。
F32：32位单精度浮点数（4个字节，占用2个寄存器）。
F64：64位双精度浮点数（8个字节，占用4个寄存器）。
示例：
# 从地址2048开始，写入一个64位双精度浮点数。
```lua
local data = {95.32105}
SetHoldRegs(id, 2048, 4, data, "F64")
```
544

#### 程序控制
#### 指令列表
程序控制函数为程序运行控制相关的通用函数。
#### 指令
#### 功能
Print
#### 打印调试信息到控制台
Wait
#### 等待指定时间或满足指定条件后再继续执行下一条命令
Pause
#### 暂停脚本运行
Halt
#### 停止脚本运行
ResetElapsedTime
#### 开始计时
ElapsedTime
#### 结束计时
Systime
#### 获取当前系统时间
Popup
#### 设置脚本运行时的弹窗
Print
原型：
```lua
Print(value)
#### 描述:
```
打印调试信息到控制台（命令名也可写成 print ）。
必选参数：
value：待打印的数据。
示例：
# 打印字符串Success到控制台。
```lua
Print('Success')
Wait
```
原型：
545

```lua
Wait(time\_ms)
Wait(check\_str)
```
```lua
Wait(check\_str, timeout\_ms)
#### 描述:
```
机械臂完成上一条指令后，等待指定时间或满足指定条件后再继续执行下一条命令。
必选参数：
time\_ms：参数值为integer类型时，表示指定等待时间，小于等于0时表示不等待。单位：
ms。
check\_str：参数值为string类型时，表示判断逻辑，逻辑为true后再继续执行下一条指令。
可选参数：
timeout\_ms：超时时间。单位：ms。
判断逻辑一直为false且等待超过该时间时，系统会继续执行下一条指令并返回false。
小于等于0时表示不等待，立刻超时。
不设置该参数时，表示不会超时，会一直等待直到判断逻辑为true。
返回：
满足条件并继续运行时，返回true。
不满足条件，因超时导致继续运行时，返回false。
示例：
# 等待300ms。
```lua
Wait(300)
# DI1为ON时继续运行。
Wait("DI(1) == ON")
```
# DO1为ON且AI(1)小于7时继续运行。
```lua
Wait("GetDO(1) == ON and AI(1) < 7")
# 根据1s内DI1的状态执行不同的业务逻辑。
if Wait("DI(1) == ON", 1000):
```
#### # DI1状态为ON
else：
#### # DI1状态为OFF且等待超过1s
Pause
原型：
546

```lua
Pause()
#### 描述:
```
暂停脚本运行。需要通过控制软件或者远程控制操作才可继续运行。
示例：
# 机械臂运行至P1点后暂停运行，通过外部控制继续运行后才会运行至P2点。
```lua
MovJ(P1)
Pause()
```
```lua
MovJ(P2)
Halt
```
原型：
```lua
Halt()
#### 描述:
```
停止脚本运行。
示例：
```lua
-- 当变量count=100时，停止脚本运行。
if(count==100)
then
```
```lua
Halt()
end
```
ResetElapsedTime
原型：
```lua
ResetElapsedTime()
#### 描述:
```
待此指令前所有指令执行完成后开始计时，需配合ElapsedTime()一起使用，可用于计算运行时
间。
示例：
请参考ElapsedTime的示例。
547

ElapsedTime
原型：
```lua
ElapsedTime()
#### 描述:
```
结束计时，返回时间差，需配合ResetElapsedTime()一起使用。
#### 返回:
从开始计时到结束计时的时间差，单位毫秒。最大可统计4294967295ms（约49.7天），超过此
时间后会从0开始重新计数。
示例：
# 计算机械臂从当前位置经由P1运动至P2的时间，打印至控制台。
```lua
ResetElapsedTime()
MovL(P1)
```
```lua
MovL(P2)
print(ElapsedTime())
```
Systime
原型：
```lua
Systime()
#### 描述:
```
获取当前系统时间。
#### 返回:
系统当前时间的Unix时间戳，单位转换为了毫秒，即从格林威治时间1970年1月1日零时到当前时
间的毫秒数，一般用于计算时间差。如需获取当地时间，请根据当地时区使用获取的格林威治时间
换算获得。
示例：
# 获取系统当前时间。
```lua
time1 = Systime()
print(time1) # 1686304295963,换算为北京时间为2023-06-09 17:51:35（加963毫秒）
time2 = Systime()
```
print(time2) # 1686304421968,换算为北京时间为2023-06-09 17:53:41（加968毫秒）
# 计算机械臂移动到P1所花费的时间，单位毫秒。
548

```lua
time1 = Systime()
MovL(P1)
time2 = Systime()
```
```lua
print(time2-time1)
Popup
```
原型：
```lua
Popup(message, title, type, logControl)
#### 描述:
```
用于在脚本运行时弹出用户可自定义信息的窗口。DobotStudio Pro只会同时显示一个弹窗，内
容为最新的弹窗信息。
如果工程运行时未打开DobotStudio Pro（例如通过IO或Modbus启动工程），则不会出现弹
窗，但警告和错误类的弹窗指令依旧会导致工程暂停。
必选参数：
message：要显示的信息内容。支持包括字符串在内的各种变量。如果是字符串，长度不能
超过128；如果是其他变量类型，会被转换为字符，转换后的字符大小不能超过512字节。
title：要显示的弹窗标题。仅支持字符串，长度不能超过128。
type：消息类型。
0：信息。该类型弹窗不影响工程运行。单击停止可停止工程，单击确定直接关闭弹窗。
弹窗出现后，用户也可通过IO或Modbus停止工程，工程停止后弹窗自动消失。
#### 1：警告。该类型弹窗会暂停工程。单击停止可停止工程，单击继续可继续运行工程。弹
窗出现后，用户也可通过IO或Modbus停止/继续工程，工程停止/继续后弹窗自动消失。
549

#### 2：错误。该类型弹窗会暂停工程。单击停止可停止工程，单击继续可继续运行工程。弹
窗出现后，用户也可通过IO或Modbus停止/继续工程，工程停止/继续后弹窗自动消失。
logControl：弹窗内容是否写入日志。
0：不写入日志。
1：写入对应类型的日志（其中“信息”类型的弹窗消息对应“自定义”类型的日志）。
日志内容为“弹窗标题：弹窗内容”。
示例1：
```lua
-- 警告类弹窗，弹窗内容为字符串，写入日志。
#### Popup("请放置工件后单击继续", "未检测到工件", 1, 1)
```
示例2：
```lua
-- 提示类弹窗，弹窗内容为变量，不写入日志。
#### Popup(a, "变量a的值为", 0, 0)
```
550

551
#### --- 第 211 页 ---