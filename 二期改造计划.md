下述是我目前改造时的想法，但是我有一个关键的思考点：
    就是我现在项目的玩法是用python去控制机械臂运行的（如果你忘了我原项目功能，你可以先看一遍COBOT+AI应用这个文件夹下的内容，所有内容都是这个项目的），
    但是工业应用时导出python程序到现场是非常不方便的，所以我需要把python程序转换成lua语言指令，然后让机器人去执行。是在编程时就转呢？还是导出时转。



1.应用场景的想法
    我想单独在左侧做一个侧边栏，类似openclaw那样的页面一样，左侧有对话控制和脚本编程的选择（从上到下排列），选择对话控制的话就是原项目内容不用改，选择脚本编程的话右侧部分就不是对话界面，而是编程界面（可以存储并编写指令的那种），就是把语音文字输入后，我的页面转译成一条对应的机器人控制语言指令，要文字说明就行，插入到页面成指令时也是用文字简单说明（比如当DI2信号输入为ON时，就执行运动到P1点的指令；或者从P1运动到P2）；并插入在页面当中；
    会有一个存点列表，可以添加存点，存点从P1开始编号P2、P3......（点击添加当前点时就是记录机械臂当前的笛卡尔坐标系下的位置【都用笛卡尔坐标系来做就行】，当机械臂运动到其他位置即坐标系位置改变之后，还有修改当前点的按钮（按下修改当前点的按钮就是修改此点记录的位置为当前位置），或者删除当前点的按钮）
    当所有的指令输入完成之后就会有个保存按钮在页面上部，点一下保存按钮后，就存到本地。本地可以存多个文件，文件名可以自定义，文件名不能重复。并且可以导入和导出。
    再来一个运行按钮就从上往下运行刚才预编程的每一行程序，就和其他机器人上位机软件的功能是一样的，只是我这个是用语音做为输入去增加指令；


2.我能想到的会遇到的问题
    1.（目前我能想到的就是奇异点问题，还有就是如果一个程序中包含多个运动指令，怎么保证运动的平滑过渡，
    2.是否能执行多个运动指令）
    

3.我能想到的需要做的任务（按照阶段及每个阶段要完成的任务细分）
    1.这台越疆的六轴协作臂是lua5的脚本编程语言，但是原厂只保留了部分的语言指令，所以我需要把能运行的lua命令都给整理出来，然后你放到我的软件中，插入到页面的指令必须是这个符合规定里面的；


改造成什么样子好用好实现，我需要你的建议，因为我也没有完整的思路，你来帮我实现。有完整的可落地的想法之后就帮我出一个“二期改造完整方案规划说明.md“，要把总任务分成多个子任务（每个子任务就是一个阶段）每个子任务需要完成的任务也要细分并列出来。
这个方案规划说明要把每个部分都说明清楚（页面如何布局设计部分、可实现哪些交互功能部分）
