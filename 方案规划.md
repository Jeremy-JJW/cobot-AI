# Cobot + AI 自然语言控臂 — 方案规划与实施指南

> **本文档**是项目唯一的规划总览：目标、技术路线、三阶段实施步骤、验收标准与相关文件索引。  
> **计划周期**：约 **30 个自然日**（起算 5.20，**6 月 20 日前**可对外演示）  
> **当前阶段**：三个阶段全部完成 ✅

---

## 一、项目定位

### 1.1 要解决什么问题

| 目标 | 说明 |
|------|------|
| 客户说人话 | 不照模板，用自己的说法 |
| 识别动作 | 从话里抽出：走直线、去某点、停、调速等 |
| **只说一次就执行** | 识别完后**自动运行**控制程序，不要多轮确认（演示关键场景） |
| 机械臂要动 | Dobot TCP，示教器远程 |
| 对外像自研 | 客户只见你们的界面/软件，不见 OpenClaw |

### 1.2 业务价值

- 客户无需学习示教器或编程，**用日常口语**即可指挥机械臂完成常见动作
- 把「AI + 协作机器人」从概念验证变成**可演示、可交付**的产品能力
- 对外可称**自研自然语言控制软件**，提升方案竞争力与客户体验

### 1.3 最终交付物

| 产出 | 形态 | 客户看到什么 |
|------|------|-------------|
| **自研控臂引擎** | Python 核心模块 + 预置动作库（Skill） | 不暴露第三方 Agent 品牌 |
| **Web 演示软件** | 浏览器输入一句话 → 机械臂执行 | 简洁自研界面，支持预览、一键执行、急停 |
| **配置与文档** | 点位标定、动作白名单、一页部署说明 | 现场 30 分钟内可跑通演示 |
| **操作日志** | 每次「说了什么 → 执行了什么」可追溯 | 便于复盘、排错与客户说明 |

**对外定位**：自研执行与安全体系 + 自研界面 + 外接大模型 API（仅用于「听懂人话」）。

---

## 二、技术路线（已定）

### 2.1 旧方案（已否定）

```
客户话术 → AI 整份生成/改写 xxx.py → 执行 py → 机械臂
```

**问题**：慢（10～60s+）、不稳定（成功率约 60～70%）、无动作边界、难维护。

### 2.2 新方案（已定）

```
自然语言
  → 规划（规则 和/或 大模型）→ { skill, params }
  → 执行器（白名单）→ skills/*.py → robot_session → 机械臂
```

**核心原则**：AI 只产出 **Skill + 参数 JSON**，由固定 **`executor` / `nl_robot.py`** 执行；**禁止**每次生成完整 Python 控制脚本。

### 2.3 产品路线总览

```
[旧方案 生成py] ✅ 已放弃
    → [Skill 仓库建好] ✅
    → [阶段1 OpenClaw 验证] 🟡 当前
    → [阶段2 路线A 自研替换]
    → [阶段3 产品化 Flask]
```

| 角色 | 阶段 1 | 阶段 2～3 |
|------|--------|-----------|
| **OpenClaw** | 验证「听懂 + 自动跑程序」 | **不用**（或仅内部调试） |
| **Cursor** | 建 Skill 仓库、写 prompt | 同左 + 写 Flask/安装包 |
| **外接 LLM API** | OpenClaw 已接的模型 | **`llm_planner` 直接调** |
| **本仓库 Python** | 被 OpenClaw 调用 | **产品核心引擎** |
| **客户** | 内部试（可能对 OpenClaw 说话） | 只对**自研界面**说话 |

**一句话总结**：OpenClaw 是**实验室**；路线 A + 本仓库才是**对外产品**；大模型只负责「听懂」，机械臂只认 Skill 执行器。

---

## 三、核心架构（三阶段不变）

```
用户自然语言
    ↓
rule_parser.py（规则，秒级，默认）
    ↓ 失败时可选 --llm
llm_planner.py（大模型兜底）
    ↓
{ "skill": "...", "params": {...} }
    ↓
executor.py（白名单 Skill）
    ↓
skills/*.py + robot_session.py
    ↓
dobot_api.py / DobotDemo.py → 机械臂
```

| 模块 | 作用 |
|------|------|
| `rule_parser.py` | 固定/接近模板的话术，**不调 LLM**，快 |
| `llm_planner.py` | **自由口语**，调**外接 LLM API** → JSON |
| `executor.py` + `skills/` | 真机执行，安全限位 |
| `skill_registry.json` | 允许的动作与参数范围 |
| `positions.json` | 命名点位（home、point_a、point_b 等） |
| `nl_robot.py` | 命令行入口 |

### 3.1 Skill 白名单

| skill | 用途 | 主要 params |
|-------|------|-------------|
| `move_relative_linear` | 相对当前位姿直线移动 | `axis`(x/y/z), `direction`(+/-), `distance_mm`, `speed_percent` |
| `move_to_named_point` | 移动到命名点 | `point`, `move_type`(movj/movl), `speed_percent` |
| `move_between_points` | 两点间运动 | `from_point`, `to_point`, `move_type`, `speed_percent` |
| `set_speed` | 设置速度比例 | `speed_percent` |
| `wait` | 等待 | `seconds` |
| `read_pose` | 读当前位姿 | `save_as`(可选) |
| `stop_or_disable` | 停止/下使能 | `action`(stop/disable) |

**全局安全限制**（`skill_registry.json` → `global_limits`）：
- `distance_mm` ≤ **200**
- `speed_percent` ≤ **50**（默认 30）
- `seconds` ≤ **10**

### 3.2 选型规则

1. 「X/Y/Z + 方向 + 距离」→ `move_relative_linear`
2. 「去某点 / 到 A 点」→ `move_to_named_point`
3. 「从 A 到 B」→ `move_between_points`
4. 「速度 xx%」→ `set_speed`
5. 「等/停 N 秒」→ `wait`
6. 「读位置/标定」→ `read_pose`
7. 「停/急停/下使能」→ `stop_or_disable`

### 3.3 奇异点与安全策略（当前已实现）

| 能力 | 策略 | 层级 |
|------|------|------|
| **Skill 白名单** | 只允许 `skill_registry.json` 注册动作 | 参数级 |
| **距离/速度/等待上限** | 单次 ≤200mm、速度 ≤50%、等待 ≤10s | 参数级 |
| **命名点位** | 常用点写入 `positions.json`，「去 A 点」不现场算坐标 | 流程级 |
| **奇异点规避** | 点间长距离默认 **movj**；仅小步 **movl**（如 Z 向 ±200 mm 内） | 策略级 |
| **奇异点恢复** | TCP 失败 → 捕获异常 → 提示换 movj；可选降速重试一次 | 策略级 |
| **急停** | `stop_or_disable` Skill + 产品化急停按钮（阶段 3） | 操作级 |

**当前未实现（首月演示不承诺）**：

| 能力 | 说明 |
|------|------|
| **关节/连杆碰撞检测** | 不会计算「运动后是否自碰」 |
| **工作空间边界检查** | 不会校验目标位姿 X/Y/Z/RX/RY/RZ 是否越界 |
| **路径仿真** | 不会 preview 整条轨迹是否安全 |
| **环境障碍物避障** | 无视觉/力控/外部传感 |

> 首月演示依赖：**标定安全点 + 小步动作 + 示教器碰撞检测（若已开启）+ 人工监护**。  
> 几何级安全检测列入**首月后迭代**，见 **§3.4** 与 **§十五**。

### 3.4 后续计划：几何级安全检测（首月后）

目标：在 `executor` 执行前增加**目标位姿/工作空间**校验，降低误动作与自碰风险（仍不替代示教器物理急停与厂商碰撞检测）。

| 优先级 | 子项 | 说明 | 建议落点 |
|--------|------|------|----------|
| P1 | **工作空间边界** | 配置 X/Y/Z（及可选 RX/RY/RZ）min/max，执行前拦截越界目标 | `safety_zones.json` + `validate_target_pose()` |
| P1 | **相对移动目标预览** | `move_relative_linear` 先算目标笛卡尔坐标，再校验是否在安全区内 | `skills/base.py` / `executor.py` |
| P1 | **命名点合法性** | 标定点写入时校验是否在安全区内；拒绝明显危险点 | `read_pose` 保存逻辑 |
| P2 | **演示模式策略** | 演示只允许 Z 小步 + 命名点；X/Y 大范围默认拒绝或需二次确认 | `skill_registry.json` / prompt |
| P2 | **运动前 dry-run 增强** | `/api/plan` 返回「目标位姿 + 是否在安全区」供界面展示 | 阶段 3 Flask |
| P3 | **厂商碰撞检测联动** | 文档化示教器碰撞等级配置；程序侧记录报警码 | 部署说明 |
| P4 | **自碰/轨迹仿真** | 需运动学模型或厂商 API，工程量大，二期评估 | 暂不实施 |

**建议配置文件草案**（后续新增）：

```json
{
  "workspace_mm": {
    "x": [100, 400],
    "y": [-500, -200],
    "z": [300, 550]
  },
  "demo_mode": {
    "allow_axes": ["z"],
    "max_relative_mm": 100,
    "named_points_only": false
  }
}
```

**验收标准（该迭代完成时）**：

- 越界目标位姿 **100%** 在执行前被拦截并返回可读错误
- 演示话术集在标定工作空间内 **0 次** 误越界执行
- `使用说明.md` / 部署说明中写明：程序安全边界 + 示教器碰撞检测 + 急停分工

---

## 四、三阶段实施路径

| 阶段 | 计划工期 | 累计 | 目标完成 |
|------|----------|------|----------|
| **阶段 1** OpenClaw 验证 | 8～10 天 | 第 1～10 天 | 约 5 月底 |
| **阶段 2** 路线 A 自研替换 | 8～10 天 | 第 9～20 天 | 约 6 月上旬（与阶段 1 尾可重叠 1～2 天） |
| **阶段 3** 产品化 Flask | 10～12 天 | 第 19～30 天 | **6 月 20 日前** ✅ 已完成 |
| **合计** | **约 30 天** | — | 全流程收尾 |

阶段间可部分并行；扩展 Skill、精美 UI 等放在首月之后迭代。

---

## 五、阶段 1：OpenClaw 验证链路（当前）

### 5.1 目的

用 OpenClaw **现成集成**（模型、对话、自动执行终端）快速验证：

| 要验证的点 | 说明 |
|------------|------|
| 自由口语能否映射到 Skill | 客户不按模板说 |
| **一句话后能否自动跑** | 理解 → 自动 `nl_robot.py`，无需第二步点确认 |
| Skill + 执行器是否稳定 | 不写 py、少报错 |
| 现场演示节奏 | 延迟、话术、真机安全 |

OpenClaw 在本阶段是**验证平台 / 实验室**，不是最终产品名。

### 5.2 子项与计划天数

| 子项 | 计划天数 | 说明 |
|------|----------|------|
| OpenClaw 配置 + 消息 1/2 验收 | **2 天** | 尽快过 dry-run、禁止写 py |
| 固定话术 + dry-run 真机小步 | **2 天** | 可与上项并行；标定 `positions.json` |
| 自由口语 + 一句话自动执行 | **3 天** | 集中试 5～10 句客户说法 |
| 对照 `nl_robot.py --llm` | **1 天** | 与 OpenClaw **并行**，不必等阶段 1 全结束 |
| 复盘（话术表） | **1 天** | 只记结论，不写长文档 |

### 5.3 阶段 1 架构

```
客户/演示员 ──→ OpenClaw（LLM + 自动执行命令）
                    │
                    └── shell → nl_robot.py → executor → 机械臂

Cursor ──→ 维护本仓库（Skill、文档、prompt）
```

### 5.4 OpenClaw 配置（两种做法）

#### 做法 A：固定 Agent（推荐，持久）

| 放哪里 | 用哪个文件 | 作用 |
|--------|------------|------|
| **系统提示** | `openclaw_system_prompt.txt` | 行为规则（必配） |
| **知识库** | `openclaw_skill知识.md` | Skill 表 + 话术示例（必配） |
| **知识库（可选）** | `使用说明.md` | 客户演示话术 |
| **首次对话** | `openclaw_首次任务.txt` | 配好后第一条，做验收 |
| **磁盘项目** | 整个 `cobot+AI应用` 文件夹 | 执行用，**不要当知识库全文上传** |

**操作步骤**：

1. 系统提示：整份复制 `openclaw_system_prompt.txt` 粘贴到 Agent 设置
2. 知识库：添加 `openclaw_skill知识.md`（建议再加 `使用说明.md`）
3. 工具：确认可执行 Shell，工作目录 = `Z:\Cursor_contents\cobot+AI应用`
4. 验收：发送 `openclaw_首次任务.txt` 内容，检查是否只跑 `nl_robot.py --dry-run`，**不生成 py**

**不要喂给 OpenClaw 的**：`dobot_api.py` / `DobotDemo.py` 全文、旧「生成 py」类知识文档。

#### 做法 B：聊天框快速试（小白）

1. 发送 `openclaw_消息1-规则与技能.txt` 全文
2. 发送 `openclaw_消息2-验收测试.txt` 全文
3. 检查是否执行 `nl_robot.py --dry-run "向 Z 正方向移动 100 毫米"` 且不写 Python

缺点：新对话可能忘记规则，需重发消息 1。试通后建议改用做法 A。

### 5.5 OpenClaw 执行命令

**项目根目录**：`Z:\Cursor_contents\cobot+AI应用`

| 步骤 | 命令 | 说明 |
|------|------|------|
| 预演 | `nl_robot.py --dry-run "<客户原话>"` | 只生成/校验计划，不动机械臂 |
| 确认后执行 | `nl_robot.py "<客户原话>"` | 规则能解析则秒级 |
| LLM 兜底 | `nl_robot.py --llm "<客户原话>"` | 自由口语 |

OpenClaw **禁止**：输出 Python 代码、改写 `dobot_api.py`、新建 `generated/xxx.py` 再执行。

### 5.6 阶段 1 成功标准

- [ ] 消息 1/2 验收：只跑 `nl_robot.py`，不生成 py
- [ ] 自由口语 + 一句话：能 plan 并自动执行（或 dry-run 后再一句「执行」）
- [ ] 真机小动作稳定（如抬高 20mm）
- [ ] 确认：**没有 OpenClaw 也能复现**（用 `nl_robot.py --llm` 同链路测一遍）

### 5.7 进入阶段 2 的前置条件

- OpenClaw 链路与 `nl_robot.py --llm` 链路**结果一致、可接受**
- 已记录：好用的话术、prompt 要点、需加强的 Skill

---

## 六、阶段 2：路线 A — 自研替换 OpenClaw

### 6.1 目的

同样能力，**对客户隐藏 OpenClaw**，逻辑收拢到本仓库。

```
客户输入（网页 / 软件 / 语音转文字）
  → Flask 或桌面壳（自研品牌）
  → rule_parser（能匹配则不用模型）
  → 否则 llm_planner（外接 LLM API）
  → executor → 机械臂
```

### 6.2 子项与计划天数

| 子项 | 计划天数 | 说明 |
|------|----------|------|
| 合并 prompt、固化试验结论 | **2 天** | 写入 `robot_planner_prompt.txt`、补 `rule_parser` |
| 公共 plan + 一句话 plan+execute | **2 天** | 供 Flask 直接调用 |
| 纯路线 A 联调（无 OpenClaw） | **3 天** | `--llm` + executor，对齐阶段 1 效果 |
| 内部彩排 | **1～2 天** | 确认可对外称自研 |

### 6.3 与阶段 1 对比

| 对比 | 阶段 1 OpenClaw | 阶段 2 路线 A |
|------|-----------------|---------------|
| 谁听懂人话 | OpenClaw 里的模型 | **`llm_planner` + 同一套 prompt** |
| 谁自动跑程序 | OpenClaw 调终端 | **软件内直接 `execute_skill`** |
| 客户看到什么 | 若用 OpenClaw 界面会暴露 | **仅自研 UI** |
| 依赖 | Gateway / Agent 常开 | **Python + LLM API + 机械臂** |

### 6.4 从 OpenClaw 迁移时要带走

- 效果好的系统提示 → `robot_planner_prompt.txt` / `openclaw_system_prompt.txt`
- 常见说法与 Skill 映射 → `rule_parser` 规则、`skill_registry` 的 examples
- 一句话自动执行策略 → 产品里单接口 `plan + execute`，不必 dry-run 两步

### 6.5 阶段 2 成功标准

- [ ] `nl_robot.py --llm "自由口语"` 与 OpenClaw 效果接近
- [ ] 无 OpenClaw 进程，链路仍端到端通
- [ ] 对外可称「自研自然语言控制软件」

---

## 七、阶段 3：产品化软件

### 7.1 目的

做成可交付的**自有软件**，只依赖：

| 依赖 | 说明 |
|------|------|
| 本仓库（Skill 引擎） | 执行、安全、点位，**自研核心** |
| 外接 **LLM API** | 仅用于「听懂」 |
| Dobot 机械臂 + 网络 | 现场硬件 |
| **不需要** OpenClaw | 对客户零暴露 |

### 7.2 子项与计划天数

| 子项 | 计划天数 | 说明 |
|------|----------|------|
| Flask `/api/plan`、`/api/run-once` | **4 天** | 最小可用即可 |
| 简单 Web 页（输入、展示、执行） | **3 天** | 样式从简，能演示 |
| 急停 + 基础日志 | **2 天** | 演示必备 |
| 部署说明 + 彩排 | **1～2 天** | 一页安装步骤 |
| 扩展 Skill / 精美 UI | **首月后** | 不占用 30 天额度 |

### 7.3 目标架构

```
客户 ──→ 自研软件 / Flask 网页（对外品牌）
              │
              ├── rule_parser（无 LLM）
              ├── llm_planner（外接 LLM API）
              └── executor → 机械臂

OpenClaw：不再出现在对客户链路中
```

**典型 API**：

```
POST /api/run-once { "text": "客户一句话" }
  → plan（规则优先，否则 LLM）→ 自动 execute
  → 返回结果给界面
```

可选：`/api/plan`（仅预览 dry-run）、急停、限位、操作日志。

### 7.4 阶段 3 完成状态（2026-05-29）

| 模块 | 状态 | 说明 |
|------|:----:|------|
| Flask 后端 API `/api/plan` + `/api/run-once` + `/api/status` | ✅ | 线程锁、状态管理、异常处理、超时保护 |
| Web 前端页面（气泡对话、5 色指示灯、三阶段反馈） | ✅ | 响应式布局、打字动画、自动执行 |
| Web 计划格式化 `web_plan_format.py` | ✅ | 10 种 Skill 中文摘要 + 20 种轨迹标签 |
| 操作日志 `logger.py`（CSV + Flask 请求日志） | ✅ | 线程安全、RotatingFileHandler |
| 语义缓存 `semantic_cache.py` | ✅ | 相同指令复用上次解析 |
| 多步移动 Skill `move_relative_sequence` | ✅ | 2～10 步顺序执行 |
| 上使能 Skill `enable_robot` | ✅ | 新增 Skill |
| 后端增强：LLM 超时区分提示 | ✅ | 区分连接失败/超时/401/429/5xx/JSON 异常 |
| 后端增强：真机看门狗超时 | ✅ | 15 秒超时 + 5 秒连通性检查 |
| 部署文档 `使用说明书.md` | ✅ | 前置条件、安装、配置、启动、故障排查 |
| 话术精简 `使用说明.md` | ✅ | 一页可打印话术速查表 |
| 真机验收 + 稳定性验收 | ✅ | 所有功能验收通过 |

**详细完成清单见**：[阶段3-任务清单.md](阶段3-任务清单.md)

**剩余风险**：
- 急停依赖示教器物理按钮（程序已去掉急停按钮）
- LLM API 依赖网络（断网时规则话术仍可用）
- 单线程锁卡死需重启 Flask

---

## 八、两个关键场景

### 场景 A：客户自由口语（不按模板）

| 阶段 | 做法 |
|------|------|
| 1 OpenClaw | Agent + Skill 提示，模型抽 skill/params |
| 2～3 路线 A | **`llm_planner.plan_from_text()`**，同一类外接 API |

**都要大模型**；差别只在是否经过 OpenClaw 这一层。

### 场景 B：只说一次 → 自动运行程序

| 阶段 | 做法 |
|------|------|
| 1 OpenClaw | 一轮对话内：理解 → 自动执行 `nl_robot.py` |
| 2～3 路线 A | 一个接口内：`plan` 后立刻 `execute_skill`（或 `nl_robot` 无 `--dry-run`） |

---

## 九、验收指标与里程碑

### 9.1 响应时间

| 环节 | 旧方案 | 目标（Skill + JSON） |
|------|--------|----------------------|
| 固定话术 → 出计划 | 10～60 s+ | **≤ 2 s**（`rule_parser`） |
| 自由口语 → 出计划 | 10～60 s+ | **≤ 6 s**（`llm_planner`） |
| 计划 → 开始动 | 不稳定 | **≤ 0.2 s** |
| 一句话闭环（说 → 动完） | 常 > 30 s | **≤ 8 s**（规则）/ **≤ 12 s**（LLM） |

### 9.2 稳定性

| 指标 | 必达目标 |
|------|----------|
| 演示话术集识别成功率 | **≥ 90%**（10 句标准话术） |
| 同句重复执行一致性 | **100%** 产出相同 JSON（规则路径） |
| 连续演示无崩溃 | **≥ 10 次** 一句话执行不重启进程 |
| 越界参数拦截 | **100%** 执行前拦截 |

### 9.3 与旧方案对比

| 维度 | 旧方案 | 新方案目标 |
|------|--------|------------|
| 单次成功率 | 约 60～70% | **≥ 90%** |
| 响应速度 | 常 > 30 s | **≤ 12 s** |
| 行为一致性 | 每次生成结果不同 | 同句话产出固定、可预期 |
| 维护成本 | 逻辑散落各次生成文件 | 改 Skill 模板即可统一行为 |

### 9.4 里程碑

| 里程碑 | 内容 | 目标日（起算 5.20） |
|--------|------|---------------------|
| **M1** | JSON + `nl_robot` 端到端 | **第 10 天** 前 |
| **M2** | 路线 A 可替代 OpenClaw | **第 20 天** 前 |
| **M3** | 演示话术稳定 | **第 25 天** 前 |
| **M4** | Flask 演示版可给客户看 | **第 30 天** 前 ✅ |

---

## 十、风险与边界

| 项 | 说明 |
|----|------|
| 首月范围 | 以**单步、单动作**为主；复杂多步编排留二期 |
| 大模型依赖 | 自由口语需外接 API；网络/API 故障时，固定话术仍可演示 |
| 机械臂安全 | 首月：**参数级限位 + 标定点 + movj 策略**；**不承诺**任意坐标无自碰。几何级安全检测见 **§3.4、§十五** |
| 非核心项延后 | 更多动作、精美界面、全自动奇异点恢复、**工作空间边界与碰撞防护程序**等，首月后不占用交付窗口 |

---

## 十五、后续迭代：几何级安全检测与工作空间保护

> **定位**：首月（M4 演示版）不阻塞交付；在阶段 2～3 稳定后或演示版上线后单独排期实施。  
> **原则**：软件层「拦明显越界」+ 示教器/控制器碰撞检测 + 人工急停，三层分工，不互相替代。

### 15.1 背景与现状

当前 `executor` + `validate_skill_plan()` 仅做：

- Skill 白名单
- 参数范围（距离、速度、等待）
- 命名点是否存在

**不做**：目标位姿是否在工作空间内、运动后是否可能导致关节自碰、路径上是否与夹具/台面干涉。

### 15.2 迭代目标（按优先级）

| 阶段 | 内容 | 产出 |
|------|------|------|
| **迭代 1** | 工作空间边界配置 + 执行前目标位姿校验 | `safety_zones.json`、`check_workspace()` |
| **迭代 2** | 演示模式（仅 Z 小步 / 仅命名点）+ plan 接口返回安全检查结果 | 配置开关 + Flask `/api/plan` 增强 |
| **迭代 3** | 与示教器碰撞检测配置文档化、报警码记录 | 部署说明、日志字段 |
| **迭代 4（可选）** | 自碰仿真 / 轨迹 preview | 需评估 Dobot API 或自建运动学，二期 |

### 15.3 建议实现位置

```
plan 生成 { skill, params }
  → validate_skill_plan()     # 已有：参数级
  → compute_target_pose()     # 新增：算出执行后目标位姿
  → check_workspace()         # 新增：边界校验
  → execute_skill()           # 通过后才真机
```

| 文件 | 变更 |
|------|------|
| `safety_zones.json`（新建） | 工作空间 min/max、演示模式策略 |
| `skills/base.py` | `validate_target_pose()`、`check_workspace()` |
| `executor.py` | 真机执行前调用工作空间校验 |
| `skills/move_relative_linear.py` | 相对移动前先算目标点再校验 |
| `使用说明.md` | 演示安全边界与话术限制 |
| `开发说明.md` | 如何标定 `safety_zones.json` |

### 15.4 计划工期（参考）

| 子项 | 计划天数 |
|------|----------|
| 工作空间配置格式 + 校验函数 | **2 天** |
| 接入 executor / 各 Skill | **2 天** |
| 真机标定安全区 + 越界用例测试 | **2 天** |
| 文档与演示模式开关 | **1 天** |
| **合计** | **约 7 天**（可与阶段 3 尾或首月后并行） |

### 15.5 验收标准

- [ ] 配置工作空间后，越界指令在执行前被拦截（dry-run 与真机均生效）
- [ ] 相对移动 `抬高/下降` 在边界外目标会被拒绝
- [ ] `去 A 点` 若 `point_a` 超出安全区，保存或执行时被拒绝
- [ ] 错误信息对用户可读（如「目标 Z=580mm 超出上限 550mm」）
- [ ] 文档说明：程序边界 ≠ 碰撞检测，示教器仍需开启碰撞/软限位

---

## 十一、快速开始（本仓库）

```powershell
cd "Z:\Cursor_contents\cobot+AI应用"
.\.venv\Scripts\pip install -r requirements.txt
copy .env.example .env
.\.venv\Scripts\python nl_robot.py --dry-run "向 Z 正方向移动 100 毫米"
.\.venv\Scripts\python nl_robot.py "去 A 点"
```

---

## 十二、相关文件索引

| 用途 | 文件 |
|------|------|
| **方案规划（本文）** | `方案规划.md` |
| 客户演示话术 | `使用说明.md` |
| 新增 Skill 开发 | `开发说明.md` |
| LLM 规划 prompt | `robot_planner_prompt.txt`、`openclaw_system_prompt.txt` |
| OpenClaw 知识库 | `openclaw_skill知识.md` |
| OpenClaw 聊天框配置 | `openclaw_消息1-规则与技能.txt`、`openclaw_消息2-验收测试.txt` |
| Skill 清单与安全限位 | `skill_registry.json` |
| 工作空间与安全区（后续） | `safety_zones.json`（计划新增，见 §十五） |
| 命名点位 | `positions.json` |
| 入口脚本 | `nl_robot.py` |
| 最后一次计划（排错） | `generated/last_plan.json` |

---

## 十三、常见问题

| 问题 | 答案 |
|------|------|
| 路线 A 要不要外接 LLM？ | **自由口语要**；固定话术可用 `rule_parser` 不要。 |
| 能否对外称自研？ | **可以**。核心是 Skill 执行器 + 你们的软件壳；LLM 是通用接口。 |
| 程序能防关节碰撞吗？ | **首月不能**。仅有参数级限位；几何级安全与工作空间边界见 **§3.4、§十五**（首月后迭代）。 |
| OpenClaw 还要吗？ | **前期要（验证）**；产品化后**对客户不要**。 |
| 一句话自动执行谁做？ | 前期 OpenClaw；后期路线 A 单接口 `plan+execute`。 |
| Cursor 还要吗？ | **要**，用于改仓库和自研软件，不是给客户用的。 |

---

## 十四、附录：话术 → JSON 示例

| 客户话术 | skill | params（要点） |
|----------|-------|----------------|
| 沿 X 正方向走 100mm | move_relative_linear | axis=x, direction=+, distance_mm=100 |
| 抬高 50 毫米 | move_relative_linear | axis=z, direction=+, distance_mm=50 |
| 去 A 点 / 去 point_a | move_to_named_point | point=point_a |
| 从 A 到 B | move_between_points | from_point=point_a, to_point=point_b |
| 速度调到 25% | set_speed | speed_percent=25 |
| 等 2 秒 | wait | seconds=2 |
| 读一下当前位置 | read_pose | {} |
| 停 / 急停 | stop_or_disable | action=stop |
