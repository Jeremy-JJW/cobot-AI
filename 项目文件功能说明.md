# 项目文件说明

> 本文档帮你快速搞清楚：**每个文件是干什么的、谁调用谁、平时该看哪个**。  
> 项目根目录：`Z:\Cursor_contents\cobot+AI应用`

---

## 一、整体是怎么跑起来的

一句话：**用户说话 → 解析成 JSON 计划 → 白名单校验 → 调用 Skill → 通过官方 SDK 控制机械臂**。

```text
用户输入（自然语言）
    │
    ▼
nl_robot.py          ← 程序入口
    │
    ├─ rule_parser.py        规则解析（优先，快，不用 API Key）
    └─ llm_planner.py        大模型兜底（--llm 或 OpenClaw 时用）
    │
    ▼
JSON 计划 { skill, params, explain }
    │
    ▼
executor.py          ← 白名单执行器
    │
    ├─ skill_registry.json   只允许这里注册的动作
    └─ skills/*.py           每个 Skill 的具体实现
    │
    ▼
robot_session.py     ← 机械臂会话封装
    │
    ├─ DobotDemo.py          厂商 Demo 基类（连接、反馈线程）
    └─ dobot_api.py          Dobot 官方 TCP SDK
    │
    ▼
Dobot 机械臂（示教器需切到 TCP/IP 远程控制）
```

**三种使用方式：**

| 方式 | 入口 | 适用场景 |
|------|------|----------|
| 本地终端 | `nl_robot.py` | 开发、调试、演示 |
| 本地 + LLM | `nl_robot.py --llm` | 规则识别不了的话术 |
| OpenClaw | OpenClaw 调 `nl_robot.py` | 阶段 1 已验证；阶段 2 会逐步替换为纯自研 |

---

## 二、核心运行代码（最重要）

| 文件 | 作用 | 何时会动它 |
|------|------|------------|
| `nl_robot.py` | **主入口**。接收一条指令或进入交互模式；先规则解析，失败可选 LLM；保存计划到 `generated/last_plan.json`；连接机械臂并执行 | 日常运行、演示、OpenClaw 调用 |
| `rule_parser.py` | **规则解析器**。把固定话术映射成 `{ skill, params }`；内置 4000+ 常见说法样例库 | 扩充话术、减少 LLM 调用 |
| `llm_planner.py` | **大模型规划器**。调用 OpenAI 兼容 API，按 `robot_planner_prompt.txt` 输出 JSON | `--llm` 兜底、复杂口语 |
| `executor.py` | **执行器**。校验计划是否在白名单内，再调用对应 Skill | 一般不用改，除非改执行流程 |
| `robot_session.py` | **机械臂会话**。连接、使能、读位姿、`movj`/`movl`/`arc`/`circle`、停、下使能 | 新增底层运动能力时改这里 |
| `DobotDemo.py` | **厂商 Demo 基类**。TCP 连接、反馈线程、端口 29999/30004 | 通常不改，被 `robot_session.py` 继承 |
| `dobot_api.py` | **Dobot 官方 SDK**。`MovJ`、`MovL`、`Arc`、`Circle`、`GetPose` 等全部原厂接口 | **禁止自创接口**，Skill 只能走这里已有的方法 |

---

## 三、Skill 模块（`skills/` 目录）

Skill = **一个可被白名单允许的原子动作**。每个 Skill 一个文件，统一有 `run(session, params, context)` 函数。

| 文件 | Skill 名称 | 做什么 |
|------|------------|--------|
| `skills/base.py` | — | 公共工具：读注册表、读点位、校验参数、点位别名（A点→point_a） |
| `skills/__init__.py` | — | 汇总所有 Skill 的 handler，供 `executor.py` 调用 |
| `skills/move_relative_linear.py` | `move_relative_linear` | 沿 X/Y/Z 相对直线移动（GetPose + MovL） |
| `skills/move_to_named_point.py` | `move_to_named_point` | 移动到命名点（MovJ / MovL） |
| `skills/move_between_points.py` | `move_between_points` | 从 A 点到 B 点（连续两次移动） |
| `skills/motion_pattern.py` | `motion_pattern` | **20 种复合路线**（画圆、螺旋、方形、XYZ 分步返回等；MovL / Arc / Circle） |
| `skills/set_speed.py` | `set_speed` | 设置全局速度比例 |
| `skills/wait.py` | `wait` | 等待若干秒 |
| `skills/read_pose.py` | `read_pose` | 读取当前位姿；可选保存到 `positions.json` |
| `skills/stop_or_disable.py` | `stop_or_disable` | 停止运动或下使能 |

**新增 Skill 时改哪些文件：**  
`skill_registry.json` → `skills/xxx.py` → `skills/__init__.py` → （可选）`rule_parser.py`

---

## 四、配置与数据文件

| 文件 | 作用 | 谁读写 |
|------|------|--------|
| `skill_registry.json` | **Skill 白名单 + 参数安全边界**（最大距离 200mm、速度 50% 等） | `executor.py`、`rule_parser` 间接、`llm_planner` 间接 |
| `positions.json` | **命名点位坐标**（home、point_a、point_b） | `read_pose` 写入；移动类 Skill 读取 |
| `.env` | **本地私密配置**：机械臂 IP、API Key、模型地址 | `nl_robot.py`、`llm_planner.py` 读取 |
| `.env.example` | `.env` 模板，不含真实密钥 | 复制后改名为 `.env` |
| `requirements.txt` | Python 依赖（numpy、requests、python-dotenv） | `pip install -r requirements.txt` |
| `robot_planner_prompt.txt` | 给 **LLM** 的系统提示：只允许输出哪些 skill、JSON 格式 | `llm_planner.py` 读取 |

---

## 五、OpenClaw 相关（阶段 1 用，阶段 2 逐步弱化）

OpenClaw 是外部 Agent，**不直接写控制代码**，只负责：听懂人话 → 调 `nl_robot.py` → 解释结果。

| 文件 | 作用 |
|------|------|
| `openclaw_system_prompt.txt` | OpenClaw **系统提示词**：禁止生成 Python、必须 dry-run 先行、白名单 skill |
| `openclaw_skill知识.md` | OpenClaw **知识库**：Skill 列表、话术示例、JSON 格式、项目路径 |
| `openclaw_消息1-规则与技能.txt` | 发给 OpenClaw 的第一条消息：让它记住规则 |
| `openclaw_消息2-验收测试.txt` | 发给 OpenClaw 的验收消息：dry-run 测试 |
| `openclaw_首次任务.txt` | 首次配置 OpenClaw 时的任务说明 |

---

## 六、文档类（给人看的，不参与运行）

| 文件 | 给谁看 | 内容 |
|------|--------|------|
| `项目文件说明.md` | **你** | 本文档：所有文件是干什么的 |
| `使用说明.md` | 演示 / 客户 | 支持哪些话术、怎么启动、安全限制 |
| `开发说明.md` | 开发者 | 如何新增 Skill、底层 API 对应关系 |
| `方案规划.md` | 项目总览 | 目标、技术路线、三阶段计划、验收标准（**最长、最完整**） |
| `项目任务计划表.md` | 项目管理 | 分阶段 checklist，当前进度，下一步做什么 |
| `项目任务计划表.xlsx` | 项目管理 | 同上内容的 Excel 版 |
| `1.cobot用户手册（脚本编程部分.md` | 参考 | Dobot 脚本编程相关手册摘录 |
| `cobot+AI应用汇报.pdf` | 汇报 | 项目汇报 PDF（由 md 导出） |
| `方案规划5.20-14.pdf` | 汇报 | 方案规划 PDF 快照 |

**文档怎么选：**

- 想**跑演示** → `使用说明.md`
- 想**改代码** → `开发说明.md` + 本文档
- 想**看全局规划** → `方案规划.md`
- 想**看今天该干啥** → `项目任务计划表.md`

---

## 七、工具脚本与运行时目录

| 路径 | 作用 |
|------|------|
| `scripts/md_to_pdf.py` | 把 Markdown 转成 PDF（汇报用，依赖 playwright） |
| `generated/last_plan.json` | **自动生成**：最近一次解析出的计划，便于排查 |
| `__pycache__/` | Python 编译缓存，可忽略 |
| `.venv/` | Python 虚拟环境，依赖装在这里，**不要提交到 git** |

---

## 八、文件关系速查（容易混淆的）

### 「解析」相关，别搞混

| 文件 | 输入 | 输出 |
|------|------|------|
| `rule_parser.py` | 中文指令 | `{ skill, params }` 或 None |
| `llm_planner.py` | 中文指令 | `{ skill, params }`（调 API） |
| `robot_planner_prompt.txt` | — | 给 LLM 的约束文本 |
| `openclaw_system_prompt.txt` | — | 给 OpenClaw 的约束文本 |

### 「控制机械臂」相关，别搞混

| 文件 | 层级 | 能不能改 |
|------|------|----------|
| `dobot_api.py` | 官方 SDK | 尽量不改 |
| `DobotDemo.py` | 厂商 Demo | 尽量不改 |
| `robot_session.py` | 项目封装 | 新增底层运动时改 |
| `skills/*.py` | 业务动作 | 新增 Skill 时改 |

### 「白名单」相关

| 文件 | 职责 |
|------|------|
| `skill_registry.json` | 定义**允许哪些 skill、参数范围** |
| `executor.py` | **强制执行**白名单，非法计划直接报错 |
| `skills/__init__.py` | 把 skill 名字映射到 Python 函数 |

---

## 九、日常操作对应哪个文件

| 你想做的事 | 主要改/看 |
|------------|-----------|
| 运行一条指令 | `nl_robot.py` |
| 增加固定话术 | `rule_parser.py` + `使用说明.md` |
| 增加新动作类型 | `skill_registry.json` + `skills/` + `robot_session.py`（如需新 SDK 接口） |
| 标定 home / A / B 点 | 真机说「记录当前位置为 xxx」或手改 `positions.json` |
| 改机械臂 IP | `.env` 里 `ROBOT_IP` |
| 换大模型 | `.env` 里 `OPENAI_*`，必要时改 `robot_planner_prompt.txt` |
| 配置 OpenClaw | `openclaw_system_prompt.txt` + `openclaw_skill知识.md` |
| 看上次解析结果 | `generated/last_plan.json` |
| 跟踪项目进度 | `项目任务计划表.md` |

---

## 十、推荐目录树（仅项目文件，不含 `.venv`）

```text
cobot+AI应用/
├── nl_robot.py                 # 入口
├── rule_parser.py              # 规则解析
├── llm_planner.py              # LLM 兜底
├── executor.py                 # 执行器
├── robot_session.py            # 机械臂封装
├── DobotDemo.py                # 厂商 Demo
├── dobot_api.py                # 官方 SDK
│
├── skill_registry.json         # Skill 白名单
├── positions.json              # 命名点位
├── robot_planner_prompt.txt    # LLM 提示词
├── requirements.txt
├── .env / .env.example
│
├── skills/                     # 各 Skill 实现
│   ├── base.py
│   ├── __init__.py
│   ├── move_relative_linear.py
│   ├── move_to_named_point.py
│   ├── move_between_points.py
│   ├── motion_pattern.py
│   ├── set_speed.py
│   ├── wait.py
│   ├── read_pose.py
│   └── stop_or_disable.py
│
├── generated/
│   └── last_plan.json          # 最近一次计划（自动生成）
│
├── scripts/
│   └── md_to_pdf.py            # 文档转 PDF
│
├── openclaw_system_prompt.txt  # OpenClaw 系统提示
├── openclaw_skill知识.md
├── openclaw_消息1-规则与技能.txt
├── openclaw_消息2-验收测试.txt
├── openclaw_首次任务.txt
│
├── 使用说明.md                 # 演示话术
├── 开发说明.md                 # 开发指南
├── 方案规划.md                 # 项目总规划
├── 项目任务计划表.md           # 进度 checklist
├── 项目任务计划表.xlsx
├── 项目文件说明.md             # 本文档
└── 1.cobot用户手册（脚本编程部分.md
```

---

## 十一、当前阶段提醒

- **阶段 1（已完成）**：OpenClaw + 规则解析 + 真机小步验证  
- **阶段 2（进行中）**：不依赖 OpenClaw，直接用 `nl_robot.py --llm` 端到端  
- **阶段 3（未做）**：Flask Web 演示界面  
- **首月后**：几何级安全检测（工作空间边界）

若仍不确定某个文件用途，可先查本文档「第二节～第六节」；若与代码行为不一致，以代码和 `skill_registry.json` 为准。
