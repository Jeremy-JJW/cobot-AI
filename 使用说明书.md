# COBOT AI 使用说明书

> 现场 30 分钟内可跑通演示。适用于 **Windows 11** + **Dobot CR 系列** 机械臂。

---

## 一、前置条件

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 |
| Python | 3.10～3.13（已安装） |
| 机械臂 | Dobot CR 系列，示教器切换至 **TCP/IP 远程控制** |
| 网络 | 电脑与机械臂同一局域网（直连网线或同一交换机） |
| LLM API（可选） | 调用自由口语需 API Key（OpenAI / DeepSeek 等兼容 API） |

---

## 二、安装步骤

### 2.1 解压项目

将项目文件夹放到电脑上，路径不含中文和空格为宜（如 `D:\cobot-ai`）。

### 2.2 创建虚拟环境（首次只需一次）

```powershell
cd D:\cobot-ai
python -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
```

### 2.3 配置环境变量

```powershell
copy .env.example .env
```

编辑 `.env` 文件：

```ini
ROBOT_IP=192.168.5.1          # 机械臂 IP（查看示教器网络设置）
OPENAI_API_KEY=sk-xxxx         # 你的 API Key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
```

> **国内推荐 DeepSeek：**
> ```ini
> OPENAI_BASE_URL=https://api.deepseek.com/v1
> OPENAI_MODEL=deepseek-chat
> ```

---

## 三、启动演示

### 3.1 启动 Web 服务

```powershell
cd D:\cobot-ai
.\.venv\Scripts\python web_app.py
```

看到输出 `COBOT AI Web 演示版: http://127.0.0.1:5000` 即启动成功。

### 3.2 浏览器打开

浏览器访问 **[http://127.0.0.1:5000](http://127.0.0.1:5000)**

- 未连接机械臂时，状态指示灯显示 **灰色**
- 连接机械臂后自动变为 **绿色 "已就绪"**

### 3.3 机械臂连接检查

1. 确认示教器已切换到 **TCP/IP 远程控制**
2. 确认示教器 IP 与 `.env` 中 `ROBOT_IP` 一致（示教器「设置 → 网络」查看）
3. 上位机软件（DobotStudio 等）需 **断开连接**，否则端口占用
4. Web 页面输入指令（如「抬高 20 毫米」），系统会自动连接并执行

---

## 四、演示注意事项

1. **安全第一**：确保示教器急停按钮位置已知且可用
2. **从小开始**：先试小距离（抬高 20mm），再试复杂动作
3. **工作范围**：建议在机械臂工作空间中央区域演示，避免边界奇异点
4. **网络稳定**：自由口语需要 LLM API，断网时只有固定话术可用
5. **无需 OpenClaw**：演示时完全使用自研 Web 界面，不出现第三方品牌

---

## 五、故障排查

| 现象 | 可能原因 | 解决 |
|------|----------|------|
| 启动报错 `No module named 'flask'` | 依赖未安装 | 运行 `.\.venv\Scripts\pip install -r requirements.txt` |
| 页面显示「未连接」 | 机械臂未连或 IP 不对 | 检查示教器 TCP/IP 模式和 `ROBOT_IP` |
| 执行报错 `Timeout connecting` | 机械臂 IP 不通 | `ping ROBOT_IP` 确认网络；关闭上位机软件 |
| 执行报错 `Dashboard timeout` | 端口 29999 被占用 | 关闭 DobotStudio 等连接软件 |
| 自由口语总是「无法理解」 | API Key 无效或网络不通 | 检查 `.env` 中 API Key 和 BASE_URL |
| LLM 解析超时 | 网络慢或模型负载高 | 重试一次；换成 `gpt-4o-mini` 等轻量模型 |
| 机械臂不动也不报错 | 示教器未切 TCP/IP 模式 | 示教器「设置 → 控制模式 → TCP/IP」 |
| 动作中途停止 | 碰撞检测触发 / 急停按下 | 查看示教器报警信息，复位后重试 |
| 页面加载样式丢失 | 静态文件缓存 | 按 `Ctrl+F5` 强制刷新 |
