# COBOT + AI 二期 使用手册与真机测试清单（脚本编程模式）

> 适用对象：现场操作与测试人员。
> 内容：第一部分讲每个功能怎么用，第二部分是连真机后要逐项测试的清单。

---

# 第一部分 · 使用手册

## 0. 启动与进入

1. 启动服务（PowerShell，项目根目录）：

```powershell
.\.venv\Scripts\python web_app.py
```

2. 浏览器打开 `http://127.0.0.1:5000`。
3. 左侧边栏有两项：
   - **对话控制**：一期功能，说一句话立即执行一个动作（不变）。
   - **脚本编程**：二期新增，点它进入编程界面。

## 1. 界面分区（脚本编程模式）

| 区域 | 位置 | 作用 |
|------|------|------|
| 顶部工具条 | 上方 | 程序选择、程序名输入、新建、保存、导入、导出 Lua、运行 |
| 程序指令 | 中间左 | 显示程序里每一行指令，可上移/下移/删除/编辑条件 |
| 存点列表 | 中间右 | 显示 P1、P2… 的坐标，可修改/删除 |
| 输入框 | 底部 | 文字或语音输入一条指令，点加号插入到程序里 |

## 2. 存点（记录机械臂位置）

存点就是把机械臂"当前所在的位置"记下来，供指令引用。统一用笛卡尔坐标。

- **添加当前点**：把机械臂（用示教器或手动拖动）移到目标位置 → 点存点列表上方的「添加当前点」→ 自动记录为 P1、P2、P3…（依次编号）。
- **修改当前点**：先把机械臂移到新位置 → 点某个点右边的「修改」→ 该点坐标更新为当前位置。
- **删除当前点**：点某个点右边的「删除」。
  - 注意：删除后编号不重排（例如删掉 P2 后剩 P1、P3），避免已写好的指令引用错乱。

## 3. 插入指令（语音 / 文字）

在底部输入框说一句话或打字，点加号（或按 Enter）插入。每句话会变成一行中文指令加到程序末尾。

常用说法：

| 你说 / 你打 | 生成的指令 |
|------------|-----------|
| 运动到 P1 / 去 P2 | 关节运动到该点 |
| 直线运动到 P1 | 直线运动到该点 |
| 从 P1 到 P2 | 依次运动经过 P1、P2（生成两行） |
| 置 DO1 为 ON / DO2 关闭 | 设置数字输出信号 |
| 等待 DI1 为 ON | 等待数字输入信号到达 |
| 当 DI2 为 ON 时运动到 P1 | 条件触发：DI2 为 ON 才执行该行 |
| 设置速度为 30% | 设置全局速度 |
| 等待 2 秒 | 暂停 2 秒 |

> 说不出的复杂动作（如"抬高 20 毫米""画圆"）也能直接插入，会沿用一期的解析能力。

## 4. 编辑指令

每行指令右侧有 4 个按钮：

| 按钮 | 作用 |
|------|------|
| ↑ | 上移一行 |
| ↓ | 下移一行 |
| ⚙ | 编辑条件：输入 DI 编号和 ON/OFF，给该行加"信号触发"；留空则取消条件 |
| ✕ | 删除该行 |

## 5. 程序管理（保存 / 切换 / 导入）

- **新建**：清空当前编辑内容，从头编一个新程序。
- **保存**：在程序名输入框填名字 → 点「保存」。
  - 文件名不可重复；若重名会提示是否覆盖。
  - 保存后存到项目的 `programs/` 目录（每个程序一个 `.json`）。
- **切换**：用顶部「选择程序」下拉，选一个已保存的程序载入继续编辑。
- **导入**：点「导入」选择本地 `.json` 程序文件，载入到编辑区。

## 6. 运行（在线测试）

点「▶ 运行」→ 软件连接真机、使能，然后从上到下逐行执行整段程序。
- 带条件的行：先读对应 DI，满足才执行，不满足就跳过。
- 执行完会提示"共 N 行，执行 X 行，跳过 Y 行"。

> 运行是用来在软件里测试逻辑的；现场最终部署请用导出的 Lua。

## 7. 导出 Lua（现场部署）

点「导出 Lua」→ 浏览器下载一个 `程序名.lua` 文件 → 拷到示教器/控制器按现场流程运行。
- 生成内容包括：点位定义、运动指令、IO、条件块、平滑过渡参数。

---

# 第二部分 · 真机测试清单

## 测试前准备

- [ ] 机械臂上电，急停按钮位置已确认、伸手可及。
- [ ] 示教器切换到 **TCP/IP 远程控制** 模式。
- [ ] `.env` 里 `ROBOT_IP` 与实际机械臂 IP 一致（默认 192.168.5.1）。
- [ ] 若测 DI/DO：确认现场 DI/DO 接线及编号，准备一个能手动给信号的方式（按钮/短接/PLC）。
- [ ] 启动 `web_app.py`，页面右上角状态由"未连接"能变为"已就绪"。

## A. 连接与基础状态

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| A1 | 服务启动 | 启动并打开页面 | 页面正常，能切换两个模式 |
| A2 | 一期未受影响 | 在「对话控制」发一条如"读取当前位置" | 一期功能照常 |
| A3 | 状态显示 | 观察右上角状态灯 | 连接后显示"已就绪" |

## B. 存点功能

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| B1 | 添加点 | 移动机械臂到位置1 → 添加当前点 | 出现 P1，坐标合理（接近示教器显示值） |
| B2 | 连续添加 | 移到位置2、位置3各添加一次 | 依次出现 P2、P3 |
| B3 | 修改点 | 移到新位置 → 对 P2 点「修改」 | P2 坐标更新为当前位置 |
| B4 | 删除点 | 删除 P2 | 列表剩 P1、P3（编号不重排） |
| B5 | 坐标准确性 | 对比 P1 坐标与示教器读数 | 误差在可接受范围 |

## C. 指令解析与编辑

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| C1 | 运动指令 | 输入"运动到 P1" | 新增一行"运动到 P1" |
| C2 | 直线运动 | 输入"直线运动到 P1" | 该行标为直线运动 |
| C3 | 多点 | 输入"从 P1 到 P3" | 生成两行（到 P1、到 P3） |
| C4 | 置 DO | 输入"置 DO1 为 ON" | 新增设置 DO 行 |
| C5 | 等待 DI | 输入"等待 DI1 为 ON" | 新增等待 DI 行 |
| C6 | 条件指令 | 输入"当 DI2 为 ON 时运动到 P1" | 该行带条件标记 |
| C7 | 语音输入 | 点麦克风说"运动到 P1" | 识别文字并可插入 |
| C8 | 上移/下移 | 用 ↑↓ 调整顺序 | 顺序正确变化 |
| C9 | 编辑条件 | 用 ⚙ 给某行加 DI3=ON，再清空 | 加上/取消条件都正确 |
| C10 | 删除行 | 用 ✕ 删除一行 | 该行消失，编号重排 |

## D. 程序管理

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| D1 | 保存 | 命名"测试1"→ 保存 | 提示已保存，下拉出现"测试1" |
| D2 | 重名拦截 | 再用"测试1"保存 | 提示是否覆盖 |
| D3 | 覆盖 | 确认覆盖 | 保存成功 |
| D4 | 切换载入 | 新建清空 → 下拉选"测试1" | 内容完整还原（点+指令） |
| D5 | 导入 | 导出过的/手写的 .json 导入 | 正确载入 |
| D6 | 非法名 | 用含 / \ 的名字保存 | 被拒绝并提示 |

## E. 在线运行（重点·注意安全）

> 第一次运行务必降低速度、手放急停。

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| E1 | 单点运动 | 程序只放"运动到 P1"→ 运行 | 机械臂运动到 P1 |
| E2 | 多步顺序 | "运动到 P1 / 运动到 P2 / 运动到 P3"→ 运行 | 依次到达三个点 |
| E3 | 速度生效 | 加"设置速度为 20%"在最前 → 运行 | 运动明显变慢 |
| E4 | 等待 | 中间加"等待 2 秒"→ 运行 | 中途停约 2 秒 |
| E5 | 置 DO | "置 DO1 为 ON"→ 运行 | 对应 DO 输出变 ON（用万用表/指示灯确认） |
| E6 | 等待 DI | "等待 DI1 为 ON"→ 运行 | 未给信号时停住等待；给 DI1=ON 后继续 |
| E7 | 条件满足 | "当 DI2 为 ON 时运动到 P2"，先给 DI2=ON → 运行 | 执行该行运动到 P2 |
| E8 | 条件不满足 | 同上但 DI2=OFF → 运行 | 跳过该行，结果提示有跳过 |
| E9 | 完成提示 | 任意程序运行完 | 提示"共N行，执行X行，跳过Y行" |

## F. 导出 Lua

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| F1 | 导出文件 | 编好程序 → 导出 Lua | 下载到"程序名.lua" |
| F2 | 点位定义 | 打开 .lua 看开头 | 有 `P1 = {pose={...}}` 等 |
| F3 | 运动语句 | 查看主体 | 有 `MovJ/MovL(P1, {v=..})` |
| F4 | IO 语句 | 查看 | 有 `DO(1, ON)` |
| F5 | 条件块 | 查看条件行 | 是 `if DI(2) == ON then ... end` |
| F6 | 上机运行 | 把 .lua 放到示教器按现场流程跑 | 动作与在线运行一致 |

## G. 异常与恢复

| 编号 | 测试项 | 操作 | 预期结果 |
|------|--------|------|----------|
| G1 | 断连提示 | 拔网线/关远程控制后运行 | 提示连接/执行失败，不崩溃 |
| G2 | 清除报警 | 触发报警后点标题栏 ✅ | 报警清除，恢复就绪 |
| G3 | 重启服务 | 点标题栏 🔄 | 服务重启、页面自动恢复 |
| G4 | 空程序 | 不加指令直接运行/导出 | 提示"程序为空" |

---

## 测试记录建议

把每条的结果记为 通过 / 不通过 / 备注，重点关注：
- 存点坐标是否准确（影响所有运动）；
- DI/DO 编号与现场接线是否对应；
- 条件触发（满足/不满足）是否符合预期；
- 导出 Lua 在示教器上跑的效果是否与在线运行一致。

发现问题把现象（哪一步、报什么、期望什么）记下来，反馈即可定位修复。
