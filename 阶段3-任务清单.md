# 阶段 3 — Flask 产品化演示版 任务清单

> **计划工期**：约 10～12 天（起算 5.20，截止 **6 月 20 日前**）
> **最后更新**：2026-05-29（完成超时保护、操作日志、后端断连提示、气泡 spinner）
> **当前状态**：🟡 开发中 — Web 功能完成，后端异常处理完成，待部署文档和真机验收

---

## 如何阅读本清单

- `[x]` = 已完成
- `[~]` = 部分完成 / 有待确认
- `[ ]` = 待做
- 每个任务都标注了**文件路径**和**验收方式**

---

## 一、Flask 后端 API

### 1.1 项目骨架

- [x] 新建 Flask 服务入口 `web_app.py`
- [x] 配置线程锁确保串行操作机械臂
- [x] 配置全局会话和状态管理
- [x] 集成 `robot_planner`、`robot_runner`、`executor`、`semantic_cache`

### 1.2 API 端点

- [x] `GET /` — 返回主页面（`templates/index.html`）
- [x] `GET /api/status` — 返回连接状态
- [x] `POST /api/plan` — 解析指令返回计划（不执行真机）
  - 输入：`{"text": "指令", "no_llm": false}`
  - 输出：`{plan, validated_plan, summary, message}`
  - 包含 dry-run 白名单校验 + 缓存记录
- [x] `POST /api/run-once` — 执行已解析的计划
  - 输入：`{"text": "指令", "plan": {...}, "ip": "可选"}`
  - 内部调用 `execute_plan()` 连接并控制机械臂
  - 输出：`{ok, message}`
- [x] `POST /api/plan` 返回 human-readable `summary` 和 `message`（通过 `web_plan_format.py`）

### 1.3 后端错误处理

- [x] `/api/plan` 捕获 `ValueError`（参数越界等）→ 400 + 可读错误
- [x] `/api/plan` 捕获通用异常 → 500
- [x] `/api/run-once` 捕获 `ValueError` 和通用异常
- [x] `/api/run-once` 执行失败时清理会话状态（`_store_session(None)`）
- [x] **增强**：LLM API 调用超时/断网时给出明确提示，而非笼统的"解析失败"
  - 区分：连接失败、超时、401、429、5xx、JSON 解析异常
  - 新增 `timeout=30` 防止请求卡死
- [x] **增强**：真机执行中超时保护（目前 `_wait_command` 没有超时机制）
  - `_wait_command()` 新增 15 秒看门狗超时
  - `connect()` 新增连通性检查（5 秒超时 + 测试指令）
  - 连接失败时给出分步骤排查提示

---

## 二、Web 前端界面

### 2.1 页面结构

- [x] HTML 骨架（`templates/index.html`）
  - 顶部栏：品牌名 + 状态指示灯
  - 主体：对话气泡区
  - 底部：输入框 + 发送按钮
- [x] CSS 样式（`static/css/app.css`）
  - 浅色风，粉色主调（`#d16d6d`）
  - 响应式布局，气泡样式，打字动画
- [x] JS 交互逻辑（`static/js/app.js`）
  - 对话气泡渲染（用户/机器人）
  - 打字中动画（bouncing dots）
  - 状态指示轮询（每 5 秒 `/api/status`）
  - Enter 发送、多行输入自适应

### 2.2 核心交互流程（已按最新 UI 要求简化）

- [x] **三阶段反馈**（去掉了多余的细节标签）：
  1. "正在理解您的需求，请稍候" + 打字动画
  2. "已明白你想要的动作：xxx" + 动作摘要
  3. "正在控制机器人运动" + 打字动画 → "动作已完成"
- [x] **自动执行**（解析后无需点"确认"，直接执行）
- [x] **已去掉停止/急停按钮**（按 UI 修改要求）
- [x] **已去掉"经验库命中/规则解析/模型解析"等来源标签**（不显示在对话框）
- [x] 执行期间禁用输入框和按钮

### 2.3 前端状态管理

- [x] 状态指示灯 5 种颜色：
  - 灰色：未连接
  - 绿色：已就绪
  - 蓝色脉冲：分析中
  - 橙色脉冲：执行中
  - 红色：错误
- [x] 状态自动轮询更新
- [x] 错误信息显示在对话框

### 2.4 前端待改进项

- [x] **输入框在发送后记得清空** — 已实现（`commandInput.value = ""`）
- [x] **页面加载时若后端未运行，给用户友好提示** — 连续 2 次 status 轮询失败后显示错误消息，后端恢复后自动提示已连接
- [ ] **移动端适配检查**（媒体查询已做，但需真机确认）

---

## 三、Web 计划格式化

- [x] `web_plan_format.py` 实现（涵盖所有 10 种 Skill 的中文摘要）
- [x] `format_plan_summary()` — 多行摘要
- [x] `format_understood_message()` — 单行"已明白你想要的动作：xxx"
- [x] `format_confirm_message()` — 含确认提示
- [x] PATTERN_LABELS 覆盖 20 种 motion_pattern 的中文标签

---

## 四、日志与可观测性

- [x] 后端状态通过 `_robot_status` 全局变量跟踪
- [x] 每次 plan 保存到 `generated/last_plan.json`
- [x] 前端状态指示灯与后端状态同步
- [x] **增加持久化操作日志**（记录到文件，如 `generated/operation_log.csv`）
  - 新建 `logger.py` 模块：CSV 日志（线程安全）+ Flask 请求日志
  - 字段：时间、用户指令、解析来源、Skill、参数、执行结果、错误信息、耗时
  - `/api/plan` 记录 `result=planned`，`/api/run-once` 记录 `result=success/error`
  - 自动创建 `generated/` 目录
- [x] **Flask 自身请求日志**（开启 `app.logger` 或配置日志文件）
  - `setup_flask_logger()` 配置 RotatingFileHandler（5MB 轮转，保留 3 份）
  - 每次 API 请求自动记录：`GET /api/plan → 200 (123ms)`
  - 日志文件：`generated/app.log`
  - 同时输出到 stdout

---

## 五、部署说明文档

- [ ] **写一页部署说明**（作为 `部署说明.md` 或在 `使用说明.md` 中补充章节）
  - 前置条件：Python、虚拟环境、依赖安装
  - 配置 `.env`（ROBOT_IP、API Key 等）
  - 启动 Flask：`.venv\Scripts\python web_app.py`
  - 浏览器访问 `http://127.0.0.1:5000`
  - 机械臂连接检查流程
  - 演示注意事项（安全、示教器 TCP 模式等）
  - 故障排查（常见错误及处理）

---

## 六、验收测试

### 6.1 功能验收

- [ ] **dry-run 验收**（不连机械臂）：
  - [ ] 启动 Flask：`.venv\Scripts\python web_app.py`
  - [ ] 浏览器打开页面，确认状态显示"未连接"
  - [ ] 输入"抬高 20 毫米"，确认返回动作摘要、不报错
  - [ ] 输入"向 Z 正方向移动 500 毫米"，确认被拒绝（超 200mm 上限）
  - [ ] 输入"抓取桌上的物体"，确认被拒绝（不支持的能力）
  - [ ] 测试 `no_llm=true` 参数可关闭 LLM 兜底

- [ ] **真机验收**（连机械臂）：
  - [ ] 确认状态显示"已就绪"
  - [ ] 输入"抬高 20 毫米"，确认机械臂执行
  - [ ] 输入"下降 20 毫米"，确认机械臂执行
  - [ ] 输入"去 A 点"，确认机械臂执行
  - [ ] 输入"停止运动"，确认机械臂停止
  - [ ] 执行中再次输入指令，确认被禁用（防重复发送）

### 6.2 稳定性验收（对应 M3 里程碑）

- [ ] **连续 5 次真机小动作成功**（不重启进程）
- [ ] **超出能力范围的话不乱执行**（LLM 拒识测试 3 条以上）
- [ ] **API 断网时规则话术仍可用**（拔网线测试或模拟）
- [ ] **越界参数 100% 被拦截**（距离 >200mm、速度 >50%、等待 >10s）

### 6.3 演示准备（对应 M4 里程碑）

- [ ] **演示话术清单**整理到 `使用说明.md`（含预期结果）
- [ ] **演示人员熟悉失败兜底说法**
- [ ] **部署说明可按步骤跑通**（从零安装到演示 ≤ 30 分钟）
- [ ] **客户界面不出现 OpenClaw 品牌**
- [ ] **示教器急停位置确认**（程序不再提供急停按钮，需依赖示教器物理急停）

---

## 七、UI 当前状态与原始需求对照

参考 `界面说明.txt`，与当前实现对照：

| 需求项 | 状态 | 说明 |
|--------|:----:|------|
| 对话式交互 | ✅ | 气泡式对话 |
| `/api/plan` 只解析不执行 | ✅ | 已实现 |
| `/api/run-once` 确认后执行 | ✅ | 已实现（但自动执行，无确认步骤） |
| 状态反馈（5 色指示灯） | ✅ | 已实现 |
| 错误提示 | ✅ | 已实现 |
| 简洁单页界面 | ✅ | 已实现 |
| "确认执行"按钮（先预览再确认） | ❌ | 按最新 UI 要求已改为自动执行，无确认步骤 |
| 急停按钮始终可见 | ❌ | 按 UI 修改要求已去掉 |
| 深色科技风 | ❌ | 当前为浅色风 |
| 展开详情 JSON | ❌ | 不展示，调试信息未实现 |
| 安全提示"机械臂执行中，请保持安全距离" | ❌ | 未显示 |

> 注：打 ❌ 的项有些是 **按 UI 修改要求主动去掉的**（急停按钮、确认步骤），不是遗漏。

---

## 八、风险与注意事项

1. **急停依赖示教器**：程序已去掉急停按钮，演示时必须确保示教器急停可用且操作人员知晓位置
2. **LLM API 依赖网络**：断网时只有规则话术可用，演示前需确认网络稳定
3. **单线程锁**：`_robot_lock` 确保串行操作，但若某次执行卡死，需重启 Flask
4. **前端无"确认"步骤**：用户输入后直接 plan+execute，没有后悔机会。如有需要可改回两步流程

---

## 九、阶段 3 完成标准（对应方案规划.md 7.2 节）

- [x] Flask 最小可用（`/api/plan` + `/api/run-once`）
- [x] 简单 Web 页（输入、展示、执行）
- [ ] 部署说明（一页安装步骤）
- [ ] 演示话术稳定 ≥ 90%
- [ ] 端到端彩排通过

---

**优先级建议**（按顺序做）：

1. 🔴 **部署说明** — 现场 30 分钟能跑通，这是演示的前提
2. 🔴 **真机验收** — 连真机跑一遍所有功能
3. 🟡 **持久化日志** — 演示复盘用
4. 🟡 **增强错误处理** — LLM 超时等边缘情况
5. 🟢 **演示准备** — 话术清单、人员熟悉
